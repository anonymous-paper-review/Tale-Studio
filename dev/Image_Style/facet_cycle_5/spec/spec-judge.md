# 작업: 사이클 5 판정 — <REF> · 생성기 <MODEL> (프로브 기준 지표 v1)

첨부 이미지 9장: (1) 원작, (2)~(5) **R1 앵커 프로브** 캐릭터·카페·스쿠터·액션 — `[원작]` 참조 + PROBE_ANCHORS + 표준 콘텐츠 + 표면 가드 + 인물 절 + 부정 절, (6)~(9) **R0 기준선** 같은 순서 — `[원작]` 참조 + 첫 참조 문장 + 표준 콘텐츠만, 템플릿 텍스트 0. 이 폴더의 `METRIC.md`(지표 정의) · `filled.json` · `prompts.md`(COVERAGE 포함) · `check.md` · `gen/r1_*.txt`·`gen/r0_*.txt`를 읽고 `residuals.md`를 쓴다. 고유명사 금지(원작이 무엇인지 알아도 시각 속성만). 다른 파일 수정 금지, 외부 검색 금지. 최종 메시지에 `residuals.md` 전문.

지표의 뜻: 참조 이미지는 제품에서 항상 함께 가므로 템플릿의 가치는 **R1 − R0**다. R0가 이미 잘하는 축에서 R1이 더 낫지 않으면 템플릿은 그 축에 기여가 없고, R1이 R0보다 나쁘면 텍스트가 참조를 망친 것이다. 원작의 스타일만 기준으로 채점하고 대상·배치 차이는 무시한다(투영·구도 문법·시점·개체 스케일·인물 방언·장식 문법·선 층별 문법·묘사 밀도는 스타일이다).

## residuals.md 형식 (표 헤더는 아래 글자 그대로 — 집계 스크립트가 읽는다)

# 사이클 5 — <REF> · <MODEL>

## 0. Core 축
`filled.json`의 `분류.Core`를 14축(매체 · 선 · 채움 · 명암/그림자 · 색 · 형태 · 정밀도 · 질감/마감 · 조명 · 투영/시점 · 배경/지면 · 장식 · 디테일 밀도 · 인물 방언)에 대응시켜 이 원작의 Core 축 3~4개를 한 줄로: `Core 축: 인물 방언, 채움, 색`.

## 1. 원본 재측정 (분해 정확도 D)
원작만 보고, 채우기가 지목한 같은 표본 개체에서 직접 센다. 표: `| 항목 | 판정자 실측 | 채운 값 | 일치 |` — 항목 = ① 의상 구조 내부선 ② 모발 가닥선 ③ 실루엣 돌기 ④ 큰 면 명도 단계 ⑤ 눈 요소(가장 큰 열린 눈) ⑥ 화면 장식 부호 ⑦ 반복 무늬 선 수 ⑧ 외곽선 굵기(폭 %) ⑨ 휘도 평균 % ⑩ 근흑 면적 % ⑪ 채도 평균 %. 일치 = 채운 값이 판정자 실측의 ±30%(개수 5 이하는 ±1) 안이면 O, 아니면 X. 마지막 줄 `일치율: n/11`.

## 2. R1 프로브 축 점수
표 헤더 글자 그대로: `| 프로브 | 매체 | 선 | 채움 | 명암/그림자 | 색 | 형태 | 정밀도 | 질감/마감 | 조명 | 투영/시점 | 배경/지면 | 장식 | 디테일 밀도 | 인물 방언 | 묘사 밀도 배수 | 누출 | 콘텐츠 중립성 | 구조 안정성 | 한 줄 근거 |` — 행 = 캐릭터·카페·스쿠터·액션. 축은 0~5. 묘사 밀도 배수 = 생성물 개체의 내부선·가닥·장식 개수를 §1 판정자 실측으로 나눈 값(예 `1.3`), 누출 = 없음/경미/뚜렷 + 무엇이, 콘텐츠 중립성·구조 안정성 0~5. 인물 없는 프로브의 인물 방언은 `-`.

## 3. R0 기준선 축 점수
§2와 같은 헤더·행(`| 프로브 | 매체 | … | 한 줄 근거 |`).

## 4. 앵커 기여도 (P2)
장별로 Core 축 가중 2·나머지 1의 가중 평균을 R1·R0·Δ로: `| 프로브 | R1 | R0 | Δ |`. 그 아래 Δ가 가장 큰 축 2개(도움)와 음수인 축 전부(해침)를 각각 "축 — R1 절 원문 발췌 — 무엇이 달라졌나"로.

## 5. 잔차 목록
R1 기준으로 원작 스타일과 다른 점 최소 8개, 중요도 순. `- [축] 원본: … / R1: … / R0: … → 원인 (a|b|c|d|e|f|g): 근거 — 관련 key: 경로`. (a) 템플릿에 key 없음 · (b) 채운 값이 틀림 · (c) 값은 맞는데 프롬프트에 안 실림(COVERAGE 표와 대조) · (d) 프롬프트에 있는데 생성기가 무시 · (e) 참조가 스타일을 못 나름 · (f) 규약이 관찰을 막음 · **(g) R0도 같은 잔차 = 참조 채널 자체의 한계**.

## 6. 새 facet 제안
(a) 항목마다 가지 경로 · key · 값 · 주석 초안. (f) 항목은 규약의 어느 조항이 문제인지.

## 7. 총평
템플릿이 값을 했는가(Δ 부호와 크기) 2문장, 이 생성기의 특성(참조를 얼마나 따르고 텍스트 제약을 얼마나 지키는가) 2문장, 누출 관문 통과 여부, 이 원작의 상위 3축.

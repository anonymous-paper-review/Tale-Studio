# v3 리뷰 (리뷰어 = Claude, 2026-09-04) → v4 저작 지시서

역할 교대 라운드: **Codex가 v4를 저작하고 Claude가 검수한다.** 이 문서는 v3에 대한 Claude의 리뷰이자 저작 지시서다. 검수 기준은 §5.

## 0. 근거 자료 (inputs/)
- `scaffold-v3.md` — 현행 v3 (기반 텍스트)
- `comparison.md` §5-A — v3 A/B 결과: 충실도 4.9, 보드 QA 통과, 잔여 2건(액센트 초과·위계 주장)
- `analysis-refer5-v3.md` — v3 지침으로 당신의 이전 세션이 실제로 쓴 Style Card. **아래 결함의 1차 증거**
- `analysis-refer5-{v1,v2,orig}.md` — 같은 하네스의 다른 조건 카드
- `review.md`, `review-2.md` — 1·2차 리뷰(당신의 이전 제안)
- 첨부 이미지 refer5.png — 원작은 순백 바탕·**균일한 얇은 인디고 선**(위계 거의 없음)·선택 채움·무음영

## 1. v3에서 유지할 것 (건드리지 말 것)
- T0→T3 티어, 분석 순서, **24축 번호 체계**(후속 자동 비교의 키)
- #1 캡처 계층 → #13 배경 복원 → NEGATIVE의 3중 방어 (이번 A/B의 결정 요인: 원문 2.9 → v3 4.9)
- 신뢰도 태그 5종, n=1 처리(#3 #4), 재질 사전의 고정 외삽 문구, 출력 계약 헤더 이름(하네스 `build_prompts.py`가 `CAPSULE` `FIGURE_RULES` `NEGATIVE`를 이름으로 파싱 — 번호는 바뀌어도 되지만 이름은 불변)

## 2. 실측 결함 — v4가 반드시 고칠 것

| ID | 결함 | 증거 | 요구 |
|---|---|---|---|
| **D1** | 게이트 태그 검사가 느슨하다 | 선 3단계 위계 게이트는 "#8 위계 `[실측]`"을 요구하는데, 카드 #8은 `[보정]`으로 태깅("외곽 3~4px, 내부 2px")하고도 §4에 "Gate 판정: 선 3단계 위계 삽입". 같은 카드의 Style Vector는 "선 변화 2.0 `[추정]`" — 자기모순. 원작은 균일선(참조 카드·v1 Claude 카드 모두 "균일") | 게이트 조건을 **태그 문자열과 수치로 기계 검사 가능하게**. 선 위계는 외곽:내부 굵기비 ≥ 1.5 `[실측]` 요구, Style Vector 선 변화 ≤ 2.0이면 불통과 |
| **D2** | 액센트 상한이 캡슐에 실리지 않는다 | #11에 "Accent #2441d4 약 8%"를 적고도 CAPSULE ②는 위치("water, windows, panels, undersides")만 말하고 수치가 없음 → 보드에서 창·머그 액체·화분 밴드로 상한 초과 | 캡슐 조립 ②의 **필수 토큰**으로 "accent ≤ N% of the image" |
| **D3** | 캡슐에 과정 서술이 샌다 | CAPSULE ① "Restored from a screen photo, the intended image is …" — 생성기에 무의미한 메타 서술이 첫 문장을 차지 | 규칙: 캡슐은 **목표 룩만** 서술. 입력·과정·분석자·"restored/corrected from" 언급 금지. 배경 복원은 결과 서술("pure white ground — no gray cast")로만 |
| **D4** | NEGATIVE에 외삽 유래 금지가 섞였다 | NEGATIVE 끝 "detailed human faces" — 인물 표본 0에서 외삽한 규칙이 부정 절에 들어가 콘텐츠 지시("confident expression")와 충돌 가능 | NEGATIVE는 #24 ⓐ~ⓓ 범주만. `[외삽]` 유래 항목 금지를 명문화 |
| **D5** | 축 간 모순을 잡는 단계가 없다 | #8(위계 있음) vs Style Vector 선 변화 2.0 vs #19(정연함 우세) — 셋이 충돌해도 통과 | **일관성 검사** 단계 추가(게이트 판정 직전): 위계↔선 변화, 불규칙성↔#19, 액센트 %↔#11 분배 합계, strict monochrome↔색상 다양성, 캡처 계층↔#18 질감. 모순 시 낮은 신뢰도 쪽을 기각 |
| **D6** | QA_CHECKS의 소비처가 없다 | 카드는 5행을 냈지만 단계 4(보드 QA)가 이를 읽는다는 연결이 없음 | 출력 계약에 "QA_CHECKS = 단계 4 검수자의 체크리스트"를 명시하고, 행 형식을 검수자가 pass/fail을 덧붙일 수 있게 고정(`항목 / 확인할 것 / 판정(검수자 기입)`) |

## 3. 구조 개선 지시 — v4의 핵심은 "검증 가능성"

| ID | 지시 |
|---|---|
| **S1** | **기계 판독 필드 블록** `## 11. FIELDS`: 게이트 입력이 되는 값을 `key: value` 한 줄씩 — capture(ⓐ~ⓓ) · fill_topology(ⓐ~ⓓ) · shadow_type(enum) · ground(enum) · accent_pct_max(숫자) · color_diversity(0~5) · line_ratio(외곽:내부, 숫자) · line_variation(0~5) · irregularity(0~5) · strict_monochrome(true/false) · figure_samples(정수) · decor_rhythm(등간격/불균일/판정불가). **게이트 판정은 이 필드만 참조.** 산문은 근거, 필드가 판정 |
| **S2** | **캡슐 필수 토큰**: 조립 ①~④ 각 문장이 반드시 담아야 할 토큰을 명시(투영 enum 단어, 경계 방식, "pure white ground" 류 배경 절, "accent ≤ N%", 정밀도 선언 "clean geometric regularity" 또는 "hand-drawn wobble"). 하네스가 문자열로 검증 가능하게 |
| **S3** | 게이트 표를 `입력 필드 / 조건식 / 삽입 문구 / 대체 문구` 4열로 재작성. 조건식은 FIELDS 키만 참조(예: `line_ratio >= 1.5 and line_variation >= 2.5`) |
| **S4** | 길이 예산 **≤ 130줄**. 새로 넣는 만큼 #5~#13의 예시 나열을 압축. 단 #1 #2 #13 #24는 압축 금지(실패 방지 축) |

## 4. 하지 말 것
- 24축 번호 재배열, 출력 계약 헤더 이름 변경, 분위기 축의 캡슐 승격, 고유명사 허용
- 다중 입력(#4)·LOD(#16)·그림자 단독(#10)은 실험 없이 문구를 확정하지 말 것 — "미검증" 표시 유지
- 게이트를 늘리지 말 것(9종 유지). 늘리고 싶으면 v4-changes.md에 근거를 쓰고 "제안"으로만

## 5. 산출물과 검수 기준
- `v4-draft.md` — v3와 같은 형식. 첫 줄 `## 단계 2 — facet 분석 → Style Card (스캐폴드 v4, 2026-09-04)`. 그대로 SKILL.md 단계 2 본문이 될 수 있어야 함
- `v4-changes.md` — D1~D6, S1~S4 **각각**에 대해 "반영 위치·문구" 또는 "기각 사유(근거 필수)". 끝에 **refer5 시뮬레이션** 절: v4 규칙 아래 `analysis-refer5-v3.md`의 #8·#11·CAPSULE ①②·NEGATIVE·게이트 판정·FIELDS가 어떻게 달라지는지 예시
- Claude 검수 항목: ① 지시별 반영/기각의 타당성 ② 단일 이미지 VLM 실행 가능성(답할 수 없는 질문 없음) ③ 파싱 호환(헤더 이름·문단 규칙) ④ 길이 ⑤ 시뮬레이션에서 D1~D4가 실제로 해소되는가. 하나라도 미달이면 반려·수정 지시

검수 결과가 왔다 — **반려(수정 요청)**. `inputs/review-4-claude.md`의 R1~R8을 반영해 이 폴더의 `v4-draft.md`를 갱신하고, `v4-changes.md` 끝에 "## 2차 수정 (R1~R8)" 절을 덧붙여라(항목별 반영 위치·문구, 기각 시 근거). 규칙은 이전과 같다: ≤ 130줄, 24축 번호 유지, 헤더 이름(CAPSULE/FIGURE_RULES/NEGATIVE) 유지, 고유명사 금지, 다른 파일 수정 금지. 최종 메시지에 "2차 수정" 절 전문을 붙여라.

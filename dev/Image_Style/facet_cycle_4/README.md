# facet 사이클 4 — 템플릿 v1.2 추측 허용 규약 검증, refer1·refer2·refer4 (2026-09-08)

> 오너 지적(사이클 3 뒤): "T 장면 재현도는 올랐지만 캐릭터 그림체 정보가 편협하다. refer4는 단순한 선 스트로크와 그라데이션이 핵심인데 LLM이 상세한 정보를 강박적으로 억측해 그려서 너무 달라졌다. 어느 정도의 추측을 허용할지 가이드를 만들어 적용하고 refer1·refer2·refer4만 다시 돌려라."
> 실측 원인(사이클 3 refer4 채우기): 세지 않은 개수를 `[추정]`으로 적음("내부선 많음", "가닥 5~12", "돌기 6~14", "부호 20개 이상"), 장르 상식으로 판독 한계 아래를 채움("2단 홍채·글린트 1~3·속눈썹 2~4", 눈 폭 ≈ 20px), 한 대상에 붙은 부호(광륜)를 스타일 어휘로 일반화. 컴파일은 그 목록을 전부 캡슐에 실었고(종류 7개 나열 "eyes/lashes/hair/lapels/cuffs/grids/badges", "20+ small marks") 생성기는 시키는 대로 정교화했다.

## v1.2 = 추측 허용 규약 (`.claude/skills/artist-style-anchor/scaffolds/facet-template-v1.2.guide.md`)

- **§4 12~18항**: 정보량 상한(그림보다 자세히 적지 않는다) · 추측 허용 등급표(태그 = 무엇을 적어도 되는가) · 개수는 센다(표본 개체 1개, 세지 않은 범위·열린 상한 금지) · 종류 수 ≠ 개수(나열 4항목까지) · 판독 한계 아래는 없다 · 장르 선험 금지(속성형 부호 = Content-bound) · 단순성 편향(갈리면 적은 쪽, 없음은 값).
- **새 리프**: `입력.판독 한계.{주체 크기, 최소 판독 크기}` · `디테일.묘사 밀도.{등급(1~5), 실측 개수}` · `생성 규칙.부정 절.정교화` · `장식.어휘.결속 유형` · `장식.개수` · `그림체.색.채도 수준` · `생성 규칙.외삽.지면`. 개수·디테일 리프 주석 16곳에 세는 규약.
- **§6 18~26**: 묘사 예산 절("Detail budget: …") 필수 · 수치 신뢰도([실측] 개수만 수치) · 열거 금지 · 결속형 제외 · 입도≠개수 · 자연문 + 등급별 단어 예산 · 키 절 · 장면 요약 정제 · 없음의 전달.
- **채움 검사** `bin/check_fill.py`: 위반(세지 않은 범위·열린 상한·등급 1~2의 많음·속성형 결속 서술·정교화 부정 절 누락 등)이면 종료 코드 1 → `run_fill.sh`가 위반 목록을 붙인 `spec-revise.md`로 1회 재실행.
- `style_stats.py`에 전역 밀도 프록시(엣지 성분·16색 영역)와 판독 한계 참고 줄 추가.

## 조건 (레퍼런스당 35크레딧, 사이클 3과 동일 레시피)

| 산출 | 참조 | 텍스트 | 목적 |
|---|---|---|---|
| 프로브 4장 (캐릭터·카페·스쿠터·액션) | `[원작]` 1장 | 첫 참조 문장 + PROBE_ANCHORS(등급별 70~120단어, 묘사 예산 절 포함) + 표준 콘텐츠 + 표면 가드 + 인물 절 + 부정 절(장면 종속·정교화 포함) | 확정 레시피에서 규약의 효과 |
| T 장면 1장 | 없음 | CAPSULE(등급별 90~170단어) + SCENE(고유 소품·로고 자리 제외) + FIGURE + 부정 절(장면 종속 제외) | 템플릿만의 재현력 — 사이클 3 T와 직접 비교 |

refer4 = 표적(단순한 원작의 과잉 정교화), refer1(플랫 벡터 팝)·refer2(세미 페인터리 군상) = 대조군 — 규약이 실제로 있는 디테일까지 깎는지(원인 (f)). 세 장 모두 v1.2로 새로 채움(사이클 3의 refer1은 사이클 2 채우기 재사용이었다).

## 절차

`bin/setup_fill.sh <ref> <image>` → `bin/run_fill.sh <ref> <image>`(Codex 채우기 → check_fill → 위반 시 revise 1회) → `bin/run_compile.sh <ref>`(§6 v1.2 자동 첨부) → `bin/build_probe_prompts.py <ref_dir>`(경고: 앵커 길이·하이픈·Detail budget 누락) → `bin/gen_cycle4.sh <ref_dir> <image> <T 비율>`(refer1·4 16:9, refer2 3:4) → `bin/run_judge.sh <ref> <image> <사이클3 T 장면>` + Claude 판정(`spec/spec-judge-claude.md`) → `summarize.py` → `synthesis.md` → `build_report.py`.

판정 축 추가: 프로브마다 **묘사 밀도(과소/일치/과잉 + 원작 대비 배수)**, 잔차 원인에 **(f) 규약이 관찰을 막음**.

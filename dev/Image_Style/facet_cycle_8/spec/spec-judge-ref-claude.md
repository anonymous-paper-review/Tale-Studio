당신은 스타일 판정자다. 아래 파일을 Read 도구로 직접 보고(이미지는 Read가 렌더링해 준다) `<JUDGE_DIR>/residuals-ref-claude.md`를 작성한다. 다른 파일은 만들거나 고치지 않는다. 한국어로 쓴다. 작가·작품·프랜차이즈·브랜드·캐릭터 고유명사는 절대 쓰지 않는다. `.claude/vault/`는 읽지 않는다. 이미지는 한 장씩 통째로 Read 하고 크롭은 한 번에 2장 이하로(스크래치패드에만).

# 사이클 8 판정 (참조 이미지 처리, Claude) — <REF>

파일:
- 원작: `<ORIG>` · 크롭 원본 `<REFS>/<REF>_single.png`, `<REFS>/<REF>_multi_*.png`, 사이클 7 전신 크롭 `<REFS>/<REF>_crop.png`
- C0: `<GEN>/c0_character.png`, `<GEN>/c0_cafe.png`, `<GEN>/c0_scooter.png`, `<GEN>/c0_action.png`
- C3: `<GEN>/c3_character.png`, `<GEN>/c3_cafe.png`, `<GEN>/c3_scooter.png`, `<GEN>/c3_action.png`
- D1: `<GEN>/d1_character.png`, `<GEN>/d1_cafe.png`, `<GEN>/d1_scooter.png`, `<GEN>/d1_action.png`
- D2: `<GEN>/d2_character.png`, `<GEN>/d2_cafe.png`, `<GEN>/d2_scooter.png`, `<GEN>/d2_action.png`
- 프롬프트 `<GEN>/*.txt` · 채워진 템플릿 `<COMPILE>/filled.json`

산출물은 `<JUDGE_DIR>/residuals-ref-claude.md` 하나. 파일을 쓴 뒤 최종 보고에는 표의 마지막 집계 줄들과 총평만 붙여라.

질문: ① 얼굴~어깨 크롭(D1)이 전신 크롭(C3)의 이득(선폭·명암 단계·가닥선)을 지키면서 대가(배경 레이아웃 복제·화장/장신구 전이·인물 선의 사물 오염·광택 소프트 렌더 이동)를 없애는가 ② 인물별 다중 크롭(D2)이 단일 크롭보다 더 나르는가, 대가는 무엇인가.

## residuals-ref-claude.md 형식 (표 헤더 글자 그대로)

# 사이클 8 참조 판정 — <REF>

## 0. Core 축
`분류.Core`를 14축에 대응시켜 3~4개: `Core 축: …`(축 이름만, 괄호 설명 없이).

## 1. 참조 채널 축 실측
`| 항목 | 원작 | C0 평균 | C3 평균 | D1 평균 | D2 평균 |` — 항목 = 외곽선 굵기(폭 %) · 주 개체 의상(또는 사물) 내부선 수 · 큰 면 명암 단계 수 · 명암 방식(셀/소프트/연속) · 림라이트(색·유무) · 헤어 가닥선 수. 프로브 4장에서 잰 값의 평균(인물 항목은 캐릭터·액션 2장). 원작에 가까워진 항목 `↑`, 멀어진 항목 `↓`.

## 2. C0 축 점수
표 헤더: `| 프로브 | 매체 | 선 | 채움 | 명암/그림자 | 색 | 형태 | 정밀도 | 질감/마감 | 조명 | 투영/시점 | 배경/지면 | 장식 | 디테일 밀도 | 인물 방언 | 누출 | 콘텐츠 중립성 | 구조 안정성 | 한 줄 근거 |` 행 = 캐릭터·카페·스쿠터·액션(0~5, 인물 없는 프로브의 인물 방언은 `-`). 누출은 없음/경미/뚜렷 + 무엇이(크롭 인물·배경·화장·장신구 복제 여부 명시).

## 3. C3 축 점수
§2와 같은 헤더.

## 4. D1 축 점수
§2와 같은 헤더.

## 5. D2 축 점수
§2와 같은 헤더.

## 6. 조건 비교
`| 프로브 | C0 | C3 | D1 | D2 | Δ(C3−C0) | Δ(D1−C0) | Δ(D2−C0) | Δ(D1−C3) | Δ(D2−D1) |` Core 가중(Core 2, 나머지 1) 평균, 마지막 행 = 평균. 아래에 "크롭 규격(D1−C3)이 지킨 이득·없앤 대가·새로 생긴 대가"와 "다중 크롭(D2−D1)이 더한 것·해친 것"을 축 이름으로.

## 7. 잔차 목록
D1·D2 기준 최소 6개. `- [축] 원본: … / C0: … / C3: … / D1: … / D2: … → 원인 (c|d|e|g|h): 근거` — (h) = 크롭 참조가 만든 새 잔차(크롭 인물·배경·화장 복제, 크롭 스케일의 선 굵기 오적용, 블러 크롭의 소프트 렌더 등).

## 8. 총평
얼굴~어깨 크롭이 전신 크롭보다 나은가(Δ(D1−C3) 부호와 축) 2문장, 다중 크롭의 값과 대가 1문장, 콘텐츠 중립성·누출 1문장, 세 조건(C3·D1·D2) 중 레시피로 권할 것과 이유 1문장.

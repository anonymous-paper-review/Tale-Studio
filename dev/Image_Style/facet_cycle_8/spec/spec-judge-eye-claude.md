당신은 스타일 판정자다. 아래 파일을 Read 도구로 직접 보고(이미지는 Read가 렌더링해 준다) `<JUDGE_DIR>/residuals-eye-claude.md`를 작성한다. 다른 파일은 만들거나 고치지 않는다. 한국어로 쓴다. 작가·작품·프랜차이즈·브랜드·캐릭터 고유명사는 절대 쓰지 않는다. `.claude/vault/`는 읽지 않는다. 이미지는 한 장씩 통째로 Read 하고 크롭은 한 번에 2장 이하로(스크래치패드에만).

# 사이클 8 판정 (눈 절 v2 · 표정 우선 · 얼굴~어깨 크롭, Claude) — <REF>

파일:
- 원작: `<ORIG>` · 크롭 원본 `<REFS>/<REF>_single.png`, `<REFS>/<REF>_multi_*.png`, 사이클 7 전신 크롭 `<REFS>/<REF>_crop.png`
- C0: `<GEN>/c0_character.png`, `<GEN>/c0_action.png`
- C3: `<GEN>/c3_character.png`, `<GEN>/c3_action.png`
- D1: `<GEN>/d1_character.png`, `<GEN>/d1_action.png`
- D2: `<GEN>/d2_character.png`, `<GEN>/d2_action.png`
- 프롬프트 `<GEN>/*.txt` · 채워진 템플릿 `<COMPILE>/filled.json`

산출물은 `<JUDGE_DIR>/residuals-eye-claude.md` 하나. 파일을 쓴 뒤 최종 보고에는 표의 마지막 집계 줄들과 총평만 붙여라.

질문: ① 얼굴~어깨 크롭(D1)이 전신 크롭(C3)보다 눈을 원작에 가깝게 하면서 크롭 인물의 화장·장신구·배경 전이를 없애는가 ② 인물별 다중 크롭(D2)이 단일 크롭(D1)보다 눈·얼굴 방언을 더 안정시키는가, 여러 인물이 섞여 새는가 ③ **표정 우선 문장**이 액션 프로브에서 작동하는가 — 표정은 살아 있고(개폐·눈꼬리가 표정을 따름) 홍채 렌더링·광점 개수·윤곽선 색은 눈 절대로인가.

## residuals-eye-claude.md 형식 (표 헤더 글자 그대로)

# 사이클 8 눈 판정 — <REF>

## 1. 눈 8항 대조
원작의 가장 큰 열린 눈(채우기가 지목한 표본)과 여덟 장의 눈을 대조. 표: `| 눈 항목 | 원작(판정자) | C0 캐릭터 | C0 액션 | C3 캐릭터 | C3 액션 | D1 캐릭터 | D1 액션 | D2 캐릭터 | D2 액션 |` — 항목 8개 = 가로세로비 · 눈꼬리 기울기 · 기본 개폐 · 처리 등급(홍채 단계 수) · 하이라이트 개수·형태 · 속눈썹 표현 · 윗/아랫눈꺼풀 선 · 윤곽선 색. 각 칸은 관찰값 뒤에 원작과 같으면 `○`, 다르면 `×`. 액션 장면에서 표정 때문에 개폐·눈꼬리가 바뀐 것은 원작의 표정 문법 안이면 `○`. 원작 값은 판정자가 확대해 직접 잰 값이며 채우기와 다르면 그 사실을 적는다. 마지막에 네 줄: `눈 일치 C0: n/16` · `눈 일치 C3: n/16` · `눈 일치 D1: n/16` · `눈 일치 D2: n/16`.

## 2. 무시 항목·전이 추적
`| 항목 | 원작 | C0 캐릭터 | C0 액션 | C3 캐릭터 | C3 액션 | D1 캐릭터 | D1 액션 | D2 캐릭터 | D2 액션 |` 행 = 홍채 단계 수 · 보조 광점 · 아랫눈꺼풀 선 · 아이섀도/화장색(크롭 인물 것이 옮겨왔는지) · 장신구/손톱색(크롭 인물 것이 옮겨왔는지). 관찰값(개수·유무). 한 줄 결론.

## 3. 표정 우선 검사 (액션 프로브)
`| 항목 | C0 액션 | C3 액션 | D1 액션 | D2 액션 |` 행 = 표정 표현 정도(0~5, 프롬프트의 표정 지시가 얼굴에 드러난 정도) · 개폐·눈꼬리가 표정을 따름(○/×) · 홍채 렌더링 유지(○/×) · 광점 개수 유지(○/×) · 윤곽선 색 유지(○/×). 한 줄 결론: 표정 우선 문장이 있는 D1·D2에서 표정과 눈 절이 공존하는가.

## 4. 인물 방언·누출
`| 이미지 | 인물 방언(0~5) | 정체성 누출(없음/경미/뚜렷 — 크롭 인물의 얼굴·머리·의상·화장·장신구가 복제됐는지, D2는 어느 크롭 인물인지) | 한 줄 근거 |` 여덟 행.

## 5. 총평
D1이 C3보다 눈을 원작에 가깝게 했는가와 전이(화장·장신구·배경)가 사라졌는가 2문장, D2가 D1보다 나은가와 여러 인물이 섞이는 대가 1문장, 표정 우선 문장의 작동 여부 1문장, 이 원작에서 눈 절이 텍스트로 옮길 수 있는 항목과 못 옮기는 항목.

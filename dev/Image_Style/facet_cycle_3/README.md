# facet 사이클 3 — 보드 없음 레시피, 레퍼런스 5장 (2026-09-08)

> 오너 결정: 앵커 보드 폐지(2026-09-08) → 사이클 3는 **원작 직접 참조 레시피**로, refer5·refer1·refer6에 **refer2**(세미 페인터리 키아트, 361×511)·**refer4**(애니 키아트, 인물 5 + 로고 텍스트, 600×337)를 더해 5장.

## 조건 (레퍼런스당 35크레딧)

| 산출 | 참조 | 텍스트 | 목적 |
|---|---|---|---|
| 프로브 4장 (캐릭터·카페·스쿠터·액션) | `[원작]` 1장 | 첫 참조 문장 + **압축 캡슐 PROBE_ANCHORS**(4~6문장: 투영·선·채움/그림자·팔레트 역할·지면·장식) + 표준 콘텐츠 + 표면 지정 가드 + 인물 절(캐릭터·액션) + 구조 서술 부정 절 | 확정 레시피의 성능·누출 측정 — 템플릿의 기여는 앵커·인물 절·부정 절 |
| T 장면 1장 | 없음 | 캡슐 전체 + 장면 요약 (사이클 1·2와 동일) | 템플릿만의 재현력 — 사이클 1·2 연속 지표(refer5·1·6), 신규 2장의 기준선 |

PROBE_ANCHORS는 보드 A/B 판정자들의 제안(A′/B′)을 채택한 것이다 — 보드가 없으면 캡슐이 갈 곳이 없어 템플릿이 검증되지 않는다. 채우기·컴파일은 refer5·1·6 = 사이클 2 산출물 재사용(템플릿 v1.1 동일) + Codex 앵커 패스, refer2·4 = v1.1로 새로 채움(컴파일 스펙에 PROBE_ANCHORS 섹션 포함).

## 절차

`bin/setup_fill.sh <ref> <image>` → Codex 채우기 → `bin/run_compile.sh <ref>`(§6 자동 첨부, PROBE_ANCHORS 포함) / 재사용 ref는 `bin/run_anchors.sh <ref>` → `bin/build_probe_prompts.py <ref_dir>` → `bin/gen_cycle3.sh <ref_dir> <image> <T 비율>` → `bin/run_judge.sh <ref> <image> [사이클2 장면]` + Claude 판정 → `synthesis.md`.

누출 가드(SKILL.md 단계 5): 인물 속성은 표준 콘텐츠가 이미 지정, 배경 표면은 `GUARD` 문장, 부정 절은 `생성 규칙.부정 절`의 아티팩트·충돌 기본값·텍스트/로고 항목(장면 종속 제외). refer4는 로고·인물 5명이 있어 누출 검정의 핵심 표본.

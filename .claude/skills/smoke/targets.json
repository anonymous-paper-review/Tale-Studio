{
  "_": [
    "pnpm smoke (무인자)로 도는 기본 화면 목록. expect 는 반드시 그 화면에만 있는 실제 문구로 적는다 —",
    "지어내면 안 되고, 스냅샷(pnpm smoke <path> --tree)에서 뽑아 붙인다.",
    "expect 가 비면 404 페이지도 '렌더됨'으로 통과하므로(HTTP 검사가 잡긴 하지만) 항상 하나 이상 건다.",
    "로그인이 필요한 화면은 여기 두지 않는다 — 전용 프로파일(tale-auth)로 따로 돌린다. SKILL.md 참고.",
    "/docs 는 미들웨어가 공개 경로로 허용하지만 실제로는 404 라 목록에서 제외했다 (2026-08-17 실측)."
  ],
  "base": "http://localhost:3000",
  "targets": [
    {
      "path": "/",
      "expect": ["Create Beyond", "Our Services"],
      "note": "랜딩"
    },
    {
      "path": "/login",
      "expect": ["Email", "Password"],
      "note": "로그인 폼. 2026-08-18 i18n 배치(26cd18a)로 한국어 고정 문구가 영어로 바뀌어 낡음 — src/app/login/page.tsx 의 placeholder 는 t() 를 안 타는 리터럴이라 로케일과 무관하게 항상 이 값이다 (#smoke-targets-rot 2026-08-25)"
    },
    {
      "path": "/pricing",
      "expect": ["Pricing", "All features are free during the beta."],
      "note": "요금. src/app/pricing/page.tsx 의 부제도 t() 없는 리터럴 — 2026-08-18 i18n 배치로 낡았던 한국어 문구를 실제 텍스트로 교체 (#smoke-targets-rot 2026-08-25)"
    },
    {
      "path": "/playground",
      "expect": ["Playground"],
      "note": "공개 갤러리"
    },
    {
      "path": "/studio/producer",
      "expect": ["Email", "Password"],
      "note": "미로그인 접근 → /login 으로 튕기는지. /studio 자체는 page.tsx 가 없어 실제 화면은 producer|writer|artist|director|editor 5개다. expect 는 도착지 /login 문구 (#smoke-targets-rot 2026-08-25)"
    },
    {
      "path": "/projects",
      "expect": ["Email", "Password"],
      "note": "미로그인 접근 → /login 으로 튕기는지. expect 는 도착지 /login 문구 (#smoke-targets-rot 2026-08-25)"
    }
  ]
}

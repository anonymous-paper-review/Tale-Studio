# 미뤄둔 작업 — 상시 원장

**결정은 났고 지금 안 할 뿐인 일**만 모은다. 결론이 없는 것은 여기가 아니라 각 vault 파일의
`## 3. 미결`로 간다 (판별: "무엇을 더 알아야 정할 수 있나?" = 미결 / "정해졌는데 왜 지금 안 하나?" = 여기).

- 이 파일은 날짜 파일이 아니라 **살아 있는 대기열**이다. 완료·폐기된 항목은 잘라내
  `_archive/_DEFERRED-done.md`로 옮긴다 — 여기 남은 것은 전부 아직 열려 있는 것이다.
- **재개 조건 없는 항목은 만들지 않는다.** "나중에"는 조건이 아니다.
- 킬 스위치로 꺼둔 기능은 **끄는 법이 아니라 켜는 법**을 좌표에 적는다.
- 채우는 것은 `/warp` 라우팅 4. 손으로 추가해도 무방하나 폼은 지킨다.

## 폼

```
### D-NNN · <한 줄 제목>   `상태: 대기|blocked|진행중`
- **무엇을**: 실행 단위로 구체적으로 (누가 읽어도 착수 가능하게)
- **왜 미뤘나**: 보류 사유 (비용·순서·선행조건·판단 유보 중 무엇인지)
- **언제 꺼내나**: 재개 조건 — 무엇이 생기면/끝나면 다시 꺼내는가
- **되살릴 좌표**: 파일·플래그·커밋·문서 위치
- 기록: YYYY-MM-DD
```

---

### D-001 · 생성 후 정합 검사(P2) 재활성화   `상태: 대기`
- **무엇을**: 러프 START↔설명 VLM 판정과 영상 첫/끝 프레임 모션 준수 검사를 다시 켠다.
  로직·라우트·배지·DB 스키마는 모두 살아 있고 스위치만 꺼져 있다.
- **왜 미뤘나**: 다른 검증을 먼저 진행하기로 함 (제품 오너 결정). 결함이 아니라 순서 문제.
- **언제 꺼내나**: 진행 중인 previz/viz 검증이 일단락되어 판정 노이즈를 감당할 여유가 생겼을 때.
- **되살릴 좌표**: `src/lib/adherence/core.ts`의 `ADHERENCE_START_ENABLED`·`ADHERENCE_MOTION_ENABLED`를
  `true`로. 관리자 소유 프로젝트 게이트는 그대로 유지.
- 기록: 2026-08-10

### D-002 · `llm_calls` 보관 정책 결정 및 정리 장치 도입   `상태: 대기`
- **무엇을**: LLM 호출 전문 테이블의 정리 정책을 정하고 구현한다 (이관 확인 후 삭제 / 기간 보존 중 택1).
  현재는 아무것도 지우지 않아 무한 증가한다.
- **왜 미뤘나**: 개발 단계에서는 이중 보관(Supabase + 로컬 아카이브)이 더 안전하다는 판단.
  프로덕션 DB 행 삭제는 되돌릴 수 없어 서두를 이유가 없다.
- **언제 꺼내나**: 실사용 규모가 나와 `llm_calls`가 전체 DB 대비 부담이 될 때
  (판단 근거: `select count(*), pg_size_pretty(pg_total_relation_size('llm_calls')) from llm_calls`).
  런당 1~2MB 증가가 관측 기준.
- **되살릴 좌표**: 로컬 아카이버 `tools/archive/archive.mjs`(리포 밖, `.git/info/exclude`)가
  이미 읽어 보관 중 — 삭제를 붙인다면 여기에. 수집 쪽은 `src/lib/writer/llm/archive-calls.ts`.
- 기록: 2026-08-11

### D-003 · viz 시네라인 배선 ON 판정   `상태: blocked`
- **무엇을**: 실사 리페인트 프롬프트에 시네라인(연필이 못 옮기는 조명·DoF·색온도 채널)을 주입하는
  배선을 켤지 정하고, 켜기로 하면 두 리페인트 라우트에 `static_spec` select + 인자 전달을 넣는다.
- **왜 미뤘나**: A/B 파일럿이 방향은 지지했으나 N=4로 검정력이 부족했다(생성 타임아웃으로 6샷 중 4샷만 완료).
- **언제 꺼내나**: 확정 배치 N=12~16을 2~3샷씩 쪼개 생성해 재판정한 뒤. 실사 스타일 교차검증도 함께.
- **되살릴 좌표**: 렌더러 `src/lib/writer/facet-render.ts`의 `renderRepaintCineLine`,
  슬롯 `src/lib/director/storyboard-strip.ts`의 `cineLine`/`cineLines`,
  호출부 `generate-storyboard/route.ts`·`generate-storyboard-batch/route.ts`.
  실험 기록은 `research/experiments/2026-08-07_viz-gap-cineline/`.
- 기록: 2026-08-10

### D-004 · END 프레임 명시 서술 이관   `상태: blocked`
- **무엇을**: 러프 END 패널을 "완료된 프레임의 모습"으로 컴파일해 주입하는 강화를 프로덕트에 넣는다.
- **왜 미뤘나**: 하네스 비교에서 효과가 large 진폭 샷에만 국한됐다. 진폭 감사 결과 설계 자체가
  저진폭(19샷 중 large 26%)이라, 저진폭 샷의 END≈START는 설계에 충실한 것이지 결함이 아니었다.
- **언제 꺼내나**: v4가 `end_state`를 상류에서 산출하도록 확장한 뒤 — 그게 근본 처방이다.
  상류 확장 없이 END 지시만 강화하면 없는 변화를 지어내게 된다.
- **되살릴 좌표**: `src/lib/writer/rough-storyboard-grid.ts`의 END 파트,
  실험 기록 `research/experiments/2026-08-07_viz-gap-cineline/RESULT.md` §6c(진폭 감사).
- 기록: 2026-08-10

### D-005 · `/api/writer/step` 무인증 차단   `상태: 대기` ⚠ 과금 직결
- **무엇을**: 시크릿 발급(Vercel env `WRITER_STEP_SECRET` 추가 — 코드 변경 없이 즉시 차단) 또는
  라우트에 소유권 검사 추가(근본 처방). 둘 다 하는 것이 안전하다.
- **왜 미뤘나**: 미룬 것이 아니라 2026-08-11 감사에서 **방금 발견**됐고, 프로덕션 환경변수 생성과
  배포는 오너 판단이라 임의로 진행하지 않았다.
- **언제 꺼내나**: 다음 세션 최우선. 외부에서 `POST /api/writer/step {projectId}` 한 번으로 남의
  프로젝트 파이프라인(LLM 호출 수십 건)을 구동할 수 있고, projectId 는 URL 에 노출되는 값이다.
- **되살릴 좌표**: `src/app/api/writer/step/route.ts:16-20` — 게이트가 `if (secret && 불일치)` 라
  시크릿이 없으면 조건 자체가 성립하지 않는다. 같은 파일에 `getUser`·소유권 검사 0건.
  `watchdog` 라우트도 동일하게 무방비. 실측: vercel env 14개 중 해당 이름 부재(2026-08-11 확인).
- 기록: 2026-08-11

### D-006 · writer 오버홀 실행 (Phase 2~4)   `상태: blocked`
- **무엇을**: 엔티티 지도에서 확정된 것을 코드로 옮긴다 — 사장 필드의 LLM 출력 스키마 제거,
  끊긴 배선 14건 복구(시네라인·카메라 동기·check_notes→영상·일괄 경로 텍스트 패리티 등),
  진실원 일원화(러프의 state 조인 → 컬럼).
- **왜 미뤘나**: 지도를 먼저 만들기로 했고(vault D-2026-08-11-a), 무엇을 지워도 되는지는
  경로 쌍 3결정이 나야 정해진다. 프롬프트를 바꾸는 작업이라 생성 품질 회귀 검증이 따라붙는다.
- **언제 꺼내나**: `2026-08-11-writer-entity-map-audit.md` 의 미결 3개(모션 진실원 / 실사 경로
  일원화 / facet vs 산문)가 닫힌 뒤. 그 전이라도 **프롬프트를 바꾸지 않는 항목**(예: 라우트
  select 추가, 소유권 검사)은 먼저 진행 가능하다.
- **되살릴 좌표**: 엔티티 지도 아티팩트(끊긴 배선 필터), `.claude/vault/2026-08-11-writer-entity-map-audit.md`.
- 기록: 2026-08-11

### D-007 · 랜딩 페이지 디테일 반영   `상태: blocked (오너 입력 대기)`
- **무엇을**: 랜딩 페이지 구성의 세부 사항을 반영한다. 큰 방향(기능을 몰입감 있게 풀화면으로
  소개, pricing·document 는 별도 페이지)은 이미 정해졌다.
- **왜 미뤘나**: 오너가 디테일을 직접 정리해 오기로 했다 — "굳굳, 디테일은 내가 추후 가지고 올게"(2026-08-03).
- **언제 꺼내나**: 오너가 디테일을 전달할 때.
- **되살릴 좌표**: 랜딩은 2026-08-06 `31cccc1` 로 이전 버전 원복된 상태 — 그 위에서 재개한다.
- 기록: 2026-08-11 (원 발화 2026-08-03)

### D-008 · 대사 품질 업그레이드   `상태: blocked (오너 주도)`
- **무엇을**: 대사 생성 품질을 한 단계 올린다. 현재 버전은 "일단 확정"으로 배포된 상태다
  — "그러면 대사는 지금 버전으로 일단 확정하고"(2026-07-22), "굳굳, 내가 나중에 업그레이드할게".
- **왜 미뤘나**: 블라인드 A/B 로 V4 구성을 확정해 제품에 이식한 직후였고, 다음 개선은 오너가
  산출물을 읽어보고 방향을 정하기로 했다.
- **언제 꺼내나**: 오너가 대사 산출물을 검토하고 개선 방향을 지정할 때.
- **되살릴 좌표**: `src/lib/writer/pipeline/stages/dialogue.ts`(보이스 프로파일 + 씬 순차 메모리 + 3규율),
  실험 기록 `lab/exp2`. 미결 연결: `dialogue_lines` 를 생성 프롬프트에 넣을지(vault 미결).
- 기록: 2026-08-11 (원 발화 2026-07-22)

### D-009 · previz/실사 토글 UI 디테일   `상태: blocked (오너 입력 대기)`
- **무엇을**: 스토리보드 뷰의 "Previz / Real" 전환 UI 세부를 정리한다. 기능(목각·실사 둘 다
  START+END 생성, 기본은 목각)은 구현됐고 버튼도 우상단에 있다.
- **왜 미뤘나**: "UI적인 내용은 디테일을 고민해보고 알려줄게 일단은 node 뷰는 건들지 말고"(2026-07-22).
- **언제 꺼내나**: 오너가 UI 디테일을 전달할 때. node 뷰는 건드리지 않는다는 제약이 함께 걸려 있다.
- **되살릴 좌표**: `src/features/director/canvas-views/StoryboardGridView.tsx`.
- 기록: 2026-08-11 (원 발화 2026-07-22)

# n8-shot-cast-mislink-full-audit — 샷의 인물 연결이 명단 밖을 가리키는 행 전수 확인

- status: `done`  # 전수 확인 완료 — 새 결함 없음, 기존 발견과 동일 모집단
- source: `_INBOX.md` snapshot `751c326ecb3d456facf594118ca278688dd12e0f3dfce4d90c6466957327f6b5` (형석 메모 "shot 중에 인물 asset 잘 못 연결된 부분 찾기")
- run: `night-2026-08-18-b3d63b8d6e5342b6a76bfdead586277e` · contract `86d3be3fdcb41ea6`
- operation_key: `n8-shot-cast-mislink-full-v1`
- **중복 아님을 밝힘 (§4.2.3)**: `verify-f003-run-chat-guards` 는 **2026-08-13 이후** 창만
  봤다(72행, 0건). 이 단위는 **전 기간 1893행**을 본다. 주제는 같고 관측 범위가 다르다.

## interpretation

형석이 "잘못 연결된 부분을 찾아달라"고 했다. 판정 기준이 DB 안에 있으므로(연결된 인물 식별자가
그 프로젝트 인물 명단에 있는가) 밤이 닫을 수 있다.

## 선기입 수용 기준 (실행 전 기록)

1. `shots.characters` 전 행을 프로젝트별 인물 명단과 대조한다. 명단은 `id`·`character_id`·
   `name` 을 모두 유효 식별자로 인정한다(어느 것으로 저장되는지 코드가 갈리므로 관대하게).
2. 명단 밖 식별자를 가진 행 수와 소속 프로젝트를 센다.
3. 읽기 전용 — 운영 데이터 쓰기 0건.
4. 기존 발견과 같은 모집단이면 새 결함으로 세지 않고 연결만 한다.

## 결과 카드

- 판정: **pass** — 수용 기준 4항목 충족. **새 결함 0건**
- created_at: 2026-08-18T02:56Z · estimated_review_min: 2 · reviewed_min: — · carryover_min: —
- 지출: $0 · DB 읽기 전용 2쿼리 · 쓰기 0건

### 확인한 것

| 항목 | 수 |
|---|---|
| 전체 샷 | 1893 |
| 인물이 연결된 샷 | 1507 |
| 연결이 명단 안 (정상) | 1491 |
| **명단 밖 식별자를 가진 샷** | **16** |
| 그 16행이 흩어진 프로젝트 수 | **1** |

16행 전부 프로젝트 `dc531572-50a4-4985-b343-d32e2b4490b0` 하나에 몰려 있고, 생성 시각은
전부 **2026-08-12**다. 명단 밖 식별자는 `girl`·`tracker` 두 개뿐이다.

**이건 이미 알려진 16행이다.** `verify-f003-run-chat-guards` 가 2026-08-16 밤에
"배포 이전은 16행/17회 girl·tracker 검출"로 기록했다. 이번 전수 확인이 더한 사실은
**그 16행이 전 기간·전 프로젝트를 통틀어 전부라는 것** — 다른 프로젝트로 번지지 않았고
2026-08-12 이후로 새로 생기지도 않았다.

감사 스크립트: `research/experiments/night-2026-08-18-v2-dynamic-spec/audit-shot-cast.mjs`

### 확인 못 한 것

- 그 16행을 **고치지 않았다**. 운영 데이터 쓰기는 비가역 행동이라 밤의 hard-stop 이다(§7.1).
- 형석이 화면에서 본 "잘못 연결"이 이 16행인지, 아니면 **명단 안이지만 그 장면에 없어야 할
  인물**이 붙은 경우인지는 구분하지 못했다. 후자는 "명단에 있는가"로는 못 잡고 장면 내용과
  대조해야 하는데, 그건 사람 판단이 필요한 축이다.

### 다음 조치 — 오너/형석 판단 필요

(1) 알려진 16행(개발용 프로젝트)을 정리할지 그냥 둘지. (2) 형석이 본 증상이 위의 "후자"라면
관측을 다시 정의해야 한다 — 그때는 어떤 샷의 어떤 인물이 이상했는지 **한 건만 지목**해주면
그 자리부터 좁힐 수 있다.

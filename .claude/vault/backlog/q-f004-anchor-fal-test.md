# 3D 스타일 앵커 교체 — 프로덕션(fal) 검증은 오너 새 프로젝트 테스트

```yaml
id: q-f004-anchor-fal-test
source: fixlog:F-004 (원문: .claude/vault/_archive/_FIXLOG.md)
kind: (해당 없음 — 오너 육안 판정)
blockers: [human-labor:새 3D 프로젝트 생성 후 시트 육안]
status: needs-owner
priority: high
```

- **맥락 (사람 말로)**: "3D 애니메이션" 스타일이 전혀 안 지켜지던 원인은 배선이 아니라 **앵커 그림
  실물**이었다 — 화면에서 고르는 미리보기와 모델이 실제로 받는 그림이 다른 물건이었다. 앵커를
  교체했고(`real_3d_v2.png`, 롤백은 UPDATE 1회) 매체어 수리 3건도 배포됐다. 지금까지 확인된 방향성은
  전부 higgsfield 경로 실측이고, **프로덕션 생성은 fal** 이라 같은 효과가 나는지는 미확인이다.
- **할 것**: 새 3D 프로젝트를 만들어 시트를 생성하고(교체 후 5분 캐시가 이미 지났음), 미리보기
  레지스터(스타일라이즈드 3D 캐릭터)에 부합하는지 육안 판정. 1차 실측(2026-08-13 `1e166e55`)은
  "개선 확인"이었으나 시트 간 시간대 표류(F-006)가 겹쳐 있어, F-006 수리 후 재생성까지 봐야 완결.
- **같이 볼 것 (프롬프트 확인은 `generation_jobs.input_snapshot`)**:
  ① 시트 프롬프트에 `art_style` 토큰이 **살아 있고** `photorealistic` 이 **없는지** (B4가 제대로 뒤집힘)
  ② `shot_video` 프롬프트에 Arri 기재 문구가 **없는지** (B7)
- **후속 판단 (이 판정 결과로만)**: B10(`image_size` 죽은 값 살리기 — 캔버스가 실제로 바뀌므로 크롭
  가드와 함께 재검증 필요) · B12(그리드에서 앵커 위치 4→1번 — "FIRST = 러프 시트" 계약과 충돌).
- **닫히는 조건**: 오너 육안 "부합" 판정. 미달이면 별도 조사(용의선상: B12 + 캐릭터 시트발 재전파).

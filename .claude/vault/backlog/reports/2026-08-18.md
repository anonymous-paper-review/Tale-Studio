# 밤 실행 기계 보고 — 2026-08-18

## 실행 상태

| 항목 | 값 |
|---|---|
| run_id | `night-2026-08-18-b3d63b8d6e5342b6a76bfdead586277e` |
| provider | claude (primary, status `claimed` → 실행) |
| contract_hash | `86d3be3fdcb41ea622c18c53df1825a49574157723f122cd586a4c5d814f9b6c` |
| 자율성 레벨 | **1 — 사실 기계** (자가 머지 금지 · 유료 발주 0 · 판정형 메모는 질문 카드) |
| snapshot_id | `751c326ecb3d456facf594118ca278688dd12e0f3dfce4d90c6466957327f6b5` |
| snapshot_fingerprint | `12dec5aac06db4409dca28640ec3ad637b247093ee5b2e91063ef3858ad3d495` |
| snapshot 범위 | byte [0, 9531) · content sha256 `7b17807fb8d5f044` |
| 기준 커밋 | `36cedb67b6c4a6434b9727a7807f69ed351667e3` |
| inbox 동기화 | `changed:false` · `friend_input_present:false` · remote/local 동일 `36cedb67` |
| 실행 시각 | 시작 2026-08-18T02:39Z · 보고 2026-08-18T03:0xZ |
| 종료 사유 | 계획한 단위 전부 종료 |

**계약 이탈 1건 (명시)**: 계약 §12는 "오전 8시가 되면 새 단위를 잡지 않는다"고 한다. 이 실행은
오너가 **11:39 KST(02:39 UTC)에 직접 발사**했고 provider gate 가 6시간 lease 로 새 claim 을
발급했다. 8시 경계는 계약이 스스로 "처리량 조절이며 안전상 hard-stop 과 혼동하지 않는다"고
규정한 조항이라, 오너의 직접 지시를 우선해 실행했다. 비가역 행동·예산 두 hard-stop 은 준수했다.

## 입력

| 출처 | 내용 | 동기화 commit |
|---|---|---|
| owner inbox | 메모 5건 (힉스필드 비교 + 실행 프롬프트 전문, 입력창 유연화, 프로젝트 상속, 뮤비·광고·웹툰 해석기, 원격저장소·harvest 상태관리 + 밤런 목적 정의) | `a4c1b81` (08-17 23:19) |
| friend inbox (형석) | 제품 항목 22건 | `b8fae0a` (08-18 11:37) — **이 실행 이전에 이미 동기화됨**. 이번 실행의 sync 는 `friend_input_present:false`(새 push 없음) |
| owner harvest | 세션 15건 다이제스트 + 기각 순간 후보 23건 | 창 83.1h (마지막 성공 이후 82.1h 복구) |

## 단위별 한 줄 결과

| 단위 | 종류 | 판정 | 한 줄 |
|---|---|---|---|
| n1 진행률 합산 | 조사 | **pass · 가설 참** | 진행률 5자리 중 3자리가 "이번 묶음"이 아니라 "프로젝트 전체"를 센다 (`pipeline-progress.ts:47-68,111-132,135-153`) |
| n2 채팅 상태 소실 | 조사 | **pass** | 네 상태 전부 메모리 전용. 단 (c)(d)는 DB 진실값으로 재계산돼 다시 뜬다 — (d)가 참조 구현 (`handoff-nudge.ts:1-6`) |
| n3 writer 재요청 갇힘 | 조사 | **pass · 단 원인이 다름** | `completed` 는 안 갇힌다. 갇히는 건 `awaiting_confirmation` — 재요청 409·resume noop·watchdog 대상 밖 3중 (`start/route.ts:157`, `resume/route.ts:43-45`, `watchdog/route.ts:33-37`) |
| n4 Direction·END 생성 시점 | 조사 | **pass** | 러프 3프레임 미완성 샷은 Direction·END 가 영영 안 생기고 정상 종료된다 (`generate-storyboard/route.ts:80-88,166-175`). "END 빼기" 분기는 **없고**, 현재는 END 를 넣고 DIRECTION 을 뺀다 |
| n5 writer·director 복원 대조 | 조사 | **pass · 전제 일부 반전** | 경과 시간 표시는 **director 에만** 있다(전제와 반대). director 탭 미복원은 버그가 아니라 기록된 오너 결정 (`director-store.ts:3103-3105`) |
| n6 producer 입력 경직도 | 조사 | **pass · 부분** | 모델 해석 경로는 있고, 막는 건 확장자 화이트리스트 6종 (`upload/limits.ts:59-82`). 텍스트·docx 가 모델을 건너뛰는 건 **의도된 결정** (`global-chat.tsx:592`). 연출 축 해석기는 없음 |
| n7 프로젝트 상속 | 조사 | **pass · 없음 확정** | 상속 장치 부재. 근접 인프라 `inventory_items` 는 쓰기 호출부 0건 + 진입 UI `disabled` |
| n8 인물 연결 전수 감사 | 조사(DB) | **pass · 새 결함 0** | 1893행 중 명단 밖 16행, 전부 한 프로젝트·2026-08-12. 기존 `verify-f003` 과 같은 모집단 |
| r1 v2 dynamic_spec 모양 | **수리** | **pass · awaiting-merge-review** | 문자열 연출값이 영상 모션 계약을 TypeError 로 끊던 것 수리. 실측 8행. 커밋 `18cf72d` |
| r2 오디오 기본값 주석 | **수리** | **pass · awaiting-merge-review** | 주석 "전 모델 OFF" vs 코드 전부 true. 커밋 이력으로 코드가 의도임을 확정 후 주석 수정. 커밋 `386af9e` |

## 수용 기준 준수

모든 단위가 실행 **전에** 수용 기준을 티켓에 선기입했다. 소급 수정 0건.
r1 은 "재현 테스트가 수리 전 실패·수리 후 통과"를 기준으로 걸었고 둘 다 관측했다.

## 지출

| 항목 | 금액 |
|---|---|
| 유료 생성 발주 | **$0** (레벨 1 — 0건 강제) |
| 모델 조사 (subagent 7 + 주 실행) | 토큰 사용, 생성 API 청구 없음 |
| DB | 읽기 전용 4쿼리 · 쓰기 **0건** |
| **합계 청구** | **$0** |

일일 한도 $50 대비 잔액 $50. 예약·초과 0건.

## 막힌 것 · 복구

막힌 단위 **0건**. 도구 실패 0건. 계약 §8.1 진단 분류 발동 0건.

의도적으로 **하지 않은 것**(레벨 1 경계):
- 유료 영상·그림 발주 0건 — 형석의 "목각 인형 성능 테스트", "END 빼고 영상 테스트",
  "previz 다른 각도" 3건은 발주가 있어야 닫히므로 실행하지 않았다.
- 운영 데이터 쓰기 0건 — r1 이 발견한 깨진 8행, n8 의 16행 모두 **고치지 않았다**(§7.1 비가역).
- 자가 머지 0건 — 수리 2건 전부 `awaiting-merge-review`.

## 지표

```text
reviewed_total        = 0
merged_reviewed       = 0
reviewed_merge_rate   = unknown   (분모 0)
self_merge_reviewed   = 0
human_merge_reviewed  = 0
```

이번 실행이 만든 카드 10장은 전부 미리뷰라 분모에 넣지 않는다.
머지 대기 작업 사본 2개(`night/2026-08-18-v2-dynamic-spec`, `night/2026-08-18-audio-default-doc`)도
만료·분모 대상이 아니다.

레벨 2 승급 조건 대비 현황: 정식 밤 실행 누적 미달, `reviewed_merge_rate` 산출 불가(리뷰 0건).
**승급 제안하지 않는다.**

## 산출물 경로

- 티켓·결과 카드: `.claude/vault/backlog/tickets/n1..n8, r1, r2`
- 사람 보고서: `.claude/vault/backlog/reports/2026-08-18.html`
- 감사 스크립트: `research/experiments/night-2026-08-18-v2-dynamic-spec/audit-dynspec.mjs`, `audit-shot-cast.mjs`
- 수확: `.claude/vault/backlog/sweep/2026-08-18/runs/night-2026-08-18-b3d63b8d6e5342b6a76bfdead586277e/`
- 작업 사본: `.claude/worktrees/night-0818-v2dynspec`, `.claude/worktrees/night-0818-audiodoc`

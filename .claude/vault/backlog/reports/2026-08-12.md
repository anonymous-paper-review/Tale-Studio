# 밤 러너 원장 — 2026-08-12

> 계약: `.claude/vault/backlog/_RUNNER.md` · 예산·불변 규칙: `_NIGHT.md`
> 사람용 리포트(HTML): `.claude/vault/backlog/reports/2026-08-12.html`
> 아티팩트: https://claude.ai/code/artifact/410d6d59-5991-4de0-a42a-1f19d17bde20
> 설계도 아티팩트(9번 티켓 산출): https://claude.ai/code/artifact/2cfd5299-de7d-4ae6-9140-55324665c5ee
> 이날 새벽 01:30 스위퍼 산출은 별도: `2026-08-12-sweep.md` / `2026-08-12-sweep.html`

## 티켓별 1줄 (실행 순서 = 우선순위 → T0 → T1 → T2)

| # | 티켓 | 판정 | 발동한 기각 조건 | 지출 |
|---|---|---|---|---|
| 1 | t0-dialogue-join-canary | **가설 유지** — 픽스 이후 런 3건(분할 2건)에서 대사 오배치 0, 저장 대조도 0 | 미발동 | $0 |
| 2 | t0-shotcheck-issue-volume-correlation | **가설 유지(잠정)** — 유효 n=10, 피어슨 지적 0.946/0.918 > 샷수 0.561 | 미발동 | $0 |
| 3 | t0-scene-schema-typo-prevalence | **가설 유지** — 위반 2씬/2런(다른 프로젝트·다른 날짜), 1건은 완료 런에서 조용한 유실 | 미발동 | $0 |
| 4 | t0-loose-schema-unguarded-fields | **가설 유지** — 무방비 접근 7건/4자리/3파일 (기각선 2 초과) | 미발동 | $0 |
| 5 | t0-previz-pump-stall-prevalence | **형식상 유지·기제 미지지** — 완주 1.0이 81.5%(<90%)지만 부분 완성은 0건 | 미발동 | $0 |
| 6 | t0-remaining-open-api-routes | 🔴 **가설 기각** — 사용자 인증 없는 9개 중 3개가 유료 호출·DB 쓰기 | **발동** | $0 |
| 7 | t0-serverless-path-parity | **가설 기각(1절)** — 진입점이 서로 다른 지휘자 함수로 갈라짐. 튜닝 누락(2절)은 미발동 | **발동** | $0 |
| 8 | t0-camera-spec-vs-board-mismatch | **측정 불가·모집단 부재** — 티켓 표기가 DB에 0건, 대상 3샷 전부 판정용 프레임 없음 | 발동 불가 | $0 |
| 9 | t1-ax-blueprint | **산출 완료** — 운영 루프 설계도 발행(실험 아님, 산출 계약으로 판정) | 해당 없음 | 외부 검색 6건 |
| 10 | t0-storyboard-ref-resolution | **가설 유지** — 표본 48/48이 짧은 변 720 미만. 원인은 시트를 칸으로 자르는 단계 | 미발동 | $0 |
| 11 | t0-verb-sequence-classify | **유보** — 순차 67.8%(사전 등록 50~70% 구간) | 미발동 | ~$0.02 |
| 12 | t0-llm-calls-persistence-trace | **가설 기각** — 예외 경로 저장이 4곳 존재. 단 실패 3/3에서 스냅샷 0건 | **발동** | $0 |
| 13 | t0-physics-threshold-enforcement | **가설 유지** — 코드 강제 1/8(12.5%), 그 1개도 정본 상수를 안 읽고 숫자 복제 | 미발동 | $0 |
| 14 | t0-constraint-target-routing-effect | **가설 유지** — 노트 12건 전부 시각 분류, 누출 0 | 미발동 | $0 |
| 15 | t0-lint-residual-scope | **가설 유지** — `src/` 에러 0. 단 총량 213(기준선 104, +105%) | 미발동 | $0 |
| 16 | t0-analytics-collection-path | **NA(판정 불가)** — 수집 경로 404(사전 등록대로 기각 아님). 문턱 통과는 확인(스크립트 200), 기록 적재는 미확인 | 미발동 | $0 |
| 17 | t2-parallax-foreground | **관찰 수집 완료**(판정·점수 금지 티켓) — 2암 + 대조 재사용 | 해당 없음 | **$2.73** / 캡 $6 |
| 18 | t2-bg-viewsheet-from-3d | **관찰 수집 완료·판정 보류**(판독 세트 부재 — 티켓 조항). 뷰 시트 팔이 참조 렌더의 **그림체를 그대로 가져옴** | 해당 없음 | **$6.05** / 캡 $8 |

## 지출 합계

| 항목 | 금액 |
|---|---|
| T0 관측 16건 | **$0** (전부 읽기 전용 — 새 생성 호출 0) |
| t0-verb-sequence-classify 분류 1콜 | ~$0.02 |
| t1-ax-blueprint | $0 (외부 검색만, 생성 발주 0) |
| t2-parallax-foreground | $2.7296 (영상 2편 $1.2698×2 + 변형 시작 그림 1장 상한 추정 $0.19) |
| t2-bg-viewsheet-from-3d | $6.0480 (영상 4편 × 5초 × $0.3024) |
| **합계** | **≈ $8.80** (T2 두 건 모두 티켓 승인 금액 이내: $2.73/$6 · $6.05/$8) |

쿼터: 밤 실행이라 유저 트래픽과 비경합. RPD 소모는 분류 1콜 + 영상 발주뿐으로 미미.

## 발동한 기각 조건 (3건)

1. **t0-remaining-open-api-routes 🔴** — "무인증 라우트 중 유료 생성 호출 또는 DB 쓰기가 1건이라도 있으면 기각".
   `fal/webhook`(서명 검증 보유·DB 쓰기), `writer/step`·`writer/watchdog`(서버 비밀 fail-closed·유료 파이프라인 트리거).
   셋 다 로그인이 아닌 다른 문지기를 갖고 있어 "누구나 부를 수 있다"는 뜻은 아니다 — 어제 선언("남은 것은 전부
   무해한 읽기 전용")이 사실과 다르다는 쪽이 정확한 결론.
2. **t0-serverless-path-parity** — "두 진입점이 다른 함수로 갈라지면 기각". 로컬 측정은 `runPipeline`,
   실사용은 `runWriterSteps`. 단 두 번째 절(튜닝 누락)은 미발동 — 동시성 3노브·전송 튜닝 모두 서버리스에도 적용됨.
3. **t0-llm-calls-persistence-trace** — "중단 경로 저장이 1곳이라도 있으면 기각". 4곳 존재.
   단 실측에서는 실패 런 3/3의 스냅샷이 비어 있었고, 실제 사망 다수인 "예산 초과" 경로에는 저장이 0곳.

## draft 제안 (실행 안 함 — 아침 자격 심사행)

- **참조 해상도 승격 실험(T2 제안)** — 같은 샷으로 ⓐ 현행 칸 그림(374급) ⓑ 3배 확대 ⓒ 큰 시트에서 재크롭
  3팔 비교. 근거: `t0-storyboard-ref-resolution`(표본 48/48이 720 미만, 시트 원본은 1664×928).
- **카메라 명세↔보드 대조 티켓 재작성** — 표기를 DB 실제 어휘(`left`/`right`)로 바꾸고, 판정 대상을
  "시작·끝 그림"이 아니라 "러프 그림의 손글씨"로. 근거: `t0-camera-spec-vs-board-mismatch` 모집단 부재.
- **호출 기록에 작업 식별자 추가 + 예산 초과 경로 스냅샷** — 근거: `t0-llm-calls-persistence-trace`.
- **검사 제외 경로 오타 수정**(`.worktrees/**` → `.claude/worktrees/**`) — 근거: `t0-lint-residual-scope`(70건).

## 러너 자신에 대한 관측 (아침 참고)

- **계기 결함이 4번 났고 4번 다 사람 대조로 잡혔다** — 대사 조인(저장 id 정규화 미반영), 열린 API(헬퍼 미추적),
  물리 임계값(리터럴 전건 오탐), 제약 라우팅(영어 대명사 오탐). 그중 둘은 **교정 전이면 판정이 뒤집혔다.**
  → 규칙 후보: "판정 도구는 답을 아는 사례로 먼저 시험한다"(이번 밤 4/4에서 그 절차가 결함을 잡았다).
- **낡은 좌표로 밤을 낭비한 티켓이 1건**(t0-camera-spec-vs-board-mismatch) — 실행 전 좌표 실재 검사가 있었으면
  즉시 blocked 로 갈 수 있었다. 설계도(9번 티켓)의 1순위 제안이 이것이다.
- `t0-spec-blender-compile-probe`는 `blocked`(오너 승인 대기: 로컬 Blender 설치)로 남아 있으나,
  **이 머신에는 Blender가 이미 설치돼 있다**(`/opt/homebrew/bin/blender`, 5.2.0). 블로커가 스테일일 수 있다 —
  러너는 blockers 비어있지 않은 티켓을 실행하지 않으므로 손대지 않았다.

## 생성 실험 2건의 결함 보고 (숨기지 않는다)

- **t2-parallax-foreground** — ⓑ암의 변형 시작 그림이 전경 제거뿐 아니라 **해상도까지 달라졌다**
  (379×257 → 1088×608). ⓑ가 안정적으로 보이는 이유가 전경 제거인지 해상도인지 이 실험으로는 못 가린다.
  같은 밤의 `t0-storyboard-ref-resolution`과 함께 읽어야 한다.
- **t2-bg-viewsheet-from-3d** — ① 발주에 쓴 3D 스냅샷 5장이 **최종본 이전 중간본**이고 그중 1장(증인석
  로우앵글)은 카메라가 벤치 안에 박힌 불량 프레임이었다. 러너가 발주 경합을 막으려 동결한 시점이 3분 빨랐다.
  재발주는 승인 금액 초과($9.07 > $8)라 하지 않았다. 최종본은 `views/`, 실제 사용본은 `views_frozen/`에 둘 다 보존.
  ② 티켓이 요구한 "카메라가 공간을 훑는 샷"을 고를 수 없었다 — 이 로케이션 54샷에 **카메라 모션 계약이
  0건**이라 판정 데이터가 없다. 배경 수요 실측 기준(wide+mid)으로 대체했고, 그 결과 **원래 실패가 보고된
  조건(측면·대각·하이앵글 큰 회전)은 이번에 시험되지 않았다.**
  ③ 부수 실측: 이 로케이션 54샷은 **생성 프롬프트가 전부 비어 있다**(0/54).

## 아침 카드 갱신 (`_MORNING.md`)

- **Q13**(서버리스 카나리아)에 밤 준비물 추가 — 두 경로 대조 결과가 카나리아가 재야 할 것을 좁혔다.
- **신규 Q18** 🔴 로그인 없이 열린 창구 3건 — 운영 비밀번호 2개 설정 여부 확인 요청.
- **신규 Q19** 시작 그림 해상도 — 시트를 키울 것인가(3팔 실험 제안 포함).
- **신규 Q20** 검사 초록불 — 제외 경로 오타 한 줄과 실험 폴더 방침.

## 집행 요약

- **예정 ready 티켓 18건 전부 소진 — 미소화 0건.** 내역: T0 관측 15건 + T0 공개 경로 확인 1건 = 16 · T1 설계도 1 · T2 생성 실험 2.
- 기각 조건 발동 3건 / 측정 불가·모집단 부재 1건 / 유보 1건 / NA 1건 — 나머지는 가설 유지.
- 제품 코드(src/) 수정 0건 · DB 쓰기 0건 · 커밋 0건(밤 규칙 준수).
- 계기 결함 4건은 전부 사람 대조로 교정 후 판정했고, 그 사실을 각 결과 문서에 남겼다.

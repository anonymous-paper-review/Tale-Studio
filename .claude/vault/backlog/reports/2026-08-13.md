# 밤 러너 원장 — 2026-08-13

**한 줄**: 실행할 티켓이 0개였다. 그 자체가 오늘의 산출이다 — 큐가 빈 이유를 좇다가 그물의 결함 2건을 잡았고, 그중 하나는 **증거를 영구 소실시키는 결함**이라 즉시 복구했다.

- **실행 시각**: 2026-08-13 02:00 KST 기동 (Orca automation `43751f8d`)
- **지출: $0.00** (T1 예산 $10 중 0 사용 · T2 발주 0건 · 생성 API 호출 0회)
- **쿼터**: 실험용 모델 호출 0회 (RPD 소모 없음)

---

## 티켓별 1줄

| 티켓 | 판정 | 발동한 기각 조건 | 지출 |
|---|---|---|---|
| — | **집을 티켓이 0개** | 해당 없음 | $0 |

`status: ready` 티켓 **0개**로 Goal 1항이 기동 시점에 이미 충족돼 있었다. 추측으로 티켓을 만들어
메우지 않았다(사유는 아래 "왜 안 만들었나").

### 원장 전수 (42장)

| status | 수 | 비고 |
|---|---|---|
| `done` | 36 | 8/11~8/12 밤에 전부 소진됨 |
| `blocked` | 3 | 전부 오너 관문 — 아래 |
| `superseded` | 3 | `t2-contract-generalize`로 통합된 모션 3종 |
| **`ready`** | **0** | **← 오늘 밤 큐** |

### blocked 3건 (밤이 열 수 없음 — 변화 없음)

| 티켓 | 막힌 지점 | 해제 조건 |
|---|---|---|
| `t0-spec-blender-compile-probe` | 로컬 Blender 설치 = 머신 상태 변경 | 오너 승인 (`_MORNING.md` Q14) |
| `t2-locked-compliance-static` | 9클립 ~$7 미승인 | 오너 승인 (`_MORNING.md` Q3) |
| `t1-leadback-beat-automation` | (리드백 서술, beat, 수동 판정) 트리플이 로컬 디스크에 부재 — 아티팩트 HTML에만 존재 | 오너가 아티팩트→기계가독 추출 승인, 또는 원본 outDir 지정 |

---

## 큐가 빈 이유 — 확인한 사실만

### ① 01:30 그물이 오늘 밤 산출을 안 냈다

| 확인 항목 | 관측 |
|---|---|
| automation `7dfda6b6` 상태 | `enabled`, `FREQ=DAILY;BYHOUR=1;BYMINUTE=30` — 정상 |
| `sweep/2026-08-13/` (기동 시점) | **부재** |
| `sweep/.last-success` (기동 시점) | `1786469127.283189` = **2026-08-12 02:25** — 하루치 안 움직임 |
| 오늘 01:30 슬롯 | 이미 경과 (기동 시각 02:00) |

→ 오늘 01:30 실행은 **산출을 하나도 남기지 않았다.** 안 떴는지, 떠서 초반에 죽었는지는
러너가 있는 자리에서 구분 불가(automation 실행 로그 조회 커맨드 부재 — `orca automations runs` 는 `Unknown command`). **NA로 남긴다.**

### ② 그물이 떴어도 러너는 여전히 0이었다 — 구조 문제

스위퍼 계약 §5는 산출 티켓을 **`draft` 고정**으로 묶고, 러너는 `ready`만 집는다.
즉 **그물이 찾은 것은 아침에 사람이 승격하기 전엔 어떤 밤에도 실행되지 않는다.**

8/12 밤이 돌았던 건 그물이 12건을 `ready`로 찍었기 때문인데, 그 근거였던
"오너가 T0 read-only 자동 ready를 허용했다"는 **8/12 검토에서 스위퍼의 창작으로 판명**됐다
(원문: "그런 허용은 없었습니다. … 조수의 제안을 오너의 결정으로 승격시킨 겁니다").

→ **그 구멍이 막힌 지금, 승격 경로가 없으면 밤 러너는 매일 0건이다.** 오너 결정 필요 (아침 카드로).

> ⚠️ 다만 automation `7dfda6b6`의 **프롬프트 마지막 줄에는 실제로 `자동 ready는 T0 read-only만`이 들어 있다.**
> 8/12 검토는 계약서(`_SWEEP.md`)와 대화만 확인하고 automation 프롬프트는 안 봤다.
> "창작"인지 "발사 프롬프트에 있던 지시를 출처만 잘못 댄 것"인지는 **오너만 판정 가능** — 러너는 NA로 둔다.
> 어느 쪽이든 위 ②의 결론(승격 경로 결정 필요)은 동일하다.

---

## 🔴 결함 1 — 그물의 성공 도장이 너무 일찍 찍힌다 (증거 영구 소실)

`_SWEEP.md:48` 의 약속:

> 성공 도장은 산출이 다 끝난 뒤에만 찍히므로, 도중에 죽은 밤은 성공으로 세지 않는다.

**코드는 그렇게 안 한다.** `harvest.py:322` 는 **수확이 끝나는 즉시** 도장을 찍는다 —
스위프 노트·티켓·리포트가 하나도 없는 시점이다.

```python
# harvest.py:319-323
# 성공 도장은 맨 마지막에 찍는다 — 도중에 죽으면 안 찍히고 ...
if not a.now:
    with open(stamp, "w", encoding="utf-8") as fh:
        fh.write(str(now))
```

주석이 의도를 정확히 적어놨는데 **위치가 스크립트 끝일 뿐 스위프 끝이 아니다.**
수확 이후 단계에서 죽으면 도장은 이미 찍혀 있고, 그 창의 세션들은 다음 밤 창 밖으로 밀려
**영영 그물에 안 걸린다.** 8/12 세션에서 오너가 요청한 failsafe("하루라도 건너뛰면 영영 안 걸리는 구멍")는
**빠진 실행만 복구하고 반쯤 죽은 실행은 복구 못 한다.**

### 오늘 이 결함을 실물로 재현했다 (그리고 복구했다)

큐가 빈 원인을 보려고 러너가 `harvest.py`를 읽기 전용 진단으로 1회 실행했다.
스위프 산출은 하나도 안 만들었는데 **도장이 `08-12 02:25 → 08-13 02:03`으로 전진**했다.
그대로 뒀으면 수확된 **미warp 세션 8개가 내일 스위퍼의 창 밖으로 영구 이탈**한다.

→ **`git checkout` 으로 도장을 원래 값(`1786469127.283189`)으로 복원 완료.** 내일 01:30이 정상 재수확한다.
→ 수확물은 증거로 남기되 스위프로 오인되지 않게 경고문을 붙였다: `sweep/2026-08-13/README.md`

**처방(실행 안 함 — 제품 코드 아니지만 티켓 밖 수정이라 제안만)**: 도장 쓰기를 `harvest.py`에서 떼어내
스위퍼 에이전트가 리포트까지 낸 뒤 호출하는 별도 커맨드(`--stamp-success`)로 분리.

---

## 결함 2 — automation 실행 이력을 조회할 수단이 없다

`orca automations runs <id>` 는 `Unknown command`. `show` 는 다음 발사 시각만 준다.
그래서 "①이 안 떴는지 떠서 죽었는지"를 **원리적으로 구분할 수 없었다.**
무인 야간 파이프라인에 실행 이력 조회가 없으면 오늘 같은 무산출 밤의 원인 규명이 매번 NA로 끝난다.

---

## 그물에 걸려 있는 것 (수확만 — 분류·티켓화 안 함)

진단 수확이 잡은 **미warp 세션 8개 / 44개 스캔** · 기각 순간 후보 24건.
내일 01:30 스위퍼가 정상 처리한다. 러너는 목록만 보고한다.

| 저장소 | 세션 | 크기 | 기각후보 | 첫 발화 |
|---|---|---|---|---|
| claude | `f1021d8e` | 31MB ⚠️oversized | 18 | 밤사이 가설·실험 자동화 워크플로우 설계 (AX·백로그·스위퍼 설계 당사자) |
| claude | `c0438f2a` | 3.1MB | 1 | 커밋·푸시·원격 동기 → 밤 파이프라인 설명·러너 on/off |
| claude | `af1fb40f` | 2.4MB | 4 | 카메라 지시 6케이스 격리 실험 결과 ($4.20 지출분 판정 대기) |
| gjc | `019ff5ac` | 1.2MB | 1 | writer 아키텍처 전면 재설계 (인지 복잡도 한계) |
| claude | `a67a54c8` | 803KB | 0 | Vercel/로컬 환경변수 차이·죽은 기술 확인 |
| claude | `eb29cfa3` | 520KB | 0 | viz 단계 firstframe 생성 워크플로우 |
| claude | `388e1942` | 286KB | 0 | gjc 세션 인덱스 로그 손상 예외 |
| gjc | `019ff588` | 29KB | 0 | gjc update |

---

## 왜 티켓을 안 만들었나 (러너의 권한 경계)

만들 수 있었고, 만들면 오늘 밤이 채워졌다. 안 만든 이유는 하나다 —
**8/12 검토의 🔴 최우선 지적이 "스위퍼가 오너 승인을 지어냈다"였다.**

러너가 (a) 티켓을 만들고 (b) 스스로 `ready`를 찍고 (c) 그걸 실행하면,
방금 지적된 거버넌스 사고의 **더 나쁜 판본**이 된다 — 생성·승격·집행이 한 세션 안에서 닫히고
사람 관문이 0개가 된다. `_RUNNER.md` 절차 2·금지 3항, `_NIGHT.md` 불변 8항에도 정면으로 어긋난다.

그래서 **수확(읽기 전용, $0)까지만 하고 판단은 아침으로 넘겼다.**

---

## draft 제안 (실행 안 함 — 아침 자격 심사행)

밤 규칙 8항대로 **초안 제안까지만**. 티켓 파일은 만들지 않았다.

1. **도장 분리** — 위 결함 1의 처방. 산출물이 "판정"이 아니라 "동작하는 물건"이라 `_SWEEP.md` 판별표상
   **가설이 아니라 T-fix/카드**다. 브랜치 준비까지만 시킬 거면 T-fix.
2. **승격 경로 결정** — 위 ②. "A와 B 중 뭘 할까"라 **가설이 아니다 → 아침 카드**.
3. **automation 실행 이력** — 결함 2. 도구 제작이라 **가설 아님 → 카드**.

셋 다 `_SWEEP.md` "티켓으로 만들지 말아야 할 것" 판별표에 걸린다 — 그래서 티켓이 아니라 카드로 올린다.

---

## 사람용 리포트

- `.claude/vault/backlog/reports/2026-08-13.html`
- Artifact 발행: https://claude.ai/code/artifact/ce70e16a-f9a0-498a-bbd5-854b70dd0aff

## 러너가 건드린 파일 (전부 `research/` 이하 · `src/` 0건 · DB write 0건 · vault 0건)

| 파일 | 무엇 |
|---|---|
| `.claude/vault/backlog/sweep/2026-08-13/` | 진단 수확물 (신규, 미추적) + 경고 README |
| `.claude/vault/backlog/sweep/.last-success` | 진단이 밀어놓은 도장 **복원** (순변화 0 — HEAD와 동일) |
| `.claude/vault/backlog/reports/2026-08-13.md` | 이 원장 |
| `.claude/vault/backlog/reports/2026-08-13.html` | 사람용 리포트 |
| `.claude/vault/backlog/_MORNING.md` | "밤 준비물" 링크 갱신 (절차 5③) |

`package.json` 의 기존 수정과 미추적 실험 폴더 10개는 **러너가 건드리지 않은 오너의 작업물**이다. 그대로 뒀다.
브랜치 전환·커밋·푸시 0건 — 아침 체크아웃은 기동 전과 동일하다.

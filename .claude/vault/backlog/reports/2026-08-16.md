# 2026-08-16 밤 루프 — 기계 리포트

기준 커밋 `fb1ad7c` · 시작 01:31 KST · 새 티켓 착수 마감 08:00 KST

## ⓪ 목적지

`.claude/vault/destination/_NOW.md` 읽음 (마지막 갱신 2026-08-15, 오너 작성).
정렬 기준: 완주율 · 재생성 불필요율 두 바늘 중 하나를 움직이는 일감이 앞.

## ① backlog 상태 조사 (착수 시점)

| 상태 | 건수 |
|---|---|
| ready | 1 |
| waiting | 21 |
| needs-owner | 21 |
| draft | 4 |
| blocked | 3 |
| done / superseded | 나머지 |

### waiting 해제 조건 확인 (읽기 전용 조회)

| 티켓 | 조건 | 실측 | 판정 |
|---|---|---|---|
| verify-f001-grid-identity | 2026-08-12T11:13Z 이후 `storyboard_real_grid` 잡 존재 | 21건 (completed 20 · queued 1), 최신 2026-08-14T03:30Z | 충족 → ready → running |
| verify-f002-split-inherit | `b32c4b2`(2026-08-12T10:46Z) 이후 `design_ref` null 샷 존재 | 0건 | 미충족 → waiting 유지 |
| verify-f003-run-chat-guards | 2026-08-13 이후 신규 프로젝트에서 writer 런 완료 | 프로젝트 2건(3036b333·a003a8c6), 런 2건 completed | 충족 → ready → running |
| verify-f005-prompt-fill | `0c8c61c`(2026-08-12T13:07Z) 이후 director 단계 프로젝트 존재 | 1건(3036b333, 2026-08-13T03:38Z). director 단계 전체 12건 | 충족 → ready → running |

### 미뤄둔 티켓 21건(`deferred-*`) 해제 조건 전수 확인

**주의 — 다음 밤을 위한 메모**: 이 21건의 꺼내는 조건은 `blockers:` 칸이 아니라 본문
**"언제 꺼내나"** 줄에 있다. `blockers:` 만 보면 전부 `[]` 라 조건이 없는 것처럼 보인다.
(오늘 밤 이 착각을 한 번 했다가 파일을 직접 읽고 정정했다.)

| 티켓 | 꺼내는 조건 | 판정 |
|---|---|---|
| deferred-api-writer-step-auth | "다음 세션 최우선" | **조건 충족 + 이미 코드로 닫힘** → `done` |
| deferred-shotcheck-channel-2 | "즉시 착수 가능(선행 n−1 완료)" | **조건 충족, 그러나 기능개발** → `needs-owner` |
| 나머지 19건 | 오너 판단 · 미착수 작업 의존 · 용량/품질 임계 | 조건 미충족 → `waiting` 유지 |

`deferred-api-writer-step-auth` 상세: 지목 결함(`if (secret && 불일치)` — 비밀값 미설정 시 가드
통째 건너뜀)이 fail-closed 로 교체됨. `src/app/api/writer/step/route.ts` 가 비밀값 없으면
프로덕션에서 500 거부, 개발에서만 통과. `watchdog` 도 `CRON_SECRET` 로 동일.
2026-08-12 `t0-remaining-open-api-routes` 판정("fail-closed 문지기 보유")과 일치.
**잔여**: 프로덕션 환경변수 실제 설정 여부는 배포 환경 정보라 확인 불가 — 다만 미설정 시
글쓰기가 프로덕션에서 동작 불가라 "조용히 열림"은 구조상 불가능.

### 소유권 가드 재확인 (원장 갱신 재료)

`ed3c4b7` 기준 원장이 "살아 있음"으로 판정한 심각 발견 22건 중 소유권 관련 6건
(색인 0·1·2·42·43·91)이 지목한 라우트 11개를 현재 HEAD `fb1ad7c` 에서 전수 확인:
**11개 전부 `requireProjectAccess`/`userOwnsProject` 보유.** 저장소 전체로는 41개 라우트가 사용.
→ 이 6건은 `fb1ad7c` 로 닫혔고 **원장은 낡았다.** 전수 재판정은 `t0-findings-ledger-recheck` 로 티켓화.

### draft 승격

| 티켓 | 승격 사유 |
|---|---|
| t0-grounding-pin-stage-coverage | 판정선 사전 등록 완료 · 예산 0원 · 선행 없음 → ready → running |
| t0-stuck-jobs-webhook-loss | 판정선 사전 등록 완료 · 예산 0원 · 선행 없음 → ready → running |
| t1-pipeline-wallclock-after-pin | 승격 안 함 — `depends:t0-grounding-pin-stage-coverage` 미해소 |
| t1-shotdesign-model-rollback | 승격 안 함 — `owner-decision:모델 수명 위험을 감수할지` |

### 착수한 티켓 (백지 subagent 위임, 티켓 경로만 전달)

- 묶음 A: verify-f001-grid-identity + verify-f003-run-chat-guards + verify-f005-prompt-fill (주제: 수리 배송 검증)
- 묶음 B: t0-stuck-jobs-webhook-loss
- 묶음 C: t0-grounding-pin-stage-coverage
- 묶음 D: audit-color-temp-ramp

## 티켓별 결과

| 티켓 | 판정 | 발동한 판정선 | 지출 |
|---|---|---|---|
| t0-stuck-jobs-webhook-loss | **가설 성립** (원인은 주소 변경으로 지목 안 함) | "12시간 이상 굳은 작업이 0건이면 기각" → 3건이라 불발. 이어 "고르게 흩어져 있으면 원인을 주소 변경으로 지목하지 않는다" 발동 | 0원 |
| t0-grounding-pin-stage-coverage | **가설 유지** | "세 단계 중 한 곳이라도 기본 모델로 가면 기각" → 기본 모델 0곳, 미발동 | 0원 |
| audit-color-temp-ramp | **생성 아티팩트 확정** | "값 4개 이상 씬 중 등차 씬이 2개 이상이면 생성 아티팩트" → 194개 중 69개, 발동 | 0원 |
| verify-f001-grid-identity | **통과** | "칸별 인물 배정문과 참고 그림 지정문이 둘 다 있으면 통과" → 20/20 | 0원 |
| verify-f003-run-chat-guards | **밤 몫 통과 · 가드 발동은 확인 못 함** | "신규 발생 0건이면 통과" → 배포 이후 72행 중 0건 | 0원 |
| verify-f005-prompt-fill | **통과** | "전 프로젝트 100%면 통과" → 12/12, 샷 486행 중 빈 행 0, 인계철선 로그 0건 | 0원 |
| t0-findings-ledger-recheck | **원장이 낡았다** | "다시 판정한 것 중 닫힘이 1건 이상이면 → 원장이 낡았다 확정" → 91건 재판정에 닫힘 8건, 발동 | 0원 |

### 상세

**t0-stuck-jobs-webhook-loss** — 굳은 작업 3건/952건(0.3%). 8/9 2건 + 8/13 1건으로 나흘 간격,
8/13 건은 앞뒤 이웃(31초 뒤)이 정상 완료 → 주소 변경 원인 지목 안 함. 업체 전수 대조 완료(조회만):
8/9 2건은 업체가 완성 그림 주소를 지금도 반환(돈 나갔고 결과물 존재, 우리만 모름), 8/13 1건은
업체가 "완료"라 답하나 결과물 요청에 영구 500. **덤: `src/lib/fal/reconcile.ts:85`
`isPermanentProviderLookupFailure` 가 4xx만 영구로 확정하고 5xx는 일시로 되돌려, 영구 500인 건은
회수 장치를 거쳐도 대기에서 못 빠져나온다 — "영영 진행 중"의 두 번째 경로.**
티켓 좌표 오류: "완료 14건만 잔존"은 사실과 다름(실제 952건). 이를 믿었으면 3건 전부 놓쳤음.

**t0-grounding-pin-stage-coverage** — 네 자리(드라마투르그·이야기 축·장면·꺼진 병합 관문) 전부
`src/lib/writer/llm/gemini.ts:73` 한 줄을 공유. `GROUNDING_MODEL` 은 `:66` 에서 고정.
저장된 기록 12건(단계당 4건) 전량 접지 모델, 예전 모델 0건, 오류 0건.
세 단계 합 337.5초 → 39.2초. 예전 모델 갈래는 `dispatch.ts:120~131` 에 남아 있으나 `catch` 안.
남는 조건: ① 요청 본문에 축 모델을 실으면 핀보다 우선(제품 화면은 안 보냄) ② 장면 교정 재호출
2종은 설계상 검색 미사용.

**audit-color-temp-ramp** — 값 4개 이상 씬 194개 중 등차 69개(평평 58 + 진짜 계단 11).
±50K 완화 시 86개. **간격이 정확히 +300인 씬은 0개** — 반복되는 것은 "+300"이 아니라
"두 끝값 사이 균등 분할" 형태. 밤인데 4000K 초과로 끝나는 모순 = 밤 씬 56개 중 19개(33.9%).
**원인 규명: 모델 버릇이 아니라 제품 지시문** — `src/lib/writer/pipeline/stages/v4_shots.ts:396`
가 "color_temp_kelvin 은 V3.lighting_arc.start_K~end_K 사이에서 진행" 을 직접 지시.
수리 방향이 "모델 억제"에서 "밤 씬에 낮 색온도가 end_K 로 들어가지 않게" 로 바뀜.
씨앗 씬(1e166e55 sc_04) 실제 값은 `2800→3000→3300→3600→3900→4200→4500` — 첫 간격 +200이라
엄격·완화 어느 등차에도 미포함. **원본 기록(F-006)의 "정확히 +300K/샷" 이 부정확했다.**
모순 19개는 원인 2종 혼재(계단형 / 밤 씬 전체 6500K 고정) — 묶어 고치면 안 됨.
확인 못 함: 8샷 경계 되감김 의심 — 경계 하락률 23.9%(17/71) vs 비경계 27.6%(333/1207), 구분 안 됨.
**티켓 좌표 오류(결론을 뒤집을 뻔함)**: `shots.scene_id` 는 씬 고유번호가 아니라 `sc_01` 형태
이름표 문자열이라 프로젝트마다 재사용됨. 티켓대로 단독 묶기 → 씬 18개·등차 0개·판정 "우연"(오답).
`(project_id, scene_id)` 조합으로 고쳐 씬 201개, 결론 역전.

**verify-f001 / f003 / f005** — 상세는 각 `research/experiments/<id>/result.md`.
f003 부수 발견 2건:
1. 배포 이후 샷 72행이 전부 파이프라인 산출 → 채팅이 샷을 만든 사건 0건 → 가드 발동 자체는 미목격.
2. **`shots.source` 는 출처 구분자로 못 쓴다** — 전체 1,796행이 전부 `pipeline` 이고 사고 당시
   채팅 생성 16행도 `pipeline`. **`source='manual'` 값은 DB에 한 번도 등장한 적이 없다.**
   → 티켓 본문의 오너 3단계 ③(`source='manual'` 확인)은 **현행 데이터로는 밟을 수 없다.**

**t0-generation-retry-never-fires** (밤이 만들고 밤이 실행한 티켓) — **판정선 2번 발동**:
"0건이고 시도 횟수를 늘리는 코드도 0곳이면 → 재시도 장치는 존재하지 않는다".
`attempts >= 2` 는 952건 중 **0건**, 값을 늘리는 코드 **0곳**.
실패 원인 태깅은 반대로 **작동**(실패 48건 전부 `error_class` 채워짐).
→ 목적지 문서 1절 "생성 실패를 원인별로 태깅하고 스스로 재시도하는 척추를 깔았다" 중
**앞 절은 참, 뒤 절은 거짓.** 아침 목록 A에 목적지 갱신 초안 등록.
곁가지: 발견 #58의 실패율 주장 20%는 실측 5.04%(48/952)와 어긋남 — 판정은 불변(티켓 사전 규정).

**t0-findings-ledger-recheck** — 8/15 원장의 alive **91건 전건** 재판정(심각 22건 전건 포함).
**닫힘 8 · 살아 있음 80 · 확인 불가 3.** 닫힌 8건: 색인 0·1·2·42·43·91·101 은 전부 `fb1ad7c`
(소유권 가드 14라우트 + 데모 차단 3곳), 색인 78 은 `.claude/vault/_archive/` 신설 —
**작업 트리에만 있고 아직 커밋 안 됨**(되돌리면 다시 열린다).
확인 불가 3건(60·68·69)은 주장의 실체가 DB 수치라 이 티켓 범위(DB 미사용)에서 못 잼 —
살아 있음으로도 닫힘으로도 밀지 않았다.
**남은 심각 16건**: 3·14·15·22·31·32·33·34·44·57·58·70·71·90·92·93.
**줄 번호 오판 실사례**: 색인 77·80 이 지목한 "합집합" 주석은 `generate-storyboard-batch`
64~66행에 없다 — 같은 커밋이 위에 줄을 넣어 **71행으로 밀렸을 뿐 그대로 살아 있다.**
줄 번호로만 판정했으면 2건을 잘못 닫았다. 색인 44 도 같은 파일이 바뀌었지만 붙은 것은
가드뿐이고 영상 이어붙이기는 여전히 TODO 주석이다.
기준: 원장 이후 제품 코드 커밋은 `fb1ad7c` **하나뿐**이고 미커밋 제품 변경 0건 —
바뀔 수 있는 자리가 원리적으로 좁았고 실제로도 거기서만 바뀌었다.
지출 0원 · 모델 판정 0회 · DB 조회 0회. 원장 원본 무수정(불변 규칙 1),
갱신본은 `research/experiments/t0-findings-ledger-recheck/ledger-updated.json`.

<!-- RESULTS -->

## ② 어제 대화에서 티켓 만들기 (sweep)

`harvest.py` — 스캔 58 / 대상 7 (claude 5 · gjc 2). 다이제스트 7개 + 기각 순간 후보 13건.
창은 마지막 성공 이후 25.07시간(건너뛴 날 복구 로직 정상 작동).
큰 다이제스트 3개(5a3338db · 0897af55 · gjc-01a00385)는 서브에이전트 위임, 작은 4개는 직접 확인.

**새 티켓 2장 (둘 다 실재 확인 완료 → 오늘 밤 실행)**

| 티켓 | 종류 | 예산 | 잴 대상 실재 확인 |
|---|---|---|---|
| audit-upload-anchor-order-coverage | audit | $0 · 40분 | 20건 (실림 4 / 안 실림 16) |
| audit-upload-anchor-missing-fields | audit | $0 · 30분 | 9건 |

**버린 가설 4건** (강제 생산 금지 규칙 적용):
- 포기 문턱이 자동 재생성을 영구 차단하는가 → 해당 자리 0건/145곳 (잴 대상 부재)
- 밑그림이 아직 터널 주소로 나가는가 → 8/15 이후 0건 (**수리 확인됨**)
- 새 문지기·갈래 분리가 실행에서 발동했는가 → 수리 8/15 vs 마지막 실행 8/13, 표본 0
- 미리보기 모델 수명 → 반증 조건 작성 불가(제공사 정보)

**기존 티켓과 중복이라 안 만든 것**: 심각 22건 재판정(→ `t0-findings-ledger-recheck`),
접지 핀 이후 벽시계(→ `t1-pipeline-wallclock-after-pin`), 샷 설계 모델 되돌림,
대사 품질 2건, 잘린 답 가드.

**rejections.md 에서 추출한 판단 기준 5개** (무인 판단의 잣대 — 다음 밤도 이걸 쓴다):
1. 증상 방어보다 **원인 축소**를 먼저 물어라
2. **재현 가능한 것은 사람에게 미루지 마라** (외부에서 재료를 구해서라도 직접 시험)
3. 사용자에게 **고르라고 시키는 설계는 반려**
4. **문서·주석 기반 주장은 코드로 다시 확인하기 전엔 문제 제기가 아니다**
5. **오너가 못 하는 조작을 전제로 깐 처방은 반려** (예: 임시 통로 재실행)

**상태 재확인 필요**: `q-repairjson-silent-loss-guard` 는 원래 물음이 코드로 이행 완료
(8/11 `9ce69ca` 외, 티켓 본문에 이행 내역 기재). 단 본문 말미 "남은 결정" 3건
(문턱값 튜닝 · 부분 적용 정책 · enum 값 검증 범위)은 오너 몫이라 `needs-owner` 유지.

## 지출

<!-- SPEND -->

## 막힌 것

<!-- BLOCKED -->

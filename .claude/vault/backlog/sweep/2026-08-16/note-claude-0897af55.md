# sweep 노트 — claude:0897af55 (글쓰기 뒷단 시간 줄이기)

원문: `.claude/vault/backlog/sweep/2026-08-16/digest/claude-0897af55.md` · 47KB · 기각 순간 3건

## 세션이 한 일

1. 한 바퀴가 436초 걸린 실행을 뜯어 어디서 시간이 나가는지 표로 만들었다.
2. 대사 쓰기를 씬마다 순서대로 돌리던 것을 동시에 돌리게 바꿨다 — 실험 3팔로 재서 **169초 → 52초**.
   순서대로 돌던 이유였던 "앞 씬에서 한 말 기억하기"는 이미 있는 자료로 미리 만들어 대신했다.
3. 샷 설계 동시 실행 수를 4에서 8로 올렸다(실측 −43%). 검수 착수 전에 남은 시간을 보고 모자라면
   착수하지 않고 저장 후 넘기는 문지기를 달았다. 두 갈래가 서로를 죽이지 않게 분리했다.
4. 잘린 답을 꿰맨 사실이 조용히 묻히던 것을 신호로 올리고 한 번 다시 묻게 했다.
5. 웹에서 사실을 확인하는 호출을 다른 모델로 되돌렸다 — 실물 확인 **154.6초 → 8.1초**.

## 열린 것 후보

### ① 검증 대기 — 마지막 수리 뒤 한 바퀴가 몇 초인지 아무도 모른다

발화 원문(오너): "일단 현재기준으로 최종 writer 백엔드가 각단계별로 몇초씩 차지하는지 한번 돌려봐줄수있음?"
→ 731초로 쟀는데, **그 뒤에 가장 큰 덩어리를 건드리는 수리가 하나 더 들어갔다.**

**이미 티켓이 있다** — `t1-pipeline-wallclock-after-pin`. 다만 지금 `status: draft` 이고 선행 티켓
`t0-grounding-pin-stage-coverage` 를 기다리게 걸려 있는데 **그 선행은 오늘 밤 이미 닫혔다.**
새로 만들지 않고 이 사실만 보고한다(sweep 은 기존 티켓을 고치지 않는다).

### ② 미결 — 모델을 바꾼 값으로 샷 설계 시간이 33% 늘었다

세션 발췌: "모델 전환이 품질을 위한 결정이었는데 샷 설계 시간에는 비용이 붙었습니다."
**이미 티켓이 있다** — `t1-shotdesign-model-rollback` (`status: draft`, 오너 결정 대기: 미리보기
모델의 수명 위험을 감수할지). 새로 만들지 않는다.

### ③ 미착수 — 대사의 좋고 나쁨은 아무도 안 쟀다

발화 원문(오너): "품질은 뭐를말하는거임? 대사의 퀄리티를 말하는거임?" → 답: "대사 퀄리티는 안
쟀습니다. 제가 잰 6개는 전부 코드가 셀 수 있는 사고입니다." 안 잰 것 셋: 인물이 아직 모르는 정보를
말하는가 · 말투가 표류하는가 · 멋부린 대사가 늘었는가. 오너 결론: **"일단 B로하고 커밋푸시하셈
내용고도화는 나중에 별도로처리해야할듯"** — 결론은 났고 지금 안 할 뿐이다.
기존 대기 티켓 `deferred-dialogue-parallel-blind-judge` · `deferred-dialogue-quality-upgrade` 가
이미 들고 있다. 새로 만들지 않는다.

### ④ 닫힘 — 여기 적지 않는 것

- 대사 동시 실행 기본값 전환 → 커밋 `f149994`
- 샷 설계 동시 실행 8 · 착수 문지기 · 갈래 분리 → 커밋 `0a892c3`
- 잘린 답 가드 + 다시 묻기 → 커밋 `9ce69ca`
- 웹 확인 호출 모델 되돌림 → 커밋 `b964a35`
- 다른 세션이 중간 상태로 남긴 타입 오류 한 줄 배선 → 코드

## 버린 후보

- **새 문지기·갈래 분리가 실제 실행에서 발동했는가** — 잴 대상이 없다. 수리는 8월 15일에 들어갔는데
  기록에 남은 글쓰기 실행은 8월 13일이 마지막이다(전체 36건, 완료 33 · 실패 3). 실행이 새로
  쌓이기 전에는 아무것도 못 센다. 위 ①의 재측정이 돌면 그 실행이 곧 표본이 된다.
- **미리보기 모델이 언제 사라지는가** — 어떤 관측이 나오면 이게 죽는지 못 쓴다(사라지는 시점은
  제공사만 안다). 지금 살아 있다는 것은 세션이 실물 호출로 이미 확인했다. 노트에만 남긴다.

## 기존 티켓과의 연결

- `q-repairjson-silent-loss-guard`[사람 답변 대기] — 이 세션이 그 물음의 처방을 **실제로 코드에
  넣고 밀어올렸다**(`9ce69ca`, 새 검사 15건 통과). 아침에 이 티켓 상태를 다시 볼 것.
- `q-shotcheck-single-call-fragility`[사람 답변 대기] · `deferred-shotcheck-channel-2`[대기] —
  검수 단일 호출 문제를 이미 들고 있다. 이번 세션은 그 앞에 문지기만 달았고 쪼개기는 안 했다.

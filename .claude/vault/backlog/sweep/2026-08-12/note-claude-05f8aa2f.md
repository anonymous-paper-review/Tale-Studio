# 스위프 노트 — claude:05f8aa2f (2026-08-11 16:42, 234KB)

## 세션이 뭘 했나

오너가 "pnpm에서 build랑 lint가 각각 뭘 의미하냐"고 물어 실제로 돌려가며 설명했다. 핵심은 **타입 검사가 build 안에 들어 있다**는 것(그래서 타입 에러는 build가 잡는다). 이어 검사 대상에서 워크트리 사본을 빼자 검사 파일이 **1098 → 564**로 반토막 났고, 에러도 **161 → 104**로 줄었다.

## 열린 것 후보

### 미결

**잔여 에러 104건의 정체가 선언에 그쳤고, 그래서 검사가 여전히 빨간불이다**
> 어시스턴트 원문: "남은 error 104건은 전부 `research/experiments/`의 `no-explicit-any`라 `pnpm lint`는 여전히 exit 1이다. 초록으로 만들려면 두 …"

문장이 끊긴 채 세션이 끝났다. "전부"라는 단정이 실제로 참인지 — 제품 코드(`src/`)의 진짜 에러가 그 104건 안에 섞여 있지는 않은지가 확인 안 됐다. 이게 중요한 이유는, 잔여가 전부 실험 코드면 **검사 대상에서 빼는 결정 하나로 빨간불이 초록이 되고**, 아니면 제품 코드에 숨은 에러가 검사 무력화 뒤에 가려져 있다는 뜻이 된다.
→ 티켓 [t0-lint-residual-scope](../../t0-lint-residual-scope.md)

**"초록으로 만드는 방법 두 가지" 중 무엇을 고를지는 오너 판단** — 실험 폴더를 검사 대상에서 뺄지, 규칙을 경고로 낮출지는 가설이 아니다. 위 티켓의 관측이 나온 뒤에 결정할 일이라 카드에 한 줄로 올린다.

## 결정 흔적

- **`.worktrees/**`를 검사 제외에 추가** (`eslint.config.mjs:17-18`) — 링크된 워크트리는 같은 소스의 다른 브랜치 사본이라 중복 검사였다.
- 감소분 57건 중 **55건이 워크트리 제거분**이고 나머지 2건은 실험 임시 파일이 두 실행 사이에 사라져서 생긴 것 — 변경과 무관함을 명시적으로 분리해 기록했다.
- **build는 타입 검사를 포함한다** — `next.config.ts`가 비어 있어 `typescript.ignoreBuildErrors` 기본값이 false.

## vault 관련 주제 링크

없음 — 빌드/검사 도구 축은 vault 주제가 아니다.

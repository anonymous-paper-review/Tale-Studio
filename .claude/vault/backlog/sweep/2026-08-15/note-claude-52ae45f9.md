# 스위프 노트 — claude:52ae45f9

- 다이제스트: `../2026-08-15-recent/digest/claude-52ae45f9.md` (3005KB 원본)
- 수확 경로: **60분 제외창 구제분**

## 세션이 뭘 했나

글쓰기 백엔드의 그림 축과 샷 층(분해·설계·검수)이 현재 어떻게 이어져 있는지를 **현행 코드에서 새로 뽑아** 클릭 가능한 배선도로 만들었다. 오너가 준 옛 발행물 링크는 권한 때문에 안 열려서 오히려 전량 재추출로 갔다.

노드 42개·6개 밴드. 노드마다 하는 일 / 받는 값 / 내보내는 값과 **소비처 유무** / 강제하는 계약 / 지시문 원문 / 실제 산출 예시 / 왜 이 모양이 됐나가 들어간다.

중간에 구조를 한 번 갈아엎었다 — 손으로 적은 지도는 낡기 때문에, **코드가 진실인 부분은 하나도 복사하지 않고 열 때마다 소스에서 떠오는 생성기**로 바꿨다. 앵커를 줄 번호가 아니라 문자열 경계로 잡아 코드가 움직여도 안 깨지게 했다. 실물 확인도 했다 — 소스에서 상한값을 2→3으로 바꾸니 페이지가 즉시 따라 바뀌었다(원복 완료).

## 열린 것 후보

### 1. 배선도가 낸 발견 11건이 원장에 안 들어갔다
**분류: 미결** — 티켓 안 만듦, **연결로 보고**

적대적 검증 8건 전부 확정(반증 0)이라 신뢰도는 있는데, 이 발견들이 어디에도 등록되지 않았다. 같은 밤 `cc9e03b1`이 만든 **150개 문제 묶음과 겹칠 가능성이 매우 높다** — 둘 다 같은 파이프라인을 같은 시간대에 훑었다.

**중복 생산이 이 그물의 최대 실패 모드**라 따로 티켓을 만들지 않는다. 아침에 두 산출을 나란히 놓고 대조하는 게 맞다:

- 이쪽: 배선도 발견 11건 (발행물 `b5ee6f2e`)
- 저쪽: `final-verdicts.json` 150건 + 새 발견 103건 (`cc9e03b1` 노트 1번의 경로)

### 2. v6/v7이 오케스트레이터에 안 물려 있다
**분류: 미결** — 기존 티켓과 같은 자리라 티켓 만들지 않음

> "`logs/`에 실제 런 산출물이 있고 `v6/v7`은 오케스트레이터가 import 안 함(끊긴 후보)"

기존 카드 **Q8**(v1/v3/v5 존폐 — 진짜 질문은 v6/v7 데이터베이스 재배선)이 이미 이 질문을 들고 있다.
기존 티켓 `t0-v6-background-ref-trace`[완료]는 배경 참조 쪽만 봤다.

## 코드로 귀결된 것 (열린 것 아님)

- 배선도 생성기 — `pnpm map:dev`(로컬 실시간) / `pnpm map:build`(정적 스냅샷) 2산출 1생성기
- `scripts/`가 통째로 무시되는 디렉토리라 추적되는 `research/`로 이동
- 남의 세션이 남긴 타입 오류 2건 정리(실험 폴더 `anchor-density-b`)
- 발행물: https://claude.ai/code/artifact/b5ee6f2e-e915-42fd-b761-28b82e3d92b1

## 판별 메모

이 세션은 산출 대부분이 **코드와 발행물로 귀결**됐다. 열린 것이 적은 게 정상이고,
남은 둘은 전부 "이미 있는 원장과의 대조"라 새 티켓이 아니라 연결로 처리했다.

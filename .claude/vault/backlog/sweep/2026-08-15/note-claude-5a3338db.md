# 스위프 노트 — claude:5a3338db

- 다이제스트: `../2026-08-15-recent/digest/claude-5a3338db.md` (5523KB 원본, 50KB 초과 → 서브에이전트 요약 경유)
- 수확 경로: **60분 제외창 구제분**

## 세션이 뭘 했나

기획 단계 채팅에 파일 첨부(그림·문서)를 붙였다. 정책 설계부터 구현·실측까지 갔다 — 허용 형식 13종 매트릭스, 세로로 아주 긴 웹툰(500×15884)까지 통과 확인. 유저가 올린 그림을 **화풍 기준으로 그대로 쓰는 경로**를 뚫으려다 데이터베이스 제약에 막혀 실제 운영 데이터베이스에 스키마 변경을 적용했다.

업로드 실패를 두 번 쫓았는데 **범인이 자기 코드가 아니었다** — 로컬 터널이 죽어서 그림 생성 업체가 우리 템플릿 그림을 못 받아가고 있었다(80건 실패). 템플릿 3종을 스토리지로 옮겨 해결하고, 쌓인 작업 86건을 지웠다. 실패 통지를 채팅에 붙이던 중 **주간 사용 한도로 끊겼다.**

## 열린 것 후보

### 1. 이 세션의 작업이 통째로 커밋되지 않았다
**분류: 검증 대기** — 판별 질문상 "코드로 귀결"이라 티켓은 안 만들지만, 위험이라 남긴다

세션 내 커밋 **0건**. 그런데 이미 벌어진 것:

- 운영 데이터베이스에 **스키마 변경이 적용됐다**(`projects.custom_style_anchor` 추가, `style_anchors` 외래키 제거)
- 운영 데이터베이스에서 **작업 기록 86건이 삭제됐다**(실패 83 · 대기 3)

**데이터베이스는 이미 새 모양인데 그걸 쓰는 코드는 아직 커밋 전이다.** 되돌리기가 대칭이 아니다.

### 2. 실패했을 때 채팅으로 알려주는 배선 — 붙였는데 확인 못 했다
**분류: 검증 대기**

> "ㅇㅇ 정리해줘 그리고 이런 재생성실패는 채팅으로 알려줘야할듯? 어떤상황이고 뭘 하면 되는지?"

닫히는 조건이 발화에 있다 — **"어떤 상황이고 뭘 하면 되는지"가 실제 실패 때 채팅에 뜨는 것.** 조용히 끝나던 자리가 실제로 말을 하는지 미확인 상태로 한도에 걸렸다.

### 3. 디자인 체계를 처음부터 다시 — 오너가 다른 세션으로 명시 이월
**분류: 이미 결론 난 보류** — 티켓 아님 (판별표 3행)

> "global.css나 정책을 그떄당시에 바이브코딩한 결과물기준으로만든거라 검은색이 배경컬러의 메인인거만 좋고 나머지 팔레트는 점차변경해야될듯? … 그 찾는 기준이 우리 사이트의 본질과철학을 담아야하는데 그게 디자인으로 너무어렵잖아? 어떻게 접근하면 좋을지 모르겠네 **다른세션에서 진행하고싶고** 일단 여기는 기능작업 이어서하게 이 작업에대해 프롬프트만 출력해줄래?"

산출은 프롬프트 한 장뿐이고 코드 변경 0. 오너가 스스로 자리를 옮겼으니 대기열이 아니라 보류 목록 감이다.
(같은 밤 `e6e0764a`의 캐릭터 작업과 **같은 주제** — 아침에 묶어 볼 것.)

### 4. 첨부 허용 형식에서 svg·hwp가 말없이 사라졌다
**분류: 미결** — 닫히는 조건 불명 → **노트에만** (판별표 4행, 강제 생산 금지)

> "txt계열(md,hwp,docs)랑 image계열(jpg,png,svg)를 구분하는 ui 만들어줘야할까?"

결론은 "md + docx"로 났는데 svg·hwp는 기각인지 누락인지 발화에 안 남았다.

### 5. 웹툰 원본이 바뀌면 하류가 낡는가
**분류: 미결** — 닫히는 조건 불명 → **노트에만**

입력 지문 이야기가 나오다가 오너가 화제를 배선 쪽으로 틀면서 정합성 질문이 답 없이 남았다.

## 터널 의존 — 티켓으로 뽑은 것

오너가 강하게 못 박은 자리:

> "뭔소리임 뭘하라는거임 **내가 ngrok은 재실행못해; 하면 주소지바뀌잖아**"

템플릿 3종은 스토리지로 옮겨 해결됐지만, 스위퍼가 확인해보니 **완료 통지가 돌아오는 주소는 여전히 터널**이다(`src/lib/fal/webhook-url.ts`, `.env.local`의 `NEXT_PUBLIC_APP_URL`). 주소가 바뀌면 통지가 유실된다. → `t0-stuck-jobs-webhook-loss`

## 코드로 귀결된 것 (열린 것 아님, 단 미커밋)

업로드 처리 모듈 4종 + 전처리 경로 신규(테스트 15 통과) · 첨부 칩 화면 · 빈 입력 차단 3곳 해제 · 그림 URL 경로(우리 스토리지 도메인만 허용) · 세로 상한 재설계 · 실패 6종 분기 문구 · 화풍 기준 저장 배선 · 템플릿 3종 스토리지 이전 · 유저 말풍선 첨부 표시

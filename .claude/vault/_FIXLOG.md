# 프로덕션 수리 원장 — 상시

**프로덕션에서 실제로 관측된 사고**만 모은다. 사고 하나 = 항목 하나이고, 항목은 `검증` 칸이
확인될 때까지 열려 있다. 코드가 고쳐졌다는 것만으로는 닫히지 않는다.

- 이 파일은 날짜 파일이 아니라 **살아 있는 원장**이다. 닫힌 항목은 잘라내 `_archive/_FIXLOG-closed.md`로 옮긴다
  — 여기 남은 것은 전부 아직 열려 있는 것이다.
- **커밋 메시지를 복제하지 않는다.** 코드로 귀결된 것은 diff 와 커밋 본문이 진실이다. 여기 적는 것은
  git 이 담지 못하는 것뿐 — 사고→원인→수리 커밋의 **연결**, 데이터 수리·오너 액션의 **진행 상태**,
  배포 후 확인할 **검증**, 미착수 처방의 **상태 전이**.
- **코드 수리 ≠ 사고 종결.** 대부분의 수리는 향후 생성만 보호한다. 이미 만들어진 데이터가 어떤
  상태인지를 따로 적고, 그것까지 정리돼야 닫는다.
- **경계**: 한 사고에서 갈라져 나온 미착수 처방은 사고를 쪼개지 않기 위해 그 항목 안에 남긴다.
  사고와 무관하게 결정된 미착수 작업은 `_DEFERRED.md`, 결론이 없는 논제는 vault 날짜 파일의 `## 3. 미결`.

## 폼

```
### F-NNN · <한 줄 제목>   `상태: 수리됨|부분수리|백로그|오너대기`
- **증상**: 프로덕션에서 무엇이 관측됐나 (프로젝트·샷·잡 id 등 실측 좌표와 함께)
- **원인**: 확정된 기전 — `파일:줄`. 노출을 만든 커밋이 있으면 그것도
- **수리**: 수리 커밋 + 그 수리가 **무엇까지 보호하는가** (대개 "향후 생성만")
- **데이터**: 이미 만들어진 데이터가 어떤 상태인가 — 소급 가능/불가, 오너 대기 중인 행동
- **검증**: 무엇이 확인되면 이 항목을 닫는가 (배포 후 실측 항목)
- 기록: YYYY-MM-DD
```

---

### F-001 · 실사 그리드 칸별 인물 바꿔치기   `상태: 수리됨` ★ 육안 확정 3건 재생성 대기

- **증상**: `a5cb2cae` 의 `sh_04_18`(왕국의 추적자 단독 샷)이 실사 스토리보드에서 **소녀로 그려져
  저장**됐다. 4샷 일괄 시트 `real_grid_5ad25058…` 는 네 칸 전부 같은 소녀 — 칸 2 의 갑주가 통째로
  사라지고 포즈만 소녀 몸에 얹혔다. 러프 연필 시트는 정상(칸 2 = 갑주 추적자). 크롭·인덱스 시프트가
  아니다. writer 데이터(`characters`/`design_ref`/`static_spec`/`dynamic_spec`/대사/check_notes)는
  **전 필드가 `kingdom_pursuer` 로 정확**하다 — 잘못된 것은 그림뿐이다.
- **원인**: 일괄 경로에 칸↔인물 배선이 아예 없었다. `generate-storyboard-batch/route.ts:80-90, 105-109`
  가 인물 레퍼런스를 **어느 URL 이 누구인지 잃어버린 익명 배열**로 넘기고, 프롬프트
  (`storyboard-strip.ts:137-141`)는 "corresponding character(s)" 라고만 말한다 — 대응 규약이 어디에도
  정의돼 있지 않다. 게다가 러프 단계가 인물 신호를 의도적으로 제거(마네킹·무의상)하므로 리페인트
  모델에 남는 단서가 없다. 노출은 **76c4e14(2026-08-05, `#real-grid-fix`)** 가 그룹 키에서 캐릭터
  세트를 빼면서 열렸다 — 그 커밋의 "프롬프트가 칸별 대응을 지시하므로 안전"은 **거짓 전제**였고,
  해당 문장은 f7111ee(8/5 최초 도입) 이후 한 번도 칸별 대응을 가진 적이 없다.
  8/10~8/11 개편(2lane·대사병렬)과 무관한 **8/5 커밋의 지연 발현**이다.
- **수리**: `cb2d56c` (`#real-grid-identity #B9`). **향후 생성만 보호한다** — 이미 생성된 시트는
  소급되지 않는다. 백스톱 후보(혼재 세트 시트 분리)는 이 배선이 실측으로 확인될 때까지 보류.
- **데이터**:
  1. **육안 확정 3건 — 전부 오너의 단일 재생성 대기.** 셋 다 수리 배포(2026-08-12 11:13Z) **이전**
     생성물이라 `cb2d56c` 가 소급하지 않는다. 세 건 모두 writer 데이터는 정확하고 그림만 틀렸다.
     - `a5cb2cae` `sh_04_18` — 시트 `[sh_04_17..20]`, 소수 1/4.
     - `dc531572` `sh_03_13` — `characters={kingdom_hunters}` 정확, 시트 `[sh_03_12..15]`, 소수 1/4,
       real_grid 08-12 07:34Z. (2026-08-12 실측 추가)
     - `e1a9fd08` `sh_03_17` — `characters={kingdom_pursuers}` 정확, 시트 `[sh_03_17..19]`, 소수 1/3,
       real_grid 08-11 10:49Z. (2026-08-12 실측 추가)

     **3건 전부 "소수 추적자가 다수 소녀에 흡수" 구도** — forensics 의 다수결 붕괴 추정과 정합한다
     (표본 3으로 확정할 수는 없으나 아직 반례가 없다).
     권장 경로는 SQL 이 아니라 **UI 단일 재생성**(`/api/director/generate-storyboard`): 이 경로는 샷
     프롬프트 텍스트를 실어 보내므로 인물 정체가 텍스트로 보증된다. `storyboard_image` 를 NULL 로
     비우는 SQL 초안도 있으나 기존 URL 을 되돌릴 수 없이 잃는다. 아키텍처 §5 원칙 2("차 있는 것
     교체는 사람의 명시적 행동")상 파이프라인이 스스로 하면 안 된다.
  2. **확산** — 8/5 이후 인물 세트가 혼재한 실사 그리드 시트 **53건 / 7개 프로젝트**(2026-08-12 실측).
     이는 "오배치 53건"이 아니라 **오배치가 구조적으로 가능했던 시트**의 수다. 그중 육안으로 실제
     오배치가 확정된 것이 위 3건이다. 전수 육안은 여전히 미실시.
- **검증**:
  1. 다음 실사 그리드 생성의 `generation_jobs.input_snapshot.prompt`(#B9 로 이제 기록된다)에
     `Column i: 이름` 배정문과 `reference image N = 이름` 규약이 실렸는지 — 디버그 프롬프트
     트레이스에서 바로 보인다. **이것이 이 수리가 실제로 탔다는 1차 증거**다.
  2. 그 시트에서 소수 인물 칸이 제 인물로 그려졌는지 육안 1회.
  3. 확산 53건 중 혼재 시트의 소수 인물 샷을 표본 육안 — 실패율이 나와야 소급 재생성 범위를 정할 수
     있다. 재측정: `generation_jobs`(kind=`storyboard_real_grid`, completed, created_at ≥ 2026-08-05)를
     `target->'writerShotIds'` 로 `shots` 와 조인해 `count(DISTINCT characters::text) > 1` 인 그룹.
- 기록: 2026-08-12

### F-002 · 분할 자식 스펙 기아   `상태: 수리됨` (데이터 소급 불가 — 확정)

- **증상**: 분할로 생긴 **둘째+ 자식** 샷의 `static_spec`/`dynamic_spec` 이 전부 null → 러프는
  db_fallback(레이어·focal·blocking 없이 액션 텍스트만), 영상은 무계약 레거시 프롬프트로 떨어진다.
  구조적 저품질이 생성 전에 확정된다. 런당 2~3개, 큰 프로젝트는 최대 **17/154**(`f32b3f50`).
- **원인**: `4f0e6b2`(2026-08-10, `#split-spec`)의 **과잉 처방**. 8/10 `sh_03_15` "분할 자식이 옆 샷
  설계를 훔치던" 사고를 막으려고 둘째+ 자식의 스펙을 통째로 비웠는데, 훔침 우려(F2)의 실제 대상은
  `first_frame_prompt` 와 blocking pose 뿐이었다. 시간이 지나면 거짓이 되는 산문은 상속하고 시간
  무관 채널(조명·렌즈·framing 레이어·소품·팔레트)은 굶기는 **비대칭**이 남은 것이 이 사고다.
- **수리**: `b32c4b2` (`#split-inherit`) — 전면 기아를 부분 상속으로 교체. 향후 런에만 적용된다.
- **데이터**: **소급 불가 — 결정 완료.** 기존 분할 자식에는 스펙이 애초에 존재한 적이 없다(값이
  지워진 게 아니라 만들어진 적 없음). 되찾으려면 그 프로젝트의 writer 런을 다시 돌리는 수밖에 없고,
  그러면 하류 산출물(러프·실사·영상·사용자 편집)을 잃는다. **소급 수리하지 않는다.**
- **검증**: 신규 런의 분할 자식(= `design_ref` 가 null 인 샷)에 대해
  1. 러프 제출 응답 `submitted[].promptSource` 가 `shotDesign`(`rough-storyboard/route.ts:472`).
     `db_fallback` 이면 상속이 실리지 않은 것. 생성 시점에만 관측된다.
  2. 사후 관측 대체 — 그 샷의 `shots.static_spec` 에 카메라·조명·팔레트가 있고
     `first_frame_prompt`/`focal_point` 는 비어 있는지(**부분 상속의 서명**), `dynamic_spec` 에
     `character_motion`/`gaze_arc` 가 없고 `camera_motion` 은 있는지.
- 기록: 2026-08-12

### F-003 · 런 중 채팅 병행 초안 — 비정본 인물 id 혼입   `상태: 부분수리` ★ 남은 백로그 R3 1건 (데이터는 오너 철회)

- **증상**: `dc531572` 에서 런(`fbb6efcd`, 4씬 20샷)이 completed 된 **25초 뒤** `sc_05~08` 16샷이
  붙었다. 파이프라인 산출이 아니라 writer 채팅 에이전트가 CRUD(`writer-store.addShot`)로 만든 **별개
  초안**이다. 지문: 파이프라인 20행은 `created_at` 이 전부 동일(단일 bulk insert)·`I2V`·design_ref 보유,
  16행은 전부 다름(개별 insert 16회)·`T2V`·design_ref 전무. 결정적 증거는 07:14:58 모델 발화
  "씬 4개, 샷 16개로 구성했어요". 결과 3갈래 — ① 샷 번호가 씬마다 01 로 리셋(씬로컬 축과 전역 축이
  같은 `sh_XX_NN` 모양을 공유) ② design_ref/spec 전무 → 하류에서 **설계 없음으로 확정**
  ③ **인물 id 가 비정본**(`girl`/`tracker` — `characters` 테이블에 없다) → 인물 에셋 조인 전면 실패.
  오너가 관찰한 "인물 asset 이 아예 안 들어감"은 ②+③ 두 결손의 합이다.
- **원인**: 세 갈래는 "이 16샷은 다른 생성 경로가 만들었다"는 한 원인의 가지다. ①은 표시상 비일관,
  ②는 설계상 정상(수동 샷엔 원래 provenance 가 없다). **진짜 결함은 ③** — `writer-chat-updates.ts:57-58,
  88-89` 의 `asStringArray` 는 모양만 보고 정본 대조를 하지 않는다. 방어가
  `api/writer/chat/route.ts:29` 의 프롬프트("Never invent new IDs") 뿐이었다. `.claude/rules/architecture.md`
  §3·§4 위반(모델 출력의 무검증 실행). **회귀가 아니다** — 2lane·대사병렬·shotcheck-gate 어느 것도
  이 파일들을 건드리지 않았고, 개편 이전에도 동일하게 재현됐을 사고다.
- **수리**: 3겹 중 2겹이 닫혔다.
  - `731f859`(`#run-chat-gate`) — **UI 입구 게이트**. 파이프라인 실행 중 writer 채팅을 LLM 없이
    결정론 즉답으로 막는다(`src/components/layout/global-chat.tsx:519-531, 543-551`). 다만 이 게이트는
    화면 한 곳이고 씬 게이트 대기 창은 덮지 못했다.
  - `2c0d0cf`(`#F-003 R1/R2`) — **R1 인물 id 화이트리스트**(DB 로스터가 진실, 드롭은 응답 말미에
    표면화, 발명 화자의 대사는 화자만 바꾸지 않고 대사째 드롭) + **R2 store 레벨 런 가드**
    (`applyChatUpdates` 가 `writer_runs` 를 pull 해 런 중 mutating update 를 skipped 로 거부 —
    731f859 가 못 덮던 **씬 게이트 대기 창**을 닫는 최종 방어).
    남은 두 창은 **의도된 설계**이지 결함이 아니다. ① 런 상태 확인이 실패하면 **막지 않는다**
    (fail-open — 심층 방어에서 가용성 우선) ② `projectId` 를 동봉하지 않는 **구 클라는 종전 무필터**.
    ②는 클라가 갱신되면 닫히므로 배포 후 한 번 확인할 것.
  - **남은 백로그 R3 1건** — persist 의 프로젝트 전체 DELETE 를 자기 소유 행으로 좁힌다
    (`shots.source`(`'pipeline'|'manual'`) 추가 = DDL 1줄 + persist 2곳). 사고 당시엔
    persist(07:14:33)가 채팅(07:14:58)보다 **먼저**라 16샷이 살아남았지만, 순서가 반대였다면 통째로
    조용히 사라졌을 것이다. R2 는 "런 중"만 막고 **R3 는 "런이 나중에 또 돌 때"까지** 막는다 —
    이미 만들어진 수동/채팅 샷은 지금도 재런 한 번에 사라질 수 있다.
  - (R4 id 축 통일은 **착수하지 않는다** — 충돌 없는 표시상 비일관이고, 기존 행의 `shot_id` 개명은
    `rough_storyboard`·`generation_jobs`·director 노드가 참조하므로 **금지**.)
- **데이터**: **철회로 닫힘(2026-08-12).** 오너 결정 — "기존 프로젝트는 수정 불요, 새 프로젝트로
  테스트". `dc531572` 인물 id 정본화(A안: `girl`→`char`, `tracker`→`kingdom_hunters`)는 **실행하지
  않는다.** 이 사고의 데이터 축은 여기서 종료되고, 남은 것은 코드(R3)와 신규 프로젝트 검증뿐이다.
  (실행했다면 필요했던 전제 — `dialogue_lines[].characterId` 동시 확인, `design_ref` 임의 부착
  절대 금지(옆 샷 설계를 훔치는 F-002 의 원 사고 재현). 같은 상황이 다시 오면 이 두 줄부터 읽을 것.)
- **검증**: **새 프로젝트에서 실측한다**(오너 결정에 따라 기존 프로젝트로는 검증하지 않는다).
  1. 런 진행 중 채팅으로 씬/샷 수정 시도 → skipped 안내가 뜨는지. **씬 게이트 대기 중에도** 거부되는지가
     핵심 — R2 가 UI 게이트보다 넓은 유일한 지점이다.
  2. 채팅이 발명 인물 id 를 뱉도록 유도 → 응답 말미에 "반영하지 않았어요" 가 붙고, `shots.characters`
     에 그 id 가 남지 않는지.
  3. 주기 점검 — `shots.characters` 에 `characters` 테이블 밖 id 가 **새로** 생기지 않는지.
  4. R3 는 착수 시 재persist 후 수동 샷이 살아남는지로 확인. **이것이 이 항목을 닫는 마지막 조건**이다.
  - **오너 관측 대조 완료(2026-08-12)**: "Scene5부터 스토리가 다시 시작"은 이 진단과 **일치**한다 —
    **배포 단절이 아니다.** 런은 07:14:34Z 에 정상 완료됐고(state 씬 4개 = 그 스토리의 전체 분량,
    20/20 persist), `sc_05~08` 은 07:14:58Z 채팅 초안이다. 오너가 사건 자체를 인지했고, 정리 방향은
    같은 날 **철회**로 결정됐다(데이터 칸).
- 기록: 2026-08-12

### F-004 · 스타일 앵커 미준수 (3D 애니메이션)   `상태: 부분수리` ★ B1 적용 (fal 검증 = 오너 새 프로젝트 테스트 대기) + 앰버 프롬프트 제어 프로브 진행 중

- **증상**: 오너 관찰 — "3D 애니메이션 앵커가 전혀 지켜지지 않는다"(`dc531572`,
  `projects.style_anchor_key = real_3d`).
- **원인**: **배선이 아니라 앵커 실물이 주범**(진단 확정, 오너 육안 판정만 남음). 픽커가 보여주는
  preview(`previews/real_3d.jpg` — 픽사풍 스타일라이즈드 3D **캐릭터**)와 모델이 실제로 받는 앵커
  (`style-anchors/real_3d.png` — **캐릭터가 한 명도 없는** 포토리얼 CG 정물)가 **다른 물건**이다.
  앵커가 운반할 수 있는 정보는 재질·조명·컬러그레이드뿐이고 캐릭터 스타일라이제이션은 0인데,
  사람이 "3D 애니메이션"이라 부를 때의 판별 근거 대부분이 바로 그 캐릭터 조형이다. 오너의 가설
  ("정보량이 너무 적나?")은 이 매체에 한해 정확하다. **이미지 경로 배선은 잡 전수 실측에서 정상**
  (character_view 2/2, world_shot 4/4, storyboard_real_grid 11/11 모두 앵커 적재).
  부수 축 3개 — **셋 다 `2c0d0cf` 로 수리됨**, 아래는 진단 시점 서술이다:
  - `art_style`(= 앵커에 부합하는 유일한 토큰 `3d_animation`)은 억제되고
    `texture_philosophy: photorealistic`(**매체어**)은 살아남는다 — 2026-07-14 판정의 취지를 정확히
    뒤집은 결과. 이는 실수가 아니라 `tests/style-anchor-routes.test.ts:226`(AC1b)이 "judged behavior"
    로 고정해 둔 동작이라, 바꾸려면 **결정 번복임을 명시**해야 한다.
  - `locations.style_description` 에 writer 가 심은 매체어("3D 리얼리스틱"·"포토리얼리스틱"·"실사적인")
    는 아무도 걸러내지 않고 월드/그리드 프롬프트에 그대로 실린다.
  - **영상 경로는 앵커가 전혀 실리지 않는다(진짜 배선 구멍)** — 게다가 `video-prompt.ts:39` 가 앵커
    매체와 무관하게 항상 "shot on Arri Alexa, 35mm" 를 붙인다. 3D 애니메이션 프로젝트에 실사 촬영을
    지시하는 셈.
- **수리**: 매체어 축(B4·B5·B7)과 **주범 처방(B1 앵커 교체)까지 집행됐다.** 남은 것은 프로덕션 실측이다.
  - `cb2d56c` — **B9 그리드 프롬프트 스냅샷**(관측 복구). F-001 수리의 부산물이다. 이전에는 최종 프레임
    전량을 만드는 경로가 관측 사각이라 이 조사도 코드 재구성에 의존해야 했다.
  - `2c0d0cf`(`#F-004 B4/B5/B7`) — **B4 토큰 정책 번복**(앵커 존재 시 `art_style` 통짜 억제 →
    **매체어를 품은 토큰만** 드롭). 이제 `3d_animation` 은 살고 `texture: photorealistic` 은 떨어진다 —
    §원인이 지적한 뒤집힌 배치가 바로잡혔다. 2026-07-14 `dark_cinematic_realism` 교훈은 보존된다
    (그 토큰은 매체어를 품어 여전히 드롭). AC1b 는 **번복 근거와 함께** 갱신됐다.
    + **B5 산문 스크럽**(`applyStyleAnchor` 가 본문 프롬프트의 매체어를 걷어낸다 — writer 가
    `locations.style_description` 에 심은 "포토리얼리스틱 식생 지대"류가 앵커를 이기던 경로).
    + **B7 영상 카메라 기재 억제**(앵커 medium 이 `live_action` 이 아니면 "shot on Arri Alexa" 를 빼고,
    `resolveStyleAnchorByKey` 가 medium 을 함께 나른다).
  - **B1 `real_3d` 앵커 이미지 교체 — 적용됨(2026-08-12 저녁).** 커밋이 아니라 **데이터 변경**이라
    여기가 유일한 기록이다.
    - 후보 3장(C1 / C1b / C2) 제작 후 **전이 프로브 6장이 전 게이트 통과** — 정체성 누수 0/4,
      의인화 누수 0/5, 그레이드 중립성 양방향 통과. (캐릭터가 든 앵커의 관건은 "주제를 재현하지
      말 것" 준수였고, 그 우려가 실측으로 해소됐다는 뜻이다.)
    - **채택은 C2 — 오너 육안 판정.** 에이전트 추천은 C1b 였으나 오너가 "털 표현 등 3D 렌더링
      디테일 수준의 동질성"을 근거로 C2 를 골랐다. 최종 미학 판정은 오너 몫이라는 스킬 규정대로다.
    - **앰버(석양) 그레이드는 의도적으로 제거**하고 중립 주광을 채택했다(오너 승인). 대신 씬
      프롬프트로 석양을 되살릴 수 있는지 확인하는 **프로브 2장이 진행 중**이다 — 되살릴 수 없다면
      앵커가 그레이드를 과결정한다는 뜻이므로 그때 다시 판단한다.
    - 집행 방식: Storage 에 **새 파일명** `media/style-anchors/real_3d_v2.png` 로 업로드하고
      `style_anchors.image_url` 만 UPDATE 했다. **기존 `real_3d.png` 는 보존 = 롤백 가능**하고
      `preview_url` 은 건드리지 않았다(픽커 화면은 그대로).
    - ⚠ **측정 주의 2건.** ① `resolveStyleAnchorByKey` 의 **5분 TTL 캐시** — 교체 직후 5분간은
      구 앵커로 생성될 수 있다. ② **B4 가 같은 날 선배포**돼(`2c0d0cf`) B1 과 효과가 겹친다.
      순서는 **B4 선배포(2c0d0cf) → B1 후교체**이므로, 이후 산출물의 개선을 둘 중 하나로
      **인과 분리할 수 없다.** 분리가 필요하면 앵커만 롤백한 대조군을 따로 떠야 한다.
    - 상세·롤백 SQL 은 `dev/Image_Style/real_3d_v2/anchor-B1.md`(§8) — gitignore 디스크 영속(런 산출물과 동거). vault 증류는 별도 예정.
  - 남은 것 — **B10 · 프로덕션 fal 경로 검증**:
  - **B10 `image_size` no-op** — `FalImageOptions` 에 필드가 없어 두 라우트의 `image_size` 가 죽은
    값이고(`as never` 캐스트로 타입 검사를 우회한 자리), 실제로는 `arToImageSize(undefined) = 'auto'`
    로 돌아 캔버스가 1번 레퍼런스 비율을 따르고 있었다. 살리면 **캔버스가 실제로 바뀌므로**
    finalize 크롭 가드와 함께 재검증해야 한다.
  - 남은 순서: **오너 새 프로젝트 테스트(재측정) → B12 판단**. B12(그리드에서 앵커를 4개 중 마지막 →
    1번 자리로)는 "FIRST = 러프 시트" 계약과 충돌하므로 재측정 결과로만 판단한다. B10 은 독립 처리.
- **데이터**: 이번 수리로 바뀐 데이터는 `style_anchors` 의 `real_3d` **1행**(`image_url` →
  `real_3d_v2.png`)뿐이다. 구 파일과 `preview_url` 이 그대로 남아 **롤백은 UPDATE 1회**로 끝난다
  (SQL 은 `anchor-B1.md` §8). 앵커 교체는 **신규 생성에만** 영향하므로 기존 산출물은 변하지 않는다 —
  기존 프로젝트의 소급 재생성은 비용·오너 선별 문제라 별건이며 지금 범위 밖.
- **검증** (닫는 조건):
  1. **B4·B5·B7 배송 확인 — 새 3D 프로젝트에서 실측한다.**
     ① 시트 프롬프트에 **`art_style` 토큰이 살아 있고**(옛 규칙이 지우던 바로 그 토큰)
     **`photorealistic` 이 없는지** — 두 조건이 동시에 성립해야 B4 가 제대로 뒤집힌 것이다.
     ② `shot_video` 프롬프트에 **Arri 기재 문구가 없는지**(B7).
     프롬프트 열람은 `generation_jobs.input_snapshot` — 그리드 경로는 B9 로 이제 여기 남는다.
  2. **B1 프로덕션 검증 — 이것이 이 항목을 닫는 마지막 조건이다.** 지금까지 확인된 것은
     **higgsfield 경로에서의 방향성**뿐이다(후보 제작과 전이 프로브가 거기서 돌았다). 프로덕션 생성은
     **fal** 이므로 같은 앵커가 같은 효과를 내는지는 **미확인**이다. **오너의 새 프로젝트 테스트가 이
     검증을 담당한다** — 교체 후 5분(캐시 TTL)이 지난 뒤 생성한 시트가 preview 레지스터(스타일라이즈드
     3D 캐릭터)에 부합하는지 육안. 이 판정 전까지 F-004 는 `부분수리` 를 유지한다.
  3. **앰버 프로브 2장**(진행 중) — 씬 프롬프트로 석양이 복원되면 중립 주광 채택은 무비용이다.
     복원되지 않으면 **앵커가 그레이드를 과결정**한다는 뜻이므로 채택을 다시 판단한다.
  4. 새 앵커로도 산출물이 미달하면 **별도 조사** — 용의선상은 그리드 경로에서 앵커가 4개 중 마지막에
     놓이는 것(B12) + 캐릭터 시트발 스타일 재전파다. 단 시트 쪽 매체어는 B4 로 빠졌으므로 재측정이 먼저.
     비교용 URL 3종(구 앵커 png / preview jpg / 최종 그리드 산출)은 오늘 조사 산출 §7.
  - 범위 메모: B5 스크럽은 `applyStyleAnchor` 를 타는 경로(시트·월드·단일 스토리보드·draft-trigger)에만
    걸린다. **배치 그리드는 이 함수를 쓰지 않지만**, 그 프롬프트에는 프로젝트 산문이 실리지 않고
    (레이아웃 지시 + 칸별 인물 배정뿐) 누출 경로가 아니다 — 이 경로에 남은 것은 앵커 **위치**(B12)다.
- 기록: 2026-08-12

### F-005 · `shots.prompt` 전량 공란   `상태: 부분수리` ★ F5-R3 복구 완료 (남은 것: F5-R2 check 제약 — 이제 적용 가능)

> 백로그 번호가 F-003 과 겹치므로 이 항목의 처방은 **F5-R1 … F5-R4** 로 쓴다(포렌식 문서의 R1~R4).
> 특히 **F5-R3(같은 행 UPDATE 복구)과 F-003 R3(persist DELETE 범위)은 완전히 다른 작업**이다.

- **증상**: `shots.prompt` 가 통째로 빈다. `dc531572` 36행 전부에서 처음 관측됐지만 개별 사고가 아니라
  **420행 / 11개 프로젝트**의 상태였다(F5-R3 로 복구 완료 — 데이터 칸). 공란 프롬프트는 하류(실사 단일·
  영상·연속성 캐리오버)를 추상 폴백으로 밀어넣는다 — 코드 주석이 "과거 실측(25샷 prompt='')의 재발"로
  경고하던 바로 그 상태다.
- **원인**: **확정됐다. persist 는 무죄다.**
  - 판별 실험 하나로 갈렸다 — 채움률을 `projects.current_stage` 로 가르면 **예외 0건**:
    **director 도달 9/9 프로젝트 = 0%**, **writer·artist 19/19 = 100%**. 같은 날 같은 코드로 돌아간
    런들이 스테이지에 따라 완전히 갈린다. 파이프라인 산출 문제가 아니라 **스테이지 전이 문제**다.
  - persist 무죄 3단 증거 — ① persist 입력(run state)에 rich 프롬프트가 260~398자로 실존
    ② 폴백 사슬(`composition || actionEn || actionNative`)상 코드가 공란을 쓸 수 없다
    ③ **같은 행에서** `static_spec.first_frame_prompt` 는 355자로 살아 있는데 `prompt` 만 0자다 —
    둘은 같은 insert 의 같은 객체 리터럴이라 insert 시점 공란이면 이 비대칭이 성립할 수 없다.
    → **insert 이후 prompt 만 지워졌다.**
  - **범인**: director 캔버스의 write-through. `director-store.ts` 의 `debouncedShotSaveToDb` 가 노드의
    **legacy 필드** `data.prompt`(항상 `''`)를 `shots.prompt` 로 덮어쓴다. `hydrateFromDb` 는 prompt 를
    노드로 되읽지 않으므로 그 필드는 영원히 `''` 다. 트리거는 첫 director 진입(신규 shot 노드 patch 가
    항상 camera·lighting 을 포함) → **전 샷 동시 발화**. 관측된 all-or-nothing 과 정확히 일치한다.
    보강 증거 — 공란 행은 전부 `''` 이고 **`NULL` 이 0건**이다(컬럼 default 는 NULL, 트리거 없음).
    prompt 를 아예 쓰지 않는 채팅 CRUD 행(NULL 이어야 한다)까지 `''` 라는 것은, 그 행들도 director 가
    한 번 더 밟고 지나갔다는 뜻이다. 컬럼 누락이 아니라 **빈 문자열이 명시적으로 기록**된 것.
  - **회귀 커밋 `2eb25ea`(2026-07-21)**. write-through 자체는 `406076e`(06-12)부터 있었지만, 그때는
    sync 가 `data.prompt` 에 writer 값을 넣어 줘 **같은 값을 되쓰는 무해한 왕복**이었다. 2eb25ea 가
    값을 `derivedPrompt` 로 이관하면서 write-through 만 따라가지 못해 **파괴적 쓰기로 바뀌었다.**
    즉 "언제부터"의 답은 프로젝트 생성일이 아니라 **director 최초 진입일**이다.
  - **자기 악화 루프**: 1차 진입에서 DB 가 비워지면, 2차 sync 의 `sourcePrompt` 가
    `'' || action_description` 이 되어 `derivedPrompt` 까지 **추상 액션으로 강등**된다. 되읽기가 없어
    복구 경로가 없다.
- **수리**: `0c8c61c`(`#F-005 R1`) — write-through 의 update 페이로드에서 `prompt` 제거 + `dbCols`
  트리거에서 `'prompt'` 제거. **출혈만 정지한다** — 이미 지워진 행은 이 커밋으로 소급되지 않는다
  (소급은 데이터 칸의 **F5-R3** 가 따로 처리했다).
  기능 손실 없음: director 의 사람 편집(`promptOverride`)은 지금도 DB 에 가지 않는다(persist 의
  `TODO(P4)` 예약석). 아키텍처 §5 원칙 1("스테이지는 다른 스테이지의 생성물을 고치지 않는다") 복원.
- **데이터**: **F5-R3 소급 복구 실행 완료(2026-08-12 저녁).** 커밋이 아니라 DB 변경이라 여기가 유일한
  기록이다.
  - **오너 명시 승인**으로 집행했다 — "F5-R3 진행해줘. 수정 후에 내가 프로젝트 페이지 들어가서
    확인해볼게". F-003 의 "기존 프로젝트 수정 불요" 방침과 어긋나 보이지만 같은 결정의 연장이다:
    거기서는 **덮어쓰기**를 안 하기로 한 것이고, 여기는 **지워진 파생값을 같은 행에서 되살리는 것**이며
    오너가 직접 확인하겠다고 했다.
  - 집행 전 카운트가 포렌식과 **정확히 일치**(공란 420행 / 복원가능 278)함을 확인한 뒤 UPDATE 했다.
    결과 **공란 0행**(전체 1697행, 평균 prompt 254자). 스팟 체크 — `dc531572` `sh_01_01~03` 이 rich
    구도 산문으로 복원됐다.
  - 순서는 포렌식 처방대로 **F5-R1(`0c8c61c`, 코드 선배포) → F5-R3(오늘)**. 반대로 했다면 다음 director
    진입에서 즉시 다시 지워졌을 것이다.
  - 롤백: 영향 420행의 id 목록이 `dev/db-backups/2026-08-12-f5r3-backup-affected-rows.json` 에 있다
    (그 id 들만 `prompt=''` 로 되돌리면 된다). gitignore 디스크 영속 — DB 데이터라 저장소엔 안 싣는다.
  - **남은 것은 F5-R2**(빈 문자열 쓰기를 막는 DB check 제약). 기존 공란을 먼저 메워야 걸 수 있었는데
    **이제 공란 0 이므로 적용 가능한 상태가 됐다.**
- **검증**:
  1. **새 프로젝트에서 재발 없음 확인** — writer 완료 → director 진입 → 카메라·조명 편집까지 한 뒤에도
     `shots.prompt` 채움률이 **100% 유지**되는지. 첫 진입과 편집 재발화가 이 사고의 두 트리거였으므로
     둘 다 밟아야 한다. 채움률 쿼리는 §원인의 스테이지 대조와 같은 것
     (`count(*) filter (where coalesce(prompt,'') <> '')`).
  2. **복원분 육안 확인 — 오너 직접**, 기존 프로젝트 페이지에서 프롬프트가 살아 있는지.
     ⚠ **하드 리프레시 후에 열 것.** 열려 있던 **구 번들 탭**은 아직 `0c8c61c` 이전 코드라 그 탭에서
     director 편집을 하면 방금 복원한 행을 **다시 지운다.** 수리는 배포됐어도 이미 로드된 탭에는
     적용되지 않는다.
  3. F5-R2 를 걸면 그 시점부터 빈 문자열 쓰기가 **에러로 승격**된다 — 같은 실수가 다시 조용히
     지나가지 못하게 되는 것이 이 항목을 닫는 마지막 조건이다.
  - 범위 메모: 이 공란은 **F-001 과 별개**다. 포렌식은 director 프롬프트 강등이 "F-001 과 같은 뿌리일
    개연성"을 적었지만, F-001 의 확정 3건은 전부 `storyboard_real_grid`(배치 그리드) 산출이고 그
    경로는 `shots.prompt` 텍스트를 아예 쓰지 않는다(같은 포렌식의 피해 등급도 D-무피해).
    **두 사고를 합치지 말 것** — F-005 를 고쳐도 F-001 은 줄지 않는다.
- 기록: 2026-08-12

# dev 프리뷰에서 직접 해 볼 것 — 운영 반영 판단용

2026-09-14 · 대상: dev 브랜치 프리뷰(`8dcbd07b`, 개발 DB에 4축 트리거 적용됨) · 소요 약 30~40분(영상 완료 대기 포함)

이번에 바뀐 것 두 가지가 운영에서 문제를 일으키지 않는지 보는 것이 목적이다.
1. 자리 부족(429)에서 자동 재시도가 없어졌다 — 안내만 뜬다.
2. 이미지 5경로가 "fal 제출 → 기록"에서 "자리 예약 → 제출 → 접수 번호"로 바뀌었고, DB가 자리를 센다.

## 준비

| 항목 | 값 |
|---|---|
| 접속 | Vercel dev 프리뷰 URL |
| 계정 | `test-d4fd109f@tale.studio` / `rrH6qAh5UQmq8_vA` (개발 DB 전용, 방금 만듦) |
| 계정을 이걸로 써야 하는 이유 | 오너 구글 계정은 관리자라 "내 영상 3개·내 이미지 6개" 한도가 면제된다. 2·3번 테스트가 안 걸린다 |
| 프로젝트 | 러프까지 끝난 프로젝트 1개(없으면 Producer→Writer 한 번 돌려서 만든다). 샷 8개 이상이면 좋다 |
| Take | 영상 단계에서 "Take 부족" 안내가 뜨면 이번 변경과 무관하다 — 말해 주면 넣어 준다 |

화면 문구는 한국어 UI 기준. 영어 UI면 같은 뜻의 영어 문장이다.

## 1. 정상 생성 — 다섯 경로가 끝까지 되는가 (필수)

순서가 바뀐 경로들이다. 각각 한 번씩 생성해서 **완성 이미지/영상이 카드에 뜨면** 통과.

| # | 화면 | 누를 것 | 통과 | 실패 |
|---|---|---|---|---|
| 1-1 | Artist 인물 | 인물 하나 "생성"(시트) | 인물 시트 이미지가 뜬다 | 30분 넘게 "생성 중" / 오류 토스트 |
| 1-2 | Artist 배경 | 배경 하나 "생성" | 배경 이미지가 뜬다 | 같음 |
| 1-3 | Writer 러프 | "누락 패널 N개 생성" | 6개씩 이어서 전부 채워진다(중간에 멈추지 않음) | 6개 뒤에 멈추고 "자리 없음" 토스트 |
| 1-4 | Director 스토리보드 | 샷 하나 "실사 생성" | 실사 이미지가 뜬다 | 같음 |
| 1-5 | Director 스토리보드 | "실사 일괄 생성" | 전부 채워진다 | 같음 |
| 1-6 | Director | 샷 하나 "영상 생성" | 영상이 재생된다 | 같음 |

1-3에서 "6개 뒤에 멈춤"이 나오면: 러프는 6개씩 나가고 내 것이 끝나면 다음 6개가 자동으로 나가야 정상이다. 멈추면 실패로 기록.

## 2. 내 이미지 한도 6개 — 거절 안내가 뜨고, 끝나면 다시 되는가 (필수)

1. Director 스토리보드에서 서로 다른 샷 7개의 "실사 생성"을 **빠르게 연속으로** 누른다(일괄 말고 개별 버튼).
2. 7번째에서 토스트: **"이미지는 동시에 6개까지 생성할 수 있어요. 진행 중인 생성이 끝나면 다시 시도해 주세요."**
3. 그 뒤 아무것도 자동으로 일어나지 않는다(자동 재시도 없음). 7번째 샷은 그대로 비어 있다.
4. 앞의 것 하나가 완료된 뒤 7번째를 다시 누르면 시작된다.

통과: 2·3·4 전부. 실패: 7개가 다 시작됨(한도 안 걸림) / 토스트에 "자동으로" 문구 / 다시 눌러도 안 됨.

## 3. 내 영상 한도 3개 + 일괄 이어가기 (필수)

**3-A 개별**
1. 영상 "생성"을 서로 다른 샷 4개에 연속으로 누른다.
2. 4번째에서 토스트: **"영상은 동시에 3개까지 생성할 수 있어요. 진행 중인 생성이 끝나면 다시 시도해 주세요."**
3. 하나 끝난 뒤 4번째를 다시 누르면 시작.

**3-B 일괄 (9/9 결정 ① — 창 닫아도 계속)**
1. 영상 없는 샷이 5개 이상인 상태에서 "전체 영상 생성"을 누른다.
2. 3개가 먼저 돌고, 하나 끝나면 4번째가 **자동으로** 시작된다(이건 내 한도 안에서 이어가는 것이라 정상).
3. 도는 중에 브라우저 탭을 닫았다가 2분 뒤 다시 연다 → 진행 표시가 복원되고 계속 돈다.
4. 전부 완료.

통과: 3-A 2·3, 3-B 2·3·4. 실패: 3-B에서 3개 뒤에 멈추고 안 이어감 / 탭 닫았더니 멈춤.

## 4. 모두의 자리가 찼을 때 (선택 — dev에서만 가능)

dev 프리뷰의 전체 칸 수는 Vercel의 **dev 환경 `FAL_KEYS`** 합계다(운영 68과 다르다. 9/1 문서 기준 10칸). 그 수를 넘기면:

- 토스트: **"지금 생성 자리가 모두 사용 중이에요. 잠시 뒤에 다시 시도해 주세요."** (자동 시작 문구 없음)
- 영상 일괄이 돌던 중이면 멈추고 같은 토스트. 하나 끝나도 자동으로 안 이어감. "전체 영상 생성"을 다시 누르면 남은 것부터 새로 나간다.

한 계정으로는 내 한도(9) 때문에 10칸을 못 채운다. 두 번째 계정(오너 구글 계정 — 관리자라 개인 한도 없음)으로 이미지 여러 장을 동시에 돌려 채우면 된다. 안 해도 운영 반영 판단에는 지장 없다 — 이 축은 개발 DB 실제 동시 실험(13연결)으로 이미 확인했다(`db-results.json`: 67→68 유지).

## 5. 30분 뒤 확인 (필수)

모든 테스트가 끝나고 30분 뒤, `/queue` 화면(또는 각 카드)에서 **"생성 중"에 멈춰 있는 것이 0개**인지 본다. 하나라도 있으면 "접수 응답을 잃은 예약"이 남은 것이다 — 이번 변경의 알려진 약점이라 몇 개인지 알려 달라.

## 운영 반영 판정

| 조건 | 결과 |
|---|---|
| 1·2·3·5 전부 통과 | 운영 반영 가능 — main 머지 + 운영 DB 마이그레이션(내가 별도 절차로) |
| 1에서 하나라도 실패 | 반영 보류. 어느 경로인지 + 토스트 문구 그대로 알려 달라 |
| 2 또는 3에서 한도가 안 걸림 | 관리자 계정으로 했을 가능성부터 확인. 테스트 계정이 맞는데도 안 걸리면 보류 |
| 5에서 멈춘 카드 있음 | 개수와 어느 경로인지 알려 달라. 1~2개면 반영은 가능(30분 뒤 자리 계산에서는 빠진다), 반복되면 보류 |

## 이 가이드가 보지 않는 것

- 계정별 칸 수·전체 68칸의 동시 경합 — 브라우저로는 재현 불가. 개발 DB 실험으로 대체(`.claude/docs/2026-09-14/capacity-axes/db-results.json`).
- 이미지·영상 품질 — 이번 변경은 순서와 자리 계산만 바꿨다.
- 운영 DB 반영 절차 자체 — 별도.

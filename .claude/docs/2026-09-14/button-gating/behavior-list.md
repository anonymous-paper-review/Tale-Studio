# 버튼 수정에서 검사한 동작

직접 입력의 활성화·비활성화·동일 전송, 이미지 진행 표시가 **같이 정한 것**입니다. 응답 유실·수동 재시도·일괄 충돌·프로젝트 전환 보호는 **혼자 정한 것**으로, 이유와 대안은 검수 화면에 정리했습니다.

- **직접 입력에 공백 외 글자가 있으면 계속하기 버튼을 활성화한다**

  왜: Producer가 선택지 안의 직접 입력으로 답할 때 버튼과 Enter가 같은 답을 한 번만 보내도록 확인합니다.

- **직접 입력을 비우면 계속하기 버튼을 비활성화한다**

  왜: Producer가 선택지 안의 직접 입력으로 답할 때 버튼과 Enter가 같은 답을 한 번만 보내도록 확인합니다.

- **직접 입력에서 버튼을 누르거나 Enter를 누르면 같은 내용을 한 번만 전송한다**

  왜: Producer가 선택지 안의 직접 입력으로 답할 때 버튼과 Enter가 같은 답을 한 번만 보내도록 확인합니다.

- **전송 중이면 버튼과 Enter로 추가 전송하지 않는다**

  왜: Producer가 선택지 안의 직접 입력으로 답할 때 버튼과 Enter가 같은 답을 한 번만 보내도록 확인합니다.

- **한글을 조합 중이면 Enter로 전송하지 않는다**

  왜: Producer가 선택지 안의 직접 입력으로 답할 때 버튼과 Enter가 같은 답을 한 번만 보내도록 확인합니다.

- **이미지 작업을 접수하거나 생성 중이면 버튼을 잠그고 진행 상태를 보여준다 (편집 팝업)**

  왜: Director 패널·팝업·재진입에서 작업 중임을 보여 주고, 응답 유실 때는 중복 생성 없이 복구하도록 확인합니다.

- **이미지 작업을 접수하거나 생성 중이면 버튼을 잠그고 진행 상태를 보여준다 (편집 패널)**

  왜: Director 패널·팝업·재진입에서 작업 중임을 보여 주고, 응답 유실 때는 중복 생성 없이 복구하도록 확인합니다.

- **서버에 이미지 작업이 남아 있으면 다시 들어온 화면에서도 생성 버튼을 잠근다**

  왜: Director 패널·팝업·재진입에서 작업 중임을 보여 주고, 응답 유실 때는 중복 생성 없이 복구하도록 확인합니다.

- **기존 이미지를 다시 만들면 교체 확인 후 생성한다**

  왜: Director 패널·팝업·재진입에서 작업 중임을 보여 주고, 응답 유실 때는 중복 생성 없이 복구하도록 확인합니다.

- **화면에 다시 들어와 생성 상태를 확인 중이면 확인이 끝날 때까지 생성 버튼을 잠근다**

  왜: Director 패널·팝업·재진입에서 작업 중임을 보여 주고, 응답 유실 때는 중복 생성 없이 복구하도록 확인합니다.

- **생성 상태를 확인하지 못하면 이미지 요청 대신 상태 확인을 다시 시도한다**

  왜: Director 패널·팝업·재진입에서 작업 중임을 보여 주고, 응답 유실 때는 중복 생성 없이 복구하도록 확인합니다.

- **이미지 접수 여부가 불명확하면 상태 확인 중으로 표시하고 서버 작업이 보이면 생성 중으로 표시한다**

  왜: Director 패널·팝업·재진입에서 작업 중임을 보여 주고, 응답 유실 때는 중복 생성 없이 복구하도록 확인합니다.

- **이미지 접수 여부가 불명확할 때 상태 다시 확인을 누르면 기존 상태만 조회하고 새 이미지를 요청하지 않는다**

  왜: Director 패널·팝업·재진입에서 작업 중임을 보여 주고, 응답 유실 때는 중복 생성 없이 복구하도록 확인합니다.

- **직접 추가한 이미지의 접수 여부가 불명확하면 다시 생성 안내를 확인한 뒤에만 새 요청을 보낸다**

  왜: Director 패널·팝업·재진입에서 작업 중임을 보여 주고, 응답 유실 때는 중복 생성 없이 복구하도록 확인합니다.

- **이미지 생성이 끝나지 않았으면 1초 뒤 다시 눌러도 추가 요청을 보내지 않는다**

  왜: 여러 화면의 클릭, 응답 유실, 일괄 작업 재사용과 프로젝트 전환에서 이미지가 중복 생성되거나 잘못 저장되지 않도록 확인합니다.

- **이미지 요청의 응답을 잃으면 진행 상태를 유지하고 다시 제출하지 않는다**

  왜: 여러 화면의 클릭, 응답 유실, 일괄 작업 재사용과 프로젝트 전환에서 이미지가 중복 생성되거나 잘못 저장되지 않도록 확인합니다.

- **서버가 이미지 요청을 명확히 거절하면 실패를 알리고 다시 시도할 수 있다**

  왜: 여러 화면의 클릭, 응답 유실, 일괄 작업 재사용과 프로젝트 전환에서 이미지가 중복 생성되거나 잘못 저장되지 않도록 확인합니다.

- **다른 프로젝트로 이동했으면 이전 이미지 요청의 오류를 새 화면에 남기지 않는다**

  왜: 여러 화면의 클릭, 응답 유실, 일괄 작업 재사용과 프로젝트 전환에서 이미지가 중복 생성되거나 잘못 저장되지 않도록 확인합니다.

- **직접 추가한 이미지 노드의 접수 응답을 잃으면 자동으로 다시 제출하지 않는다**

  왜: 여러 화면의 클릭, 응답 유실, 일괄 작업 재사용과 프로젝트 전환에서 이미지가 중복 생성되거나 잘못 저장되지 않도록 확인합니다.

- **기존 일괄 이미지 작업을 기다리면 요청한 샷의 이미지만 반영하고 완료를 알린다**

  왜: 여러 화면의 클릭, 응답 유실, 일괄 작업 재사용과 프로젝트 전환에서 이미지가 중복 생성되거나 잘못 저장되지 않도록 확인합니다.

- **재사용한 이미지 작업의 샷 결과를 읽지 못하면 예전 이미지를 완료 결과로 알리지 않는다**

  왜: 여러 화면의 클릭, 응답 유실, 일괄 작업 재사용과 프로젝트 전환에서 이미지가 중복 생성되거나 잘못 저장되지 않도록 확인합니다.

- **재사용한 이미지 작업의 샷이 아직 완료되지 않았으면 다른 샷의 이미지를 대신 쓰지 않는다**

  왜: 여러 화면의 클릭, 응답 유실, 일괄 작업 재사용과 프로젝트 전환에서 이미지가 중복 생성되거나 잘못 저장되지 않도록 확인합니다.

- **수동 이미지 생성 중 프로젝트를 바꾸면 이전 이미지의 업로드를 새 프로젝트에 요청하지 않는다**

  왜: 여러 화면의 클릭, 응답 유실, 일괄 작업 재사용과 프로젝트 전환에서 이미지가 중복 생성되거나 잘못 저장되지 않도록 확인합니다.

- **직접 추가한 이미지의 불명확한 요청을 다시 생성하기로 확인하면 연속으로 눌러도 새 요청은 한 번만 보낸다**

  왜: 서버 작업과 연결되지 않은 수동 이미지의 불명확한 요청을 사용자가 확인한 뒤 한 번만 다시 생성하도록 확인합니다.

- **Writer에 연결된 이미지이거나 접수 확인이 필요한 상태가 아니면 수동 이미지 다시 생성으로 요청하지 않는다**

  왜: 서버 작업과 연결되지 않은 수동 이미지의 불명확한 요청을 사용자가 확인한 뒤 한 번만 다시 생성하도록 확인합니다.

- **처음 들어오거나 다른 프로젝트로 이동하면 생성 상태 조회가 끝날 때까지 기다린다**

  왜: 작업 목록을 아직 확인하지 못한 상태를 빈 목록으로 오해해 중복 생성하지 않도록 확인합니다.

- **생성 상태 조회가 실패하면 실패를 알리고 다시 조회해 복구한다**

  왜: 작업 목록을 아직 확인하지 못한 상태를 빈 목록으로 오해해 중복 생성하지 않도록 확인합니다.

- **생성 화면을 떠났다가 같은 프로젝트로 돌아오면 이전의 빈 작업 목록을 믿지 않고 다시 확인한다**

  왜: 작업 목록을 아직 확인하지 못한 상태를 빈 목록으로 오해해 중복 생성하지 않도록 확인합니다.

- **같은 샷의 이미지를 동시에 요청하면 한 작업만 접수하고 나머지는 기존 작업을 안내한다**

  왜: 화면의 잠금으로 막을 수 없는 다른 탭·일괄 생성의 충돌을 실제 DB에서 확인합니다.

- **일괄 이미지와 개별 이미지가 같은 샷을 동시에 요청하면 한 작업만 접수한다**

  왜: 화면의 잠금으로 막을 수 없는 다른 탭·일괄 생성의 충돌을 실제 DB에서 확인합니다.

- **서로 다른 샷의 이미지를 요청하면 각각 접수한다**

  왜: 화면의 잠금으로 막을 수 없는 다른 탭·일괄 생성의 충돌을 실제 DB에서 확인합니다.

- **이전 이미지 작업이 끝났으면 같은 샷을 다시 생성할 수 있다**

  왜: 화면의 잠금으로 막을 수 없는 다른 탭·일괄 생성의 충돌을 실제 DB에서 확인합니다.


## 영상 중복 접수와 차감 보완

같은 작업의 중복 차감과 같은 샷의 동시 생성 차단은 같이 정한 것입니다. 종료·반환 이후의 늦은 차감 차단, 금액·작업공간 불일치 거절, 기존 영상 보존은 혼자 정한 것입니다. 이유와 대안·되돌리기는 검수 화면에 있습니다.

- **다른 요청이 같은 샷의 영상을 생성 중이면 새 카드를 남기지 않고 진행 작업을 다시 확인한다**

  왜: 여러 창의 반복 요청과 실패 처리의 순서가 엇갈려도 추가 차감·중복 반환·잘못된 영상 상태가 생기지 않도록 확인합니다.

- **다른 요청이 같은 샷의 영상을 생성 중이면 기존 완료 영상을 유지하고 실패로 표시하지 않는다**

  왜: 여러 창의 반복 요청과 실패 처리의 순서가 엇갈려도 추가 차감·중복 반환·잘못된 영상 상태가 생기지 않도록 확인합니다.

- **다른 창에서 같은 샷의 영상을 동시에 생성하면 한 작업만 접수하고 차감도 그 작업만 한다**

  왜: 여러 창의 반복 요청과 실패 처리의 순서가 엇갈려도 추가 차감·중복 반환·잘못된 영상 상태가 생기지 않도록 확인합니다.

- **같은 영상 요청을 다시 보내면 기존 작업을 재사용하고 다른 입력으로 바꾸면 거절한다**

  왜: 여러 창의 반복 요청과 실패 처리의 순서가 엇갈려도 추가 차감·중복 반환·잘못된 영상 상태가 생기지 않도록 확인합니다.

- **같은 기존 영상을 동시에 다시 만들면 기존 동시 재생성 방어를 유지한다**

  왜: 여러 창의 반복 요청과 실패 처리의 순서가 엇갈려도 추가 차감·중복 반환·잘못된 영상 상태가 생기지 않도록 확인합니다.

- **같은 샷에서 새 테이크 생성과 기존 영상 재생성이 겹치면 한 작업만 접수한다**

  왜: 여러 창의 반복 요청과 실패 처리의 순서가 엇갈려도 추가 차감·중복 반환·잘못된 영상 상태가 생기지 않도록 확인합니다.

- **같은 샷의 서로 다른 기존 영상을 동시에 재생성하면 한 작업만 접수한다**

  왜: 여러 창의 반복 요청과 실패 처리의 순서가 엇갈려도 추가 차감·중복 반환·잘못된 영상 상태가 생기지 않도록 확인합니다.

- **서로 다른 샷의 영상을 동시에 요청하면 각 샷에 작업을 접수한다**

  왜: 여러 창의 반복 요청과 실패 처리의 순서가 엇갈려도 추가 차감·중복 반환·잘못된 영상 상태가 생기지 않도록 확인합니다.

- **일괄 생성과 개별 영상 생성이 같은 샷에서 겹치면 한 작업만 접수한다**

  왜: 여러 창의 반복 요청과 실패 처리의 순서가 엇갈려도 추가 차감·중복 반환·잘못된 영상 상태가 생기지 않도록 확인합니다.

- **같은 일괄 항목이나 같은 재생성 요청을 다시 보내면 이미 접수한 작업을 재사용한다**

  왜: 여러 창의 반복 요청과 실패 처리의 순서가 엇갈려도 추가 차감·중복 반환·잘못된 영상 상태가 생기지 않도록 확인합니다.

- **예전 작업의 대상 정보가 빠져 있어도 연결된 영상이 같은 샷이면 중복 생성을 막는다**

  왜: 여러 창의 반복 요청과 실패 처리의 순서가 엇갈려도 추가 차감·중복 반환·잘못된 영상 상태가 생기지 않도록 확인합니다.

- **같은 작업의 같은 금액을 두 번 요청하면 한 번만 차감한다**

  왜: 여러 창의 반복 요청과 실패 처리의 순서가 엇갈려도 추가 차감·중복 반환·잘못된 영상 상태가 생기지 않도록 확인합니다.

- **같은 작업의 같은 금액을 동시에 요청하면 한 번만 차감한다**

  왜: 여러 창의 반복 요청과 실패 처리의 순서가 엇갈려도 추가 차감·중복 반환·잘못된 영상 상태가 생기지 않도록 확인합니다.

- **같은 작업에 다른 금액을 요청하면 추가 차감 없이 거절한다**

  왜: 여러 창의 반복 요청과 실패 처리의 순서가 엇갈려도 추가 차감·중복 반환·잘못된 영상 상태가 생기지 않도록 확인합니다.

- **작업에 속하지 않은 작업공간으로 요청하면 어느 원장에서도 추가 차감하지 않는다**

  왜: 여러 창의 반복 요청과 실패 처리의 순서가 엇갈려도 추가 차감·중복 반환·잘못된 영상 상태가 생기지 않도록 확인합니다.

- **지급분 두 곳에서 같은 작업을 다시 차감하면 분할 행의 합계만 한 번 차감한다**

  왜: 여러 창의 반복 요청과 실패 처리의 순서가 엇갈려도 추가 차감·중복 반환·잘못된 영상 상태가 생기지 않도록 확인합니다.

- **같은 작업 반환을 동시에 반복하면 원래 차감한 금액만 반환한다**

  왜: 여러 창의 반복 요청과 실패 처리의 순서가 엇갈려도 추가 차감·중복 반환·잘못된 영상 상태가 생기지 않도록 확인합니다.

- **이미 반환한 작업을 다시 차감하면 추가 차감 없이 거절한다**

  왜: 여러 창의 반복 요청과 실패 처리의 순서가 엇갈려도 추가 차감·중복 반환·잘못된 영상 상태가 생기지 않도록 확인합니다.

- **차감 재전송과 반환이 경쟁하면 반환한 금액이 다시 차감되지 않는다**

  왜: 여러 창의 반복 요청과 실패 처리의 순서가 엇갈려도 추가 차감·중복 반환·잘못된 영상 상태가 생기지 않도록 확인합니다.

- **예전 중복 차감과 일부 반환이 있으면 지급분별 미반환 총액을 모두 반환한다**

  왜: 여러 창의 반복 요청과 실패 처리의 순서가 엇갈려도 추가 차감·중복 반환·잘못된 영상 상태가 생기지 않도록 확인합니다.

- **완료된 작업의 기존 차감을 다시 요청하면 기존 처리를 반환한다**

  왜: 여러 창의 반복 요청과 실패 처리의 순서가 엇갈려도 추가 차감·중복 반환·잘못된 영상 상태가 생기지 않도록 확인합니다.

- **없는 작업의 차감을 요청하면 원장을 기록하지 않는다**

  왜: 여러 창의 반복 요청과 실패 처리의 순서가 엇갈려도 추가 차감·중복 반환·잘못된 영상 상태가 생기지 않도록 확인합니다.

- **실패한 작업을 반환하고 새 작업을 요청하면 새 작업만 정상 차감한다**

  왜: 여러 창의 반복 요청과 실패 처리의 순서가 엇갈려도 추가 차감·중복 반환·잘못된 영상 상태가 생기지 않도록 확인합니다.

- **실패 처리의 행 잠금 뒤에 차감이 도착하면 실패 확정 후 차감을 거절한다**

  왜: 여러 창의 반복 요청과 실패 처리의 순서가 엇갈려도 추가 차감·중복 반환·잘못된 영상 상태가 생기지 않도록 확인합니다.

- **차감 확정 전에 실패 처리가 도착하면 확정한 차감을 모두 반환한다**

  왜: 여러 창의 반복 요청과 실패 처리의 순서가 엇갈려도 추가 차감·중복 반환·잘못된 영상 상태가 생기지 않도록 확인합니다.

- **다른 작업이 남은 잔액을 동시에 사용하면 잔액을 넘는 차감을 거절한다**

  왜: 여러 창의 반복 요청과 실패 처리의 순서가 엇갈려도 추가 차감·중복 반환·잘못된 영상 상태가 생기지 않도록 확인합니다.

- **환불하거나 만료한 지급분이 있으면 기존 계산대로 사용 가능한 지급분에서만 차감한다**

  왜: 여러 창의 반복 요청과 실패 처리의 순서가 엇갈려도 추가 차감·중복 반환·잘못된 영상 상태가 생기지 않도록 확인합니다.

- **한도 미적용 작업을 반복하면 지급분 없는 차감도 한 번만 기록한다**

  왜: 여러 창의 반복 요청과 실패 처리의 순서가 엇갈려도 추가 차감·중복 반환·잘못된 영상 상태가 생기지 않도록 확인합니다.

- **다른 요청에서 같은 샷의 영상을 생성 중이면 기존 작업을 안내하고 추가 차감하거나 제출하지 않는다**

  왜: 여러 창의 반복 요청과 실패 처리의 순서가 엇갈려도 추가 차감·중복 반환·잘못된 영상 상태가 생기지 않도록 확인합니다.

- **차감이 승인되지 않았으면 잔액 부족 표시가 없더라도 외부 영상 생성을 시작하지 않는다**

  왜: 여러 창의 반복 요청과 실패 처리의 순서가 엇갈려도 추가 차감·중복 반환·잘못된 영상 상태가 생기지 않도록 확인합니다.

- **이미 반환된 작업의 차감을 거절하면 외부 영상 생성을 시작하지 않는다**

  왜: 여러 창의 반복 요청과 실패 처리의 순서가 엇갈려도 추가 차감·중복 반환·잘못된 영상 상태가 생기지 않도록 확인합니다.


# 그룹 C — 생성 파이프라인 버그 (생성이 깨지거나 막힘)

2026-08-27 기준 상태: **완료 — 8건 전부 닫힘**

## 이슈

| # | 화면 | 증상 원문 | 상태 |
|---|---|---|---|
| C1 | Director | 영상 생성 중에 이미지 생성을 하면 에러 (그러고 Generated Storyboard가 12→11로 줄어듦) | **닫힘** — 그룹 A 분리 풀 |
| C2 | Director | 영상 생성 눌렀으나 끊기고 생성 안 됨 | **부분 닫힘** — 한도 안내. 자동 대기열은 백로그 |
| C3 | Director | 전체 재생성 누르려는데 X표로 막힘 | **닫힘** (08-27) |
| C4 | Director | Node에서 영상생성 버튼 누르면 Storyboard로 튐 | **닫힘** (08-27) |
| C5 | Director | real 바로 생성 끄기 | **닫힘** (08-27) |
| C6 | Director | Pre-viz에서 재생성 못 함 | **닫힘** — 다른 세션 F10 |
| C7 | Artist | 정보가 생성 안 되는 부분 있음 | **닫힘** — 시트 템플릿 v3(다른 세션 #f8) |
| C8 | Editor | Draft Render 눌러도 변화 없음 | **닫힘** — 다른 세션 `#draft-render` |

## 관찰 메모

- C1+C2는 동시성 문제로 보임: 동시 호출 한도(클라이언트 큐 or fal 계정 한도)에 걸리면 에러 처리 없이 조용히 실패하고, 카운트까지 어긋남(12→11은 실패한 잡이 목록에서 사라진 것으로 추정).
- C5+C6은 "Pre-viz 단계를 건너뛰는 플로우"라는 하나의 설계 결정에서 나온 두 증상.

## 논의 필요 항목

1. **C5 real 바로 생성 끄기**: 오너 지시는 명확 ("real 바로 생성 끄기"). Pre-viz → 오너 확인 → Real 생성의 2단계 플로우로 복원하면 되는지, 아니면 설정 토글로 둘지.
2. **C2 동시 호출 한도**: fal 계정 등급별 동시 한도를 확인하고, 클라이언트에 큐(대기열)를 둘지 서버에 둘지. 큐에 쌓인 건 "대기 중 n번째"로 표시.
3. **C1 카운트 감소**: 실패한 잡을 목록에서 지우지 말고 "실패 — 다시 시도" 상태로 남길지.
4. **C8 Draft Render**: 기대 출력물이 뭔지 정의 필요 — 파일 다운로드? 미리보기 재생? (그룹 H의 출력 기능과 경계 정리)
5. **C3 X표**: 어떤 조건에서 전체 재생성이 막히는지 재현 후, 막는 게 의도(생성 중 중복 방지)라면 이유를 툴팁으로 노출.

## 완료 조건 (초안)

- 영상+이미지 동시 생성 시 에러 없이 큐잉되고, 실패는 "실패" 상태로 남아 재시도 가능.
- Real 자동 생성 꺼지고 Pre-viz 단계에서 확인 가능.
- Draft Render가 눈에 보이는 결과물을 낸다.

---

## C5 — 닫힘. 진입 자동 실사 생성 제거 (2026-08-27)

### 오너 지시

> "real 바로 생성 끄기라는 게 Director로 넘어오면 i2i 이미지가 바로 생성되는 로직인데
> 그걸 없애고, 특정 버튼으로 전체 재생성하든, 하나하나 눌러서 단독 생성하든,
> 채팅으로도 둘 다 가능하게 하는 걸로 바꾸는 것"

### 껐던 것 — 발사 지점이 둘이었다

| 위치 | 내용 |
|---|---|
| `director/page.tsx` | 진입 즉시 발사(`#real-grid-auto` 이관분) |
| `use-writer-director-sync` Pass 2.7 | "안전망"으로 남아 있던 두 번째 발사 |
| `real-batch-client` | `triggerRealBatchAutofill` 정의 자체 제거 — 남기면 누군가 다시 부른다 |

하나만 껐으면 다른 하나가 계속 쐈다.

### 세 경로 — 하나는 비어 있었다

| 경로 | 상태 |
|---|---|
| 전체 버튼 → `runRealBatch` | 이미 있었음 |
| 개별 클릭 → `generateStoryboardImage` | 이미 있었음 |
| **채팅** | **없었음 — 이번에 만듦** |

채팅 액션 목록에 영상 생성(`generateVideo`)은 있는데 **실사 이미지 생성이 없었다.**
자동 생성을 끄면서 이걸 안 메웠으면 "채팅으로도 가능하게"라는 지시가 지켜지지 않는다.
`generateImage` 액션을 신설했다 — id 를 주면 그 샷, 생략하면 미생성 전체.
라우트 허용·검증·프롬프트 문서화·스토어 실행 4지점을 배선했다.
(`real-batch-client` 가 스토어를 import 하므로 전체 일괄은 동적 import 로 순환을 끊었다.)

### 검증

실사 14개를 **전부 비운 상태**로 Director 진입 — 예전이면 즉시 발사됐을 조건이다.

```
진입 14:18:13 → 15초 후: 활성 잡 0건 · 스피너 0개 · 토스트 없음
상단 전체 버튼 클릭 → storyboard_real_grid 잡 생성(14:20:52) — 수동 경로 생존
```

## C4 — 닫힘. 생성 버튼이 화면을 뺏지 않는다 (2026-08-27)

원인은 한 줄이었다. 이미지·영상 생성 액션 둘 다 `set({ viewMode: 'storyboard' })` 로
화면을 밀고 있었다 — Node 뷰에서 눌렀는데 Storyboard 로 튀는 이유.

수리: 스토리보드 뷰에 있을 때만 실사 모드로 맞추고, Node 뷰면 그 자리에 머문다.

검증: Node 뷰에서 생성 클릭 → `viewMode: 'node'` 유지
(`evidence/c4-01-stays-on-node.png`).

## 남은 것 (3건)

| # | 증상 | 다음 행동 |
|---|---|---|
| C3 | 전체 재생성 X표로 막힘 | 어떤 조건에서 막히는지 재현. 의도된 잠금이면 이유를 툴팁으로 노출 |
| C7 | Artist 정보 생성 안 되는 부분 | **오너 스크린샷 필요** — 어느 필드인지 특정 안 됨 |
| C2 잔여 | 자동 대기열("대기 n번째") | 그룹 A 백로그와 같은 항목 — 우리가 줄을 갖는 2단계 |

회귀 잠금: `tests/director-no-auto-real.test.ts`(8) — 자동 발사 부활·화면 강제 전환·
채팅 액션 누락을 각각 잡는다.

## C7 — 닫힘. 시트의 빈 칸이 문제였다 (2026-08-27)

### 오너 스크린샷으로 특정

원문("정보가 생성 안 되는 부분 있음")만으로는 어느 필드인지 알 수 없었다. 오너 노트
(Notion "8월 개발결과" 25번 항목)의 첨부 이미지를 받아 확인했다 —
`evidence/owner-notes/c7-artist-missing-info.png`.

캐릭터 시트 3장(성기·계연·옥화)을 나란히 놓으면 **같은 템플릿인데 칸마다 채워지는 게 다르다**:

| 칸 | 성기(주인공) | 계연(조연) | 옥화(조연) |
|---|---|---|---|
| CHARACTER CONCEPT 설명글 | 없음 | 있음(영문) | 없음 |
| DETAIL NOTES | 빈 칸 | 빈 칸 | 있음(한글) |
| DETAIL POINT 3칸 | 이미지 | 이미지 | 빈 회색 칸 |

즉 "정보가 생성 안 됨"의 실체는 **모델이 채울 수도 안 채울 수도 있는 텍스트 칸을 시트가
들고 있던 것**이다. 채우면 가짜 글자, 안 채우면 빈 칸 — 어느 쪽도 쓸모가 없다.

### 이미 고쳐져 있었다

어젯밤 다른 세션의 시트 템플릿 v3(`f18daf8`, #f8)가 정확히 이 지점을 지목해 제거했다.
커밋 사유에 그대로 적혀 있다 — "빈 노트 칸(가짜 글자 유발)".

v3 스펙(`src/lib/artist/sheet-template.ts`)에 남은 칸: **PORTRAIT · PALETTE ·
TURNAROUND · POSE · DETAIL**. 문제가 됐던 CHARACTER CONCEPT 설명글·DETAIL NOTES·
SIZE GUIDE·SKETCH STYLE 은 전부 사라졌다.

기존 v2 시트는 그대로 남고 **재생성부터 v3가 적용**된다.

## 오너 원본 노트 증거 (2026-08-27 확보)

Notion "8월 개발결과" 페이지에서 첨부 이미지 11장을 받아
`evidence/owner-notes/` 에 보관했다. 그룹 C 밖의 항목도 함께 있으므로 해당 그룹에서 참조한다.

| 파일 | 연결 항목 |
|---|---|
| `c7-artist-missing-info.png` | **C7** — 캐릭터 시트 빈 칸 |
| `artist-required-color.png` | Artist "필수 색깔의 의미는?" (그룹 D) |
| `e2-mention-popup-cut.png` | E2 @멘션 팝업 잘림 |
| `e-white-clipped.png` | E "흰색으로 잘림" |
| `e-popup-vs-dashboard.png` | E 팝업과 대시보드 구분 안 됨 |
| `e-mention-shows-vars.png` | E 멘션에 변수 노출 |
| `b2-director-no-action.png` | **B2** Director 채팅 지시 미실행 |
| `b3-p4-video-take.png` | **B3** [p4] 등장·video take 오해 |
| `g-no-reaction-regen.png` | G 재생성 반응 없음 |
| `g-scene3-chaining.png` / `-2.png` | G Scene3 샷 체이닝 |

## C3 — 닫힘. 전체 재생성 경로가 아예 없었다 (2026-08-27)

### 원문이 정확했다

> "하나씩 하는 거 짜쳐서 **전체 재생성** 누를려고 하는데 X표로 막힘"

재현해 보니 두 갈래였다.

| 상황 | 예전 동작 | 문제 |
|---|---|---|
| 이미 전부 생성됨 | "이미 다 생성됨" 안내 후 종료 | **전체 재생성 경로가 없다** — 개별 더블클릭만 남음 |
| 생성 중 | 버튼 disabled | 왜 막혔는지 말이 없다(커서만 바뀜) |

오너가 겪은 건 위쪽이다. 하나씩 누르기 싫어서 전체를 누른 건데, 그 버튼은 애초에
"빈칸 채우기" 전용이라 채울 빈칸이 없으면 할 일이 없었다.

### 서버까지 관통해야 했다

라우트가 `if (s.storyboard_image) continue` 로 **빈칸만** 채우게 못 박혀 있었다.
force 를 서버→클라→UI 세 층에 관통시켰다:

- 라우트: `body.force` 수용 → `if (!force && s.storyboard_image) continue`
- 클라 러너: `runRealBatch(pid, { force: true })`
- UI: 전부 생성된 상태에서 확인 모달 → 승인 시 force 일괄

### 과금이 크니 바로 쏘지 않는다

샷 14개면 이미지 14장이 다시 나간다. 모달이 수까지 넣어 고지한다:

```
모든 촬영 이미지를 다시 만들까요?
샷 14개의 촬영 이미지를 전부 새로 만듭니다.
· 샷마다 과금됩니다 — 이미지 14장.
· 기존 촬영 이미지는 새 결과로 교체됩니다.
[취소] [전체 재생성]
```

disabled 사유도 툴팁으로 노출한다 — "생성이 진행 중이에요. 끝나면 다시 시작할 수 있어요."

### 검증

14/14 완료 상태에서 버튼 클릭 → 확인 모달 정상 표시, 취소 시 잡 0건
(`evidence/c3-01-regenerate-all-confirm.png`). 과금 없이 경로만 확인했다.

---

# 그룹 C 종합 (2026-08-27 완료)

| # | 증상 | 어떻게 닫혔나 |
|---|---|---|
| C1 | 영상 중 이미지 생성 에러 | 그룹 A 분리 풀(영상 3 / 이미지 6) |
| C2 | 영상 생성 끊김 | 한도 안내 토스트. 자동 대기열은 백로그(그룹 A와 동일 항목) |
| C3 | 전체 재생성 X표 | force 관통 + 확인 모달 (오늘) |
| C4 | Node에서 생성 시 화면 튐 | viewMode 강제 전환 제거 (오늘) |
| C5 | real 바로 생성 | 진입 자동 발사 2곳 제거 + 채팅 경로 신설 (오늘) |
| C6 | Pre-viz 재생성 불가 | 다른 세션 F10 |
| C7 | Artist 정보 생성 안 됨 | 다른 세션 시트 템플릿 v3 (#f8) |
| C8 | Draft Render 무반응 | 다른 세션 `#draft-render` |

오늘 연 것 4건(C3·C4·C5·C7 특정), 이미 닫혀 있던 것 4건.

회귀 잠금: `tests/director-no-auto-real.test.ts`(8) ·
`tests/director-regenerate-all.test.ts`(6).

남은 백로그: C2의 자동 대기열("대기 n번째")은 그룹 A 백로그의 "대기열 2단계"와 같은 항목이다.


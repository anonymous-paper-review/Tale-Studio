# 그룹 D — 챗 가이드/수다 부족 (에이전트 행동 설계)

2026-08-31 기준 상태: **D10·D11·D12·D13 닫힘(main 착륙) · D1~D9 재개 가능**

> 1차 착륙(2026-08-31): `feat/group-d-chat-guidance` 4커밋을 main에 머지(9e322ab)하고
> 브랜치·워크트리 삭제. 남은 D1~D9는 main에서 새 브랜치로 이어간다.
> 주의 — `feat/director-node-wiring`이 director chat route를, `feat/g4-character-variants`가
> global-chat-store를 물고 있다(이번 머지로 그쪽 rebase 비용 발생 — 착륙 순서 조율 필요).

오너가 쓴 4단계가 이 그룹 전체의 설계 원칙이다:
**명령 입력 → 이해한 내용 설명하고 행동 → 수행 중 표시 → 끝나면 다음 연계 작업 가이드** (현재는 2단계까지만 함)

## 이슈

| # | 화면 | 증상 원문 |
|---|---|---|
| D1 | 전체 | 채팅 메커니즘 4단계 (위 원칙) — 현재 "이해 설명+행동"까지만 함 |
| D2 | Writer | 내용 처음에 확정 버튼 누르면 채팅방에 뭔가 변화가 있었으면 |
| D3 | Writer | 아티스트가 먼저 되면 되었다고 말걸어주기 (최대한 수다스럽게) |
| D4 | Writer | 언제 Artist로 넘어가는지 큐사인이 채팅창에 떠야 |
| D5 | Writer | "Renderer탭에서 선택하면 바로 트리거할 수 있어요" 문장이 너무 어려움 → 결국 재생성 못 함 |
| D6 | Artist | Writer에서 캐릭터 추가하면 카드는 생기지만 생성은 직접 해야 함 — 이런 걸 채팅방에서 계속 가이드해야 |
| D7 | Artist | "나중에" 눌러 Director 이동 팝업 사라진 뒤 갈 길 막막 → Director 바에 호버링/활성화로 "준비되면 눌러서 넘어가세요" 신호 |
| D8 | Director | Director로 넘어올수록 채팅창이 불친절하고 조용해짐 |
| D9 | Director | 작업 많이 했는데 여전히 "러프스토리보드를 촬영이미지로 생성해보세요" 옛 문구가 떠있음 |
| D10 | Director | 다 안 했는데 Editor로 넘어가라고 함 (진행 판정 오류) |
| D11 | Producer→Writer | 핸드오프 승인 카드 전후 인터랙션 공백 (2026-08-31 오너 실측 — 아래 절) |
| D12 | Producer | 채팅이 스타일을 직접 반영 못하고 피커로 되돌려보냄 + 명시 버튼 뒤 승인 카드 이중 동의 + 승인 후 멈칫 (2026-08-31 오너 실측) |
| D13 | 전체 | 생성 진행 표시 싱크 안 맞음 — "Concept Artist가 생성하고 있습니다 0/N" 고착 | **닫힘(24a6ac9)** — artistImageWork가 활동 증거(큐·in-flight) 없이 '미완성=생성 중' 표시하던 것. 유휴 프로젝트 라이브 확인으로 핀 소멸 검증. Artist 탭 배지는 배선 정상(완료 이벤트 카운트+진입 시 클리어) — 핀 수리 후 재관찰 |

## 논의 필요 항목

1. **D1 4단계의 구현 단위**: "수행 중 표시"를 챗 말풍선(진행 카드)으로 넣을지, 별도 진행 위젯으로 넣을지. 말풍선이면 그룹 A에서 만들 잡 추적 인프라를 구독하면 됨 — A 설계에 의존.
2. **수다의 수위**: D3 "최대한 수다스럽게"의 기준 — 완료/실패/단계 전환마다 말 걸기까지는 확정으로 보이는데, 생성 중간 경과(50%…)까지 말 걸면 시끄러울 수 있음. 어디까지?
3. **D9/D10 진행 판정**: "다음 뭐 해라" 가이드의 근거가 되는 프로젝트 진행 상태(무엇이 몇 개 완료됐나)를 누가 계산하나 — 챗 프롬프트에 스냅샷으로 주입하는 구조 필요. 주입 주기와 형태 결정.
4. **D5 문구 톤 규칙**: 챗 문구에 탭 이름·기술 용어 대신 "어디를 눌러서 뭘 하면 되는지"를 쓰는 문구 규칙을 시스템 프롬프트에 명문화. 초안을 만들어 오너 검수 받을지.
5. **D7 스테이지 바 신호**: 호버링 액션의 구체 형태 — 펄스 애니메이션? 뱃지("준비됨")? 챗 알림과 중복이니 역할 분담.

## 완료 조건 (초안)

- 확정/생성/완료/단계전환 이벤트마다 챗이 반응한다.
- 가이드 문구가 실제 진행 상태와 일치한다 (옛 문구 잔존 없음).

---

## 진행 상태 (2026-08-27)

### D10 — 닫힘. Editor 이동 제안이 진행률을 본다

오너 원문: "다 안 했는데 Editor로 넘어가라고 함".

조건이 `nodes.some(video && videoUrl)` 이었다 — **영상이 하나만 있어도 참**이라
샷 14개 중 1개를 만든 사람에게도 "영상이 완성됐다"며 Editor 이동을 권했다.
문구까지 사실이 아니었다("You have a finished video").

수리:
- 샷 대비 영상 진행률로 판정 (`EDITOR_NUDGE_MIN_RATIO = 0.8`)
- 같은 샷에 테이크가 여러 개여도 **부모 샷 기준 1개**로 센다
- 100%를 요구하지 않는 이유: 마지막 한두 샷을 남기고 편집을 시작하는 흐름도 정상이다
- 문구가 실제 수치를 말한다 — "샷 14개 중 3개에 영상이 있어요. Editor로 넘어갈까요?"

회귀 잠금: `tests/director-editor-nudge.test.ts`(4).

### 나머지 9건 — 보류 (파일 충돌)

D1·D2·D3·D4·D6·D7·D8·D9 는 전부 채팅 표면을 건드려야 한다:

| 필요 파일 | 상태 |
|---|---|
| `src/app/api/{director,writer,artist,produce}/chat/route.ts` | **다른 세션 수정 중** |
| `src/components/layout/global-chat.tsx` | **다른 세션 수정 중** |
| `src/stores/global-chat-store.ts` | **다른 세션 수정 중** |

그 세션은 그룹 B(채팅 실행 끊김)를 깊게 파고 있다 — `chat_traces` 테이블 신설,
`generation_jobs.chat_trace_id` 연결, `chat-trace-server.ts`·`api/chat/trace` 신규,
DB 마이그레이션(`20260827120000_chat_trace_persistence.sql`)까지 나왔다.

**지금 D를 시작하면 서로 덮어쓴다.** B가 끝난 뒤 그 위에서 이어가는 게 맞다 —
D1의 4단계("수행 중 표시")는 B가 만든 trace 인프라를 구독하면 되므로, 순서상으로도 B가 먼저다.

참고로 D9(옛 문구 잔존)의 출처는 찾아뒀다: `src/lib/constants.ts` 의 `STAGE_PLACEHOLDER`.
스테이지별 **고정 문구**라 진행 상태와 무관하게 항상 같은 말을 한다. 렌더 지점이
`global-chat.tsx` 라 지금은 손대지 않는다.

### D11 — 핸드오프 승인 카드 전후 인터랙션 공백 (2026-08-31 오너 실측)

오너 원문 (루트 `_Note.md`):
- "넘어갈때 나중에 눌러놓았을때 인터랙션이 없음"
- "실제로 이렇게 있다가 한 5초뒤에 writer로 넘어가는데,, 너무 구림(인터랙션이없음)"
- "'승인 전에는 아무 실행도 시작하지 않습니다.' → 개구림"
- "버튼을 내가 누름 → 채팅이 저렇게 남는 게 별로임. 차라리 'writer로 넘어갈게요!' 이런 게 뜨던가"

캐처 판독(아래 증거 2장): 승인 버튼을 누른 뒤에도 채팅에는 카드의 impact 문구
("승인 전에는 아무 실행도 시작하지 않습니다.")만 남고, 승인 반응 발화·진행 안내 없이
약 5초 무반응 후 화면이 넘어간다. D1(수행 중 표시)·D2(확정 후 채팅 변화)·D5(문구 톤)의
교집합 — D 착수 시 첫 번째 후보. 승인 직후 (1) 카드 문구를 결과 발화("Writer로 넘어갈게요!")로
교체하고 (2) 이동 전 진행 피드백을 즉시 표시하는 게 골자. B가 만든 trace 인프라에
승인 이벤트가 이미 있으므로 구독만 하면 된다.

증거: `evidence/owner-notes/d11-handoff-card-after-approve-1.png` · `-2.png`

### D11·D12 — 구현 (2026-08-31, 브랜치 feat/group-d-chat-guidance)

- **D11** (b4af132): 제안 reply를 사람 말로("재료가 다 모였어요…"), 승인 즉시 반응 발화,
  실패 시 침묵 대신 사유 발화. 승인 경로에도 ⇄ 초대 연출 + 슬라이드 이동(54a7cff에서 보강).
- **D12** (54a7cff): 오너 실측 3건 반영 —
  1. 스타일 앵커를 채팅이 직접 반영: 카탈로그(키·라벨·매체)를 프롬프트 주입,
     모델이 styleAnchorKey 반환 → 카탈로그 검증 → setStyleAnchor 즉시 적용(무과금 규칙).
     피커/미리보기 카드 갱신이 UI 확인을 겸한다. 발명 키는 거절+정직 발화.
     **라이브 완주**: "그림체는 일본 애니로 가자" → 카드 "Style 실사→2D 일본 애니" 즉시 갱신
     → DB style_anchor_key='jp_anime' (real_jp_melo 함정 회피 — G3 이름 오해 방어 작동).
     증거: `evidence/owner-notes/d12-style-anchor-chat.png`
  2. "Writer 호출하기" 명시 버튼 = 동의(consentedHandoff) — 승인 카드 생략, 즉시 반응 발화 + 직접 실행.
     타이핑 요청은 종전대로 카드 1회(B 규칙 유지).
  3. 승인/버튼 후 멈칫 → ⇄ 연출 1.6초 후 스테이지 슬라이드.
- 테스트: 1,543 통과. zustand spyOn 누수(스프레드 복사로 목 생존) 제거 — setState 교체+복원으로 통일.

### 증거

오너 노트 첨부 이미지 중 이 그룹 관련:
- `evidence/owner-notes/artist-required-color.png` — "필수 색깔의 의미는?" (D 계열 가이드 부족)


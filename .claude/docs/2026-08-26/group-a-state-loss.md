# 그룹 A — 작업 날아감 (탭/스테이지 이동 시 상태 유실)

2026-08-27 기준 상태: **A3 닫힘 · A2 닫힘(Writer·Director 실측 완료) · A1 재현 대기**

## 이슈 원문

| # | 화면 | 증상 | 상태 |
|---|---|---|---|
| A1 | Writer | Artist 다녀오니 생성 눌러놓은 게 초기화, 하나는 재생성 불가 | 열림 — 재현 대기 |
| A2 | Director | Real창에서 다른 데 다녀오면 생성 중이던 게 다 죽음 | **닫힘 — 영상 stage-away 재현·수리·배포 검증** |
| A3 | Editor | 컷/편집 후 다른 탭 방문만 해도 원복 | **닫힘** |

---

## A3 — 닫힘 (2026-08-26)

재현: 실제 브라우저에서 컷 → Writer 방문(무작업) → 복귀 → 원복 확인 (`evidence/a3-01~04.png`).
원인: 트림은 저장 통로가 없었고(배속만 있었음), 컷 조각은 DB에 행이 없어 재진입 재구성에서 증발.
Writer는 무관 — 어느 탭이든 Editor 언마운트→재진입이 트리거.

수리:
- 트림 → `shots.trim_start/trim_end` 저장 (`/api/editor/trim` 신설, 배속과 같은 300ms 디바운스)
- 컷 조각 → editor_states 스냅샷에서 복원. 원본 샷이 DB에 살아있을 때만 살리고(삭제 부활 방지 유지), 재생 소스는 원본의 현재 영상을 따라감
- 컷 조각을 shots 테이블에 넣지 않은 이유: Writer 카드·Director 노드가 같은 테이블을 읽어 조각이 노출되고, v2 파이프라인의 전체 삭제에 쓸려나감

검증: 같은 시나리오 재실행에서 컷 생존(`evidence/a3-05-fix-cut-survives.png`), 회귀 테스트 6개(`tests/editor-trim-cut-persistence.test.ts`), smoke ok. 검증에 쓴 오너 프로젝트는 원상복구함.

> **결정 brief (2026-08-26)**
> 트림·컷을 DB에 남기는 방향으로 오너 동의. "편집마다 쓰면 부담 아니냐"는 질문은 손 뗄 때 1회 저장(배속과 같은 방식)임을 확인하고 종결. 캐시만으로 안 되는 이유: 복원 때 캐시를 믿으면 삭제한 영상이 되살아나는 사고가 돌아오고, 다른 기기에서는 여전히 날아간다.

후속 티켓 (열림, 별도):
- editor_states 서버 로드 — 지금은 브라우저 저장만 읽어 다른 기기에서 컷·오디오가 안 따라옴. 저장 라우트는 있고 읽기만 배선하면 됨
- 클립 삭제가 재진입 시 되살아나는 기존 버그

---

## A2 — 닫힘. Writer·Director 실제 조건 검증 완료

### 부검에서 확인한 것 (8/25 밤 오너 세션, 52잡 전수)

- 50 완료 / 2 실패 / 방치 0. 완료 소요 최대 282초 — 10분 초과 유령 취급(L3)은 이 세션에서 배제
- 장부에 "죽은 잡"이 거의 없음 → 체감 "다 죽음"의 실체는 ①한도 거부(기록 없음) ②완료됐는데 화면 반영 안 됨(기록 없음) ③사유 없는 실패 2건

### 깔린 배선 — 생성 1건의 좌표 4개

| 좌표 | 내용 | 상태 |
|---|---|---|
| ① 제출 거부 | 429가 `writer_observability_events`에 `generation_submit_rejected_quota`(종류·축·수치)로 남음. 진입 라우트 7곳 공용 헬퍼(`quotaRejectionResponse`) | 완료 |
| ② 잡 생성 | generation_jobs 행 (기존) | 기존 |
| ③ 잡 완료/실패 | 실패 사유가 이제 항상 남음 — fal 무상세 플레이스홀더(`<none>`)는 맥락과 함께 감싸고, 저장 단계 예외는 이름·상태코드·원인 합성 + `[finalize]` 접두 (`src/lib/fal/error-evidence.ts`) | 완료 |
| ④ 화면 반영 | 잡이 큐를 떠나고 재수화가 끝난 시점을 클라가 잡별 보고(`ui_reflected`). Director 캔버스 배선 완료 | 완료 (1차) |

토스트: 429 무음 삼킴 제거(자동 일괄 생성 포함). 한국어 사전에 한도 안내 문구 등록(기존엔 영어로만 떴음).

> **결정 brief (2026-08-26)**
> 오너: 429든 큐 대기든 토스트는 무조건 띄운다. "완료됐는데 반영 안 됨"은 관측 테이블(writer_observability_events) 확장으로 어느 단계에서 끊겼는지 좌표를 남긴다. 대기열 전면 전환은 백로그로 미루고 당장은 "나중에 다시 시도" 토스트 형태 유지.

### 측정 대기 — 데이터가 쌓여야 답이 나오는 것

배선은 깔렸지만 아래는 실사용 데이터 없이는 확정할 수 없다. 오너가 평소처럼 쓰면 장부에 쌓인다.

- ① 한도 거부가 실제로 얼마나 자주 나는가 (지금까지 직접 증거는 채팅 로그 1건뿐)
- ② "완료됐는데 반영 안 됨"의 실재 여부와 빈도 — completed인데 ui_reflected 없는 잡을 세면 나옴
- ②가 나온다면: 반영이 **아예 없는** 건지(감지 경로 문제), **늦는** 건지(폴링 주기·재수화 속도) — 완료 시각과 반영 시각의 차이로 구분
- 구분 안 되는 것 하나: 스토어에는 반영됐는데 리액트가 화면에 안 그리는 경우. 현 배선은 스토어 반영까지만 기록한다. 데이터에서 "반영됐다는데 사용자가 못 봄"이 나오면 그때 이미지 로드 시점 보고를 얹는다

후속 (열림):
- rough 스토리보드·Artist 뷰에도 ui_reflected 배선 (1차는 Director 캔버스만)
- 분류 리포트 쿼리 (장부 데이터 쌓인 뒤)
- A1도 같은 배선으로 부검 가능해짐

---

## 동시 생성 한도 정책 (2026-08-26 확정, 코드 반영됨)

| 항목 | 값 | 대상 |
|---|---|---|
| 유저당 동시 영상 생성 | **3** | shot_video, shot_previz_video |
| 유저당 동시 이미지 생성 | **6** | character_view, world_shot, shot_storyboard, storyboard_real_grid, shot_rough_storyboard |
| 전 유저 합계 | **18** | 전체 (fal 계정 슬롯 20 − 안전 마진 2) |
| admin 계정 | 유저당 상한 면제, 전 유저 합계 상한은 적용 | isAdminEmail 명단 |
| 좀비 방어 | 30분 넘게 진행 중(queued)인 잡은 카운트에서 제외 | 전체 |

> **결정 brief (2026-08-26)**
> 오너: 영상/이미지를 (b) 분리 풀로 나눈다. admin은 정책에서 제외. 근거는 두 가지 —
> 실측(C1)에서 영상 배치가 슬롯을 다 먹어 이미지 한 장도 못 뽑았고, 요금제(tale_pricing v4)에서
> Take는 영상 전용 과금이라 영상 동시 수가 곧 과금 통제 지점이다. 이미지는 무과금·다량(샷당 6장)이라
> 처리량을 유지한다. 수치(영상 3·이미지 6)는 에이전트 제안값 — 실사용 데이터로 조정 여지 있음.
> 한도에 걸리면 영상/이미지 어느 풀인지 문구로 구분해 안내한다.

### 한도 실측 (2026-08-26 저녁, 과금 0)

가짜 진행 중 행(이미지 6 + 영상 4, `reserved:` 표식)을 장부에 심고 테스트 계정 세션으로 실서버 호출:

- 이미지 요청 → 429 "image limit (6/6)" / 같은 상태에서 영상 요청 → 429 "video limit (4/3)" — 서로의 풀에 오염되지 않음(분리 실증). 관측 이벤트 2건 정확히 기록. 흔적 전부 삭제.
- **측정 범위 명시: 이것은 우리 서버 문지기(429)의 검증이다. fal에는 요청이 1건도 가지 않았다.** fal은 동시 한도를 넘어도 실패를 내지 않고 자기 큐에 태운다(대기가 길어질 뿐). 즉 "fal에서 몇 건에 터지나"라는 측정 대상은 실패 건수가 아니라 "동시 실행 슬롯 수"다.

### fal 동시 실행 슬롯 실측 (2026-08-26 저녁, 오너 승인 실험, 실제 과금 약 $0.85)

도구: `scripts/fal-slot-probe.mjs` (우리 서버 게이트를 거치지 않고 fal 큐에 직접 제출 → 전원 상태 폴링).

| 시도 | 모델 | 건수 | 결과 | 판정 |
|---|---|---|---|---|
| 1차 | flux-2/klein/9b ($0.006) | 22 | 건당 1.2초로 3초 만에 전부 종료, 폴링 2틱 | **측정 실패** — 첫 틱에 이미 11건 완료라 동시 최대치 과소집계(관측 9는 무의미) |
| 2차 | openai/gpt-image-2 ($0.03) | 24 | 제출 거부 0건, **24건 전원이 1.3초 만에 동시 실행 진입, 대기 0**, 건당 76.8초, 전원 완료 | **슬롯 ≥ 24 확정** |
| 3차 (천장 탐색) | openai/gpt-image-2 ($0.03) | 40 | 제출 거부 0건, **40건 전원이 1.5초 만에 동시 실행 진입, 대기 0**, 건당 76.4초, 전원 완료 | 슬롯 ≥ 40 (당시엔 천장 미도달로 판단) |
| 4차 (천장 확정) | openai/gpt-image-2 ($0.03) | 60 | 제출 거부 0건, **41건 실행 + 19건 대기**(대기 최대 139.5초), 전원 완료(총 223초) | **천장 = 40 확정** — 관측 41은 폴링 경계 오차 |

확정된 사실:
- **fal은 초과분을 거부하지 않는다.** 60건을 한꺼번에 던져도 제출 실패·429가 0건이고, 넘친 19건은 대기줄에 서서 앞 작업이 끝나는 대로 순차 실행돼 전부 완료됐다. "몇 건에서 터지나"는 성립하지 않는 질문이고, 정확한 모델은 **"넘긴 만큼 뒷사람이 기다린다"**이다.
- **계정 동시 실행 한도 = 40.** 60건 실험에서 처음으로 대기가 관측됐고(41 실행 / 19 대기), 오너가 fal 대시보드에서 본 표기($500 충전 = 30건, $1000 충전 = 40건)와 일치한다. 즉 이 한도는 고정 슬롯이 아니라 **충전액 연동 티어**다.
- 코드 주석의 "fal 슬롯 20" 가정은 **틀렸다(과소)**. 그 위에 세운 MAX_GLOBAL_INFLIGHT_JOBS=18은 실측 천장의 절반도 안 되는 보수치라 산 슬롯을 크게 놀리고 있었다.
- **대기 비용의 실측치**: 넘친 19건은 최대 139.5초를 기다렸다. 전역 상한을 두는 이유가 이것이다 — 상한이 없으면 A 유저의 배치 뒤에 선 B 유저의 첫 요청이 2분 넘게 대기한다.

확정되지 않은 것:
- 모델별·플랜별 차이 여부. 측정은 gpt-image-2 한 모델 기준이다.
- 이 실측은 **동시 실행 가능 여부**만 본 것이다. 동시 40건이 실제 프로덕션에서 안전한지는 별개 — fal 과금 속도, 우리 서버의 webhook 처리량, Supabase 쓰기 부하가 함께 걸린다.
- 충전 잔액이 줄어 티어가 30으로 내려가는 시점. 그때 34는 fal 큐로 4건이 새므로 재검토가 필요하다.

> **결정 brief (2026-08-26)**
> 오너: 전역 상한 18 → **34** (실측 천장 40 − 마진 6). 유저당 상한은 영상 3·이미지 6 유지.
> 마진을 옛 2보다 크게 잡은 이유는 티어가 충전액에 연동돼 잔액에 따라 40 → 30으로 내려갈 수 있기 때문이다.
> 40에 딱 붙이면 티어 하락 즉시 대기가 길어지고, 34면 그 경우에도 초과분이 4건에 그친다.
> 재측정은 `scripts/fal-slot-probe.mjs`로 하되 실제 과금이 발생하므로 오너 승인이 필요하다.
> 이번 실험 총 비용: 4회 합계 약 $3.9.

### "화면 밖" 정의와 재현 가이드 (좌표 ④ 해석용)

화면 반영 보고 장치는 Director 페이지 레벨(`studio/director/page.tsx`의 useQueueRehydrate)에 물려 있다:

| 상황 | 완료 시 기록 |
|---|---|
| Director 안 뷰 전환(스토리보드↔노드) | 됨 |
| 스테이지 탭 전환(Writer 등) | 안 됨 — 이게 "화면 밖" |
| 브라우저 창 최소화·다른 앱 (Director 탭 유지) | 됨 (늦을 수 있음) |
| 브라우저 탭 닫기·새로고침 | 안 됨 |

재현 절차(오너): Director 탭에서 생성 → 시나리오 A(화면 띄운 채 대기 — 여기서 미반영이면 버그 확정)와 B(탭 왕복 — 미반영이 나오면 시각 메모로 대조) 둘 다 밟기. 부검: `node scripts/generation-autopsy.mjs <projectId>` (읽기 전용 — 거부/완료·미반영/실패/지연 분포 출력). 배선 설치(8/26 저녁) 이전 데이터는 전부 미반영으로 나온다.

백로그 (닫지 않음, 2026-08-26 오너 결정으로 여기서 멈춤):

**대기열 2단계 — 우리가 줄을 갖는다**
- 지금(1단계): 상한에 걸린 요청은 fal 큐에 들어가지도 못하고 429로 되돌아온다. 유저가 직접 다시 눌러야 한다.
- 2단계: 요청을 pending 으로 받아두고 슬롯이 나면 서버가 자동 승격 → "대기 3번째" 표시. 취소도 가능해진다.
- 3단계: 실측 실행시간(이미지 76초 기준)으로 예상 시간까지. 편차가 커서 2단계 안착 후에 얹는다.
- 이유: 대기 순번·예상 시간·공정성(유저 라운드로빈)·취소는 전부 **우리가 줄을 갖고 있을 때만** 가능하다.
  fal 큐에 다 맡기면 슬롯은 채우지만 순번을 못 보여주고, 취소가 불가능하며, 큐 진입 시점에 과금이 확정된다.
- 묶어서 처리할 것: C2(영상 생성 끊김) · H3(전체 생성 + 진행바)

기타 백로그:
- 거부→대기열 전환: 자리가 나는 시점은 우리 DB로 추적 가능하므로 "자리 나면 자동 시작"까지 기술적으로 열려 있음. C2 해소·H3(전체 생성+진행바)의 기반. 오너 판단으로 보류

---

## A1 — 열림, 재현 대기

- "생성 초기화": ①~④ 배선이 깔렸으므로 다음 발생 시 장부 부검으로 원인 분류 가능
- "재생성 불가" (패널 최소화 + 썸네일 미클릭 조건): 재현 절차 확보 필요 — 별도 티켓

# 증거 스크린샷

용량이 커서(장당 2.5MB) 저장소에는 넣지 않는다. 로컬 경로: `.claude/docs/2026-08-26/evidence/`

- a3-01-before.png — 편집 전 타임라인 12클립
- a3-02-after-cut.png — 컷 직후 13클립
- a3-03-writer-visit.png — Writer 탭 방문(무작업)
- a3-04-back-to-editor.png — 복귀 후 12클립 원복 (수리 전)
- a3-05-fix-cut-survives.png — 수리 후 컷 생존

## A2 실측 1회차 결과 (2026-08-26 23:08~23:14 KST, 배포본)

에이전트가 배포본에서 직접 밟았다. 상세는 `measurement-scenarios.md` 의 "실측 1회차".

- **미반영 0건.** 완료 5잡 전부 화면 반영이 기록됐고, 완료→반영 지연 중간값 5초(폴링 4초 감안 정상).
- **탭 왕복에도 끊기지 않았다.** 23:11:41 이탈 → 23:12:19 복귀, 복귀 시 스피너 복원 확인.
  화면 밖에서 끝난 2건도 복귀 후 재수화 시점에 반영 보고됨 — 배선의 사각이 예상보다 작다.
- **분리 풀 실사용 확인.** 14개 요청에 스피너 정확히 6개, 앞 배치가 끝나자 대기분이 자동 승계.
- 한계: 이번 회차는 이미지(러프)만. 영상 경로·새로고침·A1 재현은 다음 회차.

즉 A2 의 ②(완료됐는데 반영 안 됨)는 **이번 조건에서는 재현되지 않았다.** 오너가 겪은 조건
(영상 병행·긴 배치)을 아직 안 밟았으므로 열린 상태를 유지한다.

## A2 Writer 93% 붙잡힘 — 수리·통제 검증 완료 (2026-08-26 23:52 KST)

오너 실제 조건을 "Writer 내부 탭 왕복"으로 적은 것은 잘못이었다. 생성 화면에서는 내부 탭 전환이
불가능하고, 실제 조건은 브라우저 최소화·뒤 탭이었다.

- 탭 복귀 시 즉시 상태 재조회
- 화면 유지 상태에서 요청이 매달려도 15초 후 중단하고 폴링 재개
- 통제 실측: 정상 조회 ≤3초, 매달린 조회 16.8초, 복귀 이벤트 1.3초에 완료 반영
- 상세·캡처: `measurement-scenarios.md`의 "A2 통제 실측"

Writer 진행바 경로는 닫는다. Director 영상 경로는 아래 최종 실측에서 이어서 닫았다.

## A2 최종 — Director 영상 stage-away 재현·수리·배포 검증 (2026-08-26 23:55~08-27 00:22 KST)

### 첫 영상 — 버그 재현

- 23:55:02: `sh_03_07` 5초 I2V 제출 (`shot_video`, fal)
- 23:56:31: Director → Writer 실제 이탈 (URL은 유지되고 화면만 교체되는 구조)
- 23:56:50: Director가 언마운트된 사이 영상 완료·DB URL 저장
- 23:57:07: Director 복귀
- 결과: 화면 영상 0, `ui_reflected` 없음 — **A2 영상 경로 재현 확정**

원인: `useQueueRehydrate`의 `prevRef`가 복귀 마운트마다 `[]`로 시작했다. 밖에서 이미 완료돼
active 큐에서 빠진 잡은 active→settled 전환을 한 번도 못 보므로 `hydrateFromDb` 자체를 건너뛰었다.

### 수리

1. Director 복귀 마운트에서 DB 1회 재수화
2. `includeUnreflected=1` opt-in으로 최근 15분 완료·미반영 잡 조회
   (평상시 4초 active 폴링에는 추가 쿼리 없음)
3. 실제 완료 이미지/영상 노드가 생긴 잡만 `ui_reflected` 보고
4. Zustand persist가 Shot 노드를 복원하기 전에 effect가 먼저 소진되는 초기화 레이스 수정 —
   writer-backed Shot 노드가 준비된 뒤 재수화 시작

### 두 번째 영상 — 유효 Shot에서 배포 검증

- 00:07:50: 캔버스에 실제 부모 노드가 있는 `sh_01_01` 5초 I2V 제출
- 00:08:10: Director → Writer 이탈
- 00:08:20: 10초 뒤 Director 복귀
- 00:09:40: 영상 완료
- 00:09:44: 기존 화면 유지 경로의 `director-canvas` 반영 보고
- 최종 격리 배포(`fbe6527`): 영상 노드 2개 모두 `completed + URL`,
  `Shot video · Shot 1 · take 1 · completed` 카드가 실제 DOM에 표시됨
- 초기화 레이스 수리 뒤 `director-canvas-reentry` 이벤트 정확히 1건 기록

### 판정

**A2 닫힘.** Writer 93% 붙잡힘과 Director 영상 stage-away 완료 누락 모두 실제 조건에서
재현·수리·배포 검증했다. 운영 관측은 유지한다. 이후 `ui_reflected`가 있는데도 사용자가 빈 카드를
본다면 현재 측정 경계 밖인 이미지/영상 로드·React 페인트 문제로 별도 재개한다.

### 화면 증거

`.claude/docs/2026-08-26/evidence/` (대용량이라 저장소 미포함)

- `video-tab-02-submitted.png` — 첫 영상 제출
- `video-tab-03-away-writer.png` — 첫 영상 완료 전 Writer 이탈
- `video-valid-01-submitted.png` — 유효 Shot 두 번째 영상 제출
- `video-valid-02-away-writer.png` — 두 번째 영상 Writer 이탈
- `video-valid-10-card-completed.png` — 배포본 완료 영상 카드 확대
- `video-final-01-reentry-completed.png` — 격리된 최신 배포에서 복귀 완료 상태


# 동시성 DB 실험 — 실행 전 판정 기준

작성 시점: 2026-09-11, 실험 실행 전. 대상 코드는 main `867cda83`의 영상 예약 제한이다.

사용자가 요청한 현재 정책 검증이며, 제품 정책이나 구현을 변경하지 않는다. 기존 약속은 일반 사용자 영상 3개, 이미지 작업 6개, 전체 작업은 설정된 용량 이하이다. 68칸과 80칸을 각각 검사한다.

| 시나리오 | 입력 | 기존 약속에 따른 합격 기준 | 코드에 근거한 실행 전 예측 |
|---|---|---|---|
| 영상 마지막 한 자리 경합 | 같은 사람의 영상 2개 + 영상/프리비즈 12건 동시 요청 | 1건 저장, 11건 거절, 최종 영상 3개 | 사용자별 잠금으로 합격 |
| 이미지 마지막 한 자리 경합 | 같은 사람의 이미지 5개 + 이미지 12건 동시 요청 | 1건 저장, 11건 거절, 최종 이미지 6개 | 영상용 DB 방어는 이미지에 적용되지 않아 17개 저장 가능 |
| 전체 68칸의 마지막 자리 경합 | 기존 전체 67개 + 서로 다른 사용자 12명의 혼합 요청 | 1건 저장, 11건 거절, 최종 전체 68개 이하 | 사용자별 영상 방어만으로는 전체 79개 저장 가능 |
| 전체 80칸의 마지막 자리 경합 | 기존 전체 79개 + 서로 다른 사용자 12명의 혼합 요청 | 1건 저장, 11건 거절, 최종 전체 80개 이하 | 사용자별 영상 방어만으로는 전체 91개 저장 가능 |
| 완료 후 재예약 | 같은 사람의 영상 3개 중 1개 완료 후 새 영상 요청 | 새 요청 1건 저장, 진행 영상 3개 | 합격 |

왜: 영상·이미지·전체 한도는 요청이 한꺼번에 도착했을 때도 지켜지는지 확인해야 한다. 완료 후에는 다음 요청이 정상적으로 시작되어야 한다.

검증 방법:

- 고정 개발 프로젝트 `pbiumddivadgbzxuymak`만 사용하고 TLS 인증서를 검증한다. 운영 DB에는 연결하지 않는다.
- 무작위 이름의 임시 스키마에 가짜 사용자와 작업만 만든다. 실제 사용자·작업 행을 읽거나 복사하지 않는다.
- main의 `20260909120000_generation_video_capacity_gate.sql` 원문을 읽고 대상 스키마와 가짜 사용자 참조만 치환한다. 공용 스키마 갱신 알림은 제거한다.
- 12개 독립 세션이 각자 트랜잭션을 시작하고, 변경 전 같은 작업 수를 모두 읽은 뒤 저장을 동시에 진행한다. 각각의 DB 세션 번호와 트랜잭션 번호가 다름을 검증한다.
- 영상 경합은 검사자 트랜잭션이 같은 사용자 잠금을 먼저 쥐고, 12개 요청이 실제로 잠금 대기에 들어온 것을 DB에서 확인한 뒤 해제한다.
- 관측의 유효성과 정책 충족을 별개 테스트로 남긴다. 실패한 정책 기대값을 실제 결과에 맞춰 고치지 않는다.
- 종료 시 임시 스키마를 제거하고 공용 함수 전체의 지문이 실행 전후 같은지 확인한다.

검증 한계:

- 실제 PostgreSQL의 동시 트랜잭션과 main 영상 제한 함수는 실행하지만, 최소 형태의 가짜 테이블을 사용한다. live 전체 스키마·권한·다른 트리거를 재현하지 않는다.
- 이미지·전체 시나리오는 모든 사전 조회가 통과한 뒤 DB 최종 방어가 있는지를 검사한다. 실제 HTTP 경로·fal 제출·계정 선택까지 실행하지 않는다. 저장 초과 수가 곧 fal의 실제 동시 실행 초과 수라는 뜻은 아니다.
- 실제 fal 요청과 과금은 0건이다. 생성 시간, 이미지·영상 품질, fal 모델별 제한, 실제 사용자 대기 시간은 측정하지 않는다.
- 80칸은 실험 판정 기준일 뿐이며 운영 설정을 80으로 변경하지 않는다.

## 실행 결과

2026-09-11 16:00:52~16:01:01 KST에 실행했다. 위 합격 기준은 그대로 유지했다. 원본 수치와 독립 세션·트랜잭션 번호는 `db-results.json`에 기록했다.

| 시나리오 | 저장 / 거절 | 실제 최종 진행 수 | 판정 |
|---|---|---:|---|
| 사용자 영상 2개 + 12건 동시 요청 | 1 / 11 | 3 | 통과 |
| 사용자 이미지 5개 + 12건 동시 요청 | 12 / 0 | 17 | 기존 6개 한도를 DB 최종 저장에서 지키지 못함 |
| 전체 67개 + 12명 동시 요청 | 12 / 0 | 79 | 68개 한도를 DB 최종 저장에서 지키지 못함 |
| 전체 79개 + 12명 동시 요청 | 12 / 0 | 91 | 80개 한도를 DB 최종 저장에서 지키지 못함 |
| 사용자 영상 1개 완료 후 다음 요청 | 1 / 0 | 3 | 통과 |

12건 경합은 모두 서로 다른 DB 세션과 트랜잭션에서 이루어졌다. 각 경합의 12건 모두 변경 전 동일한 수를 읽었다. 영상 시나리오는 실제 잠금 대기 12건을 확인한 뒤 진행했다. 관측 유효성 5개 검사는 모두 통과했다. 정책 검사 5개 중 2개 통과, 3개 실패이며 빨간 기대값을 남겼다.

의미: 영상 개인 한도는 동시에 몰려도 DB에서 다시 검사한다. 이미지 개인 한도와 전체 한도는 이번에 실행한 main 영상 제한만으로는 마지막 저장 단계에서 보장되지 않는다. 상위 요청 검사가 모두 동시에 빈자리를 봤다면 저장이 기준을 넘을 수 있다. 실제 HTTP 전체 흐름과 fal 발주까지 검증한 결과는 아니므로 실제 운영에서 항상 17·79·91건이 발주된다고 단정할 수 없다.

68에서 80으로 늘려도 같은 방식의 경합 여지는 남는다. 용량 확대는 빈자리를 늘리고, 마지막 빈자리의 중복 선점 문제는 별도로 다뤄야 한다.

## 빨간불 판정 카드

1. 약속: 이미지가 5개 진행 중일 때 12건을 동시에 요청해도 진행 이미지 작업은 6개를 넘지 않는다.
   실제: 격리 DB에는 12건 모두 저장되어 17개가 남았다.
2. 약속: 전체 68칸 중 1칸이 남았을 때 서로 다른 12명이 동시에 요청해도 전체 진행 작업은 68개를 넘지 않는다.
   실제: 격리 DB에는 12건 모두 저장되어 79개가 남았다.
3. 약속: 전체 80칸 중 1칸이 남았을 때 서로 다른 12명이 동시에 요청해도 전체 진행 작업은 80개를 넘지 않는다.
   실제: 격리 DB에는 12건 모두 저장되어 91개가 남았다.

각 카드의 선택지: ① 기존 약속을 유지하고 동시에 들어온 요청도 한도를 지키게 고친다. ② 오너가 약속을 바꾼다. ③ 오너가 약속을 없앤다. 이번 작업은 현상 검증까지이며 정책·제품 구현은 변경하지 않았다.

## 재실행과 확인 범위

재실행에는 main `867cda83` 스냅샷을 가리키는 `--repo-path`가 필수다. 실행기는 마이그레이션 SHA256이 아래 고정값과 다르면 자격 증명을 읽거나 DB에 연결하기 전에 중단한다. 다른 버전을 선택하는 실행기가 아니다.

재실행 명령:

```sh
node scripts/test-concurrency-capacity-db.mjs --repo-path /path/to/main-snapshot --credentials-path /path/to/.env.local
```

현재 증상에서 종료 코드 1과 정책 검사 3개 실패는 검출 결과다. 증상을 통과로 바꾸기 위한 기대값·건너뛰기 변경은 하지 않았다. 접속 설정 없이 수집할 때만 기존 수동 테스트 관례에 따라 DB 실험을 건너뛴다.

- runner 구문 검사, 두 새 파일의 ESLint, 수동 테스트 파일의 독립 TypeScript 검사를 통과했다.
- 실제 실험의 임시 스키마는 삭제 후 부재를 확인했다.
- 공용 함수 28개의 전체 지문은 실행 전후 `202afba5a2139cea0fb0da649373815e`로 동일했다.
- 실제 사용자·작업 행 읽기/쓰기 0건, 공용 마이그레이션 적용 0건, fal 요청/과금 0건이다.
- 실행한 main 마이그레이션 SHA256: `4e73f397d7a7e732275df7f8e2728cef452e9aff4e4e053d006fa1a7cf4af4a6`.
- 새로 정하거나 바꾼 사용자 정책 문장은 없다. 80칸은 요청받은 비교 실험값이다.

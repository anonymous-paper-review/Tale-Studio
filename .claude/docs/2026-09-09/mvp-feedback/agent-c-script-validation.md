# 서사 언어 검증의 문자 경계 보완

**한국어·영어 이야기의 순수 일본어 서사가 검사를 건너뛰던 경계를 수정했습니다. 실제 S3 생성 경로에서 두 반례를 먼저 실패로 재현한 뒤, 완료 저장 전에 언어 오류로 중단됨을 확인했습니다. 관련 61개 검증이 통과했습니다.**

작업 시각: 2026-09-10 00:15 KST. 공용 언어 감지나 모델 품질 정책은 바꾸지 않았습니다.

문제는 검사 진입 조건이 영문/한글의 존재만 확인한다는 점이었습니다. `二人は屋上で話している。`에는 두 문자군이 없으므로, 목표가 한국어여도 검사 자체를 건너뛰었습니다. 공용 감지기는 한글 이외를 영어로 분류하므로 영어 목표에서도 통과했습니다.

수정은 `scene_content.ts`의 서사 비트 검사에 한정했습니다.

- 등록 인물·장소 이름을 먼저 제외하는 기존 순서를 유지합니다.
- 남은 문자가 있는지는 Unicode 문자(`\p{L}`)로 확인합니다. 숫자·기호만 있으면 언어 오류로 판단하지 않습니다.
- 한국어 목표에는 기존 감지기 결과를 사용합니다. 따라서 순수 일본어도 검사에 진입하여 불일치로 알립니다.
- 영어 목표에는 **가나/한자가 있으면서 라틴 문자가 없는 서사**의 명백한 불일치만 추가로 잡습니다. 가나·한자 텍스트가 어떤 언어인지 정밀하게 식별한다는 의미는 아닙니다.
- 언어 검사는 `scene_actions`만 대상으로 합니다. 별도 발화 언어의 대사·말투에는 적용하지 않습니다.

한국어 약속과 증거:

| 약속 문장 | 왜 / 확인 |
|---|---|
| 한국어 이야기에서 순수 일본어 서사 문장이 나오면 검증에서 알리고 완료한 이야기로 저장하지 않는다. | 기존 문자 진입 조건을 우회했습니다. 실제 S3 실행이 `output_language`로 실패하고 완료 파일 저장이 0회입니다. |
| 영어 이야기에서 순수 일본어 서사 문장이 나오면 검증에서 알리고 완료한 이야기로 저장하지 않는다. | 공용 감지기의 영어 기본값으로 통과했습니다. 같은 실제 S3 경계에서 저장 전에 실패합니다. |
| 등록된 일본어 이름과 숫자만 있는 비트 및 별도 발화 언어의 대사는 언어 오류로 막지 않는다. | 고유명사와 숫자를 서사 언어로 오인하거나 일본어 대사를 막으면 안 됩니다. 한국어·영어 목표 모두 정상 반환을 확인했습니다. |

00:15:25: 두 일본어 반례가 오류 없이 반환되어 테스트 2개가 실패했습니다. 약속과 기대값을 유지하고 진입 조건 및 영어 목표의 지역 검사만 수정했습니다.

00:15:43: 새 3개를 포함한 관련 이름·표시·언어·한국어 정적 검사 **7개 파일 61개 통과**. TypeScript와 변경 파일 ESLint도 통과했습니다. 원 출력: [agent-c-script-tests.txt](agent-c-script-tests.txt).

변경 파일은 `src/lib/writer/pipeline/validators/scene_content.ts`와 기존 `tests/writer/feedback-content-boundaries.test.ts`입니다. 유료 호출·재시도 추가는 없습니다.

이 검사는 혼합 언어의 품질이나 모든 문자 체계의 언어를 판별하지 않습니다. 특히 영어와 일본어가 섞여 라틴 문자가 남는 경우를 전부 거부하는 규칙은 추가하지 않았습니다. 기존 공용 감지기의 한계는 유지하며, 이번에 확인한 순수 일본어 우회만 생성 검증 범위에서 보완했습니다.

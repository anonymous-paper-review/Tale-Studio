# 테스트 원장 — 2026-09-07

파일 272 · 케이스 2031(정적 계수 — 반복·표 주도 케이스는 1로 잼) · 머리말 없는 파일 0

묶음 = tests/ 아래 폴더. red-team 은 파일 이름 꼬리로 잡는 방식별 묶음이라 표에서만 따로 세고, 본문에서는 기능 폴더 제목에 표기한다.

| 묶음 | 파일 | 케이스 | 설명 |
|---|---:|---:|---|
| producer | 20 | 128 | Producer: 입력 카드·게이트·Writer 넘김 |
| writer | 71 | 521 | Writer: 대본·무대·러프 그림·샷·단계 전환 |
| artist | 39 | 317 | Artist: 캐릭터·배경 이미지·모습·스타일 앵커 |
| director | 39 | 373 | Director: 캔버스·선·실사·영상 생성 |
| editor | 25 | 188 | Editor: 클립·자막·내보내기·미디어 저장 |
| chat | 9 | 72 | 채팅 공통: 블록·선택지·승인 카드·추적 |
| job | 16 | 107 | 생성 작업: 큐·상태·실패 원인·fal 연결 |
| billing | 13 | 121 | 결제: Take·플랜·Paddle·관리자 적립 |
| llm-call | 3 | 19 | AI 모델 호출: 재시도·응답 형식 검사·기록 |
| ui-text | 5 | 28 | 화면 문구: 번역 사전·한글 잔존·문구 규칙·내부 식별자 숨김 |
| permission | 3 | 45 | 권한: 관리자 판별·프로젝트 접근·내보내기 로그인 경계 |
| reference | 5 | 26 | 참조 프로젝트 가져오기 |
| project | 3 | 15 | 프로젝트: 만들기·지우기·공유 데이터 캐시 |
| login | 3 | 24 | 로그인·계정: 로그인 화면·테스트 계정·데모 토큰 |
| ops | 3 | 22 | 운영 도구: 서버 오류 기록·소급 스크립트·코드 검사기 |
| red-team | 8 | 77 | 테스트 방식별 묶음: 이상한 입력·경계값 공격 (파일 이름 꼬리 .red-team, 기능 폴더에 산다) |
| unsorted | 0 | 0 | 폴더에 안 넣은 파일: tests/ 바로 아래이거나 모르는 폴더 (분류 부채, 0이어야 정상) |
| manual | 15 | 25 | 수동: 실제 AI·fal 과금이나 운영 DB 쓰기가 있어 사람이 켤 때만 돈다 |

## producer — Producer: 입력 카드·게이트·Writer 넘김

### tests/producer/background-gaps.test.ts (5)
> 약속 L — 배경 카드에 빈 칸이 있으면 채팅과 여정이 그 카드를 짚는다 (_tdd.md L, 2026-09-04)
- 배경이 둘이고 하나는 완성·하나는 목적이 비어 있으면, 넘김은 통과하되 안내 목록에 "○○의 목적이 비어 있어요"가 실린다
- 완성된 배경이 하나 있으면 다른 배경의 빈 칸 때문에 Writer 넘김을 막지 않는다
- 배경이 하나뿐이고 목적이 비어 있으면 넘김이 막히고, 안내에 카드 이름과 빈 칸 이름이 적힌다
- 안내 목록의 빈 칸은 채팅 요청에 그대로 실려 간다
- 빈 칸이 채워지면 그 안내는 사라진다

### tests/producer/card-mention.test.ts (15)
> 카드와 @언급을 고르면 이름과 대상을 헷갈리지 않게 보여준다
- 이름이 있으면 이름과 종류를 함께 안내한다
- 이름이 없으면 종류를 드러내는 이름으로 같은 대상을 가리킨다
- 같은 종류에 이름 없는 카드가 여러 개면 번호를 붙여 구분한다
- 배경 이름이 없으면 화면에서 구분할 이름을 대신 붙인다
- 글에 적힌 @언급 대상만 찾아낸다
- @언급을 지우면 해당 카드가 더 이상 선택되지 않는다
- 번호가 붙은 이름을 입력하면 비슷한 이름의 카드와 섞지 않는다
- 서로 다른 @언급을 여러 개 적으면 모두 찾아낸다
- 입력이 비어 있으면 선택한 대상을 한 칸 띄워 넣는다
- 기존 글 뒤에 대상을 한 칸 띄워 덧붙인다
- 같은 대상을 다시 누르면 @언급을 빼고 띄어쓰기를 정돈한다
- L5를 다시 눌러도 비슷한 L51은 건드리지 않는다
- 화면 이름에 내부 식별값을 넣지 않고 같은 이름도 따로 구분한다
- Director Previz와 Real 대상을 서로 따로 구분한다
- 알려진 대상을 보조키와 함께 누를 때만 이름을 돌려준다

### tests/producer/cast-slug.test.ts (3)
> 등장인물 이름을 겹치지 않는 안전한 이름으로 정리해 같은 대상을 가리킨다
- 영문 이름을 입력하면 소문자와 밑줄로 정리한다
- 한글 이름은 기본 이름으로, 영문이 섞이면 영문 부분으로 정리한다
- 이미 정한 캐릭터 이름은 유지하고 같은 이름에는 번호를 붙인다

### tests/producer/chat-sync.test.ts (6)
> 채팅에서 정한 인물과 배경은 보드에 반영하고 직접 고친 내용은 보호한다
- 직접 고치지 않은 미정 외모는 새 설정을 넣으면 바로 바뀐다
- 직접 고치지 않은 오래된 배경은 새 설정에서 빼면 바로 사라진다
- 같은 대상을 가리키는 카드가 둘이면 하나로 합쳐 최신 내용을 남긴다
- 직접 고친 카드의 내용을 바꿀 때는 먼저 사용자 확인을 받는다
- 직접 고친 카드를 지울 때는 먼저 사용자 확인을 받는다
- 직접 고친 카드라도 비어 있는 칸은 확인 없이 채우되 기존 내용은 덮어쓰지 않는다

### tests/producer/content-safety-hint.test.ts (5)
> 미성년자와 위험한 상황이 함께 나오면 안전 검토가 필요하다고 알린다
- 미성년자와 위험한 상황이 함께 나오면 주의가 필요하다고 알린다 (2026-06-28 실패 케이스)
- 미성년자만 나오고 위험한 상황이 없으면 안전하다고 판단한다
- 위험한 상황만 나오고 미성년자가 없으면 안전하다고 판단한다
- 미성년자와 위험한 상황이 모두 없으면 안전하다고 판단한다
- 영어로 적힌 위험한 상황도 알아본다

### tests/producer/draft.test.ts (5)
> 저장한 초안을 다시 열어도 이야기와 설정을 잃지 않고 최신 카드 내용을 보여준다
- 초안이 아니거나 필수 내용이 빠져 형식이 깨지면 받아들이지 않는다
- 형식에 맞는 초안이면 필요한 내용을 읽어 정상 초안으로 만든다
- 저장된 초안이 없으면 현재 보드를 그대로 둔다
- 다시 들어왔을 때 보드가 비어 있으면 저장한 초안을 복원한다
- 초안에 없는 Writer 카드도 기존 보드에 함께 남긴다

### tests/producer/gate.test.ts (15)
> 이야기와 카드의 필수 내용을 갖추고 영상 스타일을 고르면 Writer로 넘긴다
- 필수 설정이 비어 있으면 Writer로 넘기지 않는다
- 이야기 준비가 안 되면 Writer로 넘기지 않는다
- 분위기와 세부 장르가 비어 있어도 필수 조건만 갖추면 Writer로 넘긴다
- 짧은 이야기(D1, 10초)는 등장인물이 없어도 Writer로 넘긴다
- D3 이야기(120초)는 등장인물이 한 명 이상 있어야 Writer로 넘긴다
- D3 인물의 변화 과정이나 원하는 목표가 비어 있으면 Writer로 넘기지 않는다
- D3에서도 사물은 이름과 모습만 있으면 Writer로 넘긴다
- D4 이야기(600초)는 인물이 한 명뿐이면 한 명을 더 준비하라고 알린다
- 배경 카드가 있어도 모든 칸을 채운 카드가 하나는 있어야 Writer로 넘긴다
- Writer가 덜 채운 인물을 추가해도 다음 단계로 넘긴다
- Writer가 덜 채운 배경을 더해도 다음 단계로 넘길 수 있다
- Writer에서 만든 배경만 있으면 부족하고 Producer 배경이 하나는 있어야 한다
- 영상 스타일을 고르지 않으면 다른 조건이 맞아도 다음 단계로 넘기지 않는다
- 영상 스타일을 정하지 않으면 선택하지 않은 경우와 같이 다음 단계로 넘기지 않는다
- 영상 스타일을 고르면 다음 단계로 넘길 수 있다

### tests/producer/handoff-gate.test.ts (2)
> 배경을 하나라도 완성하면 Writer로 안전하게 넘기고 비어 있으면 보내지 않는다
- 완성된 배경이 없으면 Writer로 넘기지 않고 시작 요청도 보내지 않는다
- 완성된 배경이 있으면 Writer를 시작하고 다음 단계로 넘긴다

### tests/producer/handoff-intent.test.ts (9)
> 다음 작업자에게 넘겨 달라는 말을 알아듣고 바로 옆 단계로 정확히 전달한다
- 제안 버튼의 문장을 그대로 입력해도 같은 다음 단계로 알아듣는다
- 사용자가 자기 말로 부탁해도 알맞은 다음 단계로 넘긴다
- 넘겨 달라는 말이 없으면 일반적인 부탁으로 남긴다
- 넘길 상대를 말하지 않으면 일반적인 부탁으로 남긴다
- 진행이나 시작만 말하면 다음 단계로 넘기지 않는다
- 바로 다음 단계가 아닌 상대에게 넘겨 달라고 하면 받아들이지 않는다
- 마지막 단계인 Editor에서는 넘길 곳이 없다고 알린다
- Writer에서 Artist로도 넘겨 달라는 말을 알아듣는다
- 대소문자와 불필요한 공백이 달라도 같은 뜻으로 알아듣는다

### tests/producer/handoff-nudge.test.ts (3)
> 다음 단계에 아직 도달하지 않았으면 넘기기를 안내하고, 이미 도달했으면 같은 안내를 다시 띄우지 않는다 (#handoff-once 2026-08-12)
- 아직 그 스테이지까지만 도달했으면 띄운다
- 다음 스테이지에 이미 도달했으면(수락됨) 다시 띄우지 않는다
- 현재 단계보다 뒤처진 기록이어도 안전하게 넘기기를 안내한다

### tests/producer/handoff-soft-warning.test.ts (4)
> 필수 정보가 빠지면 진행을 막고, 선택 정보가 빠지면 품질 경고와 함께 다음 단계로 넘긴다 (#b 2026-08-28 오너 확정)
- 필수 배경 정보가 없으면 Writer로 넘기지 않는다
- 장르 설명과 분위기가 비어 있어도 품질 경고와 함께 Writer로 넘기기를 제안한다
- 장면 정보가 없어도 품질 경고를 보여주며 Artist로 넘긴다
- 인물의 기본 모습이 있으면 뒷모습·측면 경고 없이 Director로 넘긴다 (약속 C9 2026-09-04)

### tests/producer/lifecycle.test.ts (8)
> 같은 이야기 자료는 순서가 달라도 같은 것으로 보고, 필요한 정보가 갖춰진 다음 단계만 열어 둔다
- 등장인물 순서만 바뀌면 같은 이야기 자료로 본다
- 등장인물의 겉모습이 바뀌면 다른 이야기 자료로 본다
- 배경의 쓰임새가 바뀌면 다른 이야기 자료로 본다
- 이야기 자료가 바뀌면 Writer와 선택한 Artist 이미지를 다시 확인 대상으로 알린다
- 인물 사진이 없으면 Artist 진행을 막고 물건과 장소 사진은 경고한다
- Writer에서 사용한 인물만 확인하고 나머지 인물은 진행을 막지 않는다
- Writer 준비 여부를 알 수 없으면 Director 진행을 막는다
- 현재 단계는 유지하면서 완료한 단계까지만 다음 단계 접근을 연다

### tests/producer/output-language.test.ts (11)
> 선택한 언어로 답변과 대사를 만들고, 언어가 정해지지 않으면 기존 방식으로 처리한다 (#i18n-s5 #20)
- 영어를 선택하면 영어로 답하고 필요한 예외 안내를 함께 넣는다
- 영어 문장은 대문자로 시작하게 한다 (오너 지시 2026-08-31)
- 한국어를 선택하면 한국어로 답하게 한다
- 언어를 정하지 않으면 별도 안내 없이 예전 답변 방식을 유지한다
- 영어는 단어 수로, 한국어와 언어가 없을 때는 음절 수로 말하기 속도를 정한다
- 언어를 고정한 프로젝트는 그 언어로 답하고 다시 묻지 않는다
- 언어를 아직 정하지 않았으면 이야기에서 알아낸 언어로 답하고 그 언어를 고정한다
- 고정한 언어를 알 수 없으면 별도 언어를 정하지 않는다
- 지원하는 네 가지 언어만 대사 언어로 정하고 나머지는 출력 언어를 따른다
- 대사 언어를 정하면 답변 언어와 관계없이 그 언어로 말하라고 안내한다
- 대사 언어를 정하면 그 기준을 우선하고 없으면 답변 언어를 따른다

### tests/producer/parse-extracted-settings.test.ts (8)
> 설정 형식이 답변에 드러나지 않으면서도 이야기 설정은 빠짐없이 반영된다 (C8)
- 답변 뒤에 설정 형식이 붙어도 사용자에게는 자연스러운 문장만 보여준다
- 답변 뒤에 표시된 설정 내용이 사용자에게 드러나지 않는다
- 답변 중간에 설정 내용이 있어도 앞뒤 문장만 보여준다
- 설정 내용이 여러 번 와도 보이지 않고 마지막으로 올바른 내용만 반영한다
- 잘못된 설정 내용이 섞여도 형식이 보이지 않고 설정은 비워 둔다
- 설정 표시가 대문자로 적혀도 내용을 알아본다
- 설정 표시가 끝나지 않아도 흔적 없이 안내 문장만 보여준다
- 설정 내용이 없으면 답변은 그대로 보여주고 설정은 비워 둔다

### tests/producer/pending-proposal-store.test.ts (6)
> 이미 작업을 진행한 뒤의 변경은 먼저 확인받고, 승인한 작업만 실행하며 한 번에 하나만 보류한다
- 다음 단계로 넘긴 뒤 설정을 바꾸면 먼저 확인을 받고 나서 반영한다
- 아직 비어 있는 설정을 처음 채울 때는 바로 반영한다
- 확인할 변경이 이미 있으면 새 변경은 거절한다
- Director 이미지 작업은 승인한 뒤에만 실행한다
- 실행할 내용이 없는 Director 이미지 작업은 진행하지 않는다
- 확인할 작업은 한 번에 하나만 둔다

### tests/producer/pending-proposal.test.ts (4)
> 진행 승인을 알아듣고, 확인할 작업과 영향 내용을 잃지 않고 읽기 쉽게 보여준다
- 짧은 한국어·영어 승인 말이면 진행하겠다는 뜻으로 알아듣는다
- 하지 말라는 말이나 다른 질문은 승인으로 보지 않는다
- 확인할 작업 내용을 다시 저장해도 그대로 유지된다
- 확인 카드의 영향 내용을 읽기 쉽게 한 줄씩 보여준다

### tests/producer/ref-fill.test.ts (4)
> 이름 없는 인물·배경 카드도 기존 자리에 내용을 채우고, 같은 카드를 새로 만들지 않는다
- 이름 없는 인물 카드에 내용을 채우면 기존 카드 하나만 갱신한다
- 이름 없는 배경 카드에 내용을 채우면 기존 카드 하나만 갱신한다
- 같은 종류 카드가 여러 개여도 지정한 카드 하나만 채운다
- 이름이 있는 카드의 내용을 바꿔도 해당 카드 하나만 갱신한다

### tests/producer/suggestion-dismiss.test.ts (12)
> 제안이 겹쳐도 필요한 안내가 사라지지 않고 다시 나타나도록 보장한다 (#handoff-suggestion-drop 2026-08-07, #handoff-starved 2026-08-11, #fix-scene-gate-suggestion-resurface 2026-08-25)
- 다른 선택지가 떠 있으면 다음 단계 안내를 보류하고, 자리가 비면 다시 안내한다
- 사용자가 다른 말을 해 안내가 닫히면 같은 안내를 다시 띄울 수 있다
- 사용자가 "나중에"를 누르면 같은 안내를 다시 띄우지 않는다
- 더 중요한 안내를 요청하지 않으면 기존 안내를 그대로 유지한다
- 더 중요한 안내를 요청하면 떠 있는 선택지를 내리고 새 안내를 보여준다
- 닫을 수 없는 안내(첫 인사 등)는 새 안내가 와도 유지한다
- 같은 안내가 이미 떠 있으면 다시 요청해도 그대로 둔다
- 사용자가 거절한 안내는 새 안내 요청이 와도 다시 띄우지 않는다
- 확정하지 못해 안내를 닫아도 반드시 확인할 안내를 다시 띄운다
- 일반 안내는 사용자가 "나중에" 미루면 다시 띄우지 않는다
- 반드시 확인할 안내가 떠 있으면 다른 안내가 대신할 수 없다
- 반복해서 확인해도 이미 떠 있는 확인 안내를 바꾸지 않는다

### tests/producer/system-prompt.test.ts (0)
> 스타일은 앱에서 고르게 하고 채팅에서는 설명이나 선택을 묻지 않는다 (#i18n-s5-batch6-chat)

### tests/producer/wand-mention.test.ts (3)
> 약속 M — 완드 요청에는 어느 카드인지 이름이 붙는다 (_tdd.md M, 2026-09-04 오너 확정)
- 인물·배경 카드의 완드를 누르면 보내지 않고 입력창에 "@카드이름 "만 들어가고 커서가 그 뒤에 놓인다
- 이름이 아직 없는 카드의 완드를 눌러도 AI가 어느 카드인지 알고 그 카드를 채운다
- Artist·Director·Editor 카드에는 완드를 새로 만들지 않는다(별도 결정 1안)

## writer — Writer: 대본·무대·러프 그림·샷·단계 전환

### tests/writer/adherence-core.test.ts (10)
> 장면의 움직임 요구와 실제 변화가 다르면 정확히 알려준다 (#adherence P2, 2026-08-07)
- 행동과 인원, 중요한 대상을 넣으면 확인할 수 있는 장면 설명이 된다
- 등장인물이 없으면 인물 없음으로 분명히 적는다
- 카메라가 멈춘 장면은 멈춤으로, 이동하는 장면은 이동으로 구분한다
- 카메라나 인물의 큰 움직임이 있으면 큰 움직임으로 표시한다
- 방향을 정한 이동만 방향 있는 움직임으로 표시하고 멈춤과 무방향은 제외한다
- 움직임 정보가 없으면 판정하지 않고 건너뛴다
- 멈춤을 요구했는데 큰 변화가 생기면 과한 움직임으로 판정한다 (멈춤인데 움직이는 증상)
- 큰 움직임을 요구했는데 변화가 작으면 움직임 부족으로 판정한다 (시간 대비 변화 없음)
- 멈춤 장면의 작은 변화는 허용해 화면이 얼어붙지 않게 한다
- 왼쪽에서 오른쪽으로 움직인다고 하면 화면 기준 방향을 분명히 적는다

### tests/writer/appearance-selection.test.ts (9)
> 이야기 속 시간과 선택 조건에 맞는 인물 모습을 하나로 정한다
- 장면의 이야기 시간에 맞는 인물 모습을 하나 고른다
- 시간이 맞는 모습이 없으면 기본 모습 하나를 대신 선택한다
- 맞는 모습이 없고 기본 모습도 하나로 정할 수 없으면 선택을 거절한다
- 맞는 모습이 여러 개이고 그중 기본 모습이 하나가 아니면 선택을 거절한다
- 맞는 모습이 여러 개여도 기본으로 정한 하나를 선택한다
- 사용자가 지정한 유효한 모습이 있으면 이야기 시간에 맞는 모습보다 우선한다
- 인물에게 없는 모습을 지정하면 선택을 거절한다
- 이야기 속 현재를 기준으로 과거 장면의 모습을 선택한다
- 낮과 밤 정보는 인물 모습 선택에 영향을 주지 않는다

### tests/writer/asset_refs.test.ts (5)
> 실제로 등록된 인물과 장소만 샷에 사용해 장면 구성을 일관되게 연결한다
- 등록된 장면 정보에서 사용할 인물과 장소 목록을 만든다
- 버전 표시가 붙은 인물 이름도 원래 인물로 알아본다
- 실제로 등록되지 않은 인물과 장소는 사용하지 않는다
- 등록된 한국어 장소 이름은 그대로 장소로 알아본다
- 실제 샷 순서에서 등록된 인물과 장소만 남기고 빠진 장소는 장면 정보로 채운다

### tests/writer/camera-contract-default.test.ts (4)
> 설정을 따로 정하지 않아도 장면의 핵심이 먼저 또렷하게 보이게 한다 (#camera-contract-relax 2026-08-11)
- 설정값이 없으면 장면을 고정 화면으로 완성할 수 있는지 먼저 판단한다 (2026-08-11 승격)
- 옛 카메라 안내를 선택하면 그 안내만 사용한다
- 이전에 쓰던 두 가지 카메라 안내도 선택하면 같은 결과를 보여준다
- 알 수 없는 설정값을 넣어도 기본 카메라 안내로 안전하게 처리한다

### tests/writer/camera-motivation-geometry.test.ts (4)
> 카메라 동기 — 리빌과 시점을 무대 기하가 검사한다 (2026-09-07)
- 요정은 START 프레임 밖에 있고, 코드가 END 카메라를 요정 쪽으로 돌려 END 프레임 안에 들인다
- 리빌 대상이 START 프레임에 이미 있으면 드러남이 아니라고 알린다
- 카메라가 수인의 자리·눈높이에 놓이고 수인의 향을 본다
- 수인은 명단·배치에서 빠지고 "카메라 자신"이라는 제약이 붙으며, 비트 끝에 수인이 움직이면 END 카메라가 따라간다

### tests/writer/camera-motivation-prompts.test.ts (5)
> 카메라 동기 — 러프의 MOVEMENT 줄과 영상 계약문에 "왜"가 실린다 (2026-09-07)
- 리빌 팬은 "to reveal Elf Chief, who is outside the frame at the start" 를 단다
- 표지를 강조하는 dolly_in 은 표지 라벨로 "왜"를 단다
- 시점 샷의 START 는 카메라가 누구의 눈인지 말하고 그 인물을 그리지 말라고 한다
- 카메라 절 바로 뒤에 Purpose 절이 붙고, 이름 맵이 있으면 대상 이름을 쓴다
- 동기가 없는 옛 샷은 계약문이 그대로다

### tests/writer/camera-motivation-vocab.test.ts (10)
> 카메라 동기 — 여섯으로 닫힌 목록 (2026-09-07, 오너 결정 1번: "동기 목록을 닫는다")
- 정본 여섯 값과 분명한 동의어·한국어 표현만 동기로 인정하고, 나머지는 동기 없음으로 본다
- 동기가 움직임의 꼴을 정한다 — 감정은 느린 dolly_in 으로, 에너지는 medium 이상으로, 리빌은 회전·후진·추적으로 접는다
- "왜" 문장은 동기와 대상 이름으로 영어 한 구절이 된다
- V4 가 움직임을 적었는데 동기가 없으면(데쿠파주에도 없으면) static 으로 접고 기록을 남긴다
- V4 에 동기가 없어도 데쿠파주의 camera_motivation·camera_target 을 이어받는다
- 정지 카메라는 동기가 없어도 그대로이고, 동기가 있는 무브는 꼴을 교정해 motivation·target 을 싣는다
- 데쿠파주의 motivated_move 는 여섯 중 하나로 접히고, 접히지 않으면 static 으로 내려간다
- 데쿠파주 지시서는 여섯 동기와 camera_motivation·camera_target 을 요구한다
- V4 지시서는 camera_motion.motivation·target, camera_setup.end.subject, camera_setup.pov_of 를 스키마에 싣는다
- 에너지 예외(결정 2번, 전후 비교 뒤 켬)는 기본으로 지시서에 실리고 WRITER_ENERGY_EXCEPTION=0 으로만 꺼진다

### tests/writer/chat-id-resolve.test.ts (5)
> 채팅에서 가리킨 장면과 샷을 실제 화면의 위치에 맞게 찾아낸다 (#p4-understand A2, 2026-08-06)
- 실제로 있는 샷을 가리키면 그 샷을 그대로 찾는다
- 예전 방식의 전체 순번으로 가리켜도 해당 샷을 찾는다
- 예전 프로젝트의 샷을 새 표기로 가리켜도 원래 샷을 찾는다
- 장면 순서와 장면 안의 순서로 가리켜도 해당 샷을 찾는다
- 없는 샷을 가리키면 찾지 못했다고 알린다

### tests/writer/chat-updates.test.ts (13)
> 채팅에서 보낸 장면과 대사 수정이 올바른 내용만 반영되도록 한다
- 대사의 감정과 말투, 길이 정보는 보존하고 알 수 없거나 잘못된 내용은 버린다
- 대사 목록에서 형식이 잘못된 항목과 목록이 아닌 값은 제외한다
- 기존 샷 입력 규칙을 지키며 잘못된 값은 허용 범위로 바로잡는다
- 기존 장면 입력 규칙을 지키며 잘못된 값은 허용 범위로 바로잡는다
- 대사 수가 같거나 늘거나 새로 생긴 수정은 바로 반영한다
- 다음 대사 수가 줄면 확인을 먼저 받는다
- 올바른 대사 위치는 남기고 잘못된 표시나 빈 위치는 제외한다
- 대사 위치 목록이 아니면 비우고 200개까지만 남긴다
- 등록되지 않은 인물은 제외하고 등록된 인물만 남긴다 (제외된 인물도 기록한다)
- 등록된 인물이 하나도 없으면 샷에서 인물 정보를 빼고 장면 정보를 따른다
- 장면의 인물과 대사의 화자도 등록된 인물만 남긴다
- 인물 목록을 지정하지 않으면 기존처럼 모든 인물을 허용한다 (이전 화면 호환)
- 대사를 빈 목록으로 보내면 모두 지우려는 뜻을 그대로 반영한다

### tests/writer/depth.test.ts (2)
> 영상 길이에 따라 알맞은 이야기 깊이 단계를 정해 일관되게 보여준다
- 영상 길이의 경계마다 약속된 이야기 깊이 단계로 정한다
- 너무 짧거나 긴 영상도 정해진 범위 안의 깊이 단계로 정한다

### tests/writer/dialogue-join.test.ts (5)
> 샷이 나뉘거나 번호가 바뀌어도 각 샷에 맞는 대사를 연결한다 (#dialogue-join 2026-08-10)
- ① 모든 샷에 자기 대사가 연결되고 장면도 어긋나지 않는다
- ② 나뉜 샷은 첫 번째 샷만 대사를 받고 다음 샷은 비워 둔다
- ③ 샷을 나누지 않으면 기존처럼 각 샷에 맞는 대사가 연결된다
- ④ 예전 형식의 샷도 각 샷에 맞는 대사를 찾아 연결한다
- 대사 정보가 없으면 연결 결과도 비워 둔다

### tests/writer/dialogue.test.ts (20)
> 대사를 만들면 장면마다 빠짐없이 이어지고, 일부 문제가 생겨도 나머지 내용은 계속 만든다 (#dialogue-v4)
- 대사 화면을 선택하면 해당 화면으로 이동한다 (준비 중 시절 탭 클릭 무시 사고)
- 빠진 장면은 말없이 채우고 넘친 장면은 버린다 (원래 순서 유지)
- 빈 대사와 잘못 감싼 목록, 공백 내레이션을 알아보기 쉽게 정리한다
- 장면 목록이 없는 답변은 거부한다
- 답변에서 빠진 장면을 따로 드러내 침묵과 누락을 구분한다
- 모든 장면이 답변에 있으면 누락 표시를 남기지 않는다
- 새 내용이 없어도 이전 내용을 그대로 이어 간다
- 새 내용이 많아도 최근 사실 12개와 대사 10개만 이어 간다
- 인물 말투를 한 번 정하면 다음 장면의 대사에 이어서 반영한다
- 한 장면을 만들지 못해도 그 장면만 말없이 두고 나머지는 계속 만든다
- 시간이 부족해도 지금까지 만든 장면을 남기고 다음 작업에서 이어 간다 (인물 말투는 유지)
- 첫 장면은 앞선 내용 없이 시작한다
- 앞선 장면의 요약과 직전 감정으로 다음 장면의 배경과 관계를 준비한다
- 앞서 나온 인상적인 대사는 미리 넘기지 않는다 (동시에 장면을 준비하는 대신 치르는 대가)
- 앞선 내용은 최근 12개까지만 다음 장면에 이어진다
- 모든 장면을 준비해도 결과는 원래 순서로 정리된다
- 기본 설정으로도 앞선 장면 요약이 이어진다 (2026-08-11 채택)
- 앞선 장면 요약을 사용하면 다음 대사를 준비할 때 함께 참고한다
- 앞선 장면 요약을 끄면 다음 대사에 앞선 장면이 섞이지 않는다 (비교용)
- 여러 장면을 함께 준비해도 한 장면이 실패하면 그 장면만 비워 둔다

### tests/writer/duration-reallocation.test.ts (15)
> 인지 부하 기반 시간 재배분(#p2-pacing 2026-08-04 → #duration-surgery 2026-08-31 개정) 회귀.
- 대사 있는 2초 샷은 실발화+0.5s 로 증액된다 (이중 여백 폐지)
- 과대 배정은 ceil(needed)+slack 까지 감액된다 (양방향 밴드)
- pacing_intent='long_take' 는 감액을 면제받는다 (연출 의도 보존)
- dynamic_spec 이 있으면 동사별 magnitude 를 실제로 읽는다
- 신규 인물 첫 등장 샷은 가산받고, 재등장은 가산 없다
- 초장문 대사도 상한(10s)을 넘지 않는다
- 라틴 스크립트는 빠른 발화 속도로 계산된다
- 같은 씬 3단계↑ 점프는 WARNING, 씬 경계는 무시한다
- 인서트 복귀 문법(MS→ECU→MS)은 면제된다
- 같은 씬에서 to→from 이 끊기면 WARNING, 이어지면 무경고
- 씬 경계와 arc 미출력(분할 자식·구 산출)은 건너뛴다
- 실측 재현: 시선 비트 정지 단독 샷 + 반응 없는 다인 비트 → 경고 2건
- 시선을 따라가는 카메라 무브 또는 뒤따르는 reveal/reaction 샷이 있으면 무경고
- source_beats 미운반(구 산출·분할 자식)은 판단하지 않는다
- "전당포 노인" 캐릭터가 산문의 "노인이"로, "추적자들"이 "추적자가"로 잡힌다

### tests/writer/duration-regression.harness.test.ts (1)
> 장면 길이와 인물 움직임이 자연스럽게 이어지는지 두 장면을 다시 만들어 확인한다 (#duration-surgery #w-a 2026-08-31 U17-1 오너 지시)
- 선택하지 않으면 추가 작업을 하지 않는다

### tests/writer/facet-render.test.ts (7)
> 화면의 연출 정보가 일관되고 다시 그릴 때 필요한 안내만 전달되도록 보장한다
- 같은 화면 정보는 같은 식별값을 만들고 정보가 달라지면 식별값도 달라진다
- 같은 연출 정보를 넣으면 같은 안내가 나오고 인물 위치와 화면 크기를 담는다
- 다시 그릴 때 바뀌면 안 되는 렌즈·초점·조명·색 정보를 담는다
- 이미 화면에 정해진 인물 자세·시선·구도·소품은 다시 지시하지 않는다
- 일부 연출 정보가 없으면 빈 부분은 건너뛰고 가능한 안내만 남긴다
- 안내를 만들지 못하면 정해진 기본 안내를 대신 사용한다
- 다시 그리기 기능을 끄면 기본 안내만 사용한다

### tests/writer/lane-checkpoint.test.ts (6)
> 시간이 부족하거나 한쪽 작업이 실패해도 완성된 화면과 대사가 사라지지 않는다 (#shotcheck-gate #lane-independent 2026-08-11)
- 남은 시간이 부족하면 다음 검사를 시작하지 않고 지금 만든 화면 정보를 남긴다
- 남은 시간이 충분하면 검사와 안내 작성까지 이어 간다
- 남은 시간 제한이 없으면 검사 작업을 막지 않는다
- 대사 작업이 실패해도 화면 결과는 버리지 않는다
- 화면 작업이 실패해도 대사 결과는 버리지 않는다
- 아무것도 만들지 못한 채 작업이 실패하면 다시 시도하라는 오류를 알린다 (무한 반복 방지)

### tests/writer/ledger-gap-labels.test.ts (2)
> #ledger: Director 누락 목록 — 보여주는 샷이 없는 상태 변화만 사람 말 라벨로.
- 덮이지 않은 변화만, 씬·비트·인물·변화를 담아
- getDirectorGaps 가 라벨을 갭으로 앞세운다

### tests/writer/moderation-fallback.test.ts (14)
> 모더레이션 폴백(#moderation-fallback 2026-08-05) 회귀 — 실측 2d47b311: gemini 하드 필터
- gemini PROHIBITED_CONTENT → claude 로 같은 콜 재시도 (maxTokens 바닥 16k)
- gemini 의 다른 오류는 폴백 없이 그대로 던진다
- claude 프로바이더 오류엔 폴백하지 않는다
- 접지 콜은 gemini(접지 모델 핀)로 먼저 간다 — claude 를 거치지 않는다
- 접지 모델이 죽으면(예: preview 소멸 404) claude 로 폴백한다
- 접지 콜이 아니면 gemini 실패에 claude 폴백하지 않는다
- 접지 콜도 손실 복구 재호출 보호를 받는다 (종전엔 early return 이라 건너뛰었다)
- 무손실 복구(펜스 제거·잉여 문자 삭제)는 종전대로 값을 돌려준다
- 잘린 응답은 LossyRepairError 로 던지되 살아남은 값을 실어 보낸다
- 비-strict repairJson 의 계약은 그대로다 (기존 호출자 무영향)
- 손실 복구가 감지되면 같은 질문을 한 번 다시 던진다
- 재호출도 잘리면 살아남은 값으로 진행한다 (종전과 동일한 최악치)
- 재호출이 다른 오류로 죽으면 그 오류를 표면화한다
- 손실 복구가 없으면 재호출하지 않는다

### tests/writer/motion-contract-duration.test.ts (9)
> 장면이 길어도 동작은 자연스러운 속도로 끝까지 보이고, 짧아도 마지막 화면까지 이어진다 (#g1 2026-08-27 오너)
- 장면이 길어도 동작을 자연스러운 속도로 보여 준다고 약속한다
- 짧은 장면도 동작을 자연스러운 속도로 보여 준다 (#d3 오너 실측)
- 같은 동작이라도 장면 길이가 다르면 안내가 달라진다
- 큰 동작은 장면이 길어도 남은 시간을 채우라는 안내를 덧붙이지 않는다
- 동작의 크기는 연출자가 정한 대로 두고 임의로 바꾸지 않는다
- 같은 동작과 시간을 넣으면 같은 안내가 나와 일관된다
- 한 인물의 두 동작은 차례대로 이어진다고 안내한다
- 서로 다른 인물의 동작은 각자 동시에 진행한다고 안내한다
- 긴 장면에서 작은 첫 동작 뒤에 같은 인물의 동작이 이어지면 남은 시간 안내를 붙이지 않는다

### tests/writer/motion-contract.test.ts (11)
> 영상 모션 계약(#motion-contract 2026-08-07) 회귀.
- static 카메라 → LOCKED 계약 + cameraStatic=true (증상①의 처방)
- handheld_drift 는 이동 없음 취급(cameraStatic=true) + 여행 금지 문구
- pan 방향은 시야·화면 콘텐츠 양방향으로 못박는다 (증상②의 처방)
- duration 스케일 + 완료 조항이 실린다 (증상③의 처방)
- 시선 arc·환경 변화도 계약에 실린다
- 금지절(계약 외 카메라·인물·액션 지어내기 금지)이 항상 붙는다
- dyn 미전달 → 빈 계약 (레거시 경로 불변)
- P0: 정지 계약이면 START/END 수렴 지시가 "구도 유지" 분기로 바뀐다
- 이동 계약이면 기존 연속 이동 수렴 지시 유지
- 계약문이 프롬프트 맨 앞에 실린다
- dynamicSpec 미전달 → 기존 프롬프트와 완전히 동일 (레거시 불변)

### tests/writer/motion-vocabulary.test.ts (20)
> 모션 어휘 교정(#motion-vocab 2026-08-11) 회귀.
- pan_right → type "pan" + direction "right" 을 되살린다 (방향을 버리지 않는다)
- 업계 통용어를 정본으로 접는다
- 정본 9종은 손대지 않고 그대로 통과한다
- 못 알아본 유형은 static 으로 접지 않고 원문을 유지한다
- 카메라 스케일 낱말 "moderate" 를 인물 스케일 "medium" 으로 맞바꿔 받는다
- 미상·누락은 최소가 아니라 medium 으로 둔다 (동작이 삼켜지지 않게)
- 진짜 최소를 뜻하는 낱말은 micro 로 남는다
- 정본 입력은 교정 기록이 비어 있다
- 한 번 교정한 값을 다시 통과시켜도 같다 (소비처가 몇 번 불러도 안전)
- 교정한 내용을 사람이 읽을 수 있게 남긴다 (조용한 열화 금지)
- 명시된 direction 이 유형에서 떼어낸 방향보다 우선한다
- pan_right 이어도 pan 전용 절 "회전만 — 카메라는 이동하지 않는다" 가 살아남는다
- pan_right 은 정지 계약이 아니다
- 인물 magnitude "moderate" 가 "미세한 움직임" 으로 뒤집히지 않는다
- tracking 은 pan 과 반대로 "카메라가 실제로 이동" 을 명시한다
- pan_right 이 더 이상 전 축 0 으로 떨어지지 않는다
- pan_right 도 방향 판정 대상에 들어온다 (검수기가 눈을 감지 않는다)
- 정본 9종을 하나도 빠뜨리지 않고 싣는다 (말줄임표로 끝나던 자리)
- 두 스케일의 가운데 낱말이 다르다는 것을 명시한다
- 방향을 유형 이름에 붙이지 말라고 못박는다 (pan_right 의 직접 처방)

### tests/writer/n1-continuity.test.ts (4)
> 이전 장면의 마지막 모습이 다음 장면에 이어져 화면 흐름이 끊기지 않는다 (#n-1 2026-08-05 PREVIZ#2)
- 직전 두 장면의 마지막 모습과 움직임만 다음 장면에 전한다
- 앞선 장면이 없으면 안내를 덧붙이지 않고 첫 장면을 그대로 시작한다
- 이전 모습을 이어 그리고 110자를 넘으면 뒤를 줄인다
- 이전 내용이 없으면 새 안내를 만들지 않아 화면이 그대로다

### tests/writer/object-blocking-enforcement.test.ts (7)
> 사물은 인물로 잘못 인식되지 않고 소품으로 표시된다 (#g4 2026-08-27)
- 사물이 인물 위치에 섞이면 소품으로 옮긴다 (엿판 사례)
- 사물만 보여 주는 장면은 인물 없이도 소품으로 남긴다
- 같은 소품이 이미 제자리에 있으면 한 번만 남긴다
- 옮길 사물이 없으면 원래 화면 정보를 그대로 돌려준다
- 인물 위치가 비어 있으면 내용을 그대로 둔다
- 여러 인물과 사물을 한 번에 올바른 자리로 나눈다
- 같은 내용을 넣으면 같은 결과가 나온다

### tests/writer/persist-facets.test.ts (6)
> 장면을 저장할 때 사용자가 정한 화면 연출과 안내 문구를 잃지 않는다
- 새 장면을 저장하면 화면 연출 정보와 안내 출처를 함께 보존한다
- 새 연출 기능을 끄면 기존 안내 문구를 그대로 사용한다
- 새 연출 기능을 켜도 연출 정보가 있는 장면만 새 안내를 사용한다
- 새 안내를 만들지 못하면 정해진 기본 안내를 대신 사용한다
- 안내 출처가 같으면 새로 만들지 않고 기존 안내를 보존한다
- 사용자가 고친 화면 설정은 지키고 자동으로 만든 연출 정보만 새로 덮어쓴다

### tests/writer/persist-guard.test.ts (6)
> 저장할 때 실패를 숨기지 않고, 사용자가 다시 시도할 수 있도록 알려준다 (#persist-guard 2026-07-31)
- 저장 중 오류가 나면 조용히 성공한 것으로 처리하지 않고 알려준다
- 기존 장면을 지울 수 없으면 저장을 완료한 것으로 처리하지 않고 알려준다
- 문제없이 저장하면 이전 장면을 정리한 뒤 새 장면을 기록한다
- 자동으로 만든 장면만 다시 저장하고 사람이 만든 장면은 다음 실행에도 남긴다
- 자동으로 만든 장면임을 분명히 표시해 서로 구분한다
- 사람이 만든 장면과 같은 번호가 나오면 사람의 장면을 남기고 자동 결과를 건너뛴 사실을 알린다

### tests/writer/pipeline-progress.test.ts (26)
> 진행 중인 작업은 정확한 수와 담당자를 보여주고, 끝나면 알림을 숨긴다
- 진행 중인 장면이 하나도 없으면 진행 알림을 보여주지 않는다
- 진행 중인 장면이 있으면 완료·전체·실패 수를 정확히 보여준다
- 장면 설명이 없으면 진행 대상에서 제외한다
- Writer 작업이 진행 중이면 완료 수와 전체 수를 담은 알림을 보여준다
- 사용자 확인을 기다리는 동안에는 진행 중이라는 알림을 보여주지 않는다
- 상태가 없거나 작업이 끝났거나 실패했거나 시작되지 않았으면 진행 알림을 보여주지 않는다
- 전체 작업 수를 알 수 없으면 숫자 대신 진행 중임만 보여준다
- Writer의 새 그림은 이전에 끝난 장면을 제외하고 현재 작업만 합산한다
- Director의 장면 그림도 이전 작업을 제외하고 현재 작업만 합산한다
- Director 영상도 이전에 끝난 장면을 새 작업에 섞지 않는다
- 초기 안내가 끝나기 전에는 작업이 있으면 준비된 수와 전체 수를 보여준다
- 아직 끝나지 않은 프로젝트라도 작업이 없으면 "생성 중" 알림을 보여주지 않는다 (D13)
- 작업이 멈추거나 실패하면 "진행 중" 알림을 보여주지 않는다
- 초기 안내가 끝난 뒤에는 다시 만드는 작업 수만 진행 알림에 보여준다
- 진행 중인 장면이 없으면 진행 알림을 보여주지 않는다
- 진행 중인 장면만 세어 완료 수와 전체 수를 보여주고 다른 작업은 제외한다
- 진행 중인 영상이 있을 때만 완료·전체·실패 수를 보여준다
- 러프 그림이 아직 없어도 처리할 작업이 있으면 진행 중으로 알려준다
- 러프 그림 작업이 모두 끝나면 진행 알림이 사라진다
- Director 장면 그림이 아직 없어도 처리할 장면이 있으면 진행 중으로 알려준다
- Director 영상이 아직 표시되지 않아도 처리할 영상 수를 진행 알림에 보여준다
- Artist 화면에 작업이 없어도 처리할 그림이 있으면 진행 중으로 알려준다
- 여러 장면을 나눠 처리해도 진행 알림의 전체 수가 끝까지 유지된다
- 한 장면만 다시 만들 때는 기존처럼 진행 알림을 보여준다
- 처리할 작업이 없는 종류는 진행 알림을 만들지 않는다
- 종류마다 담당자 이름과 알맞은 색으로 진행 알림을 보여준다

### tests/writer/previz-enrich.test.ts (10)
> 러프 그림의 빛과 초점을 정확히 보여주고, 정보가 없는 칸에는 불필요한 지시를 넣지 않는다 (#previz-enrich ①+③, 2026-08-07)
- 빛과 초점이 정해져 있으면 시작 그림에 그 방향과 세부를 함께 안내한다
- 끝 그림에도 시작 그림과 같은 빛과 초점을 안내한다
- 빛이 강하면 그림자 가장자리를 또렷하게 안내한다
- 촬영 방향 줄에 카메라와 빛 정보를 함께 표시한다
- 빛의 색이 차갑거나 중간이면 COOL·NEUTRAL로 구분해 표시한다
- 카메라가 멈춘 장면에도 촬영 정보를 함께 표시한다
- 같은 인물을 가리키는 설명은 하나로 묶고 화면 밖 인물은 그리지 않게 한다
- 촬영 정보가 없어도 인물 수를 하나로 지키고 다른 사람은 그리지 않게 한다 (#split-spec)
- 촬영 정보가 없는 칸에는 빛 안내나 촬영 표기를 넣지 않는다
- 그림 안내의 금지 규칙에서도 DIRECTION 줄의 촬영 표기는 허용한다

### tests/writer/prompt-trace.test.ts (4)
> 프롬프트 트레이스 선택(#debug-prompts 확장) 회귀 — 샷 매칭 3경로 + kind 별 최신 1건 +
- 영상 잡: full_prompt=최종, prompt=소스, motionContract 분리 추출
- 그리드 잡은 writerShotIds[]/shotIds[] 로도 매칭된다
- kind 별 최신 1건만 — 최신순 입력에서 첫 매칭 채택
- 다른 샷 잡·미허용 kind·빈 프롬프트는 제외

### tests/writer/rerun-consent.test.ts (5)
> 이미 끝난 작업을 다시 시작할 때는 먼저 확인을 받고, 취소하면 아무것도 바꾸지 않는다
- 처음 Producer 자료를 Writer로 넘길 때 먼저 확인을 받고 승인 전에는 시작하지 않는다
- 명시적으로 넘기기를 누르면 두 번째 확인 없이 바로 시작한다
- 승인한 작업을 넘기지 못하면 실패 사실을 솔직하게 알린다
- 이미 끝난 작업을 다시 시작하려 하면 확인을 받고 자동으로 다시 시작하지 않는다
- 다시 시작을 승인하면 다시 실행하고 취소했을 때는 아무것도 바꾸지 않는다

### tests/writer/resolve-entity-names.test.ts (10)
> 등록된 인물과 장소는 이름으로 보여주고, 모르는 표기는 함부로 바꾸지 않는다 (#id-leak 2026-08-11)
- 등록된 인물과 장소 표기가 문장에 있으면 알아보기 쉬운 이름으로 보여준다
- 이름 표기가 서로 비슷해도 정확히 맞는 대상만 바꾼다
- 등록되지 않은 인물 표기는 모르는 채로 두고 이름을 지어내지 않는다
- 대상을 특정할 수 없는 인물 표기는 일반 문장으로 그대로 둔다
- 인물 이름을 덧붙인 괄호는 이름을 보여준 뒤 깔끔하게 지운다
- 시간처럼 이름이 아닌 괄호 정보는 그대로 남긴다
- 같은 인물을 가리키는 표기가 달라도 이름을 알아본다
- 문장이나 인물 목록이 비어 있어도 안전하게 처리한다
- 인물과 장소가 함께 있으면 한데 모아 보여준다
- 인물과 장소 정보가 없으면 빈 결과를 보여준다

### tests/writer/rough-cell-figure-index.test.ts (7)
> 같은 인물의 동작과 시선을 한 번호로 묶어 엉뚱한 인물에게 붙지 않게 한다 (#figure-index 2026-09-02)
- 같은 인물의 두 동작은 같은 번호로 표시하고 다른 인물에게 붙이지 않는다
- 동작을 적은 순서가 달라도 각 인물에게 올바른 번호를 붙인다
- 인물의 시선 변화에도 해당 인물 번호를 붙인다
- 화면에 없는 인물의 동작은 그림에 넣지 않는다
- 인물이 하나면 두 동작 모두 그 인물 번호로 표시한다
- 인물이 하나뿐이면 표기가 달라도 그 인물의 동작으로 연결한다
- 인물 배치 정보가 없으면 이름을 적은 순서대로 번호를 붙인다

### tests/writer/rough-cell-layout.test.ts (4)
> #stage(2026-09-03): 무대에서 계산된 화면 배치(screen_layout)가 러프 셀 문장에 실린다 —
- START 문장에 계산된 위치·깊이·크기·향이 실린다
- END 가 달라진 인물만 끝 배치를 적는다(수인은 다가와 커지고, 요정은 그대로)
- screen_layout 이 없으면 옛 문장(LLM 위치 낱말) 그대로
- describePlacement 낱말

### tests/writer/rough-frame-cycle-urls.test.ts (4)
> 러프 그림의 시작·방향·끝 장면을 빠짐없이 보여주고, 최신 그림 주소를 사용한다
- 세 장면 그림이 있으면 시작·방향·끝 순서로 모두 보여주고 최신 그림을 사용한다
- 예전 형식처럼 장면 그림이 하나뿐이면 그 그림만 보여준다
- 세 장면 그림을 모두 작은 미리보기로 보여주고 최신 상태를 유지한다
- 그림 주소에 새 버전 정보가 있으면 기존 정보와 함께 반영하고 없으면 그대로 둔다

### tests/writer/rough-frames-independent.test.ts (6)
> 약속 I — 러프 3장은 따로 다시 만들 수 있고 서로 자동으로 바뀌지 않는다 (_tdd.md I, 2026-09-04)
- 시작·연출·끝 중 하나만 다시 만들면 나머지 두 장은 그대로다
- 끝 장면을 다시 만들면 시작·연출은 바뀌지 않는다
- 시작 장면만 따로 다시 만들 수 있다
- 러프 3장 중 하나라도 바뀌면 Director의 그 샷 실사 카드에 "러프 바뀜" 표시가 뜬다
- 러프가 바뀌어도 Director 실사는 자동으로 다시 만들지 않는다
- 그리드에서 이미지를 눌러도 글자를 눌러도 같은 연출 편집 팝업이 열리고, 그 팝업에 3장이 나란히 멈춰 보인다

### tests/writer/rough-grid-crop-label-invasion.test.ts (3)
> 어떤 크기의 그림판도 네 칸을 같은 크기로 잘라 글씨가 그림을 침범하지 않게 한다 (#label-invasion → #fixed-crop 2026-08-17, 오너 결정)
- 그림과 글씨가 조금 어긋나도 네 칸을 같은 크기로 잘라낸다
- 그림판 크기가 달라져도 네 칸의 비율과 크기를 맞춰 잘라낸다
- 예전 형식의 그림판도 장면 칸을 빠짐없이 잘라낸다

### tests/writer/rough-regenerate-inflight-block.test.ts (8)
> 이전 생성이 끝나지 않아도 다시 만들기를 누르면 막힌 작업을 정리해 계속 진행한다 (#a1-inflight-block 2026-08-27 오너 신고 A1)
- 특정 장면을 다시 만들면 끝나지 않은 작업을 확인한 뒤 제출한다
- 끝난 이전 작업은 목록에서 빠져 같은 요청으로 다시 만든다
- 이전 작업 확인에 실패해도 중복 작업은 계속 막는다
- 서비스 오류에 설명이 없어도 실패 이유를 만들어 남긴다
- 오류 설명이 비어도 실패 이유를 남기고 마무리한다
- 다시 만들기가 모두 막히면 진행 표시를 멈추고 안내한다
- 다시 만들기가 막혔다는 안내를 한국어로 보여준다
- 자동으로 시작한 작업은 안내를 띄우지 않고 진행 표시를 유지한다

### tests/writer/rough-spec-shape-guard.test.ts (5)
> 필요한 화면 정보가 없으면 잘못된 설계를 사용하지 않아 오류 화면이 나타나지 않는다 (#v2-rough-500 2026-08-17 #split-inherit 090042eb)
- 화면 구성과 등장인물 배치가 있으면 러프 제작에 사용한다
- 필요한 화면 정보가 없는 설계는 러프 제작에 사용하지 않는다
- 화면 틀만 있고 앞·중간·뒤 구분이 없으면 사용하지 않는다
- 등장인물 배치 목록이 아니면 사용하지 않는다
- 값이 없거나 글자 하나뿐이면 사용하지 않는다

### tests/writer/rough-storyboard-quota-gate.test.ts (3)
> 생성 한도를 넘으면 시작 전에 알리고, 여유가 있으면 프로젝트 결과를 정확히 보여준다 (#A 2026-09-02 오너 결정 #initial-rough-unblocked rough-storyboard-429-unreachable-2026-08-30)
- 생성 한도를 넘으면 프로젝트를 확인하기 전에 한도 초과를 알린다
- 모든 생성 자리가 차도 한도 초과로 알린다
- 생성 여유가 있으면 프로젝트를 찾지 못했다는 결과를 알린다

### tests/writer/rough-storyboard-zoom-shortcuts.test.ts (2)
> 확대·축소 단축키를 눌러도 화면 크기를 정해진 범위 안에서 한 단계씩 바꾼다
- Ctrl/Command와 + 또는 -를 함께 누를 때만 화면 크기를 한 단계 바꾼다
- 최소·최대에서 더 내려가거나 올라가지 않는다

### tests/writer/rough-template-assets.test.ts (7)
> 화면 비율별 러프 템플릿이 정해진 칸 배치와 크기를 지켜 안정적으로 그려진다 (#sheet-formats 2026-08-17 2차 오너 육안 피드백)
- 화면 비율별 템플릿 8장이 있고 정해진 크기와 일치한다
- 모든 템플릿이 정해진 크기·비율·화질 범위를 지킨다
- 기존 템플릿 2장도 정해진 기준 크기를 그대로 유지한다 (화면 비율을 정하지 않은 경우 전용)
- 각 칸의 위치가 화면 안에 있고 칸 사이 여백이 비어 있다
- 가로형 16:9 러프와 완성본이 정해진 크기로 만들어진다 (#hd-grid)
- 세로형 띠 템플릿은 칸을 세로로 나누고, 나머지는 가로로 나눈다
- 각 칸의 가로세로 비율이 화면 비율과 맞는다 (기존 16:9 오차를 반복하지 않는다)

### tests/writer/rough-v2-cell-direction.test.ts (4)
> #v2-cell-dedup (2026-08-17) 회귀 가드.
- 같은 유닛 공통값이라도 previzDirection 이 다르면 셀 서술이 갈라진다
- previz 카메라 무브가 MOTION 재료가 된다 — static hold 균질화 방지
- previzDirection 이 없으면 기존 서술 그대로다 (레거시 무변화)
- rich spec 이 있으면 previzDirection 은 무시된다

### tests/writer/script-lines.red-team.test.ts (10) — red-team 에도 속함
> 줄을 찾거나 바꿀 때 잘못된 입력은 무시하고 사용자가 적은 내용만 안전하게 반영한다
- 줄 번호 뒤에 조사가 붙어도 해당 줄만 찾고 잘못된 표기는 무시한다 (RT-01)
- 아주 긴 글에서 같은 줄을 여러 번 언급해도 한 번만 찾는다 (RT-02)
- 대사가 없거나 비어 있으면 대사 줄을 임의로 만들지 않는다 (RT-03)
- 장면 정보가 없어도 남은 촬영 단위에 줄 번호를 붙이고 제목 없이 보여준다 (RT-04)
- 촬영 단위 번호가 겹쳐도 각 줄을 서로 헷갈리지 않게 구분한다 (RT-05)
- 줄 참조 형식이 올바르지 않으면 무시하고 올바른 형식만 남긴다 (RT-06)
- 서로를 가리키는 잘못된 줄 목록이 들어와도 멈추지 않고 올바른 줄만 남긴다 (RT-07)
- 잘못된 장면 변경 내용과 대사 목록은 모두 반영하지 않는다 (RT-08)
- 장면 번호가 글자가 아니면 삭제 요청을 무시한다 (RT-09)
- 대사 내용이 그대로이거나 비어 있어도 변경 요청으로 적용한다 (RT-10)

### tests/writer/script-lines.test.ts (9)
> 장면과 촬영 단위의 내용을 줄 번호로 정리해 대사와 함께 정확히 찾고 전달한다
- 장면 제목·행동·대사를 모두 이어지는 줄 번호로 정리한다
- 장면 제목·촬영 단위 행동·대사를 각각 한 줄로 보여준다
- 장소와 분위기가 있으면 장면 제목에 함께 보여주고, 분위기가 없으면 장소만 보여준다
- 내용이 없어도 빈 결과와 장면 없는 촬영 단위를 정해진 순서로 보여준다
- 각 줄에 L 번호와 변하지 않는 찾기 표식을 붙인다
- L4와 L45를 함께 언급해도 서로 다른 줄로 정확히 찾는다
- 전달하는 내용의 [L#] 표시가 실제 줄 번호와 맞는다
- L45와 @L45를 보내 시점의 줄 목록에서 같은 줄로 찾는다
- 없는 번호나 비슷한 번호는 빼고 중복 없이 줄을 찾는다

### tests/writer/shot-design-resolve.test.ts (4)
> 촬영 단위는 연결된 설계만 사용하고 연결이 없으면 옆 설계를 가져오지 않는다 (#split-spec 2026-08-10 e1a9fd08 sh_03_15)
- 설계 연결 정보가 있으면 연결된 설계만 사용한다
- 나뉜 촬영 단위에 연결 정보가 없으면 옆 설계를 가져오지 않는다
- 연결된 설계가 없으면 다른 설계로 바꾸지 않고 비워 둔다
- 오래된 프로젝트에서 연결 정보가 없어도 기존 촬영 단위 설계를 유지한다

### tests/writer/shot-references.test.ts (10)
> 샷에 들어갈 인물과 배경 정보를 빠짐없이 찾아, 없는 참조는 사용자에게 알려준다 (#ref-gate 2026-09-02, 실측 겨울_4 9ea9bd67)
- 시트가 없는 인물은 빠졌다고 알리고, 있는 인물은 정해진 순서로 참조에 넣는다
- 인물의 위치와 자세를 알 수 있으면 참조에 함께 보여주고, 없으면 비워 둔다
- 모습 선택 정보가 없으면 기본 모습을 사용한다
- 인물 목록을 알 수 없으면 참조를 만들지 않고 기존 흐름으로 진행한다
- 인물이 없는 샷은 참조와 누락 알림을 만들지 않는다
- 목록에 없는 인물은 이름 대신 그 값을 누락 알림에 넣는다
- 누락된 인물의 이름을 한 문장으로 나열해 알려준다
- 인물별 위치와 자세를 읽을 때 잘못된 값은 비우고, 같은 인물은 처음 정보를 사용한다
- 주소 뒤의 추가 정보가 달라도 같은 그림으로 알아본다
- 장면의 장소로 배경을 찾고, 없으면 장면 번호로 다시 찾아 연결한다

### tests/writer/shot_design_concurrency_ab.test.ts (1)
> shotDesign 병렬화 A/B 실험 하네스(#parallel-shotdesign 2026-07-21).
- concurrency를 올리면 wall-clock이 줄고, 샷 수·shot_id 순서는 불변이다

### tests/writer/shot_design_resume.test.ts (13)
> 샷 설계는 장면을 빠짐없이 이어서 만들고, 오래 걸리면 진행한 만큼 안전하게 돌려준다 (#A #B long-writer-run 2026-07-15)
- 시간 제한을 주지 않으면 모든 장면을 끝까지 만들고 장면마다 한 번씩 요청한다
- 시간이 이미 지났어도 한 번에 장면 하나는 만들고 지금까지 결과를 돌려준다
- 완성된 장면을 알려주면 그 장면은 건너뛰고 다음 장면부터 이어 만든다
- 한 장면의 컷이 많아도 여러 묶음으로 나눠 모두 만들고 하나로 합친다
- 컷이 적은 장면은 한 번에 만들고 결과를 그대로 보존한다
- 케이스 ⑤: 컷 목록을 감싼 답변도 원래 순서대로 읽는다 (2026-07-15)
- 케이스 ⑤: 한 개의 컷 목록도 빠짐없이 읽는다
- 기존 형식(①~③)도 예전처럼 읽는다
- 읽을 수 없는 답변은 오류로 알린다
- 첫 답변을 읽지 못하면 한 번 더 요청해 성공한다
- 두 번 연속 답변을 읽지 못하면 해당 장면의 실패를 알려준다
- 필요한 컷의 대부분이 빠지면 다시 요청해도 결과를 받아들이지 않고 실패를 알린다
- 컷이 조금 모자라면 다시 요청한 뒤 결과를 받아들이고 차이를 기록한다

### tests/writer/shotcheck-reconcile.test.ts (3)
> 장면에 맞는 컷 정보를 순서대로 조립하고, 빠진 장면도 안전하게 이어 붙인다 (E12b 2026-07-21)
- 컷 하나를 넣으면 결과도 하나가 되고 입력 순서를 지킨다
- 첫 화면과 움직임 안내를 입력한 내용 그대로 사용한다
- 장면 목록에 없는 장면도 입력한 설명으로 멈추지 않고 채운다

### tests/writer/shotcheck-wiring.test.ts (19)
> 장면의 행동과 검수 안내를 화면에 정확히 보여주고, 나눠진 컷에도 필요한 주의를 이어 준다 (#p2-wiring 2026-08-04)
- 장면에 적힌 실제 행동이 있으면 화면에 그 행동을 먼저 보여준다
- 실제 행동이 없으면 다른 행동 설명을 차례로 사용하고, 마지막에 장면 목적을 보여준다
- 컷을 나눠도 원래 설계와 화면 원본을 함께 보존한다
- 중요하거나 주의가 필요한 시각 안내만 붙이고, 정보용·글 전용 안내는 제외한다
- 글 내용 전용 안내는 검수 보고서에만 남기고 화면 주의사항에는 붙이지 않는다
- 나뉜 컷은 원래 컷의 주의사항을 이어받는다
- 관련된 주의사항이 없으면 컷 내용을 그대로 둔다
- F1: 나누기 전 컷의 동작 제한은 나뉜 컷에 이어 주지 않는다 (나누기가 곧 수정)
- 나뉜 컷은 첫 컷만 원래 설계를 갖고, 뒤 컷은 필요한 부분만 이어받는다 (#split-inherit: 훔치지도 굶기지도 않는다)
- 자동 생성 결과에 설계 표시가 섞여도 정해진 기준을 따른다 (실측 92948d6f)
- 나뉜 컷은 각자 자기 동작을 화면에 보여준다 (T4)
- S2: 둘째 컷은 자기 설명이 없으면 이전 컷의 전체 화면과 동작을 물려받지 않고 비워 둔다 (부모의 전체 화면과 동작은 자식에 거짓)
- S3: 둘째 컷은 카메라와 환경은 이어 가고, 인물 동작과 시선은 자기 움직임만 보여준다
- S3: 화면 전환은 첫 컷에 들어오고 마지막 컷에서 나간다
- S1: 둘째 컷의 자세는 자기 동작에 맞추고 부모의 순간 자세를 물려받지 않는다
- 한 자리 컷 번호도 같은 규칙으로 정리한다
- 정상적으로 저장된 주의사항에서 필요한 문장만 골라낸다
- 읽을 수 없는 주의사항은 비워 두고 원래 안내 문장은 그대로 둔다
- 주의사항이 있으면 안내문 끝에 한 줄로 덧붙인다

### tests/writer/stage-apply.test.ts (13)
> 무대 → 샷 적용(#stage 2026-09-03): 겨울_4 씬 1의 28·29·30 을 한 무대에서 풀면 좌우·시선이 일관되고,
- 데쿠파주 source_beats 첫 값, 없으면(added) 직전 비트
- 없는 비트는 직전 비트의 끝 상태로 잇는다
- 28: 수인 앞(축 북쪽)에 놓인 카메라는 축 남쪽으로 반사되고, 좌우는 요정 왼쪽·용족 오른쪽으로 계산된다
- 28: 트래킹 전진은 END 카메라를 따로 두고 END 배치를 남긴다
- 29: 85mm 클로즈업 프레임 밖의 수인은 blocking 에서 빠지고 용족만 남는다
- 29→27 연속: 남쪽 카메라에서 용족은 서쪽(상대)을 보므로 화면 왼쪽을 향한다
- 30: 와이드는 셋 다 프레임 안, 요정 왼쪽·용족 오른쪽·수인 가운데 — LLM 의 뒤집힌 위치를 덮어쓴다
- camera_setup 이 없으면 기본값(축 안쪽·첫 인물)으로 계산하고 경고를 남긴다
- 무대에 없는 인물의 blocking 은 유지한다(정체성 참조 보존)
- 타이트 샷에서 비피사체가 렌즈 앞을 막으면 축 안쪽 이웃 방향으로 돌리고 INFO 를 남긴다
- 카메라가 안 움직이면 START 카메라로 END 를 계산한다 — 걸어가는 셋이 작아지고 end_camera 는 없다
- 두 번째 샷의 카메라를 두 인물의 선에 반사해 좌우를 지킨다
- axis_cross=motivated 면 그대로 둔다

### tests/writer/stage-blockout.test.ts (4)
> 배치도(#blockout 2026-09-03): 무대 배치 → SVG 시트(열 = 샷, 위 START / 아래 END) → PNG.
- 열마다 START/END 패널, blocking 번호로 라벨, 프레임 밖 인물은 안 그린다
- 숫자는 7-세그먼트 선으로 그린다(폰트 불요)
- PNG 로 래스터화된다(sharp)
- 프롬프트 절은 열 수와 START/END 규약을 말한다

### tests/writer/stage-end-camera.test.ts (2)
> 무대 기하 — 정지 카메라·정지 인물이면 END 배치는 START 와 같다 (2026-09-07, 겨울_6 씬 1 shot 2 실측)
- 쌍 축 검사가 카메라를 돌린 샷에서, 정지 카메라·정지 인물의 END 배치는 START 와 같다
- 앉는 수인(상태가 바뀐 인물)의 END 는 같은 카메라에서 자세만 달라진다

### tests/writer/stage-errors.test.ts (9)
> 일시적인 문제는 한 번 다시 시도하고, 결정적인 문제는 이유를 바로 알려준다 (#stage-retry 2026-08-13, 오너 정책)
- 일시적인 연결 문제나 한도 초과·서버 오류는 비용을 쓰지 않고 다시 시도한다 (오너 정책 확장)
- 답변 형식이 잘못되면 일시적인 문제로 보고 다시 요청한다
- 권한·결제·내용 제한처럼 해결이 필요한 문제는 바로 실패로 알린다
- 원인을 모르는 문제도 한 번 다시 시도한 뒤 실패를 알려준다 (비용 상한 있음)
- 처음 발생한 일시적인 문제는 자동으로 한 번 다시 시도한다
- 두 번째 실패부터는 사람에게 알리고 다시 시작할 때 판단하게 한다
- 해결이 필요한 문제는 처음부터 다시 시도하지 않는다
- 연결 문제는 별도 경로로 다시 시도하고, 정해진 횟수를 넘으면 바로 알린다
- 다시 시도하는 횟수는 제한하고, 간격은 점점 늘되 최대 15초를 넘기지 않는다

### tests/writer/stage-geometry.test.ts (19)
> 씬 무대 기하(#stage 2026-09-03) — 핀홀 투영·카메라 풀이·180° 축·화면 낱말.
- 나침반과 향 벡터
- 샷 사이즈·렌즈 → 거리: 35mm 미디엄 ≈ 2.2m, 85mm 미디엄 클로즈업 ≈ 3.6m, 35mm 와이드 ≈ 7.8m
- 축의 왼쪽/오른쪽 판정과 거울 반사
- 반대편에 놓인 카메라는 축 안쪽으로 반사되고 이슈가 남는다
- 동기 있는 축 넘기(axis_cross=motivated)는 그대로 둔다
- 피사체·방향·샷 사이즈에서 위치가 나온다 — 남쪽에서 본 와이드 그룹
- 어깨 너머(OTS)는 그 인물 뒤에 선다
- 없는 피사체는 그룹 중심으로 대체하고 이슈를 남긴다
- 정면의 점은 화면 가운데, 왼쪽의 점은 u<0
- 낱말 경계
- 겨울_4 28번 무대: 수인 가운데(멀리·작게), 요정 왼쪽·용족 오른쪽(가까이·등)
- 누운 인물은 실효 높이가 낮다
- 카메라 무브 → END 거리 배율
- OTS 는 씬 축 보정을 받지 않고 어깨 인물 뒤에 머문다 — 어깨 인물이 축 반대편에 있어도
- OTS 의 비키는 쪽은 가능하면 camera_side 쪽
- 프레임 안 판정과 off 낱말의 경계가 같다(±1.05)
- 시야 가림: 피사체 앞을 막는 비피사체를 찾는다
- 축 인물이 프레임 밖이고 피사체도 아닌 단독 샷은 축을 넘어도 보정하지 않는다(MS 가 와이드로 변하지 않음)
- 피사체가 축 인물이면 축을 지킨다

### tests/writer/stage-ledger.test.ts (9)
> 인물 상태 장부(#ledger 2026-09-03): 변화 추출·비트 사이 변화 정규화·보여주는 샷 판정·배경 동작 보충·미덮임 경고.
- 큰 변화만 동작이 된다 — 서다↔걷다는 사소
- 비트 사이의 설명 없는 변화(누움→섬)를 직전 비트의 end_characters 로 옮긴다
- 정규화 뒤 변화 목록: 용족·요정 누움→섬, 수인 누움→앉음(비트 0)·앉음→걸음(비트 1)·이동(비트 3)
- 작가가 이미 적은 동작(수인 눈 뜸·일어나 앉음)은 보충하지 않고 "이미 있음"으로 기록
- 프레임 안의 배경 인물(용족)의 누움→섬은 그 샷에 배경 동작으로 보충되고 visual 제약이 붙는다
- 프레임 밖 인물(요정)의 변화는 보여주는 샷이 없다 — report_only 경고
- 이동(비트 3 수인 2.5m)은 와이드에서 보이고, 화면 기준 동작으로 보충된다
- 원본 shots 는 변형하지 않는다
- 바뀐 인물만 적힌 end_characters 를 전원 목록으로 채우고, 다음 비트 시작과의 차이(요정 누움→섬)도 잡는다

### tests/writer/stage-repair.test.ts (15)
> 씬 무대 수리 (2026-09-05, 오너 결정: 1 전이 소유권 규칙 승인 · 2 근거 게이트는 작은 메모 · 3 기하 수리 승인 · 4 프레임 밖 봉인 승인)
- 수인의 누움→앉음은 수인이 크게 잡힌 MS 가 소유하고, 설정 EWS 에는 동작이 들어가지 않는다
- 소유 샷 앞의 샷은 START 상태로 고정된다 — EWS 의 END 에서 수인은 여전히 누워 있다
- 인물이 화면 높이 15% 미만인 샷은 소유에서 제외된다 — 다른 후보가 없을 때만 허용
- 작가 동작이 적힌 샷이 있으면 크기보다 그 샷이 우선한다
- 핀은 소유 샷 앞이면 시작 상태, 뒤면 끝 상태로 인물을 고정한다
- 원문에 없는 변화(셋 다 웅크림)는 직전 상태로 되돌리고 gated_note 메모만 남는다
- 원문에 근거가 있으면(인용이 원문에 있으면) 변화가 유지된다
- 비트 사이의 근거 없는 변화(다음 비트 시작이 직전 끝과 다름)도 되돌린다
- 옛 무대(version 없음)에는 게이트를 걸지 않는다
- 표지 피사체는 거리 계산에 인물 키를 쓴다
- 와이드 샷에서 피사체가 표지뿐인데 인물 명단이 있으면 피사체는 인물과 표지의 합집합이다
- 화면 높이 8% 미만으로 작아지는 인물은 명단에서 빠지고 "먼 인물"로만 쓰인다
- 명단에서 뺀 인물은 screen_layout.off_frame 에 남고 "그리지 말 것" 시각 제약이 붙는다
- 러프 셀은 프레임 밖 인물을 이름으로 대고 그리지 말라고 못박고, 실사 참조에서 그 인물의 시트를 뺀다
- START 에 없고 END 에 들어오는 인물은 "가장자리 바로 밖"이 아니라 "START 에는 안 보임, 왼쪽에서 진입"으로 적힌다

### tests/writer/stage-shortcuts.test.ts (6)
> 숫자 Alt 단축키는 지정된 단계로만 이동하고, 다른 키 조합과 일반 입력은 건드리지 않는다 (#keyboard-only 2026-08-12, Alt+숫자로 이관)
- Alt 숫자 1~5를 누르면 해당 단계로 이동하고 숫자 키패드도 똑같이 동작한다
- Alt 없이 숫자를 입력하면 단계 이동 없이 글자로 입력된다
- 다른 보조 키를 함께 누르면 단계 이동을 가로채지 않는다
- 지정하지 않은 키를 누르면 아무 단계도 열지 않는다
- Mac에서 Option과 숫자를 눌러도 해당 단계로 이동한다
- 사용하는 운영체제에 맞는 보조 키 이름을 보여준다

### tests/writer/stage-transition.test.ts (11)
> 단계 화면을 옮길 때 올바른 방향으로 움직이고, 준비가 늦어도 멈추지 않는다
- 단계 주소를 열면 정해진 순서의 위치를 알아낸다
- 주소 뒤에 추가 경로가 붙어도 같은 단계로 알아본다
- 단계가 아닌 주소는 해당 위치가 없다고 알린다
- 앞 단계에서 다음 단계로 가면 오른쪽에서 화면이 들어온다
- 뒤 단계로 돌아가면 왼쪽에서 화면이 들어온다
- 처음 열거나 같은 단계이거나 단계가 아니면 화면 움직임을 만들지 않는다
- 새 화면이 준비되면 기다림을 바로 끝낸다
- 화면 준비가 늦어도 정해진 시간이 지나면 기다림을 끝내 화면을 멈추지 않는다
- 완료 알림을 두 번 보내도 문제없이 한 번만 처리한다
- 화면 움직임을 지원하지 않아도 바로 이동한다
- 움직임 방향이 없으면 효과 없이 바로 이동한다

### tests/writer/stage-validate.test.ts (7)
> 장면의 인물 위치와 움직임을 읽기 쉽게 정리하고, 빠진 사람과 잘못된 값은 알려준다 (#stage 2026-09-03)
- 장면 정보가 여러 형태로 와도 숫자와 자세와 바라보는 방향을 올바르게 읽는다
- 비슷한 자세 표현도 같은 자세로 알아본다
- 빠짐없는 장면 정보는 통과시킨다
- 처음 장면에 사람이 빠졌거나 모르는 사람이 있으면 문제로 알린다
- 사람이 겹치거나 필요한 장면 정보가 없거나 범위를 벗어나면 주의로 알린다
- 장면 움직임이 비어 있으면 문제로 알린다
- 모르는 사람은 빼고 빠진 사람은 가장자리에 세우며 관계를 알 수 없으면 비워 둔다

### tests/writer/start-rerun-payload.test.ts (1)
> 지난 작업의 장면·쇼트·대화·Producer 결정을 다시 사용해 이어서 작업한다
- 장면·쇼트·대화·Producer 결정을 넣으면 다시 시작할 내용으로 모은다

### tests/writer/status-assets.test.ts (6)
> Writer 작업 상태에서 인물·장소 이미지 준비 현황과 멈춘 이유를 보여준다
- 이미지 현황을 요청하면 Producer 인물 일부가 준비된 상태를 보여준다
- R3에서 더 진행할 수 없으면 멈춘 상태로 표시한다: $name (표)
- 오래 대기한 이미지 작업은 실패로 세고 대기 중으로 남기지 않는다
- 기존 이미지와 대표 이미지가 있으면 준비 완료로 센다
- 이미지 현황을 읽지 못해도 상태 화면은 빈 현황으로 보여준다
- 이미지 현황을 요청하지 않으면 관련 정보를 조회하지 않는다

### tests/writer/still-prompt-hygiene.test.ts (6)
> 정지 프롬프트 위생 (2026-09-05, 오너 5번: 전후 생성으로 판단 — 코드 계약은 여기서 못박는다)
- 카메라 앞의 표지만 시야에 들고, 뒤의 표지는 빠지며, 좌우·거리가 붙는다
- 배경 문장은 시야 안 표지만 나열하고, 표지가 없으면 문장이 없다
- "as the camera pushes in" 같은 앞말과 "the camera tracks left." 같은 문장을 뺀다
- 시야 안 표지 배경 문장이 실리고, moment 에서 카메라 무브 앞말이 빠진다
- 러프 라우트가 씬 무대의 표지와 화면 비율을 셀에 넘긴다
- 작가 프롬프트가 환경 사건의 출처·방향을 요구하고 정지 프롬프트에서 카메라 무브를 금한다

### tests/writer/storyboard-image.test.ts (5)
> 완료된 스토리보드 이미지만 시작 화면에 사용하고, 준비 중인 이미지는 사용하지 않는다 (#ref-gate 수리 2026-09-02)
- 스토리보드 시작·방향·끝 이미지가 모두 준비되면 시작 이미지를 사용한다
- 이전 형식의 단일 이미지가 준비되면 그 이미지를 사용한다
- 이미지 생성이 끝나지 않았거나 실패하면 사용하지 않는다
- 완료 상태 표시가 없어도 업로드된 이미지가 있으면 사용한다
- 이미지가 한 개뿐이면 사용하고 비어 있거나 여러 값이면 사용하지 않는다

### tests/writer/storyboard-real-cineline.test.ts (5)
> REAL 리페인트 프롬프트의 시네 라인 주입(#viz-gap 2026-08-07) 회귀.
- cineLines 미전달 시 현행 프롬프트와 완전히 동일하다(라이브 무변경)
- cineLines 전달 시 컬럼 번호로 라인을 붙이고 포즈·구도 보호 가드를 명시한다
- 빈 라인만 있으면 블록을 넣지 않는다
- cineLine 미전달 시 현행과 동일
- cineLine 전달 시 시네 채널 + 포즈 보호 가드를 붙인다

### tests/writer/ui-store.test.ts (1)
> Writer 화면 이름이 잘못되거나 없으면 안전하게 스토리보드 화면을 보여준다 (#dialogue-v4)
- 지원하는 화면 이름을 넣으면 그대로 열고 잘못된 값이면 스토리보드 화면을 연다 (#dialogue-v4)

### tests/writer/v0-style-anchor.test.ts (2)
> v0 스타일 앵커 연결 (2026-07-14, docs/style-anchor-art-style-authority.md §9-2 후속)
- 앵커 있으면 프롬프트에 매체-고정 제약 블록이 들어간다
- 앵커 없으면 프롬프트에 앵커 블록이 없다 (기존 동작 보존)

### tests/writer/v2-dynamic-spec-shape.test.ts (3)
> writer-v2 가 저장한 dynamic_spec 모양(문자열)이 모션 계약 컴파일을 깨뜨리는지 재현.
- character_motion 이 문자열이면 계약 컴파일이 죽지 않아야 한다
- camera_motion 문자열의 실제 뜻(tracking)이 static 으로 접히지 않아야 한다
- character_motion 문자열을 영상 계약에 보존해야 한다

### tests/writer/v2-semantic-unit.test.ts (3)
> 한 장면의 이야기와 화면 구성이 빠짐없이 이어지고, 필요한 인물과 장소를 확인한다
- 이야기·제작 참조·화면 연출이 모두 있으면 한 장면을 인정한다
- 필요한 인물이나 장소가 빠지면 장면을 인정하지 않는다
- 같은 장면이나 쇼트가 두 번 나오면 인정하지 않는다

### tests/writer/v2design-image-trigger.test.ts (4)
> Writer가 화면 구성을 저장한 뒤에만 이미지 준비를 한 번 요청하고, 실패해도 저장 결과를 지킨다
- 화면 구성과 인물·장소 정보를 모두 저장한 뒤 이미지 초안을 요청한다
- 화면 구성을 저장하지 못하면 이후 이미지 준비를 진행하지 않는다
- 이미지 초안 요청이 실패해도 앞서 저장한 화면 구성 결과는 유지한다
- Writer 시작 요청이 끝난 뒤에는 이미지 초안을 중복 요청하지 않는다

### tests/writer/v3-scene-plan-extract.test.ts (3)
> 장면 계획이 어떤 응답 형태로 와도 쇼트 수를 잃지 않고 모두 반영한다 (2026-06-28 사고 회귀)
- 장면 계획이 묶음 형태로 오면 모든 계획을 반영한다
- 장면 계획이 목록으로 바로 오면 모든 계획을 반영한다 (Gemini 변형, 사고 케이스)
- 응답이 없거나 알아볼 수 없으면 장면 계획을 만들지 않는다

### tests/writer/v4-shot-count-guard.test.ts (10)
> #p4-json-guard — v4 샷 배열 가드 (Q6 "repairJson 무신호 손실"의 현장).
- 정확히 맞으면 ok
- 기대치가 없으면(Compact) 항상 ok
- plan 경로의 ±1 은 허용(프롬프트 계약과 합치)
- 데쿠파주 경로는 ±1 도 재시도 대상(index 매핑이라 정확 일치 필요)
- Q6 시나리오: 최종 시도에 8→2 는 fatal (수용 금지)
- 경미한 어긋남(8→6)은 최종 시도에서 수용 — 배지로만 남는다
- 절반 경계(8→4)는 수용, 그 아래(8→3)는 fatal
- 초과는 소실이 아니므로 최종 시도에서 수용(배지)
- repairJson 은 복구에 성공하지만 손실 경고를 남긴다(종전: 무신호)
- 복구된 배열은 파싱을 통과하지만 개수 가드가 fatal 로 잡는다

## artist — Artist: 캐릭터·배경 이미지·모습·스타일 앵커

### tests/artist/anchor-clause-wiring.test.ts (9)
> 장면을 만들 때 정한 화풍과 조명, 그림의 우선순위를 일관되게 지킨다
- 화풍 설명을 넣으면 설명은 보존하고 장면 문구의 매체 표현은 정리한다
- 화풍 설명이 없으면 기존 장면 문구를 그대로 유지한다
- 미리보기 그림을 쓰면 기준 그림과 미리보기를 먼저 두고 인물 그림은 그다음에 따른다
- 인물 방향표를 만들 때는 미리보기 그림을 추가하지 않는다
- 장면 격자에 화풍 설명을 넣으면 기준 화풍 다음에 표시한다
- 장면 조명을 정해도 기준 그림의 색감과 색상은 유지한다 (Rule 6)
- 장면 조명을 정하면 기준 그림의 시간대와 조명을 따라 하지 않는다 (F-006)
- 화풍 그림 두 장을 쓰면 두 장은 뒤에 두고 인물은 앞 그림과 화풍 그림 사이 기준을 따른다
- 장면 조명을 정해도 기준 그림의 색감과 색상을 유지한다

### tests/artist/appearance-patch.test.ts (6)
> 인물 외형 설명은 올바른 내용만 저장하고, 비어 있거나 너무 길면 받지 않는다
- 앞뒤 빈칸이 있는 올바른 외형 설명을 넣으면 빈칸을 정리해 저장한다
- 외형 설명이 글이 아니거나 없으면 받지 않는다
- 외형 설명이 비어 있으면 받지 않는다
- 외형 설명이 정한 글자 수를 넘으면 받지 않는다
- 외형 설명이 정한 글자 수까지면 받는다
- 외형 설명을 담은 내용이 올바른 형태가 아니면 받지 않는다

### tests/artist/appearances.test.ts (9)
> 약속 C — 새 모습은 카드에서도 채팅에서도 만들 수 있고 기본 모습을 참조해서 그려진다 (_tdd.md C, 2026-09-04 오너 확정)
- 캐릭터 카드 위쪽에 모습 탭 줄이 항상 보이고, 모습이 하나뿐이어도 "+ 모습 추가"가 있다
- "+ 모습 추가"를 누르면 이름·시점(과거/현재/미래)·외형을 적는 창이 뜨고, 저장하면 새 탭이 생긴다
- 채팅에서 새 모습을 만들어 달라고 하면 승인 뒤 그 캐릭터에 그 모습이 추가된다
- 새 모습은 만든 직후 자동으로 이미지가 만들어진다
- 새 모습 이미지는 기본 모습 이미지를 참조로 해서 그려진다
- 채팅에서 특정 모습을 다시 그려 달라고 하면 기본 모습이 아니라 그 모습이 다시 그려진다
- 채팅에서 모델 이름을 말하면 그 모델로, 지시를 붙이면 그 지시로 그려진다
- 모습을 지우거나 이름을 바꾸거나 기본 모습으로 지정할 수 있다
- Artist AI는 뒷모습·측면 4뷰를 말하지 않는다

### tests/artist/asset-authority-clause.test.ts (3)
> 여러 그림의 내용이 다르면 인물과 장소 그림을 우선하고, 연필 그림은 구도만 따른다
- 인물과 장소 그림이 맡은 내용을 구분하고 연필 그림은 구도만 따른다
- 장소 그림이 없어도 지형과 구조물을 합치거나 옮기거나 새로 만들지 않는다
- 장면 묶음에서 인물과 장소 그림의 역할과 우선순위를 지킨다

### tests/artist/asset-storage-store.test.ts (1)
> 인물 정보를 등록하면 나중에 같은 인물을 다시 확인할 수 있다
- 인물 정보를 등록하면 이름과 등록·수정 시각을 확인할 수 있다

### tests/artist/background-card.test.ts (10)
> 약속 B — 배경 카드는 캐릭터 카드와 같은 일을 할 수 있다 (_tdd.md B, 2026-09-04 오너 확정)
- 배경 프롬프트에는 사람이 들어가지 않는다는 지시가 항상 붙는다
- 배경 카드 위에 "이미지 생성" 버튼이 없다. 캐릭터처럼 팝업이나 채팅으로 만든다
- 배경 팝업에서 프롬프트를 고치고 닫았다 열어도 남아 있고, 배경 원천 설명에 저장돼 Writer 씬에도 반영된다
- 배경 이미지를 다시 만들면 직전 이미지들이 히스토리에 남아 되돌릴 수 있다(최근 5장)
- 배경 팝업에서 모델을 고를 수 있고 기본은 지금 것(GPT Image 2)이다
- 채팅에서 배경 설명을 바꿔 달라고 하면 승인 카드가 뜨고, 승인하면 원천 설명이 바뀐다
- 배경 설명이 바뀐 뒤 이미지를 다시 만들지 않았으면 카드에 "설명 바뀜" 표시가 뜨고, 다시 만들면 사라진다
- 배경 이미지 생성이 실패하면 카드에 실패 표시가 뜨고 다시 시도할 수 있다
- 안전 필터에 걸리면 우회(safe)로 다시 시도할 수 있다
- 같은 슬롯에 이미 도는 잡이 있으면 새로 제출하지 않는다(캐릭터와 같은 중복 가드)

### tests/artist/background-source.test.ts (4)
> 장소와 장면 정보를 바탕으로 사람 없는 배경을 만들고, 사용자의 생성을 구분해 기록한다
- Producer 정보만 있고 장면이 없으면 사람이 없는 넓은 배경을 만든다
- 인물 설명으로 만든 안내문에서는 배경에 사람을 넣지 않는다
- 장면 정보가 있으면 배경을 다시 만들 때 시간과 분위기를 반영한다
- 사용자가 직접 시작한 생성은 수정으로 기록하고 자동 생성은 기록하지 않는다

### tests/artist/character-appearance-tabs.test.ts (8)
> 인물의 모습은 고른 항목만 바꾸고, 기본 모습과 접근 권한을 지킨다
- 젊은 모습을 고르면 현재 모습이 아닌 젊은 모습으로 만든다
- 모습을 고르지 않으면 만들지 않고 알려준다
- 자동으로 만들 때 기본 모습은 하나만 정해져야 한다
- 고른 모습만 바꾸고 다른 모습은 그대로 둔다
- 고른 모습 하나만 바꾼다
- 접근 권한이 없으면 모습을 바꾸지 않는다
- 사람을 만들면 현재 모습을 함께 저장한다
- 소품을 만들면 소품 목록에만 저장한다

### tests/artist/character-base-face-reference.test.ts (9)
> 다른 시기의 인물을 만들 때 같은 사람의 기본 얼굴과 직전 결과로 정체성을 지킨다
- 기본 얼굴이 있으면 같은 사람의 정체성을 이어가도록 안내한다
- 그대로 베끼지 말고 선택한 모습의 나이와 상태를 따른다
- 다른 모습으로 만들 때 같은 사람의 기본 모습을 찾아 사용한다
- 기본 얼굴이 없으면 다른 모습도 만들지 않는다
- 다른 사람의 얼굴을 가져와 대신 쓰지 않는다
- 다시 만들 때 직전 결과와 같은 사람인지 확인하도록 안내한다
- 처음 만들 때는 직전 결과 없이도 만든다 (첫 생성 동작 보존)
- 다시 만들면 직전 결과를 같은 사람인지 확인하는 자료로 사용한다
- 처음 만들 때는 기본 양식만 사용한다

### tests/artist/character-template-assets.test.ts (3)
> 캐릭터 양식 그림이 정해진 칸과 크기를 지켜 안정적으로 쓰인다
- 캐릭터 양식 그림이 있고 정해진 크기와 일치한다
- 모든 칸이 전체 그림 안에 들어오고 서로 겹치지 않는다
- 대표 얼굴 영역이 정해진 위치와 맞는다 (v3, v2 양식에는 쓰지 말 것)

### tests/artist/chat-updates.test.ts (15)
> 사용자가 보낸 외형 변경 요청은 승인 없이 자동 적용하지 않고, 파생 그림만 안전하게 만든다
- 새 캐릭터와 파생 이미지만 자동으로 만들고 원천 외형은 C3 승인 뒤 바꾼다 (C3)
- 기존 캐릭터의 원래 외형을 바꾸라는 요청은 거부한다 (F6)
- 알 수 없는 요청이나 잘못된 형식은 거부한다
- 파생 캐릭터 그림은 허용된 장면만 남겨 통과시킨다
- 사용자가 덧붙인 요청 문구도 그대로 전달한다 (AC13)
- 캐릭터를 가리키는 정보가 없으면 파생 그림 요청을 버린다
- 새 캐릭터는 외형을 함께 만들고 이름이 없으면 빼며 잘못된 역할은 무시한다
- 장소 그림을 다시 만들고 장소 정보가 없으면 요청을 버린다
- 여러 요청이 섞여도 허용된 그림만 남기고 원래 외형 변경은 버린다
- 허용된 이미지 모델을 지정하면 그대로 사용한다
- 알 수 없는 이미지 모델을 지정하면 해당 지정만 빼고 진행한다
- 이미지 모델을 지정하지 않으면 기본 설정으로 진행한다
- 외형 변경 요청은 자동 적용하지 않고 제안으로 모은다
- 외형 변경 요청은 지금도 자동으로 반영하지 않는다 (F6)
- 캐릭터나 외형 정보가 빠진 변경 요청은 제안에서 뺀다

### tests/artist/classify-image-stale.red-team.test.ts (13) — red-team 에도 속함
> 외형과 그림체의 작은 차이와 순서 변화에도 이미지 상태를 올바르게 알린다 (SCENARIO-6)
- 띄어쓰기 차이가 있어도 최신·반영 대기·수정 상태를 올바르게 나눈다
- 외형 기록이 없을 때와 비어 있을 때를 다르게 판단한다
- 이미지 출처 기록이 비어 있으면 최신으로 본다
- 같은 외형 기록으로 그림체만 바뀐 상태를 구분한다
- 그림체가 없거나 비어 있으면 목록 없음으로 정리한다
- 그림체 순서를 바꿔도 같고 같은 항목의 중복은 유지한다
- 의상이나 그림체 정보가 바뀌면 다른 목록으로 구분한다
- 색상 순서와 빈 항목이 달라도 같은 그림체 목록을 일관되게 만든다
- 중복이나 빈 항목이 섞여도 순서를 바꾼 결과가 같다
- 같은 외형과 그림체를 넣으면 같은 이미지 출처를 기록한다
- 외형이 없거나 공백뿐이어도 같은 이미지 출처로 기록한다
- 같은 외형이라도 그림체 적용 여부에 따라 이미지 출처를 구분한다
- 외형이 같아도 그림체가 바뀌면 이미지 출처를 다르게 기록한다

### tests/artist/classify-image-stale.test.ts (11)
> 캐릭터 외형과 그림체가 바뀐 시점을 구분해 이미지가 최신인지 올바르게 알린다 (SCENARIO-6)
- 이미지 출처를 알 수 없으면 현재 이미지를 최신으로 본다
- 현재 외형과 그림체가 이미지와 같으면 최신으로 본다
- 외형은 같고 그림체만 새로 도착하면 그림체 반영 전 상태로 알린다
- 외형만 기록된 이미지에 그림체가 도착하면 반영 전 상태로 알린다
- 외형을 바꾸면 기존 이미지가 수정 필요 상태가 된다
- 기존 이미지에 다른 그림체를 적용하면 수정 필요로 보수적으로 알린다
- SCENARIO-6: 그림체만 바뀐 재실행은 대기로, 외형을 바꾸면 수정으로 알린다
- 그림체 순서를 바꿔도 같은 목록으로 본다
- 의상이나 그림체가 바뀌면 다른 목록으로 구분한다
- 그림체가 모두 없으면 목록을 비워 둔다
- 그림체 없는 항목이 섞여도 있는 그림체만 모은다

### tests/artist/custom-style-anchor.test.ts (7)
> 사용자가 고른 그림체는 주소가 있으면 그대로 쓰고, 없으면 기본 그림체를 찾아준다
- 그림 주소가 있을 때만 사용자가 고른 그림체로 인정한다
- 그림 이름과 표현 방식은 글자로 적힌 값만 사용한다
- 사용자가 고른 그림체가 있으면 기본 목록을 조회하지 않는다
- 선택한 그림체의 이름을 그대로 유지해 같은 그림체로 이어간다
- 사용자가 고른 그림체가 없으면 기본 목록에서 찾아준다
- 그림 주소가 없는 잘못된 선택은 기본 목록에서 다시 찾는다
- 사용자 그림체와 기본 그림체가 모두 없으면 그림체 없이 진행한다 (기존 동작)

### tests/artist/draft-trigger.test.ts (9)
> 그림체가 준비된 경우에만 캐릭터와 장소 그림 초안을 만들고, 한도와 중복 요청을 안전하게 막는다
- 그림체 설정이 없으면 그림 초안을 보내지 않고 없다고 잘못 기록하지 않는다
- 그림체 설정을 읽지 못해도 초안 생성을 멈추고 없다고 잘못 기록하지 않는다
- 그림체가 있으면 캐릭터 그림 초안을 만들고 작업 공간 정보를 함께 기록한다
- Writer에서 온 캐릭터도 Director에 들어가기 전에 그림 초안을 만든다 (#ref-gate 2026-09-02)
- 장소 초안과 화면에서 만든 장소 그림이 같은 설명을 사용하고 대상 정보를 유지한다
- 같은 장소 그림을 만들라는 요청이 이미 대기 중이면 새로 보내지 않는다
- 일부 대상에서 생성에 실패해도 전체 결과에 실패 수로 반영한다
- 사용 한도를 넘으면 그림 초안을 보내지 않고 차단 사실을 알린다
- 사용 한도 안이면 그림 초안을 정상적으로 보낸다 (기존 동작 유지)

### tests/artist/fal-image-size.test.ts (15)
> 이미지 크기 요청은 모델 약속에 맞추고, 화면 형식별 시트 크기를 정확히 지킨다 (#fal-canvas 2026-08-17)
- 기존 그림을 고칠 때 가로×세로를 지정하면 숫자 크기로 전달한다
- 기존 그림을 고칠 때 정해진 크기 이름은 그대로 사용한다
- 기존 그림을 고칠 때 크기를 생략하면 화면 비율에 맞는 기본 크기를 사용한다 (기존 약속 유지)
- Flux 그림 모델은 가로×세로를 그대로 반영하고, 생략하면 16:9 크기를 사용한다
- Grok 그림 모델은 크기 설정을 받지 않으므로 크기를 보내지 않는다
- 새 그림을 만들 때 화면 비율은 그대로 사용하고 참고 그림과 별도 크기는 보내지 않는다
- 기존 그림을 고칠 때 참고 그림을 사용하고 자동 비율은 따로 보내지 않는다
- nano-banana-2로 새 그림을 만들면 설명과 화면 비율만 보낸다 (#owner-default 2026-09-02)
- nano-banana-2로 기존 그림을 고칠 때 설명과 참고 그림만 보내 비율을 따라간다
- seedream으로 새 그림을 만들 때 화면 비율에 맞는 크기를 사용한다
- seedream에서 크기를 생략하면 모델 기본 정사각형을 사용한다
- seedream으로 기존 그림을 고칠 때 참고 그림과 가로×세로 크기를 올바르게 전달한다
- 그리드 시트는 모든 화면 형식에서 칸 비율을 맞추고 빈 여백을 없앤다
- 화면 형식을 알 수 없는 예전 프로젝트만 이전 시트 크기를 유지한다
- 세로 화면 형식의 스트립은 세 칸을 가로로 놓고, 나머지는 세로로 쌓는다

### tests/artist/fal-media-host.test.ts (6)
> #fal-cdn-host (2026-08-01) 회귀 가드.
- 실제로 막혔던 호스트를 통과시킨다
- 기존 호스트도 그대로 통과한다
- 앞으로 늘어날 서브도메인도 통과한다 (정적 목록이 낡지 않게)
- 대소문자·공백에 흔들리지 않는다
- 도메인을 사칭하는 호스트는 막는다 (접미사 판정의 점 포함)
- 무관한 호스트는 막는다

### tests/artist/fal-model-schemas.test.ts (3)
> 이미지 요청에는 각 모델이 받을 수 있는 정보만 남겨 잘못된 설정을 보내지 않는다
- 허용된 정보만 보내면 빠지는 정보가 없다
- 모델이 받지 않는 정보는 빠질 항목으로 알려준다
- 등록되지 않은 모델은 비교하지 않고 빠지는 정보도 없다고 본다

### tests/artist/gate-copy.test.ts (10)
> 생성 화면에서 Artist의 오래 걸린 생성과 진짜 실패를 구분해 사용자가 다음 행동을 알 수 있게 한다 (#stale-gate 2026-08-26)
- 오래 걸린 생성과 실패한 생성을 한 문구로 합치지 않는다
- 오래 걸림 전용 안내가 있고 한국어로 표시된다
- 실패한 생성은 실패라고 분명히 알린다
- Director로 돌아오면 최근 완료됐지만 아직 반영하지 않은 작업을 찾아 알린다
- 추가 완료 확인은 돌아올 때 한 번만 하고 평소 주기 확인에는 섞지 않는다
- 실패하거나 오래 걸리면 화면에 안내를 띄운다
- 화면 안내 문구가 한국어로 준비돼 있다
- Artist 화면은 탭으로 돌아오면 상태를 다시 확인한다
- Writer 화면도 탭으로 돌아오면 상태를 다시 확인한다
- 화면을 계속 보고 있어도 오래 걸린 확인을 정리하고 다음 확인을 예약한다

### tests/artist/image-models.test.ts (11)
> 이미지 모델을 고르면 지원되는 방식으로 요청하고, 시트에 맞지 않는 모델은 안전한 선택으로 바꾼다
- 모델의 편집용 요청 주소는 편집 경로로 끝나거나 비워 둔다 (style-anchor Rule M 존중 조건)
- 사용 가능한 모델마다 필요한 요청 항목을 빠짐없이 등록해 둔다
- 모델 선택 순서는 등록된 모델과 중복·누락 없이 일치한다
- #owner-default(2026-09-02): 기본 모델은 nano-banana-2, 1세대는 선택지로 남는다
- 알려진 모델 이름과 예전 요청 주소는 맞는 모델로 바꾸고, 모르는 값은 기본 모델을 쓴다
- 지원하는 모델 이름만 채팅 선택으로 인정한다
- 참조 이미지를 지원하는 모델만 이미지와 함께 요청할 수 있다
- 참조 이미지가 있으면 편집 방식으로, 없으면 새로 만드는 방식으로 요청한다
- 참조 이미지를 지원하지 않는 모델은 참조 요청이 와도 새 이미지 만들기로 처리한다
- 시트에 맞지 않는 모델(기본 nano-banana 포함)은 검증된 모델로 바꿔서 사용한다
- 시트에 맞는 모델을 고르면 그대로 사용한다

### tests/artist/image-provenance.red-team.test.ts (8) — red-team 에도 속함
> 외모와 룩이 바뀌었는지 정확히 판단하고, 다양한 입력에서도 이미지 출처를 안전하게 구분한다
- 룩 정보가 비어 있거나 공백뿐이면 룩 정보를 만들지 않는다
- 외모 설명에 구분 문자가 있어도 룩을 붙인 결과와 섞이지 않는다
- 색상 값이 중복되거나 비어 있어도 같은 룩 정보로 정리한다
- 매우 긴 외모와 룩도 내용이 같으면 같게, 달라지면 다르게 판단한다
- 한글과 이모지는 보존하고 공백 차이는 무시한다
- 의상만 있어도 룩 정보를 만든다
- 외모나 룩이 바뀌었을 때와 정보가 없을 때 이미지 변경 여부를 올바르게 판단한다
- 배경 이미지도 기존 캐릭터 이미지와 같은 기준으로 출처를 판단한다

### tests/artist/image-provenance.test.ts (33)
> 외모와 룩이 바뀌면 이미지가 새로 필요한지 알리고, 후보 이미지는 안전하게 보관한다
- 동일 외모 → 동일 지문 (결정적)
- 사소한 공백 차이는 같은 이미지 입력으로 본다 (헛판정 방지)
- 내용이 바뀌면 다른 지문
- 외모 설명이 없거나 비어 있어도 안전한 결과를 만든다
- F1: 룩 정보가 없으면 예전 방식과 같은 결과를 낸다
- 룩 반영 시 지문이 달라진다 (룩 미반영 초안과 구분)
- 룩 정보가 전혀 없으면 결과를 비워 둔다
- 색상 입력 순서가 달라도 같은 결과를 낸다
- 그림체·형태·의상 정보가 룩에 반영된다
- 의상만 있어도 룩 정보를 만든다
- 의상 사이 공백 차이는 같은 룩으로 본다 (헛판정 방지)
- Q5: 스타일 기준이 없으면 예전 방식과 같은 결과를 낸다 (F1)
- Q5: 스타일 기준을 정하면 룩 정보에 포함하고 바뀌면 결과도 바뀐다
- Q5: 스타일 기준만 있어도 룩 정보를 만든다
- Q5: 스타일 기준이 바뀌면 룩 정보도 달라진다 (항상 같은 결과)
- Q5: 같은 스타일 기준을 쓰면 어디서 계산해도 같은 룩 정보가 나온다 (헛판정 방지)
- 스타일 기준만 바뀌고 외형이 같으면 룩 반영 대기로 분류한다 (외형 변경 아님)
- 스타일 기준이 그대로면 최신 상태로 본다
- lookVersionKey — 스타일 기준이 바뀌면 버전도 바뀐다
- F1: 룩 정보가 없으면 배경 설명만으로 예전 방식과 같은 결과를 낸다
- 룩 반영 시 달라진다
- 지문을 모르면 오래된 이미지로 표시하지 않는다 (예전 이미지 보완)
- 현재 외모와 룩이 후보와 같으면 오래된 이미지로 보지 않는다
- 외모를 고치면 이미지가 오래된 것으로 표시된다
- AC7: 룩 없이 만든 초안은 룩이 도착하면 오래된 이미지로 표시된다
- 화면의 모습 이름을 후보 이미지 이름으로 올바르게 바꾼다 (019 자료 보완과 일치)
- 선택하지 않은 후보 이미지는 5장까지 보관한다 (결정)
- 보관 한도 이하면 삭제하지 않는다
- 보관 한도를 넘고 보호한 후보가 없으면 최신 후보를 남기고 오래된 것부터 삭제한다
- 선택한 후보는 오래돼도 삭제하지 않는다
- 고정한 후보는 삭제하지 않는다
- 보호한 후보만으로 한도를 넘으면 보호하지 않은 후보를 모두 삭제하고 보호한 후보는 남긴다
- 기본 보관 수는 5장이다

### tests/artist/image-url.test.ts (8)
> 이미지 주소를 안전하게 줄여 쓰고, 원본 주소와 외부 주소는 그대로 보존한다
- 보관함의 이미지 주소는 같은 위치의 작은 이미지 주소로 바꾼다
- 주소의 버전 표시(?v=)를 그대로 보존한다
- 우리 보관함이 아닌 외부 주소는 그대로 둔다
- 다른 Supabase 프로젝트의 주소는 건드리지 않는다
- 파일 확장자가 없는 주소는 그대로 둔다
- 이미 작은 이미지인 파일은 다시 바꾸지 않는다 (_thumb.webp / 영상 _thumbnail.jpg)
- 주소가 없거나 비어 있으면 주소를 만들지 않는다
- 작은 이미지 기능을 끄면 원본 주소를 그대로 쓴다

### tests/artist/location-appearances.test.ts (5)
> 약속 C(열 번째) — 배경도 캐릭터와 같은 모습(타임라인) 탭을 갖는다 (_tdd.md B10·C, 2026-09-04)
- 배경 카드 위쪽에 모습 탭 줄이 항상 보이고 "+ 모습 추가"가 있다
- "+ 모습 추가" 창에서 저장하면 변형 행이 생기고 이미지가 바로 만들어진다(기본 배경을 참조)
- 채팅에서 배경의 새 모습을 만들어 달라고 하면 승인 뒤 추가되고, 특정 모습만 다시 그릴 수 있다
- 씬의 서사 시점과 같은 모습에 이미지가 있으면 Writer·Director 가 그것을 쓰고, 없으면 기본 모습을 쓴다
- 모습을 지우거나 이름을 바꿀 수 있고, 기본 모습은 지우지 못한다

### tests/artist/lock-gate.test.ts (10)
> 작가 이미지 준비가 끝난 프로젝트만 Artist 단계로 들어가며, 실패하면 다시 시도할 수 있다
- 작가 이미지가 준비되기 전에는 Artist로 이동하지 못하게 한다
- 기존 프로젝트의 준비된 이미지 정보가 있으면 Artist 진입을 허용한다
- 이미지 준비 실패와 멈춤 상태를 안내 정보에 반영한다
- 예전 Producer 준비 정보만으로는 Artist 진입을 허용하지 않는다
- 프로젝트를 초기화하면 작가 이미지 진입 상태도 초기화한다
- 재시도 작업이 남아 있으면 실패로 표시하지 않고, 모두 끝나면 실패를 표시한다
- 재시도를 요청해 성공하면 실패와 멈춤 표시를 지우고 상태 확인을 다시 시작한다
- 이미 작업이 대기 중이면 실패와 멈춤 표시를 지운다 (진행 중)
- 한도 초과나 다른 오류가 나면 실패와 멈춤 안내를 유지한다
- 프로젝트를 고르지 않았으면 재시도를 요청하지 않는다

### tests/artist/lock-poll.test.ts (8)
> 작가 이미지 준비 상태를 확인할 때 완료·실패·멈춤을 올바른 순서로 안내한다
- 모든 이미지가 준비되면 확인을 멈추고 잠금을 푼다
- 실패한 작업이 있으면 즉시 멈추고 실패로 표시한다
- 처음 한 번 멈춰도 바로 실패로 확정하지 않는다
- 두 번 연속 멈추면 실패로 확정하고 멈춘다
- 작업이 다시 대기하면 멈춤 횟수를 처음부터 센다
- 모든 이미지가 준비되면 멈춤 상태가 있어도 실패로 표시하지 않는다
- 실패한 작업이 있으면 멈춤보다 먼저 실패로 처리한다
- 재시도 작업이 대기 중이면 실패로 확정하지 않는다

### tests/artist/main-image.test.ts (4)
> 기본 모습의 그림표를 우선 대표 이미지로 쓰고, 없으면 다른 그림표와 예전 이미지를 차례로 확인한다 (#artist-main-authority 2026-09-03)
- 기본 모습의 그림표를 대표 이미지로 사용한다 (대표 이미지가 비어 있어도)
- 기본 모습에 그림표가 없으면 그림표가 있는 첫 모습을 사용한다
- 모습 그림표가 하나도 없으면 예전 대표 이미지를 쓰고, 그것도 없으면 비워 둔다
- 겨울_5 실측: 대표 이미지가 없어도 기본 모습 그림표가 있으면 다시 만들지 않는다

### tests/artist/portrait-paper-trim.test.ts (3)
> 가장자리의 종이 띠만 잘라내고 그림 배경은 보존한다 (#portrait-paper-trim 2026-08-26)
- 우측 테두리 선 2px와 종이 15px, 상단 종이 6px를 걷어낸다 (실측 모사)
- 띠 없는 그림(세로 그라데이션 배경)은 그대로 — 배경을 종이로 오인하지 않는다
- 전면 종이색(243) 극단 입력도 변당 10% 상한까지만 걷는다

### tests/artist/refresh-look-pending.test.ts (2)
> 룩이 확정되기 전 만든 캐릭터와 이미지 없는 Writer 캐릭터를 다시 만들고, 최신 결과는 건드리지 않는다
- 룩이 확정되기 전 만든 캐릭터와 Writer가 추가한 이미지 없는 캐릭터만 다시 만든다
- 다시 만들 대상이 없으면 결과를 바꾸지 않는다

### tests/artist/retry-drafts.test.ts (6)
> 로그인한 사용자가 초안을 다시 만들 때 권한과 사용 한도를 확인하고 처리 결과를 알려준다
- 로그인하지 않으면 접근을 막는다
- 요청 내용이 올바르지 않으면 거절한다
- 프로젝트를 맡은 사람이 아니면 접근을 막는다
- 사용 한도를 넘으면 초안을 다시 만들지 않고 거절한다
- 프로젝트에 이미 다시 만들 초안이 대기 중이면 거절한다
- 초안 다시 만들기가 정상 처리되면 항목별 결과와 건너뛴 수를 알려준다

### tests/artist/style-anchor-chat.test.ts (2)
> D12 — 채팅이 이름/느낌으로 고른 스타일 앵커 키를 카탈로그 검증 후 즉시 반영한다 (2026-08-31 오너).
- 목록에 있는 스타일을 고르면 바로 적용한다
- 목록에 없는 스타일을 고르면 새 스타일을 만들어 적용하지 않는다

### tests/artist/style-anchor-noop.test.ts (9)
> 캐릭터·배경·스토리보드를 만들 때 사용자가 고른 내용과 안전 기준만 반영하고, 전역 스타일은 몰래 덧붙이지 않는다
- 기본 인물 그림이 없으면 정해진 캐릭터 양식을 참고해 그림을 만든다 (D.1)
- 캐릭터 양식을 쓸 수 없으면 3:2 비율로 새 그림을 만든다 (D.2)
- 옆모습이나 뒷모습을 만들 때 정면 그림을 참고한다 (D.4)
- 사용자가 안전 모드를 켜면 안전한 표현으로 바꾸어 그림을 만든다 (D.5)
- 배경을 만들면 사람 없이 요청한 내용과 화면 비율을 반영한다 (D.6)
- 스토리보드를 만들면 요청한 내용과 참고 이미지를 화면 비율에 맞춰 반영한다 (D.7)
- 캐릭터 초안을 만들 때 양식이 있으면 참고하고 없으면 3:2 비율로 만든다 (D.8)
- 러프 스토리보드를 만들 때 장면 스타일을 임의로 덧붙이지 않는다 (D.9)
- 이미지 생성 요청에 전역 스타일을 몰래 덧붙이지 않는다 (D.10)

### tests/artist/style-anchor-routes.test.ts (14)
> 선택한 스타일과 참고 이미지를 반영해 필요한 그림만 만들고, 이미 있는 그림은 함부로 바꾸지 않는다
- 인물 그림을 처음 만들 때 선택한 스타일과 캐릭터 양식을 함께 반영한다 (AC1)
- 선택한 스타일과 어울리는 표현은 유지하고 충돌하는 표현만 덜어낸다 (AC1b, #F-004 B4, 2026-07-14 판정의 명시적 번복)
- 선택한 스타일이 달라지면 그림을 최신 결과로 다시 만든다 (AC10, Q5)
- 캐릭터 양식을 쓸 수 없어도 선택한 스타일을 반영해 3:2 비율로 그림을 만든다 (AC2)
- 뒷모습이나 옆모습은 정면 그림만 참고하고 전체 스타일은 따로 덧붙이지 않는다 (AC4)
- 배경 그림에도 선택한 스타일을 반영하고 요청한 비율을 지킨다 (AC5)
- 스토리보드에 선택한 스타일과 사용자가 준 참고 이미지를 함께 반영한다 (AC6)
- 참고 이미지가 없어도 스토리보드에 선택한 스타일을 반영한다 (AC6)
- 캐릭터 초안을 만들 때 선택한 스타일을 양식 유무와 관계없이 반영한다 (AC7)
- 선택한 스타일을 쓸 수 없으면 캐릭터 초안을 기존 방식으로 만든다 (AC7)
- 선택한 스타일을 사용할 수 없어도 배경 그림은 사람 없이 만든다 (AC9)
- 자동으로 만들 때 이미 그림이 있으면 다시 만들지 않는다
- 사용자가 다시 만들면 기존 그림이 있어도 새 그림을 만든다
- 자동으로 만들 때 그림이 없으면 처음 그림을 만든다

### tests/artist/style-anchor.test.ts (14)
> 선택한 스타일을 그림에 알맞게 반영하고, 사용할 수 없는 스타일은 안전하게 건너뛴다
- 스타일이 없으면 모든 방식에서 기존 내용을 그대로 돌려준다
- 스타일 안내 문구를 정해진 내용 그대로 제공한다
- 그림 종류에 따라 스타일 안내와 참고 자료 설명을 알맞게 붙인다
- 스타일 참고 이미지를 먼저 두고 기존 참고 이미지 순서를 지킨다
- 필요할 때만 화면 비율을 고정하고 비율 정보가 없으면 알린다
- 스타일 이름이 없으면 목록을 확인하지 않고 비워 둔다
- 사용 가능한 스타일을 찾으면 다음에도 바로 쓸 수 있게 기억한다
- 사용 중지된 스타일은 찾지 못한 것으로 처리한다
- 없는 스타일은 찾지 못한 것으로 처리한다
- 스타일 목록을 확인하지 못하면 찾지 못한 것으로 처리한다
- 스타일 목록을 확인하는 중 문제가 생기면 찾지 못한 것으로 처리하고 알린다
- 이미 찾은 사용 가능한 스타일은 다시 확인하지 않고 바로 쓴다
- 기억해 둔 유효 시간이 지나면 스타일을 다시 확인한다
- 없는 스타일 상태인 %s는 기억해 두지 않는다 (표)

### tests/artist/template-asset.test.ts (13)
> 필요한 그림 파일을 최신 주소로 한 번만 보관하고, 오래된 파일은 안전하게 정리한다
- 파일 내용이 달라지면 다른 주소를 사용한다
- 같은 파일은 한 번만 올려 불필요하게 다시 올리지 않는다
- 최신 파일이 이미 보관되어 있으면 다시 올리지 않는다
- 파일을 올리지 못해도 전체 작업을 막지 않고 다른 방법으로 진행한다
- 파일이 없어도 그림 만들기를 막지 않는다
- 파일마다 다른 주소를 사용한다
- 오래된 버전만 치우고 현재 버전은 남긴다
- 이름이 비슷한 다른 파일은 잘못 지우지 않는다
- 규칙에 맞지 않는 파일 이름은 건드리지 않는다
- 진행 중인 작업이 쓰는 파일은 보호한다
- 새 버전을 올릴 때 같은 파일의 오래된 버전을 지운다
- 진행 중인 작업이 쓰는 오래된 파일은 지우지 않는다
- 오래된 파일 정리에 실패해도 새 파일 주소는 돌려준다

### tests/artist/turnaround-safemode.red-team.test.ts (6) — red-team 에도 속함
> 안전 모드는 위험할 수 있는 표현만 덜어내면서 인물의 성별·피부·그림체 정보는 필요한 만큼 보존한다
- 안전 모드를 끄면 설정을 생략했을 때와 모든 입력에서 같은 그림이 나온다
- 안전 모드에서도 성별·피부 표현과 그림체, 일부 영어 표현은 그대로 남긴다
- 안전 모드를 켜면 미성년자와 유혈을 드러내는 표현을 외형과 의상에서 덜어낸다
- 안전 모드를 켜면 모든 방향의 그림에서 위험한 표현을 덜어내고 같은 인물이라는 약속을 지킨다
- 안전 모드에서 한국어 나이 표현은 덜어내되 어린이날처럼 뜻이 다른 말은 보존한다
- 안전 모드에서 값이 없거나 유니코드·긴 문장이어도 안정적으로 다룬다

### tests/artist/turnaround-safemode.test.ts (4)
> 위험한 인물 묘사가 들어오면 미성년·유혈 표현을 덜어 안전한 인물 이미지를 만든다 (#A)
- 안전 기능을 끄거나 지정하지 않으면 기존 설명을 그대로 유지한다
- 안전 기능을 끄면 원래 묘사와 나이를 유지한다
- 안전 기능을 켜면 미성년·유혈 표현을 덜고 자극 없는 성인풍 묘사로 바꾼다
- 안전 기능을 켜면 옆모습도 같은 기준으로 인물 일관성을 지킨다

### tests/artist/turnaround.test.ts (4)
> 캐릭터의 기본 인상은 지키고 사용자가 지정한 변경은 모든 방향에 반영한다
- 변경 내용을 보내지 않으면 기본 인상만 유지한다
- 변경 내용을 보내면 기본 인상을 지키면서 사용자의 변경을 우선 반영한다
- 옆모습을 요청해도 사용자의 변경과 같은 인물이라는 약속을 함께 반영한다
- 변경 내용이 공백뿐이면 기본 인상만 사용한다

### tests/artist/world-user-edited.test.ts (2)
> 제작자만 정한 장소는 자동 생성과 사용자 요청에 따라 수정 기록을 다르게 남긴다
- 처음 자동으로 만들면 제작자가 정한 장소를 그대로 쓰고 사용자 수정으로 기록하지 않는다
- 채팅으로 다시 만들면 제작자만 정한 장소도 사용자 수정으로 기록한다

## director — Director: 캔버스·선·실사·영상 생성

### tests/director/action-guard.test.ts (9)
> 빠르게 같은 작업을 눌러도 중복 요청을 막고, 다른 샷의 요청은 따로 처리한다
- 처음 누른 요청은 통과시킨다
- 같은 작업을 빠르게 다시 누르면 두 번째 요청은 막는다
- 잠시 기다린 뒤 다시 누르면 요청을 통과시킨다
- 서로 다른 샷이면 각 요청을 모두 통과시킨다
- 같은 샷을 어느 버튼에서 눌러도 짧은 중복 방지 시간을 함께 적용한다
- 화면 버튼과 내부 작업이 같은 샷이어도 서로의 요청을 막지 않는다
- 막힌 요청은 중복 방지 시간을 늘리지 않아 처음 누른 뒤 정해진 때 다시 허용한다
- 실패한 요청을 마치면 곧바로 다시 시도할 수 있다 (실패 후 재시도)
- 요청 기록이 많이 쌓여도 지난 기록만 치우고 아직 유효한 요청은 지킨다

### tests/director/batch-runner-fresh.test.ts (2)
> Director 배선 3 — 배치 러너의 갱신은 헛돌지 않는다 (2026-09-06)
- 시트가 끝난 직후의 재수화는 30초 사물함의 옛 행이 아니라 완료된 행을 받아 카드에 바로 싣는다
- 러너의 재수화 두 곳 모두 사물함 무효화가 앞선다

### tests/director/build-video-prompt.test.ts (9)
> 영상 설명에 카메라와 장면 연결 안내를 알맞게 넣고 길이 제한도 지킨다
- Kling I2V 영상은 카메라 움직임을 설명하고 이동 설정은 넣지 않는다
- T2V 영상은 선택한 움직임 설정을 설명에 덧붙인다
- 카메라 설정을 고르면 Arri Alexa 촬영 정보를 설명에 덧붙인다
- Veo 영상이 8초보다 짧으면 뒤를 검은 화면으로 채우고 설명을 1000자로 제한한다
- START와 END를 함께 고른 I2V 영상에는 두 장면 연결을 안내하고 T2V에는 넣지 않는다
- START·REF·END 순서를 설명하되 REF를 시간 흐름의 기준 장면으로 취급하지 않는다
- REF 이미지만 주면 END 장면이 있다고 잘못 안내하지 않는다
- 역할 정보가 없으면 예전 START·END 연결 안내를 유지한다
- 기본 설명이 500자를 넘으면 500자로 자른다

### tests/director/completion-shots-cache-invalidate.test.ts (1)
> 생성이 끝난 뒤 다른 화면에서도 최신 샷 목록을 바로 보여준다 (#shots-cache-invalidate 2026-08-24 티켓)
- 생성이 끝난 뒤 다른 화면에 다시 들어오면 30초가 지나지 않아도 최신 샷 목록을 다시 불러온다

### tests/director/context-menu.test.ts (14)
> 우클릭한 카드에서 할 수 있는 일과 대표 이미지를 올바르게 보여준다 (#context-menu 2026-08-31)
- Scene·Shot·Video 카드에서는 편집과 삭제를 고를 수 있다
- 이미지가 있으면 복사와 다운로드를 고를 수 있다
- 이미지가 없으면 복사와 다운로드를 고를 수 없다
- asset Image는 편집과 복사는 할 수 있지만 원본과 연결되어 있어 직접 삭제할 수 없다
- 예전에 만들어진 파생 카드는 편집 없이 삭제만 고를 수 있다 (#node-merge)
- prompt 카드에서는 삭제만 고를 수 있다
- Shot 카드에는 완성된 스토리보드 이미지를 보여준다
- 아직 만들지 않았거나 실패한 Shot과 Scene에는 대표 이미지를 보여주지 않는다
- Video 카드에는 미리보기 이미지를 보여준다
- 없는 카드를 찾아도 대표 이미지를 보여주지 않는다
- 부모 Scene이 없어도 이미지 카드가 생기고 연결선은 만들지 않는다
- 독립 Shot에서도 Video를 만들 수 있다
- 저장된 영상 정보를 받으면 Video 하나만 만들고 Shot과 연결선을 만들지 않는다
- 바꾼 설정으로 같은 영상의 새 생성을 요청한다

### tests/director/edge-delete.test.ts (4)
> 약속 F — 선을 지우면 연결이 실제로 끊기고 되살아나지 않는다 (_tdd.md F, 2026-09-04 오너 확정)
- 선을 고른 뒤 Delete 키를 누르거나 선 위의 X를 누르면 선이 지워진다
- 이미지·프레임·영상 연결 선을 지우면 받는 쪽 카드에서 그 입력이 빠지고 새로고침해도 지워진 채다
- 참조 선을 지우면 그 샷의 참조 목록에서도 빠져 다음 실사 생성에 쓰이지 않는다
- 선을 지우기 전에 확인창이 뜨지 않는다. 대신 Ctrl+Z로 되돌릴 수 있다

### tests/director/edge-truth.test.ts (3)
> 약속 G — 카드에 그려진 선은 실제 입력과 같다 (_tdd.md G, 2026-09-04)
- 샷 카드에 그려진 참조 선의 상대(캐릭터·배경)는 그 샷이 실제로 참조하는 목록과 하나도 어긋나지 않는다
- 이미지·프레임·영상 입력 선은 그 카드가 실제로 받는 입력 칸에 하나씩 대응하고, 없는 입력에는 선이 그려지지 않는다
- 팝업에서 참조를 빼면 선도 같이 사라지고, 넣으면 선도 생긴다

### tests/director/editor-nudge.test.ts (4)
> 영상이 충분히 준비되면 실제 진행 수치를 알려주고 Editor 작업을 제안한다 (#d10 2026-08-27 오너)
- 영상 하나만으로 Editor 이동을 제안하지 않는다
- 전체 샷 가운데 영상이 준비된 비율을 보고 제안한다
- 영상이 절반을 넘게 준비되면 남은 샷이 있어도 Editor에서 편집을 시작할 수 있다
- 완성됐다고 단정하지 않고 준비된 샷 수를 안내한다

### tests/director/frame-wiring.test.ts (8)
> 영상에 연결한 시작·끝 장면과 참고 이미지를 다시 불러와도 그대로 유지한다
- START와 END에는 각각 한 장면을 연결하고 같은 장면도 둘 다 쓸 수 있다
- START와 END를 다시 연결하면 이전 장면을 바꾸고 REF에는 여러 장면을 중복 없이 남긴다
- Writer에서 이어온 샷의 장면 연결은 다시 불러와도 유지된다 (#node-merge)
- 장면 연결 하나를 지우면 그 장면만 영상 입력에서 빠진다
- 여러 이미지를 Shot에 연결해도 같은 이미지는 한 번만 남긴다
- 이미지 연결 하나를 지우면 그 이미지만 Shot에서 빠진다
- 이미지 연결 요청도 화면에서 직접 연결한 것과 같은 결과가 된다
- Writer에서 이어온 샷의 이미지 연결은 다시 불러와도 유지된다 (#node-merge)

### tests/director/image-not-reverted.test.ts (3)
> Director 배선 4 — 생성 직후의 이미지는 옛것으로 되돌아가지 않는다 (2026-09-06)
- 무효화 전에 시작된 조회가 진행 중이면, 무효화 뒤의 loadShots 는 그 결과에 합류하지 않고 다시 받는다
- 뒤늦게 끝난 옛 조회가 사물함을 신선한 것으로 되돌리지 못한다 — 그 뒤 30초 안의 읽기도 새 행이다
- 단건 이미지 잡은 제출 즉시 큐를 갱신해, 정산 재수화(큐 훅)가 이 잡도 본다

### tests/director/interaction.test.ts (20)
> 카드를 누르거나 연결할 때 알맞은 편집 화면과 관계를 보여준다
- Shot·Video·asset Image를 누르면 왼쪽 패널에서 편집한다
- Scene을 누르면 편집 창에서 연다
- prompt를 눌러도 아무 동작을 하지 않는다
- 그리드 화면에서는 Scene·Shot·Video를 눌러 편집 창을 열 수 있다
- 카드 화면에서는 Scene만 편집 창을 열고 Shot·Video는 왼쪽 패널에서 연다 (#panel-unify)
- asset과 prompt는 편집 창을 열지 않는다
- Scene은 편집 창에서, Shot·Video·asset은 왼쪽 패널에서 연다 (#panel-unify)
- prompt를 더블클릭해도 아무 동작을 하지 않는다
- 같은 카드를 다시 누르면 선택을 해제한다
- 다른 카드를 누르면 그 카드만 선택한다
- 선택된 카드가 없을 때 누른 카드를 선택한다
- 프롬프트 칸에 연결하면 프롬프트 내용으로 이어진다
- Shot의 참고 이미지 칸에 연결하면 이미지로 이어진다
- Video의 START·END·REF 칸에 연결하면 장면 입력으로 이어진다
- 이전 Video의 마지막 장면을 연결하면 다음 Video로 이어진다
- 그 밖의 연결은 관계 선택 창을 연다
- 새 시도가 실패해도 가장 최근 성공한 영상을 계속 재생한다
- 이전 실패가 있어도 가장 최근 시도의 결과만 실패 안내로 보여준다
- 생성 중 표시와 실패 안내는 같은 최신 시도 결과에서 정한다
- 영상 순서 정보가 잘못돼도 결과를 일정하게 정한다

### tests/director/lines-survive.test.ts (6)
> Director 배선 1 — 손으로 이은 선은 프로젝트 전환·새 기기에서도 남는다 (2026-09-06)
- 아직 보이지 않는 에셋을 가리켜도 다시 들어온 뒤 연결을 빼지 않고 그대로 보존한다
- 에셋이 나중에 나타나면 보관한 연결이 이미지 목록과 선으로 돌아온다
- 영상 카드가 아직 없는 에셋을 가리켜도 연결을 보관했다가 에셋이 나타나면 다시 이어 준다
- 화면을 다시 불러오는 중 요청이 겹쳐도 먼저 시작한 결과로 연결이 비워졌다고 저장하지 않는다
- 다시 불러온 연결이 기존 내용과 같으면 불필요하게 저장하지 않는다
- 아직 찾지 못한 연결도 버리지 않고 보관하며 다시 합칠 때 같은 연결은 하나만 남긴다

### tests/director/no-auto-real.test.ts (16)
> 사용자가 직접 요청할 때만 그림·영상을 만들고, 보고 있던 화면을 빼앗지 않는다 (#c5, #c4, 2026-08-27 오너 지시)
- 사용자가 직접 시작하지 않으면 그림을 자동으로 만들지 않는다
- 화면에 들어갈 때 그림을 자동으로 채우지 않는다
- 전체 만들기 버튼을 누르면 모든 그림을 한꺼번에 만들 수 있다
- 채팅에서 그림 만들기 요청을 선택할 수 있다
- 대상을 지정하면 해당 그림만, 지정하지 않으면 남은 그림을 모두 만든다
- 그림 대상이 아니면 작업하지 않는다
- 채팅으로 그림 만들기를 요청하면 먼저 확인 카드를 보여준다
- 사용자가 그림 생성을 분명히 요청할 때만 그림 만들기를 시작한다
- 채팅의 영상 만들기는 전체 장면에 대해서만 허용한다고 안내한다
- 한 장면 영상 만들기를 거절하면 그 이유를 사용자에게 솔직히 알린다
- 그림 만들기를 눌러도 보고 있던 화면을 바꾸지 않는다
- 스토리보드 화면에서 그림을 만들 때만 실사 보기로 맞춘다
- 영상의 시작·끝·참조 그림을 연결하는 방법을 안내하고 허용한다
- 영상 연결 요청은 기존 연결 규칙으로 적용한다
- 장면에 참고 그림을 연결하는 방법을 안내하고 허용한다
- 참고 그림 연결 요청은 기존 연결 규칙으로 적용한다

### tests/director/previz-refresh.test.ts (2)
> Director 배선 2 — 러프·previz 재생성 결과는 새로고침 없이 카드에 뜬다 (2026-09-06)
- 그림을 새로 고치면 러프와 미리보기 영상만 최신 내용으로 바뀌고 다른 내용은 그대로 둔다
- 러프 그림 작업이 끝나면 새로고침 없이 Director 카드의 러프와 미리보기 영상이 갱신된다

### tests/director/real-grid-identity.test.ts (10)
> 인물별 그림은 지정된 사람만 담고, 한 장면의 시간대와 빛을 모든 칸에 맞춘다 (#real-grid-identity 2026-08-12, #F-006 2026-08-13)
- 인물 목록이 있으면 참조 순서와 칸별 인물을 안내한다
- 한 칸에 두 사람이 나오면 두 사람 이름을 함께 적는다
- 사람 없는 칸에는 사람을 넣지 않는다 (삽입 장면)
- 인물 목록이 없으면 이름 없는 안내를 그대로 유지한다
- 인물이 없으면 칸별 인물 안내를 넣지 않는다
- 스타일 기준 그림과 칸별 인물 안내를 함께 제공한다
- 시간대를 지정하면 전체 그림에 같은 빛을 적용하고 기준 그림과 겹치는 안내는 뺀다
- 시간대가 없거나 비어 있으면 기존 안내를 그대로 유지한다
- 기준 그림이 없어도 지정한 시간대의 빛 안내는 적용한다
- 시간대를 지정하면 세 장면 그림에 같은 빛을 적용하고 없으면 기존 안내를 유지한다

### tests/director/real-strip-identity.test.ts (7)
> 각 인물 그림은 지정된 사람과 위치·자세를 따르고, 미지정 인물은 따로 구분하며 같은 사람을 중복하지 않는다 (#ref-gate 2026-09-02, 겨울_4 sh_01_27)
- 인물 위치와 자세가 있으면 이름과 함께 설명하고, 없으면 이름만 적는다
- 그림 번호와 인물 이름을 맞추고, 같은 사람을 두 번 그리지 않으며 지정하지 않은 인형은 다른 사람으로 구분한다
- 인물 목록이 있으면 이름 없는 안내 대신 인물별 그림 번호를 알려준다
- 인물 목록이 없으면 이름 없는 안내를 그대로 유지한다
- 추가로 준 그림은 인물이나 장소가 아닌 참고 자료로 구분한다
- 칸마다 인물의 위치와 자세를 보여주고, 같은 사람을 두 번 그리지 않는다
- 이름만 알려도 해당 인물을 칸에 넣는다

### tests/director/ref-sheet-cleanup.test.ts (3)
> 기준 시트만 지우고 다른 그림과 사용자 업로드는 안전하게 보존한다 (#ref-sheet-ttl)
- 여러 장 그림의 기준 시트를 올바른 위치에서 찾는다
- 한 장 그림의 기준 시트도 주소 뒤 추가 정보와 상관없이 찾는다
- 기준 시트가 아닌 다른 그림과 사용자 업로드는 지우지 않는다

### tests/director/regenerate-all.test.ts (7)
> 전체 재생성을 누르면 이미 만든 그림도 다시 만들고, 비용이 드는 작업임을 먼저 알린다 (#c3 2026-08-27 오너)
- 전체 재생성을 요청하면 이미 만든 장면 그림도 다시 만든다
- 전체 재생성 요청이 실제 작업까지 전달된다
- 모든 그림이 있어도 전체 재생성 전에 확인을 요청한다
- 비용이 드는 전체 재생성은 확인한 뒤 시작한다
- 생성 중에는 다시 누를 수 없는 이유를 알려준다
- 전체 재생성 안내 문구를 한국어로 보여준다
- 각 장면에 저장한 인물 모습의 기준 그림만 사용하고 예전 그림은 쓰지 않는다

### tests/director/repaint-crop.test.ts (2)
> 장면 그림을 잘라낼 때 경계선과 종이 여백을 빼고 세 장면을 같은 크기로 만든다 (#repaint-crop 2026-08-25 오너 ③C)
- 구분선이 조금 어긋나도 각 장면 그림에 종이 여백이나 구분선이 섞이지 않는다
- 세 장면 그림은 영상 참고 자료로 써도 같은 크기가 된다

### tests/director/shot-chain.test.ts (6)
> Shot 체인 배선(#node-merge 2026-08-31 대공사) — rebuildShotChainNodes 검증.
- 파생 카드(shotImage/videoPlaceholder)를 만들지 않는다
- 구 persist 잔재(파생 노드)를 멱등 정리한다
- writer 샷의 Shot→Video parent 엣지를 직결 chain 엣지로 대체한다
- 수동 샷(writerShotId 없음)의 Video는 parent 엣지를 유지한다
- 멱등 — 두 번 돌려도 노드/체인 엣지 수가 늘지 않는다
- undo 후에도 체인이 재계산된다

### tests/director/stage.test.ts (15)
> Director에서 만든 장면 자료와 설명 연결은 다시 정리해도 유지되고, 최신 작업 단계를 올바르게 보여준다
- 인물 원본을 편집 가능한 카드로 만들고 다시 정리해도 편집 내용을 보존한다
- 그림이 없으면 초안 단계로 본다
- 완성된 실사 그림이 있으면 실사 단계로 본다
- 그림을 만드는 중이면 완성 전 단계로 본다
- 영상이 있으면 그림을 만드는 중이어도 영상 단계로 본다
- 없는 장면은 초안 단계로 본다
- 설명 카드를 만들면 새 카드가 추가된다
- 설명 카드를 장면에 연결하면 장면 설명이 함께 바뀐다
- 장면이 아닌 대상에는 설명을 연결하지 않는다
- 장면 설명 연결은 자료를 다시 정리해도 유지된다
- 지정한 장면의 초안 그림을 돌려준다
- 초안 그림이 없는 장면은 비워 둔다
- 장면을 지정하지 않으면 비워 둔다
- 없는 장면은 비워 둔다
- 같은 장면을 다시 찾아도 같은 그림을 가리킨다

### tests/director/standalone-video.test.ts (2)
> 독립 영상 설정은 빠짐없이 저장하고, 잘못된 설정은 사용하지 않는다
- 독립 영상을 준비하면 안전한 식별자와 서로 간섭하지 않는 기본 설정을 만든다
- 설정이 빠지거나 낯선 값이 섞이면 저장된 독립 영상 설정을 사용하지 않는다

### tests/director/state-boundaries.test.ts (24)
> 영상 생성과 최종 선택이 겹쳐도 이전 결과를 지키고 최신 상태를 정확히 반영한다
- 이전 영상이 성공했으면 뒤의 저장 확인이 실패해도 계속 재생한다
- 새 생성 시도가 있어도 최종으로 고른 영상은 따로 지킨다
- 실패한 생성은 이전 영상 주소가 남아 있어도 실패로 표시한다
- 현재 생성 시도에 맞는 복구 증표만 다시 사용한다
- 같은 장면에서 영상 생성 중이면 새 영상을 겹쳐 만들지 않고 끝나면 다시 만들 수 있다
- 같은 영상을 다시 만들 때 동시에 두 번 진행하지 않는다
- 아직 저장되지 않은 생성 중 영상은 오래된 저장 내용이 반영돼도 유지한다
- 새로 시작한 영상 생성 정보는 오래된 저장 내용으로 덮어쓰지 않는다
- 서버에 끝난 새 생성 결과가 있으면 예전 생성 중 표시를 새 결과로 바꾼다
- 불러오는 동안 바꾼 스토리보드 내용은 불러오기가 끝나도 지킨다
- 저장된 새 생성 결과가 있으면 예전 로컬 상태보다 우선한다
- 유효한 복구 증표로 다시 이어가면 완료된 영상까지 확인한다
- 직접 연결한 시작·참조·끝 화면은 역할에 맞게 보낸다
- 이전 영상의 마지막 화면을 다음 영상의 시작으로 쓰고 영상 자체는 보내지 않는다
- 영상 연결에 필요한 화면이 없으면 생성 요청을 보내지 않는다
- 연결한 장면의 그림으로 새 스토리보드를 만들고 내부 식별자는 외부에 보내지 않는다
- 지난 생성 시도의 복구는 새 시도가 시작되면 멈춘다
- 복구를 정해진 횟수만큼 시도해도 안 되면 상태 확인을 계속하지 않는다
- 성공 응답 형식이 잘못되면 실패로 표시하되 새 생성 시도 정보는 남긴다
- 상태 확인이 실패하면 영상 생성을 실패로 기록하고 같은 장면을 다시 만들 수 있게 한다
- 최종 영상을 바꾸다 실패하면 기존 선택을 되돌린다
- 오래된 최종 선택 실패가 새 선택을 덮어쓰지 않는다
- 최종 영상 선택이 거절돼도 실제 선택을 다시 맞추고 다음 선택을 할 수 있다
- 실패한 최종 선택 요청을 정리하고 다음 요청을 막지 않는다

### tests/director/sync-prompt-contract.test.ts (6)
> Writer에서 바뀐 장면 설명은 사용자의 수정 내용을 지키면서 Director에 반영한다
- 새 장면 설명을 받으면 Writer의 내용을 Director에 반영한다
- 장면 설명을 다시 받아도 사용자가 고친 내용을 지킨다
- 기존 장면 설명이 Writer 내용과 같으면 최신 내용으로 정리한다
- 기존 장면 설명이 Writer 내용과 다르면 사용자의 수정을 우선한다
- 사용자 수정과 Writer 내용과 기존 내용을 차례로 적용하고 모두 없으면 비워 둔다
- Writer 내용은 장면 설명에 반영하고 기존 입력 칸은 덮어쓰지 않는다

### tests/director/video-batch-chat-approval.test.ts (2)
> 약속 E3 — 채팅 승인 카드(directorGenerateVideoBatch)를 승인하면 버튼과 똑같이 runVideoBatch 가 돈다.
- 승인하면 카드에 적힌 만들 수 있는 수(limit)만큼 runVideoBatch 를 부른다
- 만들 수 있는 수가 0이면 승인해도 돌지 않고 충전 안내가 남는다

### tests/director/video-batch-takes.test.ts (5)
> 약속 E — 영상 일괄 생성은 Take를 미리 센다 (_tdd.md E, 2026-09-04 오너 확정)
- "영상 생성"을 누르면 확인창에 만들 영상 수, 필요한 Take 수, 지금 가진 Take 수가 함께 보인다
- Take가 모자라면 가진 만큼만 앞에서부터 만들고 확인창에 "N개 중 M개만 만들 수 있어요"라고 미리 알린다
- shadow 모드는 숫자를 보이되 막지 않는다. 0개면 충전 안내다
- 채팅에서 "영상 다 만들어줘"라고 하면 같은 확인 카드가 채팅에 뜨고, 승인하면 버튼과 똑같이 진행된다
- 진행이 끝나면 채팅에 "N개 완료, M개 실패"가 한 줄로 남는다

### tests/director/video-batch.test.ts (2)
> 여러 장면의 영상을 한 번에 만들 때 중복 없이 진행 상황과 실패 수를 정확히 보여준다
- 영상 일괄 생성 대상은 장면 순서를 지키고 이미 재생 가능하거나 생성 중인 장면은 제외한다
- 동시에 세 장면까지만 만들고 실패 수를 세어 진행 표시를 끝낸다

### tests/director/video-chain.test.ts (6)
> 앞 장면의 마지막 화면을 다음 영상의 시작으로 연결하고, 끊긴 연결은 남기지 않는다
- 완성되지 않은 이전 영상이면 연결선을 만들지 않는다
- 저장된 올바른 연결은 복원하고 순환 연결은 거부한다
- 마지막 화면을 읽을 수 없으면 임시 연결을 지운다
- 연결을 확정하기 전에 이전 영상의 마지막 화면을 저장한다
- 영상 연결을 요청하면 지정한 연결 방식으로 적용한다
- 이전 영상이 바뀌면 다음 영상의 오래된 시작 화면을 버린다

### tests/director/video-frame-from-db.test.ts (7)
> 승인된 스토리보드 화면은 영상에 우선 사용하고, 화면이 없으면 입력한 선택을 지킨다 (Director 배선 5, #ref-gate 2026-09-06)
- 그림 없이 요청해도 승인된 스토리보드가 있으면 시작과 끝 화면을 채워 영상으로 만든다
- 자동으로 화면을 고르면 오래된 그림 대신 승인된 스토리보드 화면을 쓴다
- 직접 연결한 그림은 그대로 쓰고 시작 그림이 없을 때만 승인된 시작 화면을 앞에 더한다
- 승인된 스토리보드가 없으면 독립 영상은 입력한 그림을 그대로 사용한다
- 연결 정보를 보내지 않은 예전 요청은 입력한 그림을 그대로 사용한다
- 영상 생성은 먼저 그림을 확인하고 연결한 그림인지 구분해 사용한다
- 그리드의 "영상 생성"은 새 그림을 만들기 전에 저장된 그림을 다시 확인해 승인한 그림을 지킨다

### tests/director/video-generation-api.test.ts (39)
> 영상 생성은 선택한 장면과 설정을 지키고, 실패한 요청은 안전하게 다시 이어간다
- 형식이 잘못된 대화 추적 번호는 요청 전에 거절한다
- 독립 영상은 저장된 설정을 사용하고 장면을 불러오지 않는다
- 대화하는 인물은 저장된 모습 그대로 사용한다
- 대화하는 인물의 모습이 없으면 비용이 드는 영상 작업을 시작하지 않는다
- 인물 모습 기록이 없으면 비용이 드는 영상 작업을 시작하지 않는다
- #ref-gate: 승인된 장면 그림이 없으면 영상 작업을 시작하지 않는다
- #ref-gate: 장면 그림이 만들어지는 중이면 아직 영상 작업을 시작하지 않는다
- #ref-gate 회귀: 완성된 장면 그림 형식이면 영상 작업을 예약한다
- #ref-gate: 그림 한 장만 저장된 예전 장면도 영상 작업에 사용한다
- 새 영상을 예약하면 선택한 제작 방식과 요청 내용을 저장한다
- 다시 만들기 요청은 이미 이어진 작업이면 새 제출 없이 결과를 돌려준다
- 다시 만들기 복구는 원래 영상에 속한 요청인지 확인한다
- 다른 영상의 같은 재요청 표시는 새 작업으로 처리한다
- 영상 제출이 실패하면 실패한 작업으로 기록한다
- 결과를 알 수 없는 제출은 대기 상태로 두고 자동 재제출하지 않는다
- 제출 결과를 확인하지 못한 기록 실패는 다시 시도할 수 있게 남긴다
- 같은 요청의 순서가 달라도 작업을 두 번 제출하지 않는다
- 제작 결과를 알 수 없는 재요청은 다시 제출하지 않는다
- 이전 형식의 새 영상 정보가 없으면 다시 제출하지 않는다
- 저장된 새 영상 정보가 다르면 재요청을 거절한다
- 실패 기록도 저장하지 못하면 다시 이어갈 정보를 돌려준다
- 새 예약은 처음 확정한 요청 내용으로 제출한다
- 다시 요청한 내용이 달라지면 제작 작업을 제출하지 않는다
- 끝난 작업은 예약된 자리만 남아 있어도 다시 제출하지 않는다
- 작업 연결에 실패하면 다시 이어갈 복구 증표를 돌려준다
- 작업 연결이 실패해도 서명된 복구 증표로 한 번만 제출하고 이어간다
- 새 영상 정보의 %s가 달라진 재요청은 작업을 제출하지 않는다 (표)
- 알 수 없는 작업 번호는 이미 진행 중인 요청과 충돌하므로 거절한다
- 영상 파일은 올라갔지만 완료 기록 저장이 실패하면 작업을 대기 상태로 둔다
- 변조된 복구 정보는 대기 중인 작업을 끝내지 않는다
- 허용되지 않은 영상 주소가 오면 작업을 실패로 끝내고 방치하지 않는다
- 로그인이나 작업이 %s이면 상태를 다시 확인하지 않고 결과를 돌려준다 (표)
- 다른 사람의 작업은 결과를 확인하기 전에 거절한다
- 작업이 %s 상태이면 그 상태를 결과로 보여준다 (표)
- 서버에서 작업 상태를 확인할 수 없으면 오류를 돌려준다
- 대기 중인 작업을 다시 확인해 %s 상태를 보여준다 (표)
- 조건에 맞는 복구 증표만 연결하고 작업을 다시 제출하지 않는다
- 유효하지 않은 %s 복구 증표는 연결하거나 제출하지 않는다 (표)
- 복구 증표의 %s가 다르면 연결하거나 제출하지 않는다 (표)

### tests/director/video-lifecycle.test.ts (38)
> 영상 결과 주소가 안전하고 재생 가능할 때만 보관하고 완료 처리한다
- 영상 결과를 저장하면 연결된 영상이 정확한 위치로 완료된다
- 새로운 FAL 주소라도 FAL 소속이면 영상 결과로 받아들인다 (#fal-cdn-host)
- FAL처럼 보이기만 하는 주소는 영상 결과로 받아들이지 않는다
- 같은 영상 저장 내용이 충돌하면 완료로 잘못 표시하지 않는다
- 손상된 영상 결과는 저장하지 않고 거절한다
- 영상 결과가 %s이면 저장하지 않고 거절한다 (표)
- 재생 가능한 MP4는 크기 정보가 없어도 저장한다
- 내용이 없는 MP4는 저장하지 않고 거절한다
- 영상 파일 형식이 깨졌으면 %s 경우에도 저장하지 않고 거절한다 (표)
- 영상 정보가 잘못되었으면 %s 경우에도 저장하지 않고 거절한다 (표)
- 영상에 기록된 장면 수가 비정상적으로 크면 저장하지 않는다
- 영상 구조가 지나치게 복잡하면 저장하지 않고 거절한다
- 영상 구간이 지나치게 많고 잘못되면 저장하지 않고 거절한다
- 영상 정보가 허용량을 넘으면 저장하지 않고 거절한다
- 허용된 내 컴퓨터와 FAL 주소의 영상은 저장한다
- 안전하지 않은 FAL 영상 주소 %s는 가져오지 않는다 (표)
- 허용한 내 컴퓨터 주소가 아니면 영상을 가져오지 않는다
- 영상 주소가 허용 범위를 벗어나도록 바뀌면 거절한다
- 허용된 주소 변경은 따라가고 중간 내용은 정리한다
- 주소 변경이 너무 많으면 마지막 내용을 정리하고 거절한다
- 영상 응답이 실패하면 내용을 정리하고 종료한다
- 영상 받기가 오래 걸리면 다시 시도할 수 있게 중단한다
- 영상 받기가 시작된 뒤 멈춰도 오래 기다리지 않고 다시 시도한다
- 영상 크기가 허용량을 넘으면 %s 경우에도 중단한다 (표)
- 저장 충돌과 명확한 서버 오류는 더 시도하지 않고 종료한다
- 일시적인 저장 오류 상태 %s=%s이면 다시 시도한다 (표)
- 원인을 알 수 없는 저장 실패는 다시 시도한다
- 연결된 영상 제공처가 실패하면 실패 상태로 기록한다
- 연결된 내 컴퓨터 영상은 Director 결과로 완료한다
- 연결되지 않은 내 컴퓨터 영상도 완료 결과로 기록한다
- 연결되지 않은 영상 주소가 잘못되면 실패로 기록한다
- 허용하지 않은 내 컴퓨터 영상 주소 %s는 실패로 기록한다 (표)
- 연결된 영상 제공처의 영구 오류는 실패로 기록한다
- 원인을 모르는 영상 제공처 오류는 대기 상태를 유지한다
- 일시적인 영상 제공처 오류는 대기 상태를 유지한다
- 연결된 영상 실패를 기록할 수 없으면 그 오류를 알린다
- 완료나 실패를 기록할 수 없으면 오류를 알린다
- 처리가 끝나지 않은 충돌과 이미 끝난 처리를 구분한다

### tests/director/video-models.test.ts (4)
> 영상 모델을 고르면 기본 모델과 재생 시간이 안내된 기준에 맞게 정해진다 (#owner-default 2026-08-31: Seedance 2.0)
- 기본 영상 모델은 Seedance다
- 영상 모델 목록의 첫 항목은 기본 모델이다
- 영상 모델 이름을 넣으면 지원 이름은 유지하고 예전 이름과 모르는 이름은 알맞게 바꾼다
- 영상 길이를 넣으면 모델이 허용하는 범위 안으로 맞추고 고정 길이 모델은 정해진 길이를 쓴다

### tests/director/video-prompt-dialogue.test.ts (16)
> 영상에 대사가 있으면 원문과 말하는 사람의 모습이 빠짐없이 반영된다 (#g7 2026-08-27 오너 확정)
- 대사를 입력하면 원문 그대로 영상에 반영한다 (번역·요약 금지)
- 대사가 있으면 입모양이 말과 맞게 움직인다
- 대사의 감정과 말투를 영상에 함께 반영한다
- 대사가 여러 줄이면 적은 순서대로 말한다
- 대사가 없거나 비어 있으면 영상에 대사를 넣지 않는다
- 대사에 맞춰 움직임도 말하는 시점에 맞춘다 (memo: 단어 시점 연기)
- 대사가 없는 장면에는 대사 안내가 들어가지 않는다
- 대사가 있으면 영상 안내와 세부 내용에 함께 반영된다
- 대사가 길어도 영상 안내에서 잘리지 않는다
- 움직임 안내가 대사보다 먼저 적용된다
- 말하는 사람을 등록하면 이름과 모습이 영상에 반영된다
- 사람의 모습 설명은 문장 단위로 정리해 너무 길지 않게 한다 (~120자)
- 여러 사람이 말하면 각 대사가 알맞은 사람에게 돌아간다
- 등록하지 않은 사람이 말하면 이름 없이 표시한다
- 말하는 사람이 없는 대사는 내레이션으로 처리하고 입모양을 움직이지 않는다
- 말하는 사람을 등록하면 영상 전체에 이름과 모습이 반영된다

### tests/director/video-retakes-db.integration.test.ts (7)
> 영상 다시 만들기 약속이 실제 저장 정보에도 빠짐없이 지켜진다
- 영상 다시 만들기 요청의 설정이 올바른 모양이 아니면 처음부터 거절한다
- 현재 영상은 가장 최근 시도의 결과를 보여준다
- 제출 결과를 합칠 때 기존 내용은 보존하고 요청이 바뀌면 반영하지 않는다
- 같은 다시 만들기 요청은 영상별로 한 번만 처리하고 새 영상 요청은 같은 결과를 돌려준다
- 서로 다른 장면에 같은 요청 이름을 동시에 쓰면 하나만 처리하고 충돌을 알린다
- 예전에 비어 있던 영상 정보도 새 기준에 맞게 정리한다
- 권한 없는 사용자는 다시 만들기를 실행할 수 없고 반복 요청과 완료 규칙을 지킨다

### tests/director/video-retakes.test.ts (11)
> 다시 만든 영상 중 조건에 맞는 결과를 고르고 실패와 완료를 빠짐없이 기록한다
- 사용 가능한 영상 중 최신 결과를 화면에 보여주고 최종 영상과 쓸 수 없는 결과는 건너뛴다
- 최종으로 지정한 영상이 성공했으면 넘겨주고 아니면 최신 성공 영상을 넘겨준다
- 같은 차례와 시간의 영상이 여러 개면 정해진 순서로 하나를 고른다
- 사용 가능한 성공 영상이 없으면 결과를 비워 둔다
- 영상 주소가 비어 있으면 쓸 수 없는 결과로 보고 상태와 상관없이 최신 시도를 고른다
- 영상이 많아도 최신 결과부터 같은 순서로 고른다
- 영상 주소나 보관 위치가 비어 있으면 완료 처리하지 않는다
- 예전 형식의 영상 요청 정보도 기존 내용 그대로 보존한다
- 요청 설정이 없을 때만 기본값을 쓰고 모양이 잘못되면 다시 만들기를 시작하지 않는다
- 완료 결과는 그대로 기록하고 너무 긴 실패 사유는 읽을 수 있게 줄여 기록한다
- 실패 사유가 비어 있으면 기록하지 않고 기록 중 오류는 알린다

### tests/director/video-takes-api.test.ts (34)
> 영상 카드와 그림을 올릴 때 권한을 확인하고, 잘못된 정보는 저장하지 않으며 안전한 주소로 보관한다
- 자연어로 적은 이름도 서로 다른 안전한 보관 주소로 바꾼다
- 로그인하지 않은 요청은 거절한다
- 새 영상을 만들면 장면 없이도 하나의 영상 카드로 저장한다
- 영상 카드 위치가 올바르지 않으면 저장하지 않는다
- 내 것이 아닌 프로젝트에는 변경을 허용하지 않는다
- 화면에서 바꿀 수 없는 항목을 보내면 거절한다
- 대표 영상 여부를 바꾸면 최신 영상 카드 정보를 돌려준다
- 허용된 영상 카드 정보는 값이 없어도 저장한다 (표)
- 대표 영상 지정과 다른 정보 변경을 함께 보내면 아무것도 저장하지 않는다
- %s이면 오류 상황에 맞는 안내를 돌려준다 (표)
- 삭제 중 %s이면 상황에 맞는 안내를 돌려준다 (표)
- 사용 중인 영상 카드를 삭제하면 목록에서 사라지게 한다
- 삭제 요청에 프로젝트 정보 외 내용이 있거나 정보가 없으면 거절한다 (표)
- 약속된 충돌 상황이면 변경을 막는다 (표)
- 비슷하지만 다른 오류는 충돌로 처리하지 않는다
- 최근 시도 정보가 있는 영상 카드는 그 상태를 보여주고 지난 기록은 불러오지 않는다
- 영상 카드의 %s 정보가 올바르지 않으면 저장하지 않는다 (표)
- %s이면 보관하기 전에 거절한다 (표)
- 프로젝트에 속하지 않은 대상은 보관하기 전에 거절한다
- 한글로 된 대상 이름도 프로젝트 소속을 확인한다
- Director 그림을 저장해도 원본 Character 정보는 덮어쓰지 않는다
- 보관 뒤 대상이 사라지면 성공으로 알리지 않는다
- 내용이 잘못된 그림은 거절하고 원인을 짧게 기록한다
- 영상 외 그림 보관 중 서버 오류 내용을 안전하게 안내한다
- 내 프로젝트가 아닌 영상 미리보기는 올리지 않는다
- %s이면 연결된 정보나 보관함을 건드리기 전에 거절한다 (표)
- 연결된 영상 작업이 끝나야 미리보기 그림을 올린다
- 이미 있는 미리보기 그림은 다시 올려도 같은 주소를 쓰고 영상 정보를 새로 고친다
- 연결용 장면 그림은 따로 보관하고 미리보기 정보는 바꾸지 않는다
- %s이면 보관하기 전에 거절한다 (표)
- %s이면 미리보기 그림을 인정하지 않는다 (표)
- 삭제된 영상은 보관하기 전에 걸러낸다
- 미리보기 그림을 다시 올리는 중 %s이면 주소를 공개하거나 영상 정보를 바꾸지 않는다 (표)
- 영상 정보가 동시에 바뀌면 충돌로 처리한다

### tests/director/video-webhook.test.ts (5)
> 영상 제작 결과 알림이 오면 확인된 결과만 반영하고, 저장 실패는 다시 처리할 수 있게 남긴다
- 파일을 올린 뒤 완료 정보 저장에 실패하면 다시 시도할 수 있게 남긴다
- 연결된 영상 결과 반영에 실패하면 처리를 끝내고 실패로 남긴다
- %s 결과와 내용이 맞지 않으면 실패로 끝내고 반영하지 않는다 (표)
- 알 수 없는 작업 종류는 반영하지 않고 거절한다
- 확인된 결과에 요청 이름이 없거나 올바르지 않으면 거절한다

### tests/director/wheel-notch.test.ts (5)
> Ctrl+휠 축척 스텝퍼(#a1 2026-07-15) — burst(연속 이벤트 묶음) = 1단계 계약 검증.
- 단발 노치(이벤트 1개) = 1단계, 방향은 deltaY 부호
- 스무스 스크롤이 노치 1칸을 여러 이벤트로 쪼개 보내도(간격<GAP) 1단계만
- 의도적으로 띄엄띄엄 굴리면(간격>GAP) 굴림마다 1단계
- 길게 이어지는 burst(프리스핀)는 REPEAT 간격마다만 추가 단계
- deltaY=0 이벤트는 무시한다

### tests/director/wiring-persistence.test.ts (9)
> 수동으로 연결한 카드가 다른 기기에서도 같은 대상을 가리키도록 저장하고 복원한다 (#wiring-persistence 2026-08-31)
- Writer에서 만든 장면과 영상은 같은 대상을 다시 찾는다
- 이름이 없는 수동 장면과 미완성 영상은 연결 대상으로 저장하지 않는다
- 구버전의 그림 연결도 부모 장면 카드로 되돌린다 (#node-merge 하위호환)
- 영상 연결 정보가 새 캔버스의 대응 카드로 복원된다
- 사라진 그림 연결은 버리고 같은 연결은 한 번만 남긴다
- 모양이 어긋난 연결은 버리고 유효한 대상만 남긴다
- 영상 연결 정보가 없거나 잘못되면 없는 것으로 처리한다
- 영상 연결이 비어 있으면 저장할 내용이 없다고 판단한다
- 완료된 영상의 이름을 연결 대상으로 사용한다

## editor — Editor: 클립·자막·내보내기·미디어 저장

### tests/editor/export-core.red-team.test.ts (17) — red-team 에도 속함
> 파일 이름과 내려받을 자료가 어떤 입력에도 안전하게 묶여, 빠진 자료를 알 수 있게 한다
- 이름이 비어 있거나 쓸 수 없으면 안전한 기본 이름을 쓴다
- 한글 이름은 글자 수를 기준으로 알맞은 길이로 줄인다
- 컴퓨터에서 특별한 이름으로 쓰이는 파일명도 안전하게 바꾼다
- 경로처럼 보이는 이름도 한 파일 이름 안에서 안전하게 바꾼다
- 조합된 글자도 올바르게 합친 뒤 이름을 정리한다
- 같은 이름의 자료가 여러 개면 차례대로 다른 이름을 붙인다
- 대소문자만 다른 이름도 같은 이름으로 보고 겹치지 않게 한다
- 서로 다른 위치에서는 같은 이름을 각각 그대로 쓸 수 있게 한다
- 같은 위치의 자료 이름은 종류가 달라도 서로 겹치지 않게 한다
- 자료를 받지 못하면 실패 목록에 기록하고 다른 자료는 계속 묶는다
- 자료를 내려받을 수 없으면 해당 자료를 넣지 않고 실패 목록에 기록한다
- 내보낼 자료가 없으면 빈 묶음과 0건 결과를 만든다
- 모든 자료를 받지 못해도 실패 목록을 담은 묶음을 내려받는다
- 같은 자료를 여러 곳에서 써도 한 번만 받고 모든 위치에 넣는다
- 글 내용이 없으면 실패로 알리고 빈 글은 그대로 담는다
- 줄바꿈과 제목·인용 표시가 글 형식을 깨뜨리지 않게 바꾼다
- 자료 형식과 주소가 달라도 안전한 파일 확장자를 정한다

### tests/editor/export-g003.red-team.test.ts (8) — red-team 에도 속함
> 작품 자료를 모아 내보낼 때 이름과 설명은 읽기 쉽고, 준비되지 않은 자료는 빠짐없이 알려 준다
- 같은 이름의 인물이 여러 명이어도 각각 보존하고, 자료가 없어도 목록에 남기며, 이상한 이름은 안전하게 바꾼다
- 한국어 설명을 우선 보여 주고 낯선 표시가 드러나지 않게 읽기 쉽게 정리한다
- 잘못된 자료가 섞여도 오류 없이 같은 결과를 만들고 원본 자료는 바꾸지 않는다
- 감독 자료를 불러오면 한국어 장면 설명과 분위기를 함께 확인한다
- 스토리보드 이미지가 완료된 경우에만 넣고 빠진 이유는 목록에 알린다
- 최종본이 있으면 최신 작업보다 먼저 쓰고, 없으면 최근 완료본을 쓰며, 그것도 없을 때 기존 영상 주소를 사용한다
- 한국어 설명을 우선 보여 주고 특수 표시와 자료 구조가 글을 깨뜨리지 않게 정리한다
- 잘못된 감독 자료가 섞여도 오류 없이 같은 결과를 만들고 원본 자료는 바꾸지 않는다

### tests/editor/export-manifest.test.ts (6)
> 프로젝트를 내보내면 네 단계 상태와 자료 수를 한눈에 확인하고 안전한 이름으로 내려받는다
- 네 단계를 내보내면 안내 문서에 각 단계 상태와 자료 수를 적는다
- 자료가 없는 단계는 비어 있다고 알리고 빈 폴더는 만들지 않는다
- 한 단계에 문제가 생겨도 오류를 알리고 나머지 단계는 계속 내보낸다
- 오류 내용에 줄바꿈이나 세로줄이 있어도 안내 문서 표가 흐트러지지 않는다
- 화면을 열지 않아도 저장된 Producer 자료를 내보내고 단계 수를 맞춘다
- 프로젝트 이름에 기호나 경로 표시가 있어도 프로젝트와 단계의 내려받기 이름을 안전하게 만든다

### tests/editor/export-md-artist.test.ts (4)
> 아티스트 자료를 내보내면 이미지와 설명이 읽기 쉬운 목록으로 정리된다
- 있는 아티스트 자료는 안전한 파일 이름으로 정리하고 같은 이름도 각각 보존한다
- 아티스트 자료 목록은 한국어 설명을 우선 보여 주고 이름이 바뀐 이유도 알린다
- 아티스트 자료가 없으면 한국어로 비어 있음을 알린다
- 자료를 모아도 원본을 바꾸지 않고 다른 저장 화면 없이 같은 결과를 만든다

### tests/editor/export-md-director.test.ts (5)
> 감독 자료를 내보내면 장면과 촬영 자료가 최신 완료본을 기준으로 읽기 쉽게 정리된다
- 완료된 스토리보드 이미지만 넣고 주소가 없는 자료는 넣지 않는다
- 사용할 수 있는 최종본을 우선하고 없으면 최근 완료본을 쓰며, 연결된 영상이 없을 때만 기존 주소를 쓴다
- 장면 목록은 한국어 설명을 우선 보여 주고 낯선 기호가 드러나지 않게 읽기 쉽게 정리한다
- 최종본과 최근 완료본을 고르고 실패·삭제·준비 중이거나 주소가 비어 있는 자료는 제외한다
- 시간이 같은 자료도 고유한 이름으로 순서를 정해 항상 같은 결과를 고른다

### tests/editor/export-md-producer.test.ts (5)
> Producer 자료를 내보내면 이야기와 인물·배경 설정을 읽기 쉬운 문서로 확인한다
- 내용이 있는 Producer 자료는 이야기·설정·인물·배경 문서로 정리한다
- 이야기·인물·배경이 비어 있으면 한국어 안내를 적는다
- 영어 화면에서는 영어 안내를 쓰고 정하지 않은 값은 미설정으로 표시한다
- 현재 배경 자료에는 배경 이미지를 따로 만들지 않는다
- 저장된 이야기·인물·장소를 합쳐 Producer 자료로 불러온다

### tests/editor/export-md-writer.test.ts (2)
> Writer 자료를 내보내면 이야기와 장면·촬영 계획이 읽기 쉬운 문서로 정리된다
- Writer 자료를 네 문서로 정리하고 한국어 설명과 영어 제작 문구를 알맞게 보여 준다
- 일부 내용이 준비되지 않아도 미완료 안내를 표시하고 저장된 장면과 촬영 자료를 함께 보여 준다

### tests/editor/export-menu.test.ts (5)
> 내보내기 전에 예상 용량과 대상 단계를 알려 주고, 큰 작업은 확인을 거친다
- 현재 화면의 단계를 내보낼 단계로 올바르게 연결한다
- 자료 종류별 개수로 예상 용량을 계산하고 잘못된 개수는 세지 않는다
- 예상 용량이 큰 작업일 때만 한 번 더 확인한다
- 저장된 자료를 확인할 수 없으면 예상치를 알 수 없음으로 표시해 안전하게 확인을 요구한다
- 기본 인물의 인물표와 초상화만 세고 예전 보기 자료는 세지 않는다

### tests/editor/export-sanitize.test.ts (11)
> 파일 이름에 위험한 문자가 들어와도 누구나 쓸 수 있는 안전한 이름으로 바꾼다
- 공백과 글자 조합이 섞인 이름을 넣으면 한 줄의 이름으로 정리한다 (RT-01)
- 파일 이름에 금지된 기호가 섞여도 안전하게 바꾸고 다른 글자는 보존한다 (RT-02)
- 한국어와 여러 언어의 글자·숫자가 든 이름을 넣으면 그대로 파일 이름으로 쓴다 (RT-03)
- 윈도우에서 사용할 수 없는 예약 이름을 넣으면 안전한 이름으로 바꾼다 (RT-04)
- 이름 앞뒤에 점이나 빈칸이 있으면 안전한 이름으로 다듬는다 (RT-05)
- 80글자를 넘는 이름을 넣으면 80글자 안으로 줄인다 (RT-06)
- 80글자에서 잘린 끝에 점이나 줄표가 남으면 없앤다 (RT-06b)
- 쓸 수 있는 글자가 하나도 없으면 untitled로 표시한다 (RT-07)
- 한 폴더에 같은 파일을 여러 번 만들면 이름 뒤에 번호를 붙여 구분한다 (RT-08)
- 서로 다른 폴더의 같은 파일 이름은 각각 그대로 쓴다 (RT-09)
- 대소문자만 다른 같은 이름이 겹치면 요청한 표기를 살려 번호를 붙인다 (RT-10)

### tests/editor/export-zip.test.ts (5)
> 글과 미디어를 묶어 내려받을 때 모든 파일을 빠짐없이 담고 실패 내역을 남긴다
- 응답에 파일 형식이 있으면 우선 쓰고 없으면 주소에서 안전한 형식을 찾는다
- 글과 미디어를 묶어 내려받으면 각 파일이 제자리에 담긴다
- 미디어를 받을 수 없으면 _failed.txt에 이유를 남기고 빈 파일은 만들지 않는다
- 같은 미디어 주소를 여러 곳에서 써도 한 번만 받아 모든 곳에 같은 파일을 쓴다
- 글 내용이 없으면 _failed.txt에 이유를 남기고 빈 글은 그대로 담는다

### tests/editor/media-cache-control.test.ts (5)
> 미디어를 올릴 때 정해진 보관 기간과 파일 형식·덮어쓰기 설정을 그대로 적용한다
- 보관 기간 값은 초 단위 숫자로 시작한다
- '%s' 를 포함하지 않는다 — Supabase 가 접두사를 이미 붙인다 (표)
- 보관 기간을 정하지 않으면 정해진 기본 기간을 적용한다
- 원하는 보관 기간을 정하면 그 기간을 적용한다
- 보관 기간을 정해도 파일 형식과 덮어쓰기 설정을 함께 적용한다

### tests/editor/media-migration-plan.test.ts (13)
> 화면에서 쓰이는 파일은 옮기고, 임시 파일과 연결 없는 파일은 이유를 남겨 구분한다
- 작업공간과 프로젝트가 함께 적힌 경로에서 프로젝트를 알아낸다
- 공용 자산 경로에는 프로젝트를 찾지 않는다
- 화면에 쓰이는 결과물은 옮긴다
- 작은 그림도 같이 옮긴다 — 다시 만들려면 원본과 변환 도구가 필요하다
- 공용 자산은 프로젝트와 무관하게 옮긴다
- 생성용 임시 시트는 옮기지 않는다
- 임시로 만든 자료라면 프로젝트가 살아 있어도 옮기지 않는다
- 연결된 프로젝트가 없으면 옮기지 않는다
- 시험·샘플 프로젝트도 옮긴다 — 버릴지는 이전 후에 정한다
- 올린 파일과 그 조각도 옮긴다
- 경로 앞에 슬래시가 붙어도 같은 결과를 낸다
- 빈 경로는 사람이 본다
- 판정마다 사람이 읽을 근거가 붙는다

### tests/editor/media-upload-retry.test.ts (3)
> 파일을 올릴 때 일시적인 서버 오류는 다시 시도하고, 계속 실패하면 오류를 알린다 (2026-09-03)
- 일시적인 서버 오류 뒤 성공하면 파일을 올리고 이후에는 기존 파일도 덮어쓴다
- 파일이 이미 있거나 권한이 없으면 다시 시도하지 않는다
- 서버 오류가 계속되면 세 번 시도한 뒤 오류를 알린다

### tests/editor/media-url-env.test.ts (3)
> 주소 끝에 붙은 공백이나 줄바꿈이 있어도 내 파일 주소를 올바르게 알아본다 (#env-newline, 2026-08-24)
- 주소 설정에 줄바꿈이나 공백이 붙어도 정상 주소로 만들고 내 파일로 알아본다
- 다른 주소를 지정해도 끝의 공백·줄바꿈·슬래시를 안전하게 정리한다
- 저장된 주소에 줄바꿈이 끼어 있어도 내 파일로 알아본다

### tests/editor/media-url.test.ts (14)
> 미디어 주소를 만들고 되짚을 때 우리 파일만 알아보며, 다른 주소나 잘못된 입력은 막는다
- 파일 위치를 넣으면 어디서나 열 수 있는 주소가 된다
- 파일 위치 앞에 슬래시가 있어도 주소가 하나로 이어진다
- 다른 주소를 지정하면 파일 위치는 그대로 두고 앞부분만 바뀐다
- 만든 주소를 다시 넣으면 원래 파일 위치를 찾는다 (왕복)
- 주소 뒤에 붙은 임시 표시는 무시하고 원래 파일 위치를 찾는다
- 주소 형식이 바뀌어도 예전 주소와 새 주소를 모두 알아본다
- 우리 파일 주소가 아니면 사용하지 않는다
- 다른 보관 공간의 주소는 우리 파일로 보지 않는다
- 파일 위치가 비어 있으면 사용하지 않는다
- 우리 파일 주소만 사용한다
- 주소가 글자로 적히지 않았으면 사용하지 않는다
- 주소가 지나치게 길면 사용하지 않는다
- 파일 위치를 속여 다른 곳으로 빠져나가려 하면 사용하지 않는다
- 내부 전용 주소를 우리 파일 주소처럼 꾸며도 사용하지 않는다

### tests/editor/media-word-scrub.test.ts (8)
> 기준 이미지가 있으면 매체를 가리키는 표현을 덜어내고 나머지 설명은 지킨다 (#F-004 B4/B5, 2026-08-12)
- 매체를 설명하는 말이 섞인 표현 묶음은 통째로 빼낸다 (2026-07-14)
- 매체를 설명하지 않는 표현은 그대로 남긴다
- 영어나 한국어로 된 매체 표현을 빼도 문장 나머지는 그대로 둔다
- 줄을 나눈 모양은 설명의 구조이므로 그대로 둔다
- 매체를 설명하는 말이 없으면 원문을 그대로 둔다
- 매체 표현이 다른 말 속에 들어 있어도 찾아내고 비슷한 말은 구별한다
- 기준 이미지가 있으면 설명에서 매체 표현을 빼낸다
- 기준 이미지가 없으면 설명을 바꾸지 않는다

### tests/editor/render-loop-guard.test.ts (3)
> 편집 화면을 열어도 같은 준비 작업이 되풀이되지 않는다 (#editor-render-loop 2026-08-24)
- 언어 설정이 그대로이면 화면 안내를 매번 새로 준비하지 않는다
- 화면 안내가 바뀌어야 할 때만 화면 준비 작업을 다시 진행한다
- 편집 화면을 열면 저장된 내용이 반복 호출 없이 안정적으로 준비된다

### tests/editor/route-writer-export.test.ts (6)
> 로그인한 소유자에게만 완성된 이야기 내용을 보여주고, 준비된 내용이 없으면 비워 둔다
- 로그인하지 않은 요청이면 접근을 거절한다
- 로그인했어도 다른 사람의 프로젝트면 보여주지 않는다
- 완성된 이야기 내용이 있으면 소유자에게 단계별 결과를 보여준다
- 새 작업이 실패했으면 이전에 완성된 이야기를 보여준다
- 최근 작업이 모두 실패했으면 더 이전의 완성된 이야기를 찾아 보여준다
- 아직 작업이 없으면 이야기 내용을 비워서 보여준다

### tests/editor/server-save.test.ts (3)
> 저장할 수 없는 오류는 곧바로 멈추고, 잠시 뒤 해결될 오류만 다시 시도한다 (#editor-save-500 2026-08-07)
- 삭제된 프로젝트에 저장하려 하면 한 번만 시도하고 바로 멈춘다
- 잠시 장애가 생기면 몇 차례 다시 시도하고 다음 저장도 이어간다
- 저장에 성공하면 같은 내용을 다시 보내지 않는다

### tests/editor/storage-immutable-object.test.ts (5)
> 같은 파일을 다시 올릴 때 내용과 형식이 같으면 안전하게 이어가고, 다르면 덮어쓰지 않는다
- 같은 파일을 다시 올릴 때 내용과 파일 정보가 모두 같으면 그대로 이어간다
- 같은 파일을 다시 올릴 때 내용과 파일 정보가 맞으면 그대로 이어간다 (%s) (표)
- 같은 이름이라도 내용이 다르면 기존 파일을 대신 쓰지 않는다
- 파일 정보가 다르거나 가져오기에 실패하면 다시 올리지 않는다 (%s) (표)
- 충돌이 아닌 업로드 실패는 원래 이유를 그대로 알린다

### tests/editor/subtitles.test.ts (9)
> 약속 K — 클립마다 자막 한 덩어리를 쓰고 옮길 수 있고 내보낸 영상에도 박힌다 (_tdd.md K, 2026-09-04)
- 클립마다 자막 한 덩어리를 쓸 수 있다
- 자막은 그 클립이 재생되는 동안만 보인다
- Writer가 쓴 대사가 있는 클립은 그 대사가 자막 초기값으로 들어 있다
- 미리보기에서 자막을 누르면 바로 고칠 수 있다
- 자막을 마우스로 끌거나 방향키로 옮길 수 있고, 옮긴 위치가 저장된다
- 자막 내용과 위치가 내보낸 영상에 그대로 반영된다
- 자막을 지우면 아무것도 보이지 않는다
- 자막은 새로고침해도 남는다
- 처음 위치는 아래 가운데다

### tests/editor/title-card.test.ts (9)
> 약속 J — 검은 화면 클립은 다른 클립처럼 다뤄지고 내보내기와 미리보기가 같다 (_tdd.md J, 2026-09-04 오너 확정)
- 검은 화면 클립은 다른 영상 클립과 똑같이 자르고 늘이고 옮길 수 있다
- 가운데서 자르면 두 조각 모두 검은 화면 클립으로 남고 새로고침해도 사라지지 않는다
- 5초보다 길게 늘릴 수 있고, 늘린 만큼 미리보기와 내보낸 영상의 재생 시간이 길어진다
- 길이는 손잡이와 초 단위 숫자 입력으로 바꾼다
- 기본 길이는 5초다
- 이미지를 Artist·Director에서 만든 것 중 고르거나 내 컴퓨터 파일을 올려 넣으면 미리보기와 내보낸 영상 둘 다에 보인다
- 글자를 비우면 내보낸 영상에 아무 글자도 찍히지 않는다
- 내보낸 영상의 글자 줄바꿈이 미리보기와 같다
- 이미지와 글자는 자유 배치이고 레이어 순서는 우클릭 메뉴로 정한다

### tests/editor/trim-cut-persistence.test.ts (6)
> 다른 화면을 다녀와도 잘라 낸 영상 구간과 최신 영상이 그대로 유지된다 (#a3-state-loss 2026-08-26)
- 잘라 낸 조각과 끌어 놓아 만든 복사본을 구분한다
- 원본 영상이 남아 있으면 잘라 낸 조각에 최신 영상을 이어 붙인다
- 원본 영상이 삭제됐으면 잘라 낸 조각을 되살리지 않는다
- 영상 구간을 바꾸면 화면에 먼저 반영하고 잠시 뒤 저장한다
- 잘라 낸 조각만 바꾸면 원본 영상의 구간은 저장하지 않는다
- 끝이 시작과 같거나 앞선 구간은 저장하지 않는다

### tests/editor/upload-ingest.test.ts (19)
> 올린 글과 그림은 읽기 쉬운 형식과 크기로 처리하고, 안전하지 않은 첨부는 이유를 알려주며 받지 않는다
- 지원하는 파일 형식이면 알맞은 종류로 알아본다
- SVG·HWP·PDF 파일은 이유를 알려주고 받지 않는다
- 파일이 너무 크면 이유를 알려주고 받지 않는다
- 허용 크기 안의 글은 그대로 보존한다
- 긴 글도 한글이 깨지지 않게 허용 범위에 맞춘다
- 우리 서비스에서 올린 주소만 첨부로 허용한다
- 허용하지 않은 첨부 주소 수를 알려준다
- 주소 목록이 아니면 첨부할 주소가 없다고 처리한다
- 파일 형식 표기와 실제 내용이 다르면 이유를 알려주고 받지 않는다
- 지원하지 않는 파일 형식이면 가능한 형식을 알려주고 받지 않는다
- 세로로 긴 스크롤 웹툰도 끝까지 받아준다 (2026-08-13 회귀)
- 세로로 너무 긴 그림은 무엇이 문제인지 알려준다
- 읽기 좋은 크기의 그림은 그대로 둔다
- 세로로 긴 웹툰은 읽기 좋은 여러 장으로 나누고 크기를 넘기지 않는다
- 너무 넓은 그림은 읽기 좋은 크기로 줄여 나눈다
- 끝에 지나치게 얇은 조각을 남기지 않는다
- 긴 스크롤 웹툰도 한 화 전체를 빠짐없이 나눈다 (2026-08-13 회귀)
- 흑백이나 투명한 그림도 색이 깨지지 않게 처리한다
- 받은 그림을 바로 볼 수 있는 JPEG로 만든다

### tests/editor/video-handoff.test.ts (14)
> 다른 프로젝트와 늦게 도착한 결과가 섞여도 현재 프로젝트의 최신 영상을 안전하게 보여준다
- Final 영상의 주소와 미리보기, 완료 상태를 더 최신 결과보다 우선해 보여준다
- 나중 작업이 실패해도 가장 최근에 성공한 영상을 유지한다
- 현재 영상 정보를 쓸 수 없으면 예전 영상을 되살리지 않는다
- 현재 영상 정보가 하나도 없을 때만 예전 영상을 사용한다
- 새 프로젝트에 영상이 없거나 불러오기에 실패하면 이전 프로젝트 영상을 남기지 않는다
- 초기화한 뒤 늦게 도착한 결과가 있어도 현재 프로젝트를 바꾸지 않는다
- 초기화한 뒤 늦게 도착한 저장 내용이 있어도 현재 프로젝트를 바꾸지 않는다
- 새 프로젝트를 불러온 뒤 이전 프로젝트 결과가 늦게 와도 새 프로젝트 내용을 지킨다
- 새 프로젝트를 불러온 뒤 이전 프로젝트 불러오기가 늦게 실패해도 새 프로젝트 내용을 지킨다
- 같은 프로젝트를 여러 번 불러오면 가장 최근 결과를 지킨다
- 같은 프로젝트의 이전 불러오기가 늦게 실패해도 가장 최근 결과를 지킨다
- 새로 불러오기에 성공하면 이전 오류를 지운다
- 저장 중 새 내용이 여러 번 바뀌면 최신 내용만 이어서 저장한다
- 오래된 저장 실패는 다시 보내지 않고 최신 내용만 다시 저장한다

## chat — 채팅 공통: 블록·선택지·승인 카드·추적

### tests/chat/agentic-reply-guard.test.ts (12)
> 답변이 중간에 끊겨도 내부 자료가 보이지 않고 안내만 보여 준다 (임시 조치 2026-07-15)
- 일반 답변에 표시용 기호가 없으면 그대로 보여 준다
- 답변이 중간에 끊겨 내부 자료가 보이면 내용을 숨기고 다시 요청하라고 알린다
- 답변 없이 내부 자료가 시작되면 내용을 숨기고 다시 요청하라고 알린다
- 일반 답변은 오류로 보지 않고 대화 내용 그대로 보여 준다
- 변경 내용이 포함된 답변은 설명만 보여 주고 변경 사항을 반영한다
- 중간에 끊긴 답변은 온전한 변경만 반영하고 이어서 요청하라고 알린다 (종전: 전부 폐기)
- 내용을 읽을 수 없으면 내부 자료를 숨기고 반영하지 못했다고 알린다
- 변경 안내 뒤에 설명이 더 있어도 변경 내용을 반영한다
- 마지막 변경 내용이 중간에 끊기면 반영하지 않는다
- 일부만 반영하면 반영한 수와 이어서 요청할 지점을 알려 준다
- 반영된 변경 수만 알리고 전체 요청 수를 부풀리지 않는다
- 모든 변경을 반영한 답변에는 추가 안내를 붙이지 않는다

### tests/chat/chat-blocks.test.ts (19)
> 사용자와 답변을 올바른 말풍선과 단계 안내로 보여 주고 첨부 글을 그대로 보존한다 (#oiioii-chat)
- 사용자가 보낸 말은 사용자 말풍선으로 보여 준다
- 완료(✓)나 실패(⚠) 알림은 상태 안내로 보여 준다
- 일반 답변은 본문 한가운데 ✓가 있어도 일반 말풍선으로 보여 준다
- 연이어 온 두 답변에는 첫 답변에만 이름표를 붙인다
- 사용자가 다시 말하면 다음 답변에 새 이름표를 붙인다
- 완료 안내 다음 답변에 새 이름표를 붙인다
- 답변 뒤의 완료 안내 다음에는 이름표를 다시 붙이지 않는다
- 답변으로 시작하는 구간도 첫 답변에 이름표를 붙인다
- 대화가 없으면 빈 화면을 보여 준다
- 단계 넘김 표시를 만들었다가 읽어도 보낸 단계가 그대로 남는다
- 잘못된 단계 넘김 표시는 일반 글로 보여 준다
- 단계 넘김 표시는 답변 묶음을 열지 않고 다음 답변에 이름표를 붙인다
- 첨부를 붙였다 떼어도 본문이 그대로 보인다
- 첨부 없이 보내면 글을 그대로 보여 준다
- 글 없이 첨부만 보내도 첨부를 보여 준다
- 여러 줄 글에 첨부해도 전체 글을 그대로 보여 준다
- 사용자가 글에 직접 입력한 📎는 첨부로 잘못 인식하지 않는다
- 인터넷 주소가 아닌 글이 섞인 첨부 안내는 첨부로 처리하지 않는다
- 첨부가 붙은 사용자 글도 사용자 말풍선으로 보여 준다

### tests/chat/chat-choices.test.ts (5)
> 채팅 선택지 파서(#p4-choices 2026-08-06) 회귀 — [CHOICES] a | b | c 추출·제거.
- 선택지를 추출하고 본문에서 그 줄을 제거한다
- 마커가 없으면 원문 그대로, 선택지 빈 배열
- 공백으로 둘러싸인 슬래시도 선택지 구분자로 읽는다
- 후보가 2개 미만이면 무시한다 (버튼 1개는 선택지가 아님)
- 후보는 최대 4개로 자른다

### tests/chat/chat-persistence-drafts.test.ts (2)
> 새로고침해도 선택지 이름은 다시 보여 주고 이미 지난 선택을 실행하지 않는다 (오너 결정 2026-08-18)
- 새로고침하면 선택지 이름만 다시 보여 주고 바로 실행되지는 않게 한다
- 읽을 수 없는 선택 안내는 화면에 보이지 않게 버린다

### tests/chat/chat-sections.test.ts (7)
> 채팅 stage 구간 분할 (#chat-continuity 2026-07-31) — 채팅방은 프로젝트당 하나이고
- 연속된 같은 stage 메시지는 한 구간으로 묶는다
- 되돌아온 stage 는 새 구간이다 — 시간순이 진실
- 발화 없는 stage 로 이동하면 빈 현재 구간을 끝에 만든다
- 마지막 구간이 현재 stage 면 빈 구간을 덧붙이지 않는다
- current 는 마지막 구간에만 붙는다 (같은 stage 가 앞에 또 있어도)
- 메시지가 하나도 없어도 현재 stage 구간 하나를 돌려준다
- 입력 배열을 변형하지 않는다

### tests/chat/chat-trace-api.test.ts (3)
> 대화 기록은 로그인한 사용자의 프로젝트에서만 안전하게 저장하고 다시 읽는다.
- 로그인하지 않았거나 다른 사람 프로젝트를 요청하면 접근을 막는다
- 허용된 대화 기록만 저장하고 나머지 요청 내용은 무시한다
- 대화 기록을 읽고 허용된 변경만 저장해 돌려준다

### tests/chat/chat-trace.test.ts (2)
> 대화 요청의 길이와 사용량은 기록하되 실제 대화 내용은 남기지 않는다.
- 대화 요청을 기록할 때 본문은 저장하지 않고 길이와 사용량만 남긴다
- 사용량 정보가 없으면 기본값으로 안전하게 기록하고 대기 중 제안은 비워 둔다

### tests/chat/inline-markdown.test.ts (16)
> 채팅 글의 꾸밈 표시는 읽기 쉽게 보여 주고 입력한 태그는 실행하지 않는다.
- 굵은 글씨 표시를 넣으면 글자만 보여 준다
- 기울임 표시를 넣으면 기울인 글씨로 보여 준다
- 밑줄 두 개로 감싼 글자는 밑줄 없이 보여 준다 (오너 실측: 밑줄 노출)
- 굵은 글씨 표시를 섞어도 표시 기호 없이 글자만 보여 준다
- 문장 앞의 제목 표시는 없애고 제목 글자는 남긴다
- 문장 중간의 #은 해시태그 글자로 그대로 둔다
- 코드 표시로 감싼 내용은 코드처럼 보여 준다
- 여러 굵은 글씨 표시를 모두 없애고 글자만 보여 준다
- 태그를 입력해도 화면에서 실행되지 않고 글자로 보여 준다
- 위험한 태그를 굵게 표시해도 실행되지 않고 안전한 글자로 보여 준다
- 특수 기호와 따옴표를 넣어도 안전한 글자로 보여 준다
- 일반 문장은 바꾸지 않고 그대로 보여 준다
- 문장 첫머리나 띄어쓰기 뒤 @이름을 넣으면 하늘색으로 보여 준다
- @이름 뒤 구두점은 이름에 포함하지 않는다
- 이메일 주소의 @는 이름 색으로 바꾸지 않는다
- @이름 뒤에 태그를 넣어도 안전하게 글자로 보여 준다

### tests/chat/produce-chat-locale-follow.test.ts (6)
> 사용자가 쓰는 언어를 따라 프로젝트와 답변 언어를 안전하게 맞춘다 (#chat-locale-follow 2026-08-31)
- 한국어로 말하면 프로젝트 언어를 한국어로 바꾸고 바로 한국어로 답한다
- 영어로 말해도 이미 정한 한국어 응답을 영어로 바꾸지 않는다 (비대칭)
- 영어로 말한 프로젝트는 한국어로 바꾸지 않고 영어로 답한다
- 이미 작성된 내용이 있는 프로젝트는 한국어로 말해도 응답 언어를 바꾸지 않는다
- 언어 저장에 실패하면 바뀐 언어로 답하지 않는다
- 내 프로젝트가 아니면 언어를 확인하거나 바꾸지 않는다

## job — 생성 작업: 큐·상태·실패 원인·fal 연결

### tests/job/classify-fal-failure.red-team.test.ts (5) — red-team 에도 속함
> 생성 서비스의 실패 사유를 헷갈리지 않게 구분해 알맞게 안내한다
- 차단을 뜻하는 단어가 단독으로 있을 때만 콘텐츠 정책 문제로 분류한다
- 콘텐츠 정책의 표기 방식과 대소문자가 달라도 같은 문제로 분류한다
- 정책 위반이나 허용되지 않음을 나타내면 콘텐츠 정책 문제로 분류한다
- 네트워크나 서버 같은 일반 오류는 콘텐츠 정책 문제로 잘못 보지 않는다
- 오류 내용이 비어 있거나 특수 문자와 긴 문장을 포함해도 일관되게 분류한다

### tests/job/classify-fal-failure.test.ts (4)
> 생성 서비스의 실패 사유를 구분해 알맞은 재시도 방법을 선택한다
- 콘텐츠 정책 위반이나 차단 사유는 일반 오류와 다르게 구분한다
- 일반적인 네트워크와 서버 문제는 콘텐츠 정책 문제로 잘못 보지 않는다
- 실패 사유가 없거나 비어 있어도 일반 오류로 구분한다
- 차단과 콘텐츠 정책 표현의 대소문자가 달라도 같은 문제로 구분한다

### tests/job/fal-keys.test.ts (11)
> 여러 생성 요청이 몰려도 사용 가능한 연결로 나누어 처리하고, 잘못된 설정은 알려준다 (#fal-key-pool)
- 설정값이 없으면 오류를 알려준다
- 설정 형식을 읽을 수 없으면 오류를 알려준다
- 사용할 연결 목록이 비어 있으면 오류를 알려준다
- 연결 이름이 중복되면 오류를 알려준다
- 설정 오류는 실제 사용 시점에만 알려준다
- 처리 여유가 가장 큰 연결로 새 요청을 보낸다
- 여유가 같으면 먼저 등록된 연결을 선택한다
- 모든 연결이 한도에 닿아도 가장 덜 바쁜 연결을 고른다
- 등록되지 않은 연결 이름을 찾으면 없다고 알려준다
- 등록된 연결 이름을 찾으면 해당 연결을 돌려준다
- 등록된 모든 연결의 처리 한도를 합산한다

### tests/job/fal-observability-capture.test.ts (2)
> 생성 요청에 사용한 내용과 빠진 항목을 사람이 확인할 수 있게 기록한다
- 지원하지 않는 요청 항목은 빠졌다고 알려준다
- 생성 서비스에 보낸 요청 내용을 그대로 기록한다

### tests/job/fal-reconcile-duplicate-finalize.test.ts (3)
> 같은 생성 요청이 겹쳐도 저장된 결과를 잃지 않고 성공 여부를 정확히 보여준다 (#dup-finalize-null-url 2026-07-31)
- 같은 생성이 겹쳐도 저장된 결과 주소를 되돌려준다
- 먼저 끝난 생성이 실패하면 그 실패를 그대로 알려준다
- 겹치는 요청이 없으면 생성 결과 주소를 그대로 사용한다

### tests/job/finalize-frame-thumbs.test.ts (2)
> 여러 장면을 저장해도 각 장면의 미리보기를 빠짐없이 바로 보여준다 (#thumb-pipeline)
- 각 장면의 시작·진행·끝 세 컷을 저장하면 같은 그림으로 미리보기도 모두 만든다
- 실사 그리드도 시작·진행·끝 세 컷 모두 미리보기를 만든다

### tests/job/generation-failure-evidence.test.ts (7)
> 생성 실패 이유를 사람이 알아볼 수 있게 보여주고, 끝난 작업을 빠짐없이 알려준다 (#a2-observability 2026-08-26)
- 실패 이유가 없다는 표시만 있으면 작업 정보를 덧붙여 알아볼 수 있게 한다
- 의미 없는 실패 표시는 상세 내용 없음으로 바꾼다
- 실제 실패 내용은 원문 그대로 남긴다
- 오류 이름과 상태 번호와 원인을 함께 알려준다
- 오류가 아닌 값도 사람이 읽을 수 있는 내용으로 보여준다
- 진행 중 목록에서 사라진 작업을 완료된 작업으로 알려준다
- 새 작업만 추가되면 끝난 작업이 없다고 알려준다

### tests/job/generation-failure.test.ts (10)
> 생성에 실패해도 무엇이 문제인지와 다음에 할 일을 사용자에게 알려준다
- 참고 이미지를 가져오지 못하면 다시 준비하라고 안내한다 (실제 원문)
- 참고 이미지 주소가 만료됐다는 내용도 같은 문제로 설명한다
- 콘텐츠 정책에 걸리면 완화하거나 안전한 방식으로 다시 시도하라고 안내한다
- 요청 한도를 넘으면 잠시 기다리라고 한다
- 접근 권한 문제는 관리자가 해결해야 한다고 알려준다
- 모르는 오류는 지어내지 않고 원문을 짧게 보여준다
- 복잡한 오류 내용에서는 핵심 안내만 뽑아 짧게 보여준다
- 실패 내용이 없어도 안전하게 설명한다
- 실패를 알리는 문장은 경고 표시로 시작한다
- 자동 재시도를 포기하면 사용자가 다시 생성을 눌러야 한다고 알려준다

### tests/job/generation-ghost-reconcile.test.ts (8)
> 오래 멈춘 작업을 다시 확인해 끝난 작업은 회수하고, 영구 실패는 실패로 알린다 (#ghost-reconcile 2026-08-17, S2·S3·S5·M5·M11)
- 끝난 유령 작업을 회수하고, 다시 확인하는 조건이 약속과 같다 (S5)
- 목록에서 찾은 뒤 이미 끝난 작업은 다시 확인해 건너뛴다 (M11)
- 아직 처리 중인 작업은 건드리지 않는다
- 영구적으로 확인할 수 없는 유령 작업은 실패로 끝낸다 (M5)
- 인증 정보 문제면 작업을 없애지 않고 대기 상태로 남긴다 (S2)
- 같은 프로젝트를 짧은 시간 안에 다시 요청해도 한 번만 확인한다
- 확인 작업이 진행 중이면 시간이 지나도 같은 프로젝트를 다시 시작하지 않는다 (S3)
- 목록을 확인하지 못해도 다른 작업을 막지 않고 0건으로 끝낸다

### tests/job/generation-jobs-client.test.ts (2)
> 작업 상태를 알리고, 끝난 작업은 결과를 돌려주며 실패는 이유와 함께 알린다
- 작업 상태를 순서대로 알리고, 끝난 결과는 돌려주되 상태 알림에는 결과 주소를 넣지 않는다
- 작업이 실패하면 오류로 끝내기 전에 실패 상태를 먼저 알린다

### tests/job/generation-jobs-columns.test.ts (4)
> 작업을 끝낼 때 필요한 정보를 보존하고, 인물 모습별 작업을 서로 섞지 않는다
- 작업을 끝낼 때 필요한 입력 정보를 포함한다
- 작업 대상을 확인하는 정보를 포함한다
- 인물의 모습 선택을 작업 칸에 보존한다
- 대기 중이거나 실패한 작업은 인물과 모습과 방향별로 서로 구분한다

### tests/job/generation-jobs-terminal.test.ts (13)
> 작업을 중복으로 끝내지 않고, 결과와 실패를 정확히 기록하며 잘못된 요청은 실행하지 않는다
- 처음 만들 때 내용이 없으면 빈 내용으로 기록한다
- 이미 끝난 작업은 같은 결과 주소일 때만 그대로 인정한다
- 이미 끝난 작업의 결과가 다르면 거절하고, 같은 실패만 그대로 인정한다
- 대기 중인 작업이 실패하면 끝난 시각을 기록한다
- 끝난 근거가 비어 있으면 아무것도 바꾸지 않고 거절한다
- 영상 조각에 연결된 작업은 일반 종료 처리로 바꾸지 않고 연결되지 않은 작업만 처리한다
- 작업을 조회하면 응답 내용을 함께 보여준다
- 응답 내용 일부를 바꿀 때 한 번에 안전하게 처리하고 요청 형식을 확인한다
- 응답 내용을 바꿀 요청 번호가 비어 있으면 실행하지 않고 거절한다
- 대상 작업이 없거나 응답 내용 변경 중 다른 오류가 나면 그대로 알린다
- 권한 확인과 목록·개수 확인에서 난 오류를 그대로 알린다
- 제공처나 시스템에서 난 일시 오류는 포기 횟수에 세지 않는다 (#error-class 오너 정책 2026-08-13)
- 작업 수 확인에서 예전 형식의 특정 오류일 때만 다른 방식으로 다시 확인한다

### tests/job/generation-prerequisite.test.ts (12)
> 필요한 자료가 준비되지 않으면 안내하고, 준비될 때까지 기다렸다가 자동으로 작업을 이어간다 (#ref-gate 2026-09-02, 오너 결정 1번)
- 선행 자료가 빠졌다는 충돌 응답이면 준비되지 않은 것으로 판단한다
- 무엇을 기다리는지 사람 말로 — 시트는 이름, 스토리보드는 샷
- 대기 안내는 화면 알림과 채팅 둘 다에 남긴다
- 시트: 빠졌던 모든 인물과 모습에 그림 주소가 생기면 준비된 것으로 본다
- 러프: 시작·방향·끝 세 장면이 모두 있으면 준비된 것으로 본다
- 실사: 스토리보드 그림이 있으면 준비된 것으로 본다
- 실사: 작업이 끝난 그림만 준비된 것으로 보고, 만드는 중이거나 실패한 그림은 제외한다
- 확인 결과 자료가 준비되면 끝내고, 그전에는 일정한 간격으로 다시 확인한다
- 기다릴 수 있는 시간이 지나면 시간 초과로 끝낸다
- 사용자가 취소하면 취소된 상태로 끝낸다
- 확인에 실패해도 기다림을 끝내지 않고 다음 간격에 다시 시도한다
- 같은 자료를 새로 기다리기 시작하면 이전 기다림은 취소한다

### tests/job/generation-queue.test.ts (6)
> 진행 중인 작업을 대상별로 정확히 표시해 해당 카드만 기다리게 한다 (#queue-restore)
- 여러 장면을 묶은 작업은 해당 장면 모두를 진행 중으로 표시한다
- 예전 방식으로 지정한 장면과 Director가 지정한 장면도 진행 중으로 표시한다
- 요청하지 않은 영상 작업은 그림을 진행 중으로 표시하지 않는다
- 작업 대상이 비어 있어도 오류 없이 지나간다
- 인물과 장소를 각각 따로 진행 중으로 표시한다
- 해당 작업이 하나라도 있으면 진행 중으로 판단한다

### tests/job/job-error-class.test.ts (2)
> 작업 실패 이유를 사람 말로 나누어, 다시 시도할 수 있는지 올바르게 판단한다 (#error-class 2026-08-13)
- 내용이 없거나 처음 보는 실패는 알 수 없음으로 모아 둔다
- 내용이 제한되면 그 사유로 분류하고, 그 밖의 실패는 일반 실패로 처리한다

### tests/job/number-sync.test.ts (16)
> 약속 D — 핀·탭 숫자·버튼 숫자는 서버 생성 큐 하나만 본다 (_tdd.md D, 2026-09-04 오너 확정)
- 이미지 생성이 N건 쌓이면 채팅 핀에 "0/N"이 뜬다
- 탭을 바꾸거나 다른 채팅을 하거나 새로고침해도 핀 숫자는 바뀌지 않는다
- 한 건 완료되면 핀이 "1/N"이 되고 왼쪽 Artist 탭 숫자가 1 올라간다
- N건 다 되면 핀이 사라진다
- 서버에 도는 작업이 없으면 핀이 보이지 않는다. 화면에서만 기다리는 표시는 세지 않는다
- 한 건 실패하면 그 건은 핀의 N에서 빠지고 카드 표시와 "N개 완료, M개 실패" 줄이 남는다
- Director도 같다: "영상 생성" 버튼의 숫자와 채팅 핀의 N이 같고, "스토리보드 생성" 버튼도 같다
- Artist 탭을 세 번 떠났다 돌아와도 같은 생성 작업의 상태 확인은 하나만 돈다
- 배경 첫 이미지가 만들어지는 동안 Artist↔Writer를 왕복해도 그 배경의 생성 요청은 하나만 나간다
- 이미 만들어지고 있는 배경은 자동 생성이 다시 요청하지 않는다
- 캐릭터 이미지가 띄엄띄엄 완성돼도 채팅에는 한 줄만 보이고 N이 하나씩 올라간다
- 사이에 다른 대화가 끼면 그 뒤 완료는 새 줄로 시작한다
- 완료 알림은 새로고침해도 합쳐진 채로 보인다
- 지금 보고 있는 단계의 완료는 채팅에 알리지 않는다. (지금 동작 유지)
- Artist 탭을 떠났다 돌아오면 보던 탭(인물/배경)과 고른 카드가 그대로다
- 배치 창은 도는 잡 기준 2분 — 한참 전에 따로 만든 이미지는 이번 배치에 섞이지 않는다

## billing — 결제: Take·플랜·Paddle·관리자 적립

### tests/billing/admin-billing-route.test.ts (10)
> 관리자는 무료로 처리하고, 권한에 따라 크레딧 적립과 조정 내역을 정확히 기록한다 (#payments-phase-2)
- 로그인하지 않은 사용자는 접근할 수 없다
- 관리자가 아닌 사용자는 접근할 수 없다
- 정보를 조회해도 관리자가 아니면 접근할 수 없다
- 요금제를 바꾸면 해당 월 크레딧을 함께 적립한다
- 무료 요금제로 바꾸면 추가 크레딧을 적립하지 않는다
- 조정 사유가 없으면 처리하지 않는다
- 조정 사유가 있으면 크레딧 변경 내역을 기록한다
- 지원하지 않는 적립 종류면 거부한다
- 유효한 크레딧 적립 요청은 변경 내역을 기록한다
- 요금제와 사용 권한, 남은 크레딧을 보여준다

### tests/billing/billing-account.test.ts (13)
> 내 플랜과 Take를 한 화면에서 본다 — 계정·결제 페이지가 읽는 요약(종류별 잔액·구독 상태·최근 내역)의 약속.
- 잔액은 무료·플랜·충전으로 나뉘고 합계는 장부 전체 합과 같다
- 플랜 Take의 만료일은 남은 플랜 lot 중 가장 이른 날이다
- 잔액이 음수면 회수분(환불·기타)이 얼마인지 같이 나온다
- 관리자 수동 조정은 기타로 잡히고 합계에 들어간다
- 같은 영상에 걸린 여러 줄은 한 줄로 합쳐지고 최신순이다
- 실패로 돌려받은 줄은 돌려받음으로 보이고 양수다
- 구독이 없으면 무료이고 결제일이 없다
- 갱신 결제가 실패한 상태면 실패 배너 신호가 켜진다
- 구독 취소가 예약된 상태면 이용 가능 종료일이 나오고 다음 결제일은 없다
- 정상 구독이면 다음 결제일이 나온다
- 무료 플랜이고 팩을 산 적 있으면 다시 살 수 없다
- 무료 플랜이라도 처음이면 살 수 있다
- 구독 중이면 몇 번이든 살 수 있다

### tests/billing/generation-quota-rejection-event.test.ts (3)
> 생성 한도가 가득 차면 사용자에게 이유를 알리고, 거절 기록도 남긴다 (#a2-observability 2026-08-26)
- 거절되면 어떤 한도에서 얼마나 대기 중인지 기록한다
- 한도 초과 응답에는 화면에 필요한 상태 정보를 담는다
- 기록에 실패해도 한도 초과 응답은 그대로 반환한다

### tests/billing/generation-quota-split-pools.test.ts (7)
> 영상과 이미지 생성 한도를 따로 관리해 서로의 작업을 막지 않고, 영상 크레딧 사용량을 통제한다 (오너 결정 2026-08-26, tale_pricing v4 정합)
- 영상 작업은 영상 한도 3개만 계산한다
- 이미지 작업은 이미지 한도 6개만 계산한다
- 이미지 한도가 가득 차도 영상 작업은 계속할 수 있다 (C1 회귀)
- 관리자는 개인별 생성 한도에 걸리지 않는다
- 관리자도 전체 동시 작업 한도는 지킨다
- 관리자 확인에 실패하면 일반 사용자 한도를 적용한다
- 가득 찬 종류를 알려 영상과 이미지를 구분해 안내한다

### tests/billing/paddle-catalog.test.ts (6)
> 가격 페이지의 플랜·팩 가격과 포함 Take는 v4 시트와 같고, Paddle 상품 ID를 모르는 상품은 결제 버튼이 비활성이다.
- 플랜 9개의 이름과 월 가격은 v4 시트와 같다(S-1 $15 … P-30 $1,299)
- 플랜의 축 4개(프로젝트당 분·연결·Take·Export)와 Account는 v4 시트와 같다
- 팩 4개의 Take 수와 가격은 v4 시트와 같다(Mini 50/$29 … Studio 5000/$2,199)
- 스타터는 S-1~S-10, 프로덕션은 P-10~P-30 으로 나뉜다
- 무료 플랜과 스튜디오 티어는 결제 상품 목록에 없다
- Paddle 가격 ID를 모르는 상품은 결제 버튼이 비활성이다

### tests/billing/paddle-checkout.test.ts (10)
> 결제창은 서버가 열어 준다 — 열지 말아야 할 때는 안 열고, 열 때는 어느 워크스페이스 결제인지 실어 보낸다 (#payments-phase-3 P7).
- 무료 플랜이 처음 팩을 사면 결제창이 열린다
- 무료 플랜이 구독을 시작하면 결제창이 열린다
- 무료 플랜이 이미 팩을 샀으면 팩 결제창이 열리지 않고 정액 가입을 권한다
- 구독 중이면 팩을 몇 번이든 살 수 있다
- 이미 구독 중이면 플랜 결제창이 열리지 않는다(플랜 변경은 나중 슬라이스)
- 갱신 결제가 밀린 상태면 새 구독 결제창 대신 카드 확인을 권한다
- Paddle 상품 ID가 없는 상품은 결제창이 열리지 않는다
- 거래에는 어느 워크스페이스 결제인지 표시가 실린다
- 같은 이메일은 Paddle 고객 하나를 다시 쓴다
- Paddle이 거래 생성을 거절하면 오류로 끝난다

### tests/billing/paddle-portal.test.ts (3)
> 구독 관리·결제 수단 변경은 유저가 우리 계정 페이지에서 한다 — Paddle 고객 포털 주소를 서버가 만들어 준다 (#payments-phase-3 P9).
- Paddle 고객이 없는 워크스페이스는 포털을 열 수 없다
- 고객이 있으면 포털 주소와 구독별 바로가기(취소·결제 수단 변경)를 돌려준다
- 구독이 없는 고객은 포털 주소만 온다

### tests/billing/paddle-webhook.test.ts (24)
> Paddle 결제 알림이 Take와 플랜의 유일한 진실이다 — 웹훅 핸들러의 약속 (.claude/docs/2026-09-07/paddle-promises.md §P1 · §P11).
- Paddle이 보낸 것이 아니면(서명이 틀리면) 받지 않고 장부에 아무것도 넣지 않는다
- 서명이 맞아도 5분 넘게 지난 알림은 받지 않는다(재전송 공격 방어)
- 같은 알림이 몇 번 와도 Take는 한 번만 들어가고 플랜도 한 번만 바뀐다
- 받은 알림 원문은 처리 성공·실패와 무관하게 전부 남는다
- 알림 처리 중 오류가 나면 Paddle에 "실패"로 답해 다시 보내게 하고, 재전송에서 처리되면 "성공"이다
- 알림에 어느 워크스페이스 결제인지 표시가 없으면 넣지 않고 원문만 남긴다
- 알림에 적힌 상품이 우리 상품 목록에 없으면 넣지 않고 원문만 남긴다
- 팩 결제 알림이 오면 그 팩의 Take가 결제일부터 12개월 유효로 들어간다
- 무료 워크스페이스에 이미 충전 기록이 있는데 팩 결제 알림이 또 오면 Take는 들어가되 경보가 울린다
- 구독 첫 결제 알림이 오면 플랜이 그 플랜으로 바뀌고 포함 Take가 결제 주기 끝까지 유효로 들어간다
- 갱신 결제 알림이 오면 그 주기 포함 Take가 다시 들어간다
- 구독을 취소해도 낸 기간이 끝날 때까지는 플랜과 Take가 유지되고, 끝나면 무료로 돌아간다
- 갱신 결제가 실패하면 그 즉시 무료 플랜으로 돌아가고 그 주기 Take는 들어가지 않으며, 재시도가 성공하면 다시 그 플랜으로 돌아가고 Take가 들어간다
- 플랜이 바뀌었다는 알림이 오면 플랜 표시를 바꾼다. 이미 들어간 Take와 충전 Take는 건드리지 않는다
- 환불 알림이 오면 그 결제로 들어간 Take를 회수한다. 이미 써서 모자라면 잔액이 음수가 된다
- 부분 환불이면 환불 금액 비율만큼 회수한다
- 같은 환불 알림이 두 번 와도 회수는 한 번이다
- 아직 승인되지 않은 환불 요청은 회수하지 않는다
- 부분 환불이 여러 번 와도 회수 합은 그 결제로 들어간 양을 넘지 않는다
- 카드사 분쟁(차지백) 알림도 환불과 똑같이 처리한다
- 처리 실패·워크스페이스 없음·상품 매핑 없음·갱신 결제 실패는 경보로 간다
- 결제창에서 카드가 거절된 것은 경보로 가지 않고, 구독 갱신 실패만 경보로 간다
- 알 수 없는 종류의 알림은 원문만 남기고 성공으로 답한다
- 경보 주소가 없으면 보내지 않고, 있으면 그 주소로 한 번 보낸다

### tests/billing/plan-entitlements.test.ts (11)
> 요금제에 따라 작업 시간과 연결 수, 내보내기, 좌석, 월별 Take를 정확히 보장한다 (v4 3_요금제 시트)
- 무료 요금제는 내보내기와 월별 Take를 제공하지 않는다
- S1 요금제는 1분 작업과 월 16 Take를 제공한다
- S2 요금제는 2분 작업과 월 30 Take를 제공한다
- S5 요금제는 5분 작업과 월 60 Take를 제공한다
- S10 요금제는 10분 작업과 월 100 Take를 제공한다
- P10 요금제는 10분·2개 연결·월 150 Take와 내보내기를 제공한다
- P15 요금제는 15분·3개 연결·월 200 Take와 내보내기를 제공한다
- P20 요금제는 20분·3개 연결·월 360 Take와 내보내기를 제공한다
- P25 요금제는 25분·4개 연결·월 410 Take와 내보내기를 제공한다
- P30 요금제는 30분·4개 연결·월 710 Take와 내보내기를 제공한다
- 알 수 없는 요금제는 무료 요금제 기준으로 처리한다

### tests/billing/take-cost.test.ts (5)
> 영상과 미리보기 생성에 필요한 Take 수를 모델별로 일관되게 계산한다 (#payments-phase-2)
- v4 기준 모델별 영상 Take 비용을 사용한다
- 모델을 선택하면 정해진 영상 Take 비용을 적용한다
- 모델을 지정하지 않으면 영상 Take 1을 적용한다
- 알 수 없는 모델도 영상 Take 1을 적용한다
- 미리보기 영상은 언제나 Take 1을 사용한다

### tests/billing/take-hold-wiring.test.ts (5)
> 영상 생성이 시작될 때 Take를 안전하게 처리하고, 실패하면 사용량을 되돌린다 (#payments-phase-2 #gen-quota-atomic-gate)
- 사용량을 받지 않는 모드에서는 영상 생성을 정상 제출한다
- 사용량을 실제로 차감하는 모드에서 잔액이 부족하면 생성을 막고 실패로 기록하며 상태를 바꾸지 않는다
- 기록만 하는 모드에서는 잔액이 부족해도 생성을 정상 제출한다
- 영상 생성 실패를 기록하면 사용량 반환도 요청한다
- 사용량 반환에 실패해도 생성 실패 기록은 성공한다

### tests/billing/take-hold.test.ts (13)
> 영상 생성에 필요한 Take를 설정에 맞게 처리하고, 잔액 부족과 실패 때 사용량을 정확히 관리한다 (#payments-phase-2 #gen-quota-atomic-gate)
- 설정값이 없으면 사용량을 받지 않는다
- 알 수 없는 설정값이면 사용량을 받지 않는다
- 기록만 하는 설정과 실제 차감 설정을 구분한다
- 사용량을 받지 않는 설정에서는 차감 없이 통과시킨다
- 관리자 작업 공간은 사용량을 차감하지 않고 통과시킨다
- 기록만 하는 설정은 잔액이 부족해도 통과시킨다
- 실제로 차감하는 설정에서 잔액이 부족하면 부족 상태를 알린다
- 잔액이 부족하면 생성 거절 기록을 남긴다
- 사용량 처리에 실패하면 오류를 알린다
- 관리자 확인에 실패하면 일반 사용자처럼 사용량을 차감한다
- 사용량을 받지 않는 설정이어도 이전에 기록한 사용량은 반환한다
- 반환량이 없으면 0으로 처리한다
- 사용량 반환에 실패하면 오류를 알린다

### tests/billing/take-ledger.test.ts (11)
> 사용자가 얻거나 쓰는 Take 내역을 기록하고 남은 Take를 정확히 계산한다
- 기록된 Take 변화를 합산해 잔액을 계산한다
- 내역이 없으면 잔액을 0으로 알려준다
- 내역을 읽지 못하면 오류를 알린다
- 양수 Take를 지급하면 지급 내역을 기록한다
- 지급량이 0 이하이면 내역을 기록하지 않고 거부한다
- 지급량이 정수가 아니면 내역을 기록하지 않고 거부한다
- 지급 종류가 허용된 범위가 아니면 거부한다
- 조정 사유가 있으면 Take 변경 내역을 기록한다(차감 허용)
- 조정 사유가 없으면 내역을 기록하지 않고 거부한다
- 관리자 정보가 없으면 내역을 기록하지 않고 거부한다
- 변경량이 0이면 내역을 기록하지 않고 거부한다

## llm-call — AI 모델 호출: 재시도·응답 형식 검사·기록

### tests/llm-call/llm-archive.test.ts (6)
> 인공지능 요청 내용을 안전하게 남기되, 기록에 실패해도 작업은 계속 진행한다 (#llm-archive 2026-08-10)
- 인공지능이 주고받은 내용과 걸린 시간을 그대로 기록한다
- 프로젝트로 확인되지 않는 실행은 기록하지 않는다
- 기록을 끄는 설정값이 켜지면 아무것도 기록하지 않는다
- 너무 긴 응답은 정해진 길이까지만 남기고 잘렸음을 표시한다
- 기록에 실패해도 작업을 중단하지 않는다
- 기록할 내용이 없으면 저장 요청을 보내지 않는다

### tests/llm-call/llm-retry.test.ts (6)
> 일시적인 오류는 다시 시도하고 사용 한도 초과는 별도로 센다 (#llm-quota 2026-08-10)
- 네트워크가 불안정하거나 요청이 몰리면 다시 시도할 오류로 판단한다
- 요청 시간이 초과되거나 중단되면 다시 시도할 오류로 판단한다
- 잘못된 요청이나 읽을 수 없는 응답은 다시 시도하지 않고 즉시 실패한다
- 사용 한도 초과와 일시적인 요청 몰림을 서로 다른 오류로 구분한다
- 사용 한도 초과로 다시 시도한 횟수만 센다
- 다시 시도를 모두 써도 사용 한도 초과 횟수를 빠짐없이 센다

### tests/llm-call/llm-schema-gate.test.ts (7)
> 인공지능이 만든 이야기 결과에 필수 내용이 빠지면 막고, 정상 결과는 통과시킨다 (#p4-json-guard, Q6, #g4 2026-08-27)
- 이야기 설계에 세계 설정이 빠지면 결과를 받지 않는다
- 장면이 하나도 없으면 이야기 결과를 받지 않는다
- 장면마다 행동 내용이 없으면 이야기 결과를 받지 않는다
- 장면의 이야기 시점이 없거나 정해진 값이 아니면 받지 않는다
- %s 결과에서 이야기 시점과 하루 중 시간을 따로 안내한다 (표)
- 정하지 않은 추가 내용이 있어도 나머지 이야기 결과를 받아들인다
- 이야기의 막 구성이 비어 있으면 결과를 받지 않는다

## ui-text — 화면 문구: 번역 사전·한글 잔존·문구 규칙·내부 식별자 숨김

### tests/ui-text/copy-rules.test.ts (7)
> 약속 A — 화면 문구에 긴 대시가 없고 영어는 첫 글자가 대문자다 (_tdd.md A, 2026-09-04 오너 확정)
- 한국어와 영어 화면 문구 전체에 긴 대시(—·–·―)가 하나도 없다
- 화면 문구의 영어는 문장 첫 글자가 대문자이고 단계 고유명(Writer 등)은 어디서든 대문자다
- Producer 보드 카드의 출처 표시는 "writer"가 아니라 "Writer"로 적힌다
- AI 답변 안에 단계 이름이 소문자로 들어 있어도 화면에는 첫 글자가 대문자로 보인다
- 위 대문자 처리는 과거에 저장된 채팅에도 적용된다 — 저장본이 아니라 그릴 때 바꾼다
- AI에게 주는 안내문(채팅 시스템 프롬프트) 안에 소문자 단계 이름이 없다
- AI 답변에 긴 대시가 들어 있으면 화면에 보일 때 지운다

### tests/ui-text/display-name.test.ts (5)
> 화면에는 사람이 읽을 이름을 보여 주고, 없는 이름은 알아보기 쉽게 대신 표시한다 (#opencast-name 2026-08-06)
- 번호나 단어를 이어 쓴 이름을 사람이 읽는 이름으로 바꾼다
- 한국어처럼 이미 읽기 쉬운 이름은 그대로 둔다
- 사람이 정한 이름이 없거나 번호처럼 보이면 알아보기 쉬운 이름을 대신 쓴다
- 인공지능이 이름 대신 번호를 반복하면 사람이 읽을 이름으로 바꾼다
- 새 장소는 읽기 쉬운 이름을 쓰고 연결 정보는 그대로 둔다

### tests/ui-text/i18n-korean-scan.test.ts (1)
> 화면에 번역되지 않은 한글이 새로 남지 않도록 찾아낸다 (#i18n-s5)
- 화면에 번역되지 않은 한글이 더 늘지 않는다

### tests/ui-text/i18n-missing-key-scan.test.ts (6)
> 한국어 화면에서 번역이 빠져 영어가 보이지 않도록 사전을 확인한다 (2026-08-28)
- 화면에 표시할 문구가 한국어 번역 사전에 모두 등록되어 있다
- 화면 문구를 실제로 찾아내며, 찾지 못한 채 통과하지 않는다
- 나뉘어 적은 문구도 하나의 번역 문구로 합쳐 확인한다
- 미리 정한 문구도 번역 대상으로 확인한다
- 내용이 중간에 달라지는 문구는 자동으로 판정하지 않는다
- 한국어 프로젝트의 첫 Producer 안내는 한국어로 나온다

### tests/ui-text/internal-id-scrub.test.ts (9)
> 화면에는 내부 번호 대신 사람이 읽는 장면 이름과 카드 이름만 보여준다
- 장면과 샷 번호를 보여주면 읽기 쉬운 이름으로 바꾼다
- 장면 번호가 아닌 값은 이름으로 바꾸지 않는다
- 대화에 내부 장면 번호가 나오면 사람이 읽는 이름으로 보여준다
- 옛 단계 표시는 숨기되 [L3] 대사 줄 참조는 남겨둔다
- 새 대화에서도 옛 단계 표시는 숨긴다
- 인물과 장소 이름을 알면 대화 속 번호 대신 그 이름을 보여준다
- 구분 기호만 있는 줄은 화면에서 숨긴다
- 문장 안의 하이픈과 "- " 목록 표시는 그대로 보여준다
- 멘션에는 사람이 읽는 이름만 보이고 내부 번호는 숨긴다

## permission — 권한: 관리자 판별·프로젝트 접근·내보내기 로그인 경계

### tests/permission/admin-gate.test.ts (6)
> 관리자 계정이 자신의 작업 공간에서만 관리 기능을 사용할 수 있게 한다 (#debug-prompts 2026-08-07)
- admin@tale.studio 계정을 관리자로 인정한다
- 개인 관리자 계정도 인정한다
- 대소문자와 앞뒤 공백이 달라도 같은 관리자 계정으로 인정한다
- 일반 계정이나 값이 없는 입력은 거부한다
- 설정값으로 추가 관리자 계정을 등록할 수 있다
- 관리자 계정이 해당 작업 공간의 소유자일 때만 관리 기능을 허용한다

### tests/permission/api-project-access-guard.test.ts (29)
> 로그인한 사람이나 허가받은 공유 링크만 프로젝트를 열 수 있게 한다 (2026-08-11 보안 감사)
- 로그인하지 않은 사람은 프로젝트에 접근할 수 없다고 알린다
- 소유자에게는 요청한 프로젝트를 열어 준다
- 다른 사람의 프로젝트에는 접근하지 못하게 한다
- 작업 공간이 없는 사람도 프로젝트에 접근하지 못하게 한다
- 잘못된 프로젝트 주소는 조회하지 않고 요청을 거부한다
- 프로젝트 주소가 없으면 요청을 거부한다
- 공유 링크를 허용하지 않으면 로그인하지 않은 사람은 접근하지 못한다
- 유효한 공유 링크를 사용하면 로그인하지 않아도 프로젝트를 연다
- 주소에 붙인 유효한 공유 링크도 프로젝트를 연다
- 취소된 공유 링크는 사용할 수 없다
- 기한이 지난 공유 링크는 사용할 수 없다
- 다른 프로젝트의 공유 링크로는 이 프로젝트를 열 수 없다
- 형식이 잘못된 공유 링크는 확인하지 않고 바로 거부한다
- 다른 사람의 인물 그림을 만들 수 없다
- 다른 사람의 배경 그림을 만들 수 없다
- 다른 사람의 인물 정보를 저장하거나 바꾸지 않는다
- 다른 사람의 인물 정보를 찾거나 바꾸지 않는다
- 다른 사람의 인물 모습을 저장하거나 바꾸지 않는다
- 다른 사람의 스토리보드 초안을 만들 수 없다
- 다른 사람의 스토리보드를 만들 수 없다
- 다른 사람의 스토리보드를 한꺼번에 만들 수 없다
- 다른 사람의 미리보기 영상을 만들 수 없다
- 다른 사람의 편집 상태를 볼 수 없다
- 다른 사람의 편집 상태를 바꿀 수 없다
- 다른 사람 영상의 재생 속도를 바꿀 수 없다
- 다른 사람 이야기의 장면을 고칠 수 없다
- 다른 사람 이야기의 대사를 만들 수 없다
- 다른 사람 이야기의 촬영 구성을 만들 수 없다
- 로그인하지 않은 사람은 이야기 장면을 바꿀 수 없다

### tests/permission/export-g002.red-team.test.ts (10) — red-team 에도 속함
> 프로젝트 주인만 Writer 내보내기를 열고, 문제가 있는 자료도 안전한 문서로 정리한다 (G002)
- 로그인하지 않은 사람은 프로젝트를 내보낼 수 없다
- 다른 사람의 프로젝트는 내보낼 수 없다
- 새 자료가 실패했으면 이전에 완성된 자료를 우선 내보낸다
- 모든 자료가 실패했으면 가장 최근 자료를 내보낸다
- 최근 자료가 유효하지 않으면 내용 없는 내보내기를 돌려준다
- 자료를 불러오는 중 저장소 오류가 나면 원인을 숨기지 않고 알린다
- 자료가 심하게 망가져도 문서 생성을 중단하지 않고 읽기 쉬운 내용만 담는다
- 장면 설명에 특수 문자가 있어도 문서 표가 깨지지 않는다
- 번역된 설명이 있으면 기본 영어 설명보다 번역문을 우선한다
- 내용이 빠진 인물과 배경 자료도 오류 없이 문서로만 내보낸다

## reference — 참조 프로젝트 가져오기

### tests/reference/produce-reference-digest.test.ts (4)
> Producer 대화에 참고 작품을 안전하게 활용하되, 현재 작품 내용과 권한을 지킨다
- 대화 결과에 사용량을 기록하되 실제 이야기 내용은 노출하지 않는다
- 현재 작품 설정 뒤에 참고 작품 정보를 읽기 전용으로 덧붙인다
- 참고 작품이 없거나 권한이 없으면 해당 정보를 조용히 제외한다
- 참고 작품을 불러오지 못해도 대화는 계속 제공한다

### tests/reference/project-reference-gate.test.ts (8)
> 로그인한 사람이 작업 공간 한도와 참고 작품 규칙을 지키며 새 프로젝트를 만든다
- 로그인하지 않은 사용자는 작업 공간을 확인하기 전에 거절한다
- 작업 공간 한도 확인에 실패하면 프로젝트를 만들지 않는다
- 무료 작업 공간이 한 개 한도에 도달하면 두 번째 프로젝트를 막는다
- 알 수 없는 결과가 오면 프로젝트를 만들지 않고 오류를 알린다
- 참고 작품을 고를 수 있는 요금제에서는 선택한 작품을 새 프로젝트에 연결한다
- 내용 없이 요청해도 Untitled 프로젝트를 만든다
- 계정에 언어 설정이 없으면 기본 언어를 고정하지 않는다
- 계정에 저장된 언어가 있으면 그 언어로 프로젝트를 고정한다

### tests/reference/reference-digest.test.ts (4)
> 참고 작품은 소유자와 이용 권한을 확인한 뒤 필요한 내용만 안전하게 보여준다
- 작품 소유자와 이용 권한을 다시 확인한 뒤 참고 작품을 1500자 안에 정리한다
- 다른 사람의 작품이거나 참고 권한이 없으면 내용을 제공하지 않는다
- 참고 작품을 불러오지 못해도 대화를 이어갈 수 있게 오류를 알린다
- 현재 프로젝트가 가리키는 참고 작품만 읽는다

### tests/reference/reference-import-plan-limits.test.ts (3)
> 요금제에 따라 만들 수 있는 프로젝트 수와 참고 작품 사용 가능 여부를 분명히 제한한다 (v4)
- %s 요금제는 프로젝트 %i개까지 허용한다 (표)
- 알 수 없는 요금제는 무료 요금제의 한도인 1개만 허용한다
- 프로젝트를 두 개 이상 허용하는 요금제만 참고 작품을 가져올 수 있다

### tests/reference/reference-import.test.ts (7)
> 참고 작품을 가져올 때 권한을 확인하고 안전한 이미지 자료만 새 프로젝트에 복사한다
- 다른 작업 공간의 참고 작품은 존재 여부도 알리지 않고 거절한다
- 참고 작품을 허용하지 않는 요금제면 소유자를 다시 확인해도 거절한다
- 선택한 스타일과 마지막 쇼트 시작 화면을 안전한 내부 공간으로 복사한다
- 완성된 스토리보드의 시작 화면이 없으면 러프 스토리보드의 화면을 사용한다
- 외부 스타일 이미지 주소는 복사하지 않고 경고만 남긴다
- 스타일 이미지를 복사하지 못해도 프로젝트를 만든 뒤 경고로 알린다
- 참고 작품의 장면을 찾지 못하면 화면 복사 경고로 알린다

## project — 프로젝트: 만들기·지우기·공유 데이터 캐시

### tests/project/project-delete-route.test.ts (6)
> 로그인한 사용자의 프로젝트 삭제 결과를 알기 쉬운 말로 정확히 알려준다
- 로그인하지 않은 사람이 삭제하면 프로젝트를 지우지 않고 거절한다
- 로그인한 사용자가 삭제하면 해당 프로젝트를 지우고 성공을 알린다
- 없는 프로젝트를 삭제하려 하면 찾을 수 없다고 알린다
- 권한 없는 프로젝트를 삭제하려 하면 권한 부족이라고 알린다
- 서버에서 삭제하지 못하면 성공으로 알리지 않고 오류를 알린다
- 예상하지 못한 삭제 결과가 오면 성공으로 처리하지 않는다

### tests/project/project-store-create-errors.test.ts (2)
> 새 프로젝트를 만들 수 있는지 먼저 알리고 성공하면 결과와 주의사항을 전한다
- 만들 수 있는 프로젝트 수를 넘으면 실패를 알리고 기존 프로젝트는 유지한다
- 새 프로젝트를 만들면 생성 결과와 주의사항을 알려준다

### tests/project/shots-cache.test.ts (7)
> 여러 화면이 같은 프로젝트의 장면 목록을 함께 보되 바뀐 내용은 다시 확인한다
- Writer와 Director가 동시에 장면 목록을 열면 한 번만 불러온다
- 30초 안에 다시 열면 인터넷에 다시 묻지 않고 바로 보여준다
- 다른 프로젝트의 장면 목록은 서로 섞이지 않는다
- 내용이 바뀌었다고 알리면 30초가 지나지 않아도 최신 장면 목록을 다시 불러온다
- 다른 프로젝트의 장면 목록은 다시 불러오지 않는다
- 성공하면 장면 목록을 오류 없이 돌려준다
- 불러오기에 실패해도 멈추지 않고 오류 이유를 함께 돌려준다

## login — 로그인·계정: 로그인 화면·테스트 계정·데모 토큰

### tests/login/demo-seam.test.ts (13)
> 데모에서는 프로젝트 자료를 안전하게 보여 주고 실제 자료 변경은 일어나지 않게 한다
- 데모 자료에서 선택한 프로젝트의 등장인물만 보여 준다
- 정렬과 개수 제한을 적용해 첫 번째 자료만 보여 준다
- 데모에서 자료를 바꿔도 실제 내용은 바뀌지 않는다
- 없는 자료 종류를 요청하면 결과가 없다고 보여 준다
- 공유 링크와 화면 자료 요청은 그대로 통과시킨다
- 읽기 요청은 데모 자료로 응답한다
- 자료를 바꾸거나 만들기 요청은 실제로 처리하지 않는다
- 모든 작업 단계에 데모 대화가 준비되어 있다
- 데모 공유 표시가 있는지 확인한다
- 데모 공유 상태에서는 자료 변경을 막는다
- 올바른 64자리 공유 링크를 읽는다
- 형식이 잘못된 공유 링크는 거부한다
- 브라우저 밖에서는 주소를 그대로 둔다

### tests/login/login-capslock.test.ts (1)
> 비밀번호 입력 중 대문자 잠금이 켜져 있으면 알아보기 쉽게 알려 준다
- 비밀번호를 입력할 때 대문자 잠금 상태를 알려 주고 입력을 벗어나면 안내를 숨긴다

### tests/login/seed-test-accounts.test.ts (10)
> 테스트 계정을 요청한 수만큼 안전한 주소와 비밀번호로 만든다
- 개수를 입력하지 않으면 10개를 만든다
- 개수를 숫자로 입력하면 해당 개수를 사용한다
- 개수 설정에 숫자를 넣으면 해당 개수를 사용한다
- 최대 100개를 넘겨도 100개까지만 만든다
- 0 이하이거나 정수가 아닌 개수는 거부한다
- test-로 시작하는 8자리 영문 숫자 계정을 만든다
- @tale.studio 주소를 만든다
- 여러 번 만들어도 계정 주소가 겹치지 않는다
- 비밀번호는 16자리 안전한 문자로 만든다
- 여러 번 만들어도 비밀번호가 겹치지 않는다

## ops — 운영 도구: 서버 오류 기록·소급 스크립트·코드 검사기

### tests/ops/backfill-filter.test.ts (13)
> 화면에 보여야 하는 그림만 작은 미리보기로 만들고 임시 자료는 제외한다
- 여러 장을 묶어 만든 스토리보드 재료는 화면용 미리보기를 만들지 않는다
- 한 장짜리 스토리보드 참고 그림은 화면용 미리보기를 만들지 않는다
- 고정 템플릿은 화면용 미리보기를 만들지 않는다
- 원본으로 올린 파일은 화면에 보일 때까지 미리보기를 만들지 않는다
- %s 는 화면용 미리보기를 만든다 (표)
- 프로젝트 밖의 그림도 화면에 쓰이면 미리보기를 만든다
- 임시로 만든 자료의 작은 미리보기는 지운다 (141개 대표 케이스)
- 화면에 보이는 정상 그림의 작은 미리보기는 절대 지우지 않는다
- 원본 그림은 이름이 제외 대상처럼 보여도 지우지 않는다
- 여러 장을 모은 격자 그림은 화면용 미리보기를 만들지 않는다
- 참고용 격자 그림도 화면용 미리보기를 만들지 않는다
- 격자 그림에 잘못 붙은 작은 미리보기는 지운다
- 장면 그림은 격자로 잘못 분류하지 않는다

### tests/ops/instrumentation.test.ts (6)
> 서버 오류를 운영 기록에 남기되, 기록이 실패해도 사용자의 요청을 막지 않는다 (#C, 2026-09-02 observability-audit)
- 서버에서 발생한 오류를 운영 기록에 남긴다
- 서버 밖에서 처리하는 경우 운영 기록을 남기지 않는다
- 오류 설명과 발생 위치는 정해진 길이까지만 기록한다
- 오류가 아닌 값도 글자로 바꿔 기록한다
- 오류 기록에 실패해도 실제 요청 처리는 막지 않는다
- 운영 기록 기능 자체에 문제가 생겨도 실제 요청 처리는 막지 않는다

### tests/ops/unstable-hook-deps.test.ts (3)
> 훅 deps 불안정 참조 감지 — 실패 모드 (2026-08-25 오너 결정으로 경고→실패 승격).
- 검사기가 크래시하지 않고 src/ 전체를 검사한다
- 베이스라인 밖 새 검출이 있으면 실패한다
- 베이스라인 3건은 여전히 검사기 결과에 unknown 으로 존재한다 (참고용 — 사라지면 검토)

## manual — 수동: 실제 AI·fal 과금이나 운영 DB 쓰기가 있어 사람이 켤 때만 돈다

### tests/manual/appearance-flashback-live.manual.test.ts (7)
> 회상 장면은 알맞은 모습을 고르고 잘못된 지정은 저장하지 않는다 (실제 AI 호출 0회·fal 과금 없음·운영 DB에 시험 자료를 쓰고 지우며 저장소에는 쓰지 않는다)
- 과거 모습이 둘이면 지정 없이는 자동 선택하지 않는다
- 그 장면에 모습을 지정하면 지정한 것이 이긴다
- 현재 장면은 기본 모습을 고른다
- 미래 장면은 맞는 모습이 없어 기본 모습으로 떨어진다
- 기본 모습은 하나만 등록할 수 있다
- 등록되지 않은 모습을 지정하면 저장하지 않는다
- 허용하지 않는 이야기 시점을 넣으면 저장하지 않는다

### tests/manual/i18n-layer-bleed-live.manual.test.ts (1)
> 같은 이야기를 영어와 한국어로 끝까지 만들고 언어별 결과를 남겨 섞임을 확인한다 (실제 AI 호출 다수·fal 과금 없음·운영 DB·저장소에는 쓰지 않고 로컬 결과 파일만 쓴다 #i18n-layer-bleed·#20)
- 같은 이야기를 영어와 한국어로 끝까지 만들면 언어별 결과 파일을 남긴다

### tests/manual/i18n-output-locale-live.manual.test.ts (1)
> 같은 한국어 이야기를 영어와 한국어로 만들면 설명도 선택한 언어로 나온다 (실제 AI 호출 2회·fal 과금 없음·운영 DB·저장소에는 쓰지 않는다 #i18n-s5)
- 영어를 고르면 설명은 영어가 되고, 한국어를 고르면 설명은 한국어가 된다

### tests/manual/narrative-time-live.manual.test.ts (1)
> 회상이 있는 이야기를 만들면 장면마다 현재·과거·미래 중 알맞은 시점을 표시한다 (실제 AI 호출 2회·fal 과금 없음·운영 DB·저장소에는 쓰지 않는다·2026-08-28)
- 회상이 있는 이야기를 넣으면 장면마다 현재·과거·미래 중 하나를 표시한다

### tests/manual/produce-attach-live.manual.test.ts (1)
> 저장된 이미지를 첨부하면 모델이 그림 속 내용을 읽어 설명한다 (실제 AI 호출 1회·fal 과금 없음·운영 DB·저장소에는 쓰지 않고 기존 저장소를 읽기만 한다 #p1-attach·2026-08-18)
- 저장된 이미지를 첨부하면 안전하게 전달되고 그림 속 내용을 읽어 설명한다

### tests/manual/queue-console-live.manual.test.ts (1)
> 생성 작업 목록을 안전하게 살피고 실패한 시험 작업은 지우며 끝난 작업은 지우지 않는다 (실제 AI 호출 0회·fal 과금 없음·운영 DB에 시험 작업을 쓰고 지우며 저장소에는 쓰지 않는다 #queue-console)
- 목록을 보고 오래된 작업을 정리하며 실패 작업은 지우고 끝난 작업은 지우지 않는다

### tests/manual/rough-crop-battery.manual.test.ts (1)
> 여러 화면 비율의 시트를 자르면 모든 장면 칸이 같은 크기이고 빈 띠 없이 내용이 남는다 (실제 AI 호출 0회·fal 과금 없음·운영 DB·저장소에는 쓰지 않고 로컬 자료만 읽는다 #detect-normalize)
- 자료 ${files.length}장을 자르면 모든 장면 칸의 크기가 같고 빈 띠 없이 내용이 보인다

### tests/manual/rough-sheet-live.manual.test.ts (1)
> 화면 비율이 다른 러프 시트를 만들면 모든 장면 칸을 같은 크기로 잘라낸다 (실제 AI 호출 1회·fal 과금 1회·운영 DB·저장소에는 쓰지 않고 로컬 결과 파일만 쓴다 #sheet-formats·#geometry-contract·2026-08-18)
- ${FMT} 형식으로 시트를 만들면 장면 칸을 고르게 잘라낸다

### tests/manual/sheet-formats-e2e.manual.test.ts (1)
> 여러 화면 비율의 시트와 장면 그림을 다시 만들면 올바른 모양으로 저장된다 (실제 AI 호출은 설정에 따라 여러 회·fal 과금 발생·운영 DB와 저장소에 결과를 쓴다 #sheet-formats·오너 지시 2026-08-17)
- 시트 형식을 준비하면 러프 장면과 개별 장면을 화면 비율에 맞게 다시 저장한다

### tests/manual/sheet-formats-live.manual.test.ts (1)
> 여러 화면 비율의 러프 시트를 만들고 장면 칸을 잘라내면 세로 장면도 가로 시트로 다시 그려진다 (실제 AI 호출 6회·fal 과금 약 $0.4·운영 DB·우리 저장소에는 쓰지 않고 fal 저장소와 로컬 결과 파일에 쓴다 #sheet-formats)
- 여러 형식의 시트를 만들면 장면 칸이 맞는 모양으로 잘리고 세로 장면도 가로 시트로 다시 그려진다

### tests/manual/stage-blockout-preview.manual.test.ts (1)
> 장면 배치 정보를 넣으면 한눈에 보는 배치도 그림을 만든다 (실제 AI 호출 0회·fal 과금 없음·운영 DB·저장소에는 쓰지 않고 지정한 로컬 파일에 쓴다)
- 장면 배치 자료를 넣으면 한눈에 보는 배치도 그림을 만든다

### tests/manual/stage-live-dryrun.manual.test.ts (1)
> 한 장면을 다시 만들면 무대 배치와 카메라 구도가 정리된다 (실제 AI 호출 2회·fal 과금 없음·운영 DB는 설정 시 장면과 그림 정보를 갱신하고 저장소에는 쓰지 않으며 결과 파일을 쓴다 #stage 2026-09-03)
- 한 장면을 넣으면 무대 배치와 카메라 구도를 다시 만든다

### tests/manual/stage-reapply.manual.test.ts (1)
> 저장된 장면 자료에 무대 규칙을 다시 적용하면 배치와 움직임을 빠짐없이 정리한다 (실제 AI 호출 0회·fal 과금 없음·운영 DB·저장소에는 쓰지 않고 결과 파일만 쓴다)
- 저장된 장면에 무대 규칙을 다시 적용해 모든 결과를 빠짐없이 만든다

### tests/manual/storyboard-appearance-live.manual.test.ts (5)
> 그림과 영상은 장면에 저장된 모습을 쓰고 잘못된 모습이면 유료 생성을 막는다 (실제 AI 호출 0회·fal 과금 없음·운영 DB를 읽고 시험 행을 잠시 고쳤다가 되돌리며 저장소에는 쓰지 않는다)
- 운영 장면에 나오는 인물마다 모습이 하나씩 기록되어 있다
- 저장된 모습이 실제 인물 자료와 연결된다 (끊어진 참조 0건)
- 과거 장면으로 바꾸면 그 장면의 인물 모습이 젊은 시절 자료로 바뀐다
- 필요한 모습 정보가 없으면 유료 생성을 시작하지 않는다
- 영상도 장면에 저장된 모습을 그대로 사용한다

### tests/manual/template-asset-live.manual.test.ts (1)
> 낡은 시트 파일을 넣으면 최신 파일로 바꾸고 다른 자료는 남긴다 (실제 AI 호출 0회·fal 과금 없음·운영 DB에는 쓰지 않고 운영 저장소에 시험 파일을 쓰고 지운다 #template-latest-only)
- 낡은 시트 파일을 넣으면 최신 파일로 바꾸고 다른 자료는 남긴다

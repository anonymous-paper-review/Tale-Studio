# Delta: layers/director_canvas.md

> 이 파일은 `redesign-director-canvas` change가 source-of-truth spec (`specs/layers/director_canvas.md`)에 적용할 ADDED / MODIFIED / REMOVED Requirements를 명세합니다.
>
> **1차 셋업 단계**: 본 delta는 placeholder. 실제 ADDED/MODIFIED/REMOVED Requirements는 D-4 ~ D-8 작업 진행과 함께 채워집니다.
>
> 현재는 `specs/layers/director_canvas.md` 본문이 직접 source-of-truth로 동작하며, change 전반의 의도는 `proposal.md`를 참조하세요.

## ADDED Requirements

(TBD — D-4 Writer↔Director 양방향 sync, D-5 영상 생성 wire-up, D-6 Preset Library 등 신규 기능을 진행하며 채움)

## MODIFIED Requirements

(TBD)

## REMOVED Requirements

(TBD — D-8 완료 시점에 구 Inspector 패널 / `director-store.ts` 의존 / 구 `/studio/director` 컴포넌트들의 deprecate 명세)

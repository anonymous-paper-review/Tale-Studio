# July 2026 UI/QC Revisions — Requirements Spec

> **출처**: 사용자 수정사항 요청 (2026-07-13 캡처, 노션/구두).
> **목적**: 랜딩 · Meeting Room · Writer(스토리보드/트리트먼트) · Artist(캐릭터/월드) · Director(Node/Storyboard) UI·UX 수정 + 생성 품질(QC) 이슈를 코드 기준으로 박제. **본 문서는 명세 정의 단계 — 구현 착수 전.**
> **상태**: Open Questions 확정 → `ralplan` 배치 그룹핑 → pending-approval. 아직 어떤 코드도 수정하지 않음.
> **참조 이미지**: 원문에 인라인 스크린샷 다수(버그 증거). repo에 아직 미아카이빙 → `specs/assets/july-2026/` 필요 (**OQ-0**).
>
> **항목 ID 규칙**: `LP`=랜딩, `MR`=Meeting Room, `WB`=Writer 스토리보드, `WT`=Writer 트리트먼트, `AC`=Artist 캐릭터, `AW`=Artist 월드, `DN`=Director Node, `DS`=Director Storyboard, `QC`=퀄리티컨트롤(교차 트랙).
> **타입 태그**: `[copy]` 문구 · `[ux]` 상호작용 · `[bug]` 결함 · `[naming]` 식별자 노출 · `[feat]` 신규 · `[qc]` 생성 품질 · `[q]` 확정 필요(→ Open Questions).

---

## 코드 앵커 (1차 grep — 라인 매핑은 ralplan에서 확정)

| 표면 | 파일 | 확인된 문자열/위치 |
|---|---|---|
| 랜딩 | `src/app/page.tsx` | `New Project` 버튼 ~L495; 프로젝트 타일·연필(편집) 버튼(삭제 버튼 추가 지점) |
| Meeting Room 보드·버튼 | `src/features/producer/readiness-board.tsx` | `프로듀서에게 채워달라` L370/L477, `삭제`(Trash2) L372/L480, `채팅으로 촬영 가능한 스토리를 정리해 주세요.` L586, 장르/세부장르 Input L618–636 |
| 채팅 placeholder·badge | `src/lib/constants.ts` | `STAGE_PLACEHOLDER` (producer L91 `스토리에 대해 말해주세요…`, director L94 `촬영 기법에 대해 물어보세요…`), `STAGE_BADGE.producer='P1'` L57 |
| Producer 스토어(설정·케이싱) | `src/stores/producer-store.ts` | `projectSettings.genre/subGenre/tone`; 이름 매칭 `toLowerCase` 다수 |
| Writer 러프 스토리보드 | `src/features/writer/rough-storyboard-view.tsx` | `스토리를 촬영할 수 있게 정리해뒀어요.` L113 |
| Writer 트리트먼트(스크립트) 뷰 | `src/features/writer/script-view.tsx` | `SC_`/`L1`/`LOCATION`/`dr_` **하드코딩 없음** → 식별자는 writer 엔진/DB 데이터에서 유입. 표시-매핑 레이어 필요 |
| Director 노드 라벨/상태 | `src/stores/director-store.ts` | `SHOT_STAGE_LABEL` rough `실사화` L717 / live `영상 생성` L718 / video `새 영상 테이크` L719 |
| Director 샷 상세 패널 | `src/features/director/canvas-panels/ShotDetailPanel.tsx` | `REGENERATE` L373 |
| Director 캔버스 | `src/features/director/canvas-views\|canvas-popups\|canvas-nodes/` | Node/Storyboard 뷰·상단 메뉴·넘버링·포커싱·원클릭/팝업 |
| Export | `src/lib/export/` (`index.ts`·`zip.ts`·`producer.ts`·`writer.ts`·`director.ts`·`artist.ts`) + `src/components/export-menu.tsx` | 현재 전 표면 `.md`→**ZIP** 번들; `전체 프로젝트 ZIP` L145 |
| 이미지/영상 모델 | `src/lib/writer/llm/fal.ts` | 이미지=`gpt-image-2` / 영상=`happy-horse/reference-to-video` (DN17 확인 대상) |

---

## 교차 테마 인덱스 (반복 불변항)

> 표면별로 흩어진 요청을 성격별로 묶은 것. ralplan에서 이 축으로 배치를 그룹핑하면 중복 작업이 준다.

| 테마 | 항목 |
|---|---|
| **A. 문구/카피 변경** | MR3, MR4, MR7, MR10, MR11, MR13, MR15, MR17, MR18, WB1, WB2, WB4, WB10, WB14, WT1, WT2, WT3, DN1, DN3, DN9, DN11, DN15, DN16, DS1, DS5 |
| **B. 식별자 노출(naming leak)** | MR7(케이싱), WT4, WT5, WB16(Char_2), DN5, DN13, DN18(take_v1), DS2 |
| **C. 커서/포커스/키보드 입력** | MR1, MR5, MR6, MR9, MR12 |
| **D. 호버 affordance·원클릭·팝업** | MR16, MR19, WB11, DN6, DN8, DS3, DS7 |
| **E. 진행/생성중 상태 표시** | WB3, WB4, WB7, DN10, DN14, DS6, DS7, DS8 |
| **F. 내보내기 단일 파일 포맷** | MR20(JPEG), WB18(JPEG), WT7(TXT) |
| **G. 상단 메뉴/레이아웃 정합** | WB17, DN1, DN2, DN7, DN12, DS1, DS3 |
| **H. 용어 친절화** | MR8(arc), MR17(want/need), MR18(arc 라벨) |
| **I. 생성 결함(bug)** | MR2, WB6, WB9, WB12, WB13, WB15, WB16, WT6, AC4, AC6, AW1, AW3, DN17 |
| **J. 생성 품질(QC 트랙 — 별도)** | AC1, AC2, AC7, AW2, DN19, QC1, QC2, QC3, QC4 |
| **K. 신규 기능(feat)** | LP1, LP2, WB3, WB8, MR22 |
| **L. 삭제→재생성 유도/proactive** | MR14, WB6(상태-채팅 정합) |

---

## 0. 랜딩 페이지 (LP)

- **LP1** `[feat][ux]` — `New Project` 클릭 시 **프로젝트 이름 지정 팝업(모달)** 노출 후 생성. (현재: 즉시 생성) 앵커: `page.tsx` ~L495.
- **LP2** `[ux]` — 프로젝트 타일에 **삭제 버튼 추가** — 연필(편집) 버튼 **우측**에 배치.

---

## 1. Meeting Room (MR)

### 1-1. 진입·채팅
- **MR1** `[ux]` — 첫 진입 시 에이전트 채팅이 **타이핑 애니메이션**으로 등장.
- **MR2** `[copy][bug]` — 초기 채팅 메시지의 **마크다운 기호(`**` 등) 원문 노출 제거** — 렌더링 적용. *(june `C6` 계열 재발/잔존.)*
- **MR3** `[copy]` — 예시 문구 교체: `"비오는~~"` → **`예를 들면 "비 오는 도시, 기억을 잃은 형사가 문득문득 떠오르는 장면으로 보내는 하룻밤과 마지막 장면에서 드러난 형사의 살인 범죄"`**.
- **MR4** `[copy]` — 초기 채팅 **마지막에 추가**: **`미리 작성한 스토리 파일이 있으면 아래 업로드 버튼으로 저에게 공유해주세요.`**
- **MR5** `[ux]` — 입력창 placeholder(`스토리에 대해 말해주세요…`)에 **깜빡이는 커서(caret)** 표시. 앵커: `constants.ts` `STAGE_PLACEHOLDER.producer` L91.
- **MR6** `[ux][bug]` — **엔터 전송 후 입력창 포커스 유지** — 현재 포커스 소실로 마우스 재클릭 필요. 연속 입력 가능해야 함.
- **MR7** `[copy][bug]` — 특정 단어 **소문자만 노출** 교정 → 적절 표기: `sci-fi`→`Sci-Fi`, `thriller-to-action`→`Thriller-to-Action` 등. (표시 케이싱 transform, 원천 = genre/subGenre 자유 텍스트)
- **MR8** `[copy][q]` — **"아크"(arc)** 용어에 일반 사용자용 설명 병기 — `아크(…)` 형태. → **OQ-4**.
- **MR9** `[ux]` — 입력창 내부 **위/아래 방향키의 이전 메시지 히스토리 리콜 제거** — 방향키는 프롬프트 내부 커서 이동만.

### 1-2. Synopsis(구 Brief Story) 섹션
- **MR10** `[copy]` — `Brief Story` 제목 → **`Synopsis`** 로 텍스트 변경 + **Story Foundation과 동일 폰트 크기로 위계 상향** + **별 아이콘 삭제**. *(june `D5`("Brief Story"로 변경)를 supersede.)*
- **MR11** `[copy]` — `기본적인 스토리를 AI Producer에게 알려주세요` 문구 위계를 **러닝타임과 동일 레벨**로.
- **MR12** `[ux]` — 현재 (제목 영역) 더블클릭 시 채팅창이 반응하는 동작이 좋음 → **이 동작을 아래 글박스에도 적용**.
- **MR13** `[copy]` — 내부 글박스에는 **`채팅으로 촬영 가능한 스토리를 정리해주세요`만 남기기**. 앵커: `readiness-board.tsx` L586.

### 1-3. Casting / Background 카드·버튼
- **MR14** `[ux][q]` — 캐릭터/배경 **삭제 버튼 클릭 시 채팅창에서 재생성 유도** 필요 여부. → **OQ-11**.
- **MR15** `[copy][q]` — `프로듀서에게 채워달라` **재명명**. 후보 충돌: `AI Producer에게 작성 부탁` vs `AI Producer와 상의하기`. → **OQ-3**. 앵커: `readiness-board.tsx` L370/L477.
- **MR16** `[ux]` — `삭제` / `채워달라` 버튼이 **버튼처럼 보이게**(시각 affordance 강화). 앵커: L370·L372 / L477·L480.
- **MR17** `[copy][q]` — 동기 `want/need` → **`내적 욕구 / 외적 욕구`로 풀어쓰기** (약어는 모르는 사용자 많음). → **OQ-4**.
- **MR18** `[copy][q]` — 아크 `시작/끝/유형` → **`시작/계기/끝`**로 재라벨(현재 라벨 불친절). → **OQ-4**.
- **MR19** `[ux][bug][q]` — `채워달라` 클릭해도 **변화 없음** + 설명 난해 + 결국 채팅으로 말해야 채워짐. 요구: (a) 라벨 정리(→ **OQ-3**), (b) 안내문구 **삭제**: `현재 정보를 말해줄 필요없음` 취지 문구 삭제, `Producer, Writer와 Artist가 바로 쓸 수 있는~` 문구 삭제. 상황: 배경 카드 1개 채워짐 + 필수 이미 충족 상태에서 클릭(참조 이미지). → **OQ-3**(버튼이 실제 auto-fill인가, 채팅 열기만인가).

### 1-4. 내보내기·핸드오프·제작 방식
- **MR20** `[ux][q]` — 내보내기: 현재 ZIP 안 `.md`(열람 불가) → **단일 파일** 선호. 사용자 제안 = **JPEG**. → **OQ-1**. 앵커: `src/lib/export/`, `export-menu.tsx` L145.
- **MR21** `[ux][q]` — 스테이지 전환 메시지 시간 조정: **"넘어갈 때" 경고는 너무 짧음 → 더 길게**, **"설계하는 중" 메시지는 너무 김 → 짧게**. + Writer 사전 진행 가능? → **OQ-6**.
- **MR22** `[feat][q]` — 결과물이 **애니메이션 톤**으로 나옴(스릴러인데 귀여운 청춘만화 톤). 요구: **"톤" 대신 "제작 방식" 선택** — `실사(Live Action) / 2D 애니메이션 / 3D 애니메이션`. → **OQ-2**.

---

## 2. Writer — 러프 스토리보드 (WB)

- **WB1** `[copy]` — `스토리보드` → **`러프 스토리보드`** 표기.
- **WB2** `[copy]` — `러프 스토리보드 preview` 라벨 **삭제**.
- **WB3** `[feat][ux][q]` — 전체 진행을 **progress bar**로 표현, **바 안에 단계별 진행 상황을 글자로** 표기, 퍼센트 유지. 초 표시는 **"전체 초"** 기준. → **OQ-5**.
- **WB4** `[copy]` — `멈춘 게 아니라 진행 중입니다` 문구 **삭제**.
- **WB5** `[feat][q]` — 대기 시간을 **Tale 사용법/프로모션 광고**로 활용(후순위). → **OQ-5** 연계.
- **WB6** `[bug][ux]` — 타이밍/정합: writer가 **découpage·shotDesign에서 유독 지연**; **artist가 먼저 완성**될 때 에이전트 채팅이 (실제 fail인데) **"성공(정리해뒀다)"로 오표기**. 재생성 토큰 비용 우려. → 게이트/상태-채팅 정합 + 실패 전파 필요.
- **WB7** `[feat][ux]` — 진행 중 **생성된 이미지 썸네일(작게) 일부 프리뷰** 노출.
- **WB8** `[feat][q]` — Writer에 **업로드 버튼** — 여러 png/jpeg 업로드 시 인식해 **우리 포맷으로 재활용**(vision 인제스트). → **OQ-7**.
- **WB9** `[bug]` — 샷 개수가 0개 아님에도 **"샷 0개"로 표기**.
- **WB10** `[copy]` — `스토리를 촬영할 수 있게 정리해뒀어요.` → **`스토리를 구체화할 수 있는 틀을 마련했어요.`** 앵커: `rough-storyboard-view.tsx` L113.
- **WB11** `[ux]` — 스토리보드 카드 **호버 시 "재생성" 버튼 제거**(동일 이미지가 재생성되어 무의미).
- **WB12** `[bug]` — (재발) **씬1 샷6·샷7부터 내용/이미지 밀림**; **샷7 얼굴 없음 + 주인공 2명**. 참조 이미지.
- **WB13** `[bug][qc]` — 프롬프트-이미지 불일치; 재생성물이 **손가락 등 겹침**. 샷6 영향 의심되나 샷6 자체 재생성해도 동일. 참조 이미지. (→ QC 연계)
- **WB14** `[copy]` — 저장 버튼 라벨 명확화: `저장` → **`스크립트에 저장`**, `저장 후 재생성` → **`저장 후 이미지 재생성`**.
- **WB15** `[bug]` — (재발) 나머지 씬들이 **샷 1개씩만** 생성됨.
- **WB16** `[bug][naming]` — **반복 내용 등장**(씬4~7 첫 샷) + 인물이 **`Char_2` 식별자**로 노출. 참조 이미지.
- **WB17** `[ux]` — 상단 메뉴를 **Artist와 동일 위치·동일 UI**로 통일.
- **WB18** `[ux][q]` — 내보내기: **스토리보드만 JPEG** 출력. → **OQ-1**.

---

## 3. Writer — 트리트먼트 (WT) *(구 "스크립트")*

- **WT1** `[copy]` — `스크립트` → **`트리트먼트`** 명칭 변경.
- **WT2** `[copy]` — `대본 뷰어` **삭제**.
- **WT3** `[copy][ux]` — 트리트먼트 탭 진입 시 채팅방에서 Writer가 **사용법 안내**:
  - `클릭한 부분의 스토리를 수정할 수 있어요.`
  - `클릭한 부분의 앞뒤에 스토리를 추가할 수 있어요.`
  - `수정한 스토리 기반으로 전체적인 스토리를 재구성 해달라고 저에게 요청할 수 있어요.`
- **WT4** `[naming][bug]` — 식별자 노출 교정: 인물명이 producer가 정한 이름 아닌 **`dr_lee`** 식, `Scene` 아닌 **`SC_01/SC_02`**, `LOCATION` 아닌 **실제 배경(월드) 이름**으로 나와야. *(앵커: `script-view.tsx`에 하드코딩 없음 → writer 엔진/DB 코드가 그대로 렌더됨. 표시-매핑 레이어에서 처리.)*
- **WT5** `[copy][naming]` — `L1` 등 depth 표기 제거 → **`Scene1`, `Scene1-Shot1`** 형태.
- **WT6** `[bug]` — **`L4`·`L9`가 대사처럼** 잘못 표현됨. 참조 이미지.
- **WT7** `[ux][q]` — 내보내기: **트리트먼트만 TXT** 출력. → **OQ-1**.

---

## 4. Artist — 캐릭터 (AC)

- **AC1** `[qc]` — 캐릭터 4개 중 **2+2가 서로 너무 유사**(구별성 부족). → **OQ-10**.
- **AC2** `[qc]` — 4개 **키(신장)가 모두 비슷** — 체형/신장 다양성 부족. → **OQ-10**.
- **AC3** `[copy][ux]` — 캐릭터 컨셉 이미지 위 **글자(텍스트 오버레이) 제거**.
- **AC4** `[bug]` — **컬러 팔레트가 배경 색상까지 추출** — 인물 색만 추출하도록.
- **AC5** `[q]` — **"디테일 노트"가 무엇을 생성하는지 불명확**. → **OQ-9** (현 동작 코드 조사 후 존치/삭제/재정의).
- **AC6** `[bug][qc]` — "스케치 스타일"인데 **피규어 컨셉처럼 렌더**될 때 있음 — 스타일 일관성.
- **AC7** `[qc]` — 캐릭터 시트 내부 **비율(proportion)이 매번 다름** — 최소한 한 시트 내에서는 consistency 보장. → **OQ-10**.
- **AC8** `[bug]` — 캐릭터 시트 샘플의 **"곰발바닥" 제거** (샘플/플레이스홀더 에셋 추적 필요; 후보 `src/features/artist/image-placeholder.tsx`).

---

## 5. Artist — 월드 (AW)

- **AW1** `[bug]` — 월드 이미지 내에 **캐릭터가 등장하면 안 됨**(사이드이펙트). 참조 이미지.
- **AW2** `[qc]` — 월드 이미지들 **그림체가 모두 제각각** — 스타일 통일.
- **AW3** `[bug]` — **2개 이미지가 하나의 이미지 안에 동시 등장**.

---

## 6. Director — Node (DN)

- **DN1** `[copy][ux]` — 상단 GUI 메뉴 부재 → 추가: 제목 **`The Set`** + 설명 **`러프 스토리보드와 등장인물, 월드를 바탕으로 실제 이미지/영상으로 촬영을 시작하세요`**.
- **DN2** `[ux]` — 하단 `Node/Storyboard` 토글 바를 **상단으로 이동**.
- **DN3** `[copy]` — 토글 아래 안내문: Node→`각 노드들을 클릭해서 실사화 해보세요`, Storyboard→`각 스토리보드를 클릭해서 실사화 해보세요`. *(단어 "실사화" = DN9와 정합 확인 — "실사 이미지"로 통일?)*
- **DN4** `[ux][bug]` — 샷 노드가 **상하로 겹치지 않게 배치**.
- **DN5** `[naming][copy]` — 노드 상단 `SCENE`/`SHOT` → **`SCENE1`/`SHOT2`처럼 넘버링 포함**, 하단에 `sc_01`/`shot_1`/`sho_2` 식별자 **재노출 제거**.
- **DN6** `[ux]` — 노드 상호작용: **더블클릭 → 원클릭**, Node·Storyboard **둘 다 팝업**으로.
- **DN7** `[ux]` — Node↔Storyboard 전환 시 뷰가 정중앙으로 재포커싱되는 문제 → **Node는 최근 위치 유지**.
- **DN8** `[ux]` — 생성 버튼을 **호버 상태에서 클릭 가능**하게.
- **DN9** `[copy]` — `실사화` → **`실사 이미지 생성`**. 앵커: `director-store.ts` `SHOT_STAGE_LABEL.rough` L717.
- **DN10** `[ux]` — 생성 버튼 클릭 시 **동작 피드백 부재** → 해당 노드 **생성중 표시**(테두리 색이 회전하는 러닝 보더). 참조 이미지.
- **DN11** `[copy][q]` — `REGENERATE` → **`Generation`**. 앵커: `ShotDetailPanel.tsx` L373. → **OQ-12**(영어 유지 vs 한글화).
- **DN12** `[ux]` — 최초 진입 시 **Scene1 위치에서 시작**(현재 정중앙 시작).
- **DN13** `[naming][bug]` — `sc_01` 식별자 노출 **잔존 → 제거**.
- **DN14** `[ux][bug]` — 진입 시 **에이전트 채팅 무반응** → 진입 메시지 표시.
- **DN15** `[copy]` — Director placeholder `촬영 기법에 대해 물어보세요…` → **`러프스토리보드를 실제 촬영 이미지로 생성해보세요.`** 앵커: `constants.ts` `STAGE_PLACEHOLDER.director` L94.
- **DN16** `[copy]` — 스토리보드 생성 버튼 **호버 툴팁**: `러프 스토리보드를 실제 촬영 이미지 스토리보드로 한번에 생성할 수 있어요`.
- **DN17** `[bug][qc]` — 이미지 생성에도 (영상용) **happy-horse 모델이 쓰이는 것으로 보임** → 이미지=`gpt-image-2` 경로 확인. 앵커: `fal.ts`.
- **DN18** `[naming][bug]` — 영상 생성 후 **모든 노드 이름이 `take_v1`로 동일** → 고유/의미 있는 이름. 앵커: `director-store.ts` `SHOT_STAGE_LABEL.video` L719.
- **DN19** `[qc]` — 영상 결과가 **스토리와 무관하게 서로 비슷** — 월드·캐릭터 레퍼런스 주입 영향 큼(특히 월드·캐릭터). → QC 트랙.

---

## 7. Director — Storyboard (DS)

- **DS1** `[copy][ux]` — 상단 GUI 메뉴 부재(=DN1과 동일) → **`The Set` + 설명문** 추가.
- **DS2** `[naming]` — `sc_01`/`location_2` → **`Scene01`/실제 장소명**.
- **DS3** `[ux]` — `미사용 에셋 불러오기` / `자동정렬` / `프롬프트 노드` **3버튼은 이 GUI에서 제거** → **Node 캔버스 안에만** 존재.
- **DS4** `[ux]` — 스토리보드의 **빈 이미지 대신 러프 스토리보드 이미지** 표시.
- **DS5** `[copy][ux]` — **빨리감기 버튼** 역할이 영상생성이면 라벨을 **`영상 생성`**으로.
- **DS6** `[ux][bug]` — 생성중 상태 표시 여부 불명 → **생성중 표시 필요**(DN10 동일 계열).
- **DS7** `[ux]` — 생성 완료 시 **가운데 재생 버튼** 등장 → 클릭 시 영상 재생.
- **DS8** `[ux]` — **카드 상태 머신 정리**(요구 명세):
  1. 기본: 러프 스토리보드
  2. 호버: `실사 이미지 생성` 버튼
  3. 생성중: 생성중 아이콘
  4. 생성 후 호버: `영상 생성` 버튼
  5. 생성중: 생성중 아이콘
  6. 생성 후 호버: `영상 재생` 버튼(클릭 시 재생)

---

## 8. 퀄리티 컨트롤 (QC) — *사용성과 별개 트랙*

> 사용자 명시: "사용성/서비스와 별개로 QC 이슈가 있음". 이 항목들은 결정적 UI 수정이 아니라 **생성 파이프라인·프롬프트·수용 기준** 작업 → 별도 ralplan 트랙 권장.

- **QC1** `[qc]` — 프롬프트가 **지나치게 형용사적이라 구속력 없음** (예: `광활하고 단조롭게 반복되는`, `체제적인 억압을 강조한다`) → 구체적·지시적 프롬프트로.
- **QC2** `[qc]` — **전 샷 대비 현재 샷의 변화가 미반영**(매 샷 독립 생성) → 샷 간 **연속성 반영** 방식으로 개선.
- **QC3** `[qc]` — **샷마다 그림체 달라짐** → 근원 = Artist 캐릭터 시트·월드 품질의 파급. **Artist 단계에서 QC**(AC/AW와 직결).
- **QC4** `[qc][q]` — Director 전체 구조 문제: **레퍼런스 방식은 앞뒤 안 맞는 프레임** 발생 → **First Frame / End Frame(키프레임) 생성 방식으로 변경** 필요. → **OQ-8**(대형 파이프라인 재설계, 별도 트랙).

---

## Open Questions (실행 전 확정 필요 — 사용자 요청)

1. **OQ-0 · 참조 스크린샷 아카이빙**: 원문 인라인 이미지(다수)를 `specs/assets/july-2026/`에 넣어줄 수 있나? 항목↔이미지 매핑 필요 대상: MR19, WB12, WB13, WB16, WT6, AW1, DN10.
2. **OQ-1 · 내보내기 포맷(MR20/WB18/WT7)**: 표면별 단일 파일 포맷 확정.
   - Meeting Room 산출물은 **텍스트**(스토리/설정/캐스트)인데 **JPEG 단일 파일**이 맞나? 아니면 "요약 카드 이미지 1장"?
   - Writer 스토리보드 = **contact-sheet JPEG 1장** vs **샷별 개별 파일**?
   - 트리트먼트 = **TXT** 확정?
   - 기존 `전체 프로젝트 ZIP` 옵션은 유지/삭제?
3. **OQ-2 · 제작 방식 vs 톤(MR22)**: `실사/2D/3D` 선택이 기존 `tone` 필드를 **대체**하나 **병행**하나? 어느 화면(Story Foundation?)에 두나? 선택값이 **이미지·영상 모델/프롬프트에 어떻게 반영**되나? (스타일 일관성 QC3와 직결)
4. **OQ-3 · "채워달라" 버튼 동작+라벨(MR15/MR19)**: `AI Producer와 상의하기`(채팅 열기만) vs `AI Producer에게 작성 부탁`(실제 auto-fill agentic) — **하나 확정**. auto-fill이면 별도 생성 로직 필요.
5. **OQ-4 · 용어 카피(MR8/MR17/MR18)**: 아크(arc) 설명 정확 문구, `내적 욕구/외적 욕구` 라벨·툴팁, 아크 `시작/계기/끝` 툴팁 문구 확정.
6. **OQ-5 · Progress bar(WB3/WB5)**: 바 안에 표기할 **내부 단계 라벨 목록**(일반인 친화명). 초 표시는 **총 경과초**인가 **예상 잔여초(ETA)**인가.
7. **OQ-6 · Writer 사전 진행/경고(MR21)**: 전환 경고를 얼마나 길게 / "설계 중" 메시지 얼마나 짧게? Writer는 **이미 핸드오프 시 백그라운드 실행**(CLAUDE.md) — 요구가 "이미 돌고 있음을 더 오래 노출"인지, 실제 착수 시점을 앞당기라는 것인지.
8. **OQ-7 · Writer 이미지 업로드 재활용(WB8)**: 이번 배치 포함 여부 — vision 인제스트는 대형 신규. **별도 트랙** 권장?
9. **OQ-8 · First/End Frame 재설계(QC4)**: Director 영상 생성 파이프라인 재설계(reference-to-video → keyframe). 이번 UI 배치와 **분리된 별도 대형 트랙**으로 잡는 게 맞나?
10. **OQ-9 · 디테일 노트(AC5)**: 존치/삭제/재정의 방향. (코드로 현 동작 먼저 조사 가능)
11. **OQ-10 · 캐릭터 다양성 수용 기준(AC1/AC2/AC7)**: "너무 비슷/비율 제각각" 합격 기준을 정성 기준으로 둘지, **시드/프롬프트 다양화 규칙으로 강제**할지.
12. **OQ-11 · 삭제→재생성 유도(MR14)**: 인물/배경 삭제 시 채팅이 재생성 유도 메시지를 **자동으로** 띄울지 + 문구.
13. **OQ-12 · 라벨 언어 정책(DN11)**: `REGENERATE`→`Generation`은 영어 유지인데 다른 라벨은 한글화 추세 → 라벨 언어 정책 일관 확인(`Generation` vs `재생성`).

---

## 다음 단계

1. (현재) 수정사항 → 본 spec 박제 + 그룹핑 완료.
2. **Open Questions 사용자 확정** (특히 OQ-1/2/3/8 — 포맷·데이터모델·파이프라인 결정 항목).
3. `/skill:ralplan` 진입 — 본 spec 입력, **교차 테마 축으로 배치 그룹핑** + 항목↔라인 확정 + OQ 해소, pending-approval 정지.
4. 승인 후 `ultragoal`/`executor`로 실행. **QC 트랙(§8 + AC/AW 품질 항목)은 UI 배치와 분리**해 별도 실행 권장.

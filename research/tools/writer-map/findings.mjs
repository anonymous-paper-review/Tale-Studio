// 끊긴 곳·새는 곳. 각 항목은 코드에서 직접 확인했고, verified:true 는 반증 시도를 한 번 더 통과한 것.
//   probe 가 붙은 항목은 매 요청마다 코드에서 재확인한다 — 배선이 생기면 페이지가 스스로 알려준다.

export const FINDINGS = [
  {
    id: 'camera-motivation',
    title: '카메라가 왜 움직여야 하는지가, 카메라를 설계하는 단계에 도착하지 않는다',
    badge: '끊긴 배선', tone: '', verified: true,
    body: `<p>카메라 규율은 이동을 고를 때마다 <b>그 동기를 반드시 적게</b> 강제한다. 완화판으로 승격시킨 근거 자체가 "금지되는 건 무빙이 아니라 동기 없는 무빙"이었으니, 동기는 이 계약의 핵심 산출물이다.</p>
<p>그런데 샷 설계에 데쿠파주를 넘길 때 쓰는 한 줄 형식에 그 항목이 없다. 사이즈·길이·비트·카메라 의도·리듬 역할·목적·내용·추가 사유는 실리는데 동기만 빠진다. 실제로 카메라 모션을 설계하는 단계가 감독이 적은 이유를 못 본다.</p>
<p>제품 코드에서 이 값을 읽는 곳은 0건이다. 실제로 읽는 것은 연구 표면뿐 — 카메라 추종 실험의 리포트 생성기가 "이동 동기"로 출력한다. 데쿠파주 산출물 자체에는 남으므로 <b>사후 감사는 가능하고 실시간 전달만 끊겨 있다</b>.</p>`,
    ev: 'decoupage.ts (생산·지시) · v4_shots.ts 의 데쿠파주 한 줄 형식 · types/pipeline.ts (타입에만 존재)',
    probe: { match: 'camera_move_motivation', produce: ['src/lib/writer/pipeline/stages/decoupage.ts', 'src/lib/writer/types/pipeline.ts'] },
  },
  {
    id: 'camera-intent-guarantee',
    title: '카메라 의도는 코드가 보증하지 않는다 — 프롬프트 순종에만 기댄다',
    badge: '보증 공백', tone: '', verified: true,
    body: `<p>샷 설계는 데쿠파주에서 네 필드를 코드로 복사해 심는다. 그 목록은 연산·출처 비트·샷 기능·리듬 역할 정확히 넷이고, <b>카메라 의도는 없다</b>. 받는 타입에 카메라 필드 자체가 없다.</p>
<p>대신 지시문이 "동적 스펙의 카메라 유형은 카메라 의도를 따른다(고정이면 static)"라고 <b>말로만</b> 요구한다. 모델이 안 따르면 아무 신호 없이 어긋난 값이 확정된다 — 사이즈나 길이처럼 코드가 되돌려 놓는 축이 아니다.</p>
<p>카메라 계약 완화를 채택할 때 "하류 전달 100%"로 측정한 축이 바로 이것이다. 그 측정은 사후 조인으로 확인한 결과이지 코드가 보장하는 값이 아니다.</p>`,
    ev: 'v4_shots.ts 보존 4필드 · v4_shots.ts 프롬프트 지시 · types/pipeline.ts ShotIntent · decoupage.ts (데쿠파주 산출물에는 보존)',
  },
  {
    id: 'provenance-dropped',
    title: '비트→샷 추적성을 위해 심은 네 필드를, 바로 다음 단계가 하나도 읽지 않는다',
    badge: '사장 필드', tone: '', verified: true,
    body: `<p>샷 설계는 데쿠파주에서 연산·출처 비트·샷 기능·리듬 역할을 코드로 복사해 의도 블록에 심는다. 주석이 "데쿠파주 출처를 결정론적으로 보존 — 비트에서 샷으로의 추적성"이라고 목적을 명시한다.</p>
<p>바로 다음인 결정론 조립기는 의도 블록에서 샷 id·씬 id·길이·극적 목적·씬 내 위치만 읽는다. 최종 샷 스키마에는 대응 필드가 아예 없다 — 완성된 샷에서 "이 샷이 어느 비트를 담는가"를 되짚을 방법이 없다.</p>`,
    ev: 'v4_shots.ts (심는 곳) · c_application_2.ts (읽지 않는 곳) · types/pipeline.ts (최종 스키마에 대응 필드 없음)',
    probe: { match: 'source_beats', produce: ['src/lib/writer/pipeline/stages/v4_shots.ts', 'src/lib/writer/pipeline/stages/decoupage.ts', 'src/lib/writer/types/pipeline.ts'] },
  },
  {
    id: 'v3-blind',
    title: '씬 촬영계획이 시간대·날씨·광원을 한 글자도 못 본 채 조명을 정한다',
    badge: '눈가림', tone: '', verified: true,
    body: `<p>이 단계가 하는 일의 절반이 조명 아크(시작·끝 색온도, 키필 비율, 광질)를 정하는 것이다. 그런데 프롬프트의 씬 요약 줄에는 씬 id·초·막·목적·감정·로케이션 id·등장 인물 일곱 항목만 실린다.</p>
<p>씬 타입에 엄연히 있는 시간대와 날씨가 빠지고, 월드 비주얼에서도 로케이션 <b>id 나열</b>만 가서 v2 가 설계한 광원 목록을 못 본다. 같은 로케이션 정보가 데쿠파주와 샷 설계에는 객체째로 직렬화된다 — 이 단계만 굶는다.</p>
<p>같은 자리에서 "대화 씬은 180° 축 필수"라는 자기 규칙도 대사 유무를 모른 채 인물 수로만 추정한다. 인물도 이름과 배역만 받는다.</p>`,
    ev: 'v3_scene_plan.ts 유저 프롬프트 · types/pipeline.ts (씬에 존재) · decoupage.ts (같은 정보가 여기엔 실림)',
  },
  {
    id: 'plan-missing-scene',
    title: '촬영계획이 빠진 씬은 샷 설계가 통째로 건너뛴다',
    badge: '증발 경로', tone: '', verified: true,
    body: `<p>샷 설계는 해당 씬의 플랜을 못 찾으면 경고 한 줄을 찍고 빈 배열을 돌려준 뒤 <b>그 씬을 완료로 마킹한다</b> — 재시도도 없다. 데쿠파주가 이미 저작해 둔 샷들이 그대로 사라진다.</p>
<p>상류에서 "씬에 플랜 없음"은 <b>경고 등급</b>이라 교정 재생성을 유발하지 않고, 교정 프롬프트도 치명 위반만 실어 보내므로 문구에도 안 들어간다. 게다가 교정본 채택 규칙이 치명 위반 <b>개수</b>만 비교해서, 씬 10개짜리 원본(치명 1건)이 씬 2개짜리 교정본(치명 0건)에 밀려날 수 있다. 막는 조건은 완전히 빈 배열뿐이다.</p>
<p>완전한 무흔적은 아니다 — 콘솔 경고가 남고 그 경고 이슈는 검수 리포트까지 실려 간다. 다만 샷 수 배지도, 커버리지 게이트도, 하류 복원도 없어서 <b>결과적으로는 아무도 모르게 줄어든다</b>.</p>`,
    ev: 'v4_shots.ts (스킵) · validators/scene_cinematography.ts (경고 등급) · v3_scene_plan.ts (교정 채택 규칙)',
  },
  {
    id: 'check-no-gate',
    title: '검수는 판정을 내리지만 아무것도 멈추지 않는다',
    badge: '무행동 관찰', tone: '', verified: true,
    body: `<p>검수 리포트는 네 갈래 이슈를 합치고 치명 이슈가 하나라도 있으면 통과 실패를 낸다. 그런데 그 값을 게이트로 쓰는 코드가 없다 — 저장 직후 곧바로 다음 단계로 진행한다.</p>
<p>리포트 본체도 종착지가 없어 런 상태에만 남고, 내보내기도 이 리포트를 투사하지 않는다. 화면에 닿는 유일한 통로는 <b>시각으로 분류된 제약</b>뿐인데 그건 리포트가 아니라 앞 단계에서 이미 샷에 붙어 나간다.</p>
<p>결과적으로 씬 단위 판정(액션 예산·촬영계획 위반)은 화면에 닿을 경로 자체가 없다.</p>`,
    ev: 'c_application_2.ts 리포트 조립 · steps.ts (저장 후 즉시 진행) · api/writer/export (투사 목록)',
  },
  {
    id: 'v6-v7-unreachable',
    title: '이미지·영상 생성 단계는 프로덕션에서 구조적으로 도달할 수 없다',
    badge: '경로 단절', tone: '', verified: true,
    body: `<p>두 라우트 모두 살아 있고 소유자 인증도 걸려 있다. 문제는 입력을 <b>로컬 파일</b>에서만 읽는다는 것이다 — 렌더 프롬프트 스테이지 파일을 로거로 불러온다.</p>
<p>로거는 배포 환경에서 파일 시스템을 처음부터 끈다(읽기 전용). 그래서 불러오기가 항상 비어 돌아오고 요청은 오류로 끝난다. 로컬 실행에서만 동작하는 경로다.</p>
<p>같은 이유로 샷 수 불일치 배지와 모션 어휘 교정 기록도 운영 환경에서는 어디에도 남지 않는다 — "조용한 열화를 막으려고" 만든 채널이 정작 운영에서 조용히 사라진다.</p>`,
    ev: 'logger/index.ts (환경변수로 파일 로깅 차단) · api/writer/generate/images · v4_shots.ts 배지·교정 기록',
  },
  {
    id: 'shot-count-drift',
    title: '샷 수가 어긋나도 절반만 넘으면 인덱스 매핑으로 확정된다',
    badge: '조용한 어긋남', tone: '', verified: false,
    body: `<p>샷 id 표준화가 데쿠파주 배열의 인덱스 직결이라, 개수가 어긋나면 어긋난 채로 짝이 지어진다. 모자라면 뒤쪽 데쿠파주 샷이 통째로 사라지고, 넘치면 초과분이 폴백 id 를 받는다.</p>
<p>가드는 절반 이하만 거부하고 그 위는 배지를 남기고 수용한다. 8개 기대에 6개가 오면 뒤 2샷이 사라진 채 확정되고, 정렬이 맞는지 확인하는 검증은 없다. 게다가 그 배지가 운영 환경에서는 남지 않는다.</p>`,
    ev: 'v4_shots.ts (주석이 위험을 스스로 적어 둠) · judgeShotCount · 반환부 인덱스 매핑',
  },
  {
    id: 'persist-wipe',
    title: 'writer 를 다시 돌리면 그림과 영상이 함께 지워진다',
    badge: '재실행 시 손실', tone: '', verified: false,
    body: `<p>샷 기록은 해당 프로젝트의 샷 행을 <b>통째로 지우고 다시 넣는다</b>. 사람이 편집한 값을 잃지 않으려고 이어받기 목록을 두는데, 거기 든 것은 카메라 설정·조명 설정·캔버스 위치·속도·트림 시작/끝·로케이션 id 일곱 개뿐이다.</p>
<p>같은 테이블의 영상 URL 과 스토리보드 이미지는 그 목록에 없다. 이미 그림과 클립을 뽑아 둔 프로젝트에서 writer 를 한 번 더 돌리면 그 산출물 참조가 함께 사라진다.</p>
<p>이어받기 목록 주석에는 "수동 프롬프트 편집 출처 컬럼은 이관 후 여기 들어온다"는 예정 항목만 적혀 있고, 생성물 컬럼은 언급되지 않는다.</p>`,
    ev: 'persist_manifest.ts 이어받기 목록 · 삭제 후 재삽입 · types/database.ts (컬럼 존재)',
  },
  {
    id: 'renderprompts-no-db',
    title: '렌더 프롬프트 묶음은 DB 로 내려가지 않는다',
    badge: '착지 실패', tone: 'note', verified: false,
    body: `<p>샷 기록 함수는 렌더 프롬프트를 <b>인자로 받지도 않는다</b>. 그래서 이미지·영상 프롬프트 묶음(화면비·해상도·fps·참조 에셋까지 정리된 형태)이 DB 에 남지 않는다.</p>
<p>실무상 손실이 크지는 않다 — 같은 값이 이미 샷의 프롬프트 컬럼과 동적 스펙으로 따로 내려가기 때문이다. 다만 이 단계의 산출물 자체는 런 상태와 로컬 파일에만 살고, 의도 블록도 마찬가지로 컬럼이 없다.</p>`,
    ev: 'persist_manifest.ts (인자 목록) · index.ts · steps.ts · shot-design-state.ts',
  },
  {
    id: 'compact-off',
    title: 'Compact 모드는 버그가 아니라 꺼둔 스위치다',
    badge: '읽는 사람 주의', tone: 'note', verified: false,
    body: `<p>Compact 판정 목록이 <b>빈 배열</b>이라 어떤 깊이도 Compact 로 걸리지 않는다. 짧은 영상도 씬 촬영계획을 거치게 하려는 <b>의도적 결정</b>이고, 되살리려면 깊이를 배열에 다시 넣으면 된다는 주석이 붙어 있다.</p>
<p>다만 그 결과 역추론 모듈, 전용 이어달리기 파일, 샷 설계 지시서의 Compact 절, 두 오케스트레이터의 분기가 모두 실행되지 않는다. 각 호출 지점에는 이 사실이 안 적혀 있어 코드만 보면 살아 있는 경로로 읽힌다.</p>`,
    ev: 'types/pipeline.ts (COMPACT_DEPTH_LEVELS) · index.ts · steps.ts · util/infer_v3.ts',
  },
  {
    id: 'dead-inputs',
    title: '시그니처만 남은 죽은 입력이 세 자리 있다',
    badge: '잔해', tone: 'soft', verified: false,
    body: `<p><b>v2 의 디자인 방향 seed</b> 와 <b>샷 설계의 샷 레시피 seed</b> 는 두 오케스트레이터 모두 빈 문자열을 넘긴다 — 프롬프트에는 "(없음)"만 찍힌다. 중간 산물을 삭제한 결정의 잔해이고, 하위 호환용으로 인자 자리만 남아 있다.</p>
<p><b>월드의 설정 한 줄</b>도 v2 프롬프트가 읽지만 프로덕션 경로에서 채우는 곳이 없다 — 시작 라우트는 로케이션만 싣고 병합 함수도 이 필드를 만들지 않아 항상 빈 문자열이 들어간다.</p>`,
    ev: 'v2_design.ts · steps.ts · index.ts · api/writer/start/route.ts',
  },
  {
    id: 'local-runner',
    title: '로컬 오케스트레이터는 프로덕션 경로가 아니다',
    badge: '읽는 사람 주의', tone: 'note', verified: false,
    body: `<p>파일 캐시로 이어달리는 로컬 러너를 제품 코드가 부르지 않는다 — 실험 스크립트만 불러 쓴다. 프로덕션은 상태를 DB 에 체크포인트하는 서버리스 단계 배열로 돈다.</p>
<p>두 경로가 같은 스테이지 함수를 쓰지만 <b>세부 계약이 다르다</b>. 디자인 토큰 기록은 로컬에서는 실패를 흡수하고 서버리스에서는 표면화하며, 샷 설계 실패 처리도 시간 예산 유무에 따라 갈린다. 지도를 읽을 때 어느 쪽 경로인지 먼저 확인해야 한다.</p>`,
    ev: 'pipeline/index.ts (제품 호출자 없음) · steps.ts 헤더 · 두 경로의 토큰 기록 분기',
  },
]

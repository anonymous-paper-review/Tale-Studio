```yaml
id: writer-예상-시간-실측값-주입
source: .claude/vault/_DEFERRED.md D-013 — 2026-08-15 한 원장으로 통합하며 옮겨옴
종류: (조건이 차면 그때 정한다)
budget: { usd: 0, runs: 1, wall_min: 30 }
blockers: []
status: 조건대기   # 원래 상태: blocked (오너 실측 대기)
priority: normal
```

# writer 예상 시간(ETA) 실측값 주입


- **무엇을**: writer 진행 화면의 예상 소요 시간을 실측값으로 채운다. 현재는 이전 기록이 없으면 숨긴다.
- **왜 미뤘나**: 추정치를 지어내지 않기로 했다 — "나중에 테스트해보고 실측값으로 적으려고 하니
  일단 비워놔줘"(2026-07-14).
- **언제 꺼내나**: 오너가 테스트로 실측값을 확보했을 때. 지금은 `writer_runs.state._timings` 에
  단계별 실측이 쌓이고 있어 그것으로 대체할 수도 있다.
- **되살릴 좌표**: run-store ETA 추정, `state._timings`.
- 기록: 2026-08-11 (원 발화 2026-07-14)

---

> 옮겨온 문서다. **"언제 꺼내나"가 이 항목의 판정선이다** — 밤 루프가 매일 그 조건이 찼는지 확인하고,
> 찼으면 `종류` 를 정해 `실행대기` 로 올린다. 조건이 사람만 알 수 있는 것이면 `사람대기` 로 바꾼다.

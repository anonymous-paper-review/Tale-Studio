```yaml
id: t2-25-blockout-isolation
source: research/experiments/seedance-25-timing/notes.md (3암 전부 블록아웃 동반 — 교란 미제거) · 에이전트 권고 "다음 1순위"
tier: T2
budget: { usd: 6, runs: 1, wall_min: 90 }
blockers: []
status: done  # 2026-08-11 밤 — 기각 조건 대입 "3D가 공로자"(TC 블록아웃 없음 완료 ~3.25s, 허용 1.5~2.5s 밖). 잠정(n=1·시드 없음). 2암만 실행($6.4714/$6.5 — 영상참조 제거로 0.6 할인 소멸, 3암 $9.71은 캡 초과). 결과: research/experiments/seedance-25-timing/no-blockout/
priority: high
```

- **맥락 (사람 언어)**: 새 모델(2.5)로 바꿨더니 카메라 전환이 주문한 시각에 거의 맞았다 — 지금 모델이 2.25초 늦던 게 0.25초로 줄었다. 그런데 **시간을 한 글자도 안 쓴 조건도 똑같이 맞췄다.** 세 조건 모두 3D 블록아웃 영상을 참조로 함께 받았고, 그 블록아웃의 스윙 구간이 정확히 주문 시각과 같다. 즉 시간을 나른 게 문장인지 3D인지 아직 못 가른다. 3D를 빼고 문장만으로 다시 재면 갈린다. 이 답이 중요한 이유: 3D라면 낮에 내린 "3D는 공간만 옮긴다"가 모델 세대에 한정된 이야기가 되고, 3D의 값어치가 시간까지 넓어져 조작대 설계(시간 축은 프리셋으로 충분)를 다시 계산해야 한다.
- **가설**: 2.5에서 타이밍을 나르는 것은 프롬프트의 초 표기가 아니라 3D 블록아웃 영상이다.
- **전제**: ① 2.5 3암 실측 — TC 완료 1.95s / SEQ 2.75~3.0s / CTL(시간 언급 0) 1.75s, 주문 1.0~2.0s ② 2.0 동일 구성 기준선 3.0→4.25~4.5s ③ 2.5 입력 스키마에 시간 파라미터 없음(fal·higgsfield 양쪽 확인) ④ **2.5 ref2vid에는 시드가 없다** — 같은 입력이 같은 결과를 보장하지 않으므로 조건당 1편은 분산과 못 가른다.
- **예측**: 참이면(3D가 공로자) 블록아웃을 뺀 3암 전부에서 전환 시각이 크게 밀린다(2.0 수준까지는 아니어도 유의하게). 거짓이면(문장이 공로자) 블록아웃 없이도 타임코드 조건이 주문 시각을 맞춘다.
- **측정**: 같은 샷(sh_04_16)·같은 시작 그림·2.5·720p·7s로 **video_urls 없이** 3암 × 1회 = 3클립. 프롬프트는 `seedance-25-timing/inputs/prompt_{tc,seq,ctl}.txt` **바이트 그대로 재사용**(변인은 블록아웃 유무 하나). 판독은 같은 8fps 프로토콜, 스윙 시작·완료 시각 측정. 기존 3암(블록아웃 있음)과 나란히 표로.
- **기각 조건**: 블록아웃 없는 타임코드 조건의 스윙 완료가 주문 창(1.0~2.0s)의 ±0.5초 안이면 → "문장이 공로자" — 3D는 시간 축에서 불요, 조작대 설계 유지. 그 밖이면 → "3D가 공로자" — 3D 값어치가 시간까지 확장, 조작대의 시간 축 설계 재검토 카드를 `_MORNING.md`에 추가. `(제안)` **시드 부재로 n=1 판정은 잠정** — 어느 쪽이든 결론에 "잠정" 표기 필수, 확정하려면 암당 3편 필요(추가 $6).

## 좌표 (동결)

- 프롬프트·발주 선례: `research/experiments/seedance-25-timing/` (inputs/·manifest.json·러너). 모델 `bytedance/seedance-2.5/reference-to-video`, 단가 $0.2838/s.
- 기준선 2종: 2.5+블록아웃 3암(같은 폴더) · 2.0+블록아웃(`previz-video-reference-ab/qual3-timed/out_t3d.mp4`).
- 시작 그림: `seedance-25-timing/inputs/` 또는 `qual3-timed/inputs/start.jpg`. **Blender 실행 금지**(참조 자체를 빼는 실험이라 블록아웃 불필요).

## 산출 계약

- `research/experiments/seedance-25-timing/no-blockout/` — 클립 3 + 480p 프리뷰 + frames/(8fps 스윙 구간 포함) + manifest.json + notes.md(측정 시각표 + 블록아웃 있음/없음 대조).
- status 갱신 + reports 1줄. 갤러리·판정은 낮 세션(오너 육안).

```yaml
id: writer-오버홀-실행
source: .claude/vault/_DEFERRED.md D-006 — 2026-08-15 한 원장으로 통합하며 옮겨옴
종류: (조건이 차면 그때 정한다)
budget: { usd: 0, runs: 1, wall_min: 30 }
blockers: []
status: 조건대기   # 원래 상태: blocked
priority: normal
```

# writer 오버홀 실행 (Phase 2~4)


- **무엇을**: 엔티티 지도에서 확정된 것을 코드로 옮긴다 — 사장 필드의 LLM 출력 스키마 제거,
  끊긴 배선 14건 복구(시네라인·카메라 동기·check_notes→영상·일괄 경로 텍스트 패리티 등),
  진실원 일원화(러프의 state 조인 → 컬럼).
- **왜 미뤘나**: 지도를 먼저 만들기로 했고(vault D-2026-08-11-a), 무엇을 지워도 되는지는
  경로 쌍 3결정이 나야 정해진다. 프롬프트를 바꾸는 작업이라 생성 품질 회귀 검증이 따라붙는다.
- **언제 꺼내나**: `2026-08-11-writer-entity-map-audit.md` 의 미결 3개(모션 진실원 / 실사 경로
  일원화 / facet vs 산문)가 닫힌 뒤. 그 전이라도 **프롬프트를 바꾸지 않는 항목**(예: 라우트
  select 추가, 소유권 검사)은 먼저 진행 가능하다.
- **되살릴 좌표**: 엔티티 지도 아티팩트(끊긴 배선 필터), `.claude/vault/2026-08-11-writer-entity-map-audit.md`.
- 기록: 2026-08-11

---

> 옮겨온 문서다. **"언제 꺼내나"가 이 항목의 판정선이다** — 밤 루프가 매일 그 조건이 찼는지 확인하고,
> 찼으면 `종류` 를 정해 `실행대기` 로 올린다. 조건이 사람만 알 수 있는 것이면 `사람대기` 로 바꾼다.

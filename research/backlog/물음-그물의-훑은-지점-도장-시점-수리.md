```yaml
id: 그물의-훑은-지점-도장-시점-수리
source: research/backlog/_MORNING.md Q22 — 2026-08-15 한 원장으로 통합하며 옮겨옴
종류: (해당 없음 — 사람이 답할 것)
budget: { usd: 0, runs: 0, wall_min: 0 }
blockers: []
status: 완료   # 2026-08-15 harvest.py 수리로 닫힘 — 도장을 리포트 뒤에 찍도록 분리했다
priority: normal
```

# 그물의 "훑은 지점" 도장 시점 수리 — 🔴 증거 영구 소실 결함 (8/13 밤 러너 신규)



- **원문** (`_SWEEP.md:48`): "성공 도장은 산출이 다 끝난 뒤에만 찍히므로, 도중에 죽은 밤은 성공으로 세지 않는다."
- **분해** (증거: `reports/2026-08-13.md` 결함 1):
  1. **코드가 약속과 다르다.** `harvest.py:322`는 **수확이 끝나는 즉시** 도장을 찍는다 — 노트·티켓·리포트가 0인 시점. "맨 마지막"이 **스크립트의 끝**일 뿐 **스위프의 끝**이 아니다.
  2. 따라서 수확 이후 단계에서 죽으면 도장은 이미 찍혀 있고, **그 창의 세션들은 다음 밤 창 밖으로 밀려 영영 안 걸린다.** 8/12에 요청한 failsafe는 **안 뜬 밤만** 복구하고 **뜨다 죽은 밤은 복구 못 한다.**
  3. **오늘 실물 재현**: 러너가 진단으로 수확만 돌렸는데 도장이 `08-12 02:25 → 08-13 02:03` 전진 → 미warp 세션 8개가 이탈 직전. **`git checkout`으로 원복 완료**(현재 값은 HEAD와 동일, 순변화 0).
- **선택지**: (a) 도장 쓰기를 `harvest.py`에서 분리해 그물이 리포트까지 낸 뒤 호출하는 별도 커맨드(`--stamp-success`)로 — 제안 처방 / (b) 도장을 "수확 시각"이 아니라 "직전 성공 시각"으로 의미 변경 / (c) 보류(그러면 반쯤 죽는 밤마다 대화가 조용히 증발).
- **성격**: 산출물이 판정이 아니라 **동작하는 물건** → `_SWEEP.md` 판별표상 **가설 아님**. 브랜치 준비까지 시키려면 T-fix 티켓으로.

---

> 옮겨온 문서다. **오너만 답할 수 있는 것**이라 밤 루프는 집지 않는다.
> 답이 나오면 그 답에서 나오는 실행 단위를 새 일감으로 만들고 이건 `완료` 로 닫는다.

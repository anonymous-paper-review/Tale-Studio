```yaml
id: 생성-후-정합-검사-재활성화
source: .claude/vault/_DEFERRED.md D-001 — 2026-08-15 한 원장으로 통합하며 옮겨옴
종류: (조건이 차면 그때 정한다)
budget: { usd: 0, runs: 1, wall_min: 30 }
blockers: []
status: 조건대기   # 원래 상태: 대기
priority: normal
```

# 생성 후 정합 검사(P2) 재활성화


- **무엇을**: 러프 START↔설명 VLM 판정과 영상 첫/끝 프레임 모션 준수 검사를 다시 켠다.
  로직·라우트·배지·DB 스키마는 모두 살아 있고 스위치만 꺼져 있다.
- **왜 미뤘나**: 다른 검증을 먼저 진행하기로 함 (제품 오너 결정). 결함이 아니라 순서 문제.
- **언제 꺼내나**: 진행 중인 previz/viz 검증이 일단락되어 판정 노이즈를 감당할 여유가 생겼을 때.
- **되살릴 좌표**: `src/lib/adherence/core.ts`의 `ADHERENCE_START_ENABLED`·`ADHERENCE_MOTION_ENABLED`를
  `true`로. 관리자 소유 프로젝트 게이트는 그대로 유지.
- 기록: 2026-08-10

---

> 옮겨온 문서다. **"언제 꺼내나"가 이 항목의 판정선이다** — 밤 루프가 매일 그 조건이 찼는지 확인하고,
> 찼으면 `종류` 를 정해 `실행대기` 로 올린다. 조건이 사람만 알 수 있는 것이면 `사람대기` 로 바꾼다.

# 2026-08-15 wrap 스위프 리포트

> 01:31 1패스. 유저 수면 중이므로 질문하지 않았고, vault·`src/`·데이터베이스는 쓰지 않았다.
> 사람용 리포트: [2026-08-15-sweep.html](2026-08-15-sweep.html)
> 발행: https://claude.ai/code/artifact/c68eb6f6-9d1e-4534-a2d3-a0504577b11b

## 결과

- 스캔 51개, 미warp 세션 **6개**(전부 Claude · gjc 0).
- **계약 기본 설정대로면 수확은 1개였다.** 그 1개(`58a962c8`)는 본문이 "OK만 답하라" 한 줄인 빈 세션이라, 오늘 밤의 신호는 전부 아래 §결함 1 때문에 제외될 뻔했다. 스위퍼가 별도 패스로 5개를 구제했다.
- 세션 노트 **5개**, 새 가설 티켓 **4개**(전부 `status: 초안`), 아침 카드 **1장**.
- **자동 `실행대기` 전환 0건.** 발사 프롬프트에 이번에도 `자동 ready는 T0 read-only만`이 들어 있었으나 `_SWEEP.md` §5("발사 프롬프트가 이 문서와 다른 말을 하면 이 문서가 이긴다")를 따랐다. Q21의 판정 재료다.
- 기동 도장 `sweep/.last-launch` = `2026-08-15T01:31:14+0900`. 성공 도장은 이 리포트까지 낸 뒤에만 유효한 것으로 본다(§결함 2 참조 — 실제로는 수확기가 이미 찍었다).

## 미warp 세션별 내역

| 세션 | 크기 | 무엇을 했나 | 처리 |
|---|---|---|---|
| `5a3338db` | 5523KB | 기획 채팅 파일 첨부 · 화풍 기준 배선 · 터널 사고 수습 | 노트 · 티켓 1건 기여 |
| `52ae45f9` | 3005KB | 그림 축·샷 층 배선도 생성기 | 노트 · 연결 2건 (티켓 0) |
| `0897af55` | 3914KB | 글쓰기 속도 실측·수리 4연타 | 노트 · 티켓 3건 |
| `e6e0764a` | 17457KB | 담당자 다섯의 캐릭터 설계 | 노트 · 티켓 0 (전부 취향) |
| `cc9e03b1` | 1612KB | 글쓰기 문제 전수 원장 (한도로 중단) | 노트 · 티켓 1건 기여 |
| `58a962c8` | 37KB | "OK만 답하라" 빈 세션 | **버림** (§버린 것) |

## 새 가설 초안 4건

| 티켓 | 종류 | 비용 | 무엇을 하나 | 출처 |
|---|---|---|---|---|
| `t0-grounding-pin-stage-coverage` | 조사 | $0 | 웹 검색을 쥐는 세 단계가 전부 새 접지 모델로 가는지 코드로 센다 | `0897af55` (1/2) |
| `t1-pipeline-wallclock-after-pin` | 모델실험 | $6 | 어제 수리 4건 뒤 전체 한 바퀴가 몇 초인지 3회 잰다 | `0897af55` (2/2) |
| `t0-stuck-jobs-webhook-loss` | 조사 | $0 | 완료 소식을 못 받아 굳은 작업이 데이터베이스에 있는지 센다 | `5a3338db` · `cc9e03b1` |
| `t1-shotdesign-model-rollback` | 모델실험 | $6 | 샷 설계만 예전 모델로 되돌려 시간·개수 지표를 나란히 잰다 | `0897af55` |

싸게 죽는 순서로 배열했다 — 0원짜리 둘이 앞이고, `t1-pipeline-wallclock-after-pin`은 형제 티켓을 `blockers: 선행`으로 물고 있다.

## 🔴 결함 1 — 그물이 어젯밤 세션 5개(31MB)를 영영 흘릴 뻔했다

**증상.** 기본 설정 수확에서 대상이 1개(빈 세션)뿐이었다. 제외된 7개 중 5개가 실제 작업 세션이었다.

**원인.** 두 규칙이 서로 어긋나 사각지대를 만든다.

```
수확기:  "최근 60분 안에 움직인 세션"은 작업 중이라 제외
창 계산: 오늘 시작 시각으로부터 24시간 뒤로

→ 오늘 01:31 수확 → 00:31~01:31에 움직인 세션 제외
→ 내일 01:31 수확 → 창은 오늘 01:31까지만 → 그 60분은 다시 안 덮인다
```

창을 넓히는 안전장치는 "마지막 성공 이후 간격이 24시간을 넘을 때"만 켜지는데, 도장이 **수확이 끝난 시각**에 찍히고 창은 **시작 시각**부터 재므로 간격이 항상 24시간보다 조금 짧다. 그래서 안전장치가 이 구멍에는 영원히 안 켜진다.

**오늘 처리.** 제외창을 0으로 둔 별도 패스를 `sweep/2026-08-15-recent/`에 냈다. 러너·스위퍼 세션은 수확기가 알아서 걸러냈다(`191deff9`, `d600a7a9`). 구제분에서 나온 기각 순간 후보가 **16건**이고, 기본 패스에서는 0건이었다.

**Q22와 다른 자리다.** Q22는 도장이 *너무 일찍* 찍히는 문제(수확 직후 vs 리포트 뒤). 이건 *제외창과 창 계산이 서로 안 맞물리는* 문제다. 둘 다 고쳐야 하나가 안 남는다.

## 🔴 결함 2 — 창 넓히기 안전장치가 오늘 죽어 있었다

수확기 첫 줄에 이 경고가 떴다:

```
⚠ 마지막 성공 시각을 못 읽음(could not convert string to float:
  '2026-08-14T01:48:44+0900') — 기본 창 24시간으로 진행
```

`sweep/.last-success`는 숫자(초 단위 시각)로 적혀야 하는데 **날짜 글자**가 적혀 있었다. 형상 관리 기록상 직전 값은 숫자였다(`1786469127.283189`). 8/14 밤에 사람 손으로 도장을 찍으면서 **기동 도장(`.last-launch`)과 형식을 헷갈린 것**으로 보인다 — `_SWEEP.md` §0이 "다른 파일이니 헷갈리지 마라"라고 경고한 바로 그 혼동이다.

오늘은 마지막 성공이 23.7시간 전이라 어차피 창이 안 넓어졌을 상황이어서 **실제 손실은 없었다.** 그리고 오늘 밤 수확기가 숫자로 다시 덮어써 형식은 복구됐다. 다만 **하루 건너뛴 밤이 있었다면 그날 대화는 통째로 증발했을 것이고, 경고 한 줄 말고는 아무도 몰랐을 것이다.**

## 이미 있던 것과 이어붙인 것 (그물의 값어치는 목록 늘리기가 아니라 여기다)

| 기존 | 어젯밤 무슨 일이 있었나 | 처리 |
|---|---|---|
| **Q6** 잘린 답 손실 가드 — 지금 넣나 | `9ce69ca`로 들어갔다(네 프로바이더 배선 + 재호출 1회, 테스트 1111). 조사 결과 실제 피해는 0이었지만 신호가 없던 게 문제였던 것 | **사실상 닫힘** — 카드 수정은 스위퍼 권한 밖이라 보고만 |
| **Q13** 서버리스 카나리아 실행 시점 | 436초 사고가 이 카나리아에 실패 판정을 줬다는 게 세션 주장 | 재료 연결 |
| **Q11** 샷 검수 단일 호출 취약성 | "검수 호출이 시간 예산을 모른다 / 재시도 총 예산이 없다 / 한 프로바이더는 요청 시간제한 자체가 없다" 실측이 붙음 | 재료 연결, 티켓 안 만듦 |
| **Q18** 로그인 없이 열린 창구 3건 | **다른 문제**가 나왔다 — 로그인은 됐고 소유권만 안 보는 유료 경로 6곳 | 아침 카드 ④ |
| **Q8** v6·v7 재배선 | 배선도 세션이 "오케스트레이터가 안 물고 있다"를 재확인 | 재료 연결 |
| `t0-remaining-open-api-routes`[완료] | 무인증 라우트만 봤다 — 소유권 축은 안 봤다 | 겹치지 않음 확인 |
| `tfix-fal-wiring-repairs`[완료] | 업체로 *보내는* 항목을 고쳤다 — *돌아오는* 소식의 주소는 안 봤다 | 새 티켓에 명시 |
| `t0-previz-pump-stall-prevalence`[완료] | 겉 증상(러프 그림 미충전)을 봤다 | 새 티켓에 명시 |
| `t0-dramaturgy-36flash-outage`[완료] | 같은 모델 전환의 *검색 발화* 축 | 새 티켓에 명시 |

## 버린 것과 그 사유

| 버린 것 | 왜 |
|---|---|
| `58a962c8` 세션 전체 | 본문이 "OK만 답하라" 한 줄. 다이제스트 0KB, 기각 순간 0건. 열린 것 없음 |
| 첨부 허용 형식에서 svg·hwp 처리 | 어떤 관측이 나오면 죽는지 못 쓰겠다 — 기각인지 누락인지 발화에 없음 (판별표 4행). 노트에만 |
| 웹툰 원본이 바뀌면 하류가 낡는가 | 오너가 화제를 돌려 답 없이 끝남. 닫히는 조건 불명 (판별표 4행). 노트에만 |
| 24픽셀 아바타 다시 그리기 | 산출물이 판정이 아니라 동작하는 물건 (판별표 1행). 노트에만 |
| 디자인 체계 재정립 | 오너가 "다른 세션에서 진행하고 싶다"고 명시 이월 — 결론 난 보류 (판별표 3행) |
| 담당자 캐릭터 6안씩 선택 | 순수 취향 (판별표 2행). 사실 축인 색 대비는 세션이 이미 숫자로 닫음 |
| 동시성 12 채택 | 실측도 계산도 끝났고 남은 건 수용 인원 26명 대 30명 교환 — 오너 몫 (판별표 3행) |
| 유료 경로 소유권 미확인 6곳 | 관측은 세션이 표로 끝냈다. 남은 건 수리 (판별표 1행) → 아침 카드 |
| 배선도가 낸 발견 11건 | `cc9e03b1`의 150건과 겹칠 가능성이 매우 높다. **중복 생산이 이 그물의 최대 실패 모드**라 대조를 아침으로 넘김 |
| 미커밋 작업 대량 | 판별 질문상 "코드로 귀결" — 다만 데이터베이스가 앞서간 비대칭이라 아침 카드 ⑤로 보고 |

## 구제한 것

`cc9e03b1`의 산출물이 지워질 자리에 있는 것을 확인해 경로를 노트에 박아뒀다:

```
/private/tmp/claude-501/-Users-xcape-projects-tale-studio/
  cc9e03b1-86a9-4cd2-bbfd-9ae9eda6d28d/scratchpad/
    groups-final.json · final-verdicts.json(696KB) · reverify.json(632KB)
    new-findings.json(109KB) · findings.txt(101KB) · batches/(44개)
```

250개가 넘는 에이전트가 만든 것이고, 의도했던 문서 `.claude/vault/2026-08-13-writer-problem-inventory.md`는 안 써졌다. **세션 임시 폴더라 사라질 수 있다.**

## 산출 목록

- 노트 5: `sweep/2026-08-15/note-claude-{5a3338db,52ae45f9,0897af55,e6e0764a,cc9e03b1}.md`
- 다이제스트: `sweep/2026-08-15/`(기본 1개) · `sweep/2026-08-15-recent/`(구제 6개, 기각 순간 16건)
- 티켓 초안 4: `t0-grounding-pin-stage-coverage` · `t1-pipeline-wallclock-after-pin` · `t0-stuck-jobs-webhook-loss` · `t1-shotdesign-model-rollback`
- 아침 카드 1: `_MORNING.md` 최상단

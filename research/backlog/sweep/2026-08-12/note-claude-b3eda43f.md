# 스위프 노트 — claude:b3eda43f (2026-08-11 15:37, 330KB)

## 세션이 뭘 했나

오너가 프로젝트 링크 하나를 던지며 "이 프로젝트의 러프 그림이 지금 돌고 있는 거냐, 아니면 아예 안 만든 거냐"를 물었다. DB를 직접 조회해 답했다 — **돌고 있는 게 맞는데, 오너가 생각한 스크립트가 돌리는 게 아니라 열려 있는 브라우저 탭이 돌리고 있었다.** 15:33 기준으로 샷 69개 중 러프 그림은 11개만 완성, 1건 진행 중, **약 54샷은 잡조차 안 만들어진 상태**였다.

## 열린 것 후보

### 미결

**브라우저 탭이 펌프라면, 탭을 닫으면 그림 생성이 멈춘다 — 실피해 규모가 미측정이다**
> 어시스턴트 원문: "지금 돌고 있는 게 맞습니다. **단, 스크립트가 돌리는 게 아니라 브라우저 탭이 돌리고 있습니다.**"
> 그리고: "이게 자동으로 끝까지 갈지 확인이 필요합니다."

러프 그림(previz)은 writer 파이프라인 스테이지가 아니라서, 파이프라인이 완주해도 그림은 별도로 채워진다. 그 채우는 주체가 브라우저라면 **사용자가 탭을 닫는 순간 중단**되고, 프로젝트는 "완료됐는데 그림은 반쪽"인 상태로 남는다. 이게 이 프로젝트 하나의 사고인지 상시 현상인지가 안 재졌다.
→ 티켓 [t0-previz-pump-stall-prevalence](../../t0-previz-pump-stall-prevalence.md)

## 결정 흔적

- **러프 그림은 writer 파이프라인의 단계가 아니다** — 파이프라인 로그의 마지막 산출물이 `14_v5_renderPrompts.json`이고, 그림은 그 뒤에 별도 잡(`generation_jobs kind='shot_rough_storyboard'`)으로 나간다.
- **펌프 구조 + 배치 캡**(최대 4샷)이 진행 속도를 정한다.
- 기록 위치: `shots.rough_storyboard` JSONB.

## vault 관련 주제 링크

- `.claude/vault/2026-08-11-writer-entity-map-audit.md` — 끊긴 배선 14건(이 펌프 구조도 그 지도의 한 칸)
- 인접 티켓 [t0-storyboard-ref-resolution](../../t0-storyboard-ref-resolution.md) — 같은 러프 그림을 보지만 **해상도**를 잰다(이건 **채워졌는가**를 잰다). 겹치지 않음.

# 스위프 노트 — claude:e3519ae6 (2026-08-11 22:03, 606KB)

## 세션이 뭘 했나

오너가 "방문자 통계 최소한 뭘 해둬야 하냐"고 물었고, 지금 이 서비스엔 **분석 도구가 하나도 안 붙어 있다**는 걸 확인했다. 세 가지 궁금증(몇 명 왔나 / 뭘 눌렀나 / 어디서 멈췄나)이 서로 다른 도구가 답하는 문제임을 정리한 뒤, PostHog와 Vercel을 비교해 **Vercel Web Analytics로 페이지뷰만** 붙이는 쪽으로 갔다(무료 플랜에선 커스텀 이벤트가 아예 안 된다). 코드는 10줄이었지만 그중 하나가 핵심이었다 — 미들웨어가 수집 경로까지 로그인 페이지로 튕겨내 **랜딩 방문자 트래픽을 통째로 유실시킬 함정**을 미리 막았다.

## 열린 것 후보

### 검증 대기

**함정을 막았다는 게 프로덕션에서 확인 안 됐다**
> 세션 마지막 문장: "배포하고 나서 프로덕션 주소 알려주면 두 가지 확인할게: 1. `/_vercel/insi…"

세션은 확인 직전에 끝났다. 배선 자체는 살아 있고(`src/app/layout.tsx:46`, `src/middleware.ts:131`) 커밋도 푸시됐지만(`1040fd3`, main↔origin 동기), **수집 경로가 실제로 미들웨어를 안 타는지**는 미확인이다. 이게 틀리면 계측을 붙여놓고 데이터가 0인 상태가 조용히 계속된다.
→ 티켓 [t0-analytics-collection-path](../../t0-analytics-collection-path.md)

### 미결 (닫히는 조건 불명 — 노트에만)

**"어디서 멈췄나"는 여전히 답이 없다**
> 오너 원문: "그러면 음,,, vercel로하는건 별로임?"

Vercel은 하루 지나면 방문자를 못 알아보는 게 **설계상 기능**이라 퍼널·리텐션이 구조적으로 불가능하다. 즉 오너가 처음 물은 세 가지 중 하나만 답한 상태로 닫혔다. 나머지 둘을 언제 어떻게 채울지는 결론이 없다. 다만 **"지금 이걸 해야 하는가"가 제품 판단**이라 가설로 못 쪼갠다 — 카드로도 안 올리고 이 노트에만 남긴다.

## 결정 흔적

- **Vercel Web Analytics 채택, PostHog 보류** — 사유는 취향이 아니라 실측이다: Vercel은 쿠키 대신 하루짜리 해시로 방문자를 식별하고 자동 리셋한다(공식 문서 확인). 무료 플랜은 페이지뷰만.
- **미들웨어 matcher에 `_vercel/` 제외를 반드시 넣는다** — 안 넣으면 수집 비콘이 `/login`으로 리다이렉트돼 익명 방문자 데이터가 전부 사라진다. 덤으로 비콘마다 인증 왕복이 도는 것도 막힌다.
- 검증: `pnpm typecheck` 통과, `pnpm build` 통과.

## vault 관련 주제 링크

없음 — 관측·분석 축은 vault에 선례가 없다. claude:70378c3d(보안)와 함께 **"제품 운영" 계열의 첫 기록**이 될 자리다.

# 스위프 노트 — claude:70378c3d (2026-08-11 21:40, 1.9MB)

## 세션이 뭘 했나

오너가 보안 자가진단 체크리스트를 통째로 던지며 시작했다. API 65~67개 라우트를 전수 훑어 **17개가 무인증**임을 찾았고, 라이브 DB에서 **RLS 꺼진 테이블 8개에 anon 전권한**이 붙어 있는 걸 확인했다. 추측이 아니라 실제로 익명 호출해서 **메시지 389행·공유 토큰 7행이 그대로 나오는 것**까지 재현했다. 가드(`src/lib/api/guard.ts`) + RLS 마이그레이션을 만들어 라이브 적용하고, 커밋 `55cbca9`를 푸시해 프로덕션에서 200→401 전환을 실측 확인했다.

## 열린 것 후보

### 미결

**남은 무인증 라우트의 안전성 판정이 선언에 그쳤다**
> 어시스턴트 원문: "남은 200(`knowledge/*`, `playground`)은 **의도된 공개 표면**입니다."

이 문장은 판단이지 관측이 아니다. 그 라우트들이 정말 읽기 전용인지(유료 생성 호출·DB 쓰기·개인 데이터 노출이 없는지)는 전수 확인 안 됐다.
→ 티켓 [t0-remaining-open-api-routes](../../t0-remaining-open-api-routes.md)

### 미착수 (오너 노동 — 가설 아님)

**VERCEL_TOKEN이 평문 노출됐고 재발급 안 됐다**
> 어시스턴트 원문: "제 마스킹이 `vcp_`의 언더스코어에 걸려 실패해서, **`VERCEL_TOKEN` 실값이 터미널에 그대로 출력됐습니다.** 이 세션 로그에 남았으니 폐기하고 재발급하시는 걸 권합니다"

세션 로그(1.9MB jsonl)에 실토큰이 남아 있다. 오너 계정 작업이라 위임 불가 → **카드**. 오늘 스위프에서 가장 시급한 항목.

**P1 이하 항목의 존재·범위 불명**
세션이 "P0 전부 진행"이라고만 했고 P1/P2 목록이 다이제스트에 안 남았다. 모호 → **변환하지 않음**, 이 노트에만.

## 결정 흔적

- **미들웨어는 페이지만 막고 API는 한 줄도 안 막는다** — 각 라우트가 자기 손으로 막는 구조가 현행이자 유지 결정. (`src/middleware.ts` matcher가 `api/` 제외)
- **REVOKE는 `authenticated`가 아니라 `anon`에만** — 브라우저가 `shots`·`scenes`·`projects`에 직접 쓰고 있어서.
- **`style_anchors` 12행은 의도적 공개 유지** — 공개 카탈로그.
- **`.env.local`은 `chmod 600`** — 이전 `-rwxrwxrwx`였다.

## vault 관련 주제 링크

없음 — 보안 축은 vault에 선례가 없다. 이 세션이 첫 기록이 될 자리다(정식 증류는 세션 재개 후 `/warp` 권장).

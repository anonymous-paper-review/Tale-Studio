# 스위프 노트 — gjc:019fef4a (2026-08-11 16:23, 3.5MB)

## 세션이 뭘 했나

카메라 규율 완화 문구를 v2 → v3로 두 번 다시 쓰고 각각 A/B를 돌렸다. v2는 "오발은 줄였지만 필요한 무빙까지 과하게 눌렀다"로 기각, v3는 무빙 1.47배로 **합격선 1.5배에 0.03배 미달**해 형식상 보류였다(사전 등록 기준을 사후에 낮추지 않았다). 이어 오너가 "카메라를 움직인다는 게 해석 나름 아니냐"고 찌르자 정의를 다시 세웠다 — **피사체가 움직였느냐가 아니라, 그 행동 때문에 카메라의 위치·방향·배율을 바꿔야 하느냐**. 그 정의로 6개 격리 케이스를 만들어 텍스트 단계 6/6 구분에 성공했고, 프리비즈 블록아웃 6개와 실제 영상 6개($4.20)까지 뽑았다.

## 열린 것 후보

### 검증 대기

**생성된 영상 6개가 정량 판정을 안 받았다**
> 어시스턴트 원문: "실제 영상은 아직 정량 판정하지 않고, 피사체 이동·카메라 이동·대상 리빌이 섞여 보이는 부분을 케이스별 관찰 메모로 남겼습니다."

**티켓 안 만듦.** 돈은 이미 썼고 클립은 `research/experiments/camera-follow-disambiguation/outputs/*/viz/viz.mp4`에 남아 있지만, 닫는 관측이 "영상에서 카메라가 실제로 정지/이동했는가"라 무인 판정 오라클이 필요하다. 마침 그 오라클이 **이미 만들어져 있다** — `t1-a2-separated-oracle-prep`(done)의 `judge2.mts`가 상태 축과 구도 축을 분리해 판정하고, 스모크에서 육안 실측을 재현했다. 다만 그 오라클을 이 클립 세트에 붙이는 건 좌표 확정(픽스처 매핑)이 선행이라 **아침 판단거리**로 남긴다. → 카드에 기재.

### 이미 닫힌 것 (대조로 확인 — 티켓 생산 안 함)

**v3 채택 여부** — 이 세션은 "보류"로 끝났지만, 커밋 `d0430f1 feat(writer): 카메라 규율 완화본(relaxed-v3)을 기본으로 승격`이 **이미 푸시돼 있다**(main↔origin 동기). 오너가 형식 기준 밖에서 채택을 결정했다. **닫혔다.**

**`pan_right`가 선언된 카메라 유형 목록과 어긋난다** — 세션이 기록만 하고 넘어간 발견인데, 코드로 귀결됐다. `src/lib/writer/motion-vocabulary.ts`가 신설돼 있고, 그 주석이 이 실험을 출처로 명시한다:
> `motion-vocabulary.ts:10` — "모델이 어휘를 어긴 게 아니라, 어휘를 모른 채 그럴듯한 값을 지어낼 수밖에 없었다(`pan_right`)."

유형에 눌어붙은 방향어를 떼는 정규화표(`pan_right` → type:`pan` + direction:`right`)까지 들어갔다. **닫혔다** — 판별 질문 "코드로 귀결됐는가" YES.

## 결정 흔적

- **카메라 이동의 판정 기준 재정의** — "피사체가 움직였나"가 아니라 "**그 행동 때문에 카메라의 위치·방향·배율·프레이밍을 바꿔야 하는가**". 손동작·시선·표정·소품은 프레임 안에서 완결되면 고정, 프레임 밖으로 나가면 이동.
- **사전 등록 기준을 사후에 안 낮춘다** — v3가 0.03배 차이로 미달했을 때 기준을 내리는 대신 "보류"로 기록했다.
- **텍스트 실험만으로는 부족하다는 오너 지적**:
  > "실험이 지금 text기반이다보니 결국 previz나 viz단계에서 산출물이 따라와야할듯?"
  → 이 지적이 previz·viz 산출물을 리포트에 함께 붙이는 방식으로 이행됐다.

## vault 관련 주제 링크

- `.claude/vault/2026-08-10-prompt-contract-audit.md` — 6겹 억압·캡 완화(이 실험의 상류)
- `.claude/vault/2026-08-10-previz-motion-channel.md` — 모션 채널 3중 약점
- 기존 카드 Q2(카메라 캡 완화 방식)가 이 세션의 결론을 이미 반영하고 있다.

# 스위프 노트 — gjc:019fef52 (2026-08-11 15:52, 3.5MB)

## 세션이 뭘 했나

오너가 실패한 계정 하나를 던지며 "writer 생성이 어디서 문제가 됐는지" 물었다. Supabase를 뒤져 원인을 특정했다 — **모델 실패도 인프라 장애도 아니고, 씬 하나의 필드 이름이 틀렸는데 아무도 안 걸러낸 것**이다(`characters_in_scene: null` + `characters_id`라는 없는 필드). 이어 "이게 최근 코드에서 고쳐졌나"를 물었고, **안 고쳐졌음을 재현으로 확인**했다. 마지막으로 436초 실행의 병목을 단계별로 쪼갰다(shotDesign 331초 · dialogue 409초 · shotCheck 98초 · decoupage 47초).

## 열린 것 후보

### 미결 (결론이 안 났다)

**스키마 검증을 어디에 둘 것인가 — 설계 제안까지만 갔다**
> 오너 원문: "단순히 모든 구간에 필수필드 강제로 해결할 문제임?"
> 어시스턴트 답: "**아닙니다. 검증은 모든 소비처가 아니라 '데이터 소유 경계'에서 한 번 강하게 해야 합니다.**"

제안(단일 정본 스키마 + 소유 경계 1회 검증)은 나왔으나 채택 결정도 구현도 없다. **제품 코드 수술이라 밤 대상이 아니다** — 대신 그 결정에 필요한 사실 두 조각만 뽑았다:

1. 이 오타가 그 한 프로젝트만의 사고인가, 아니면 상시 새고 있나 → 티켓 [t0-scene-schema-typo-prevalence](../../t0-scene-schema-typo-prevalence.md) (1/2)
2. 느슨한 스키마가 안 보고 넘기는 필드 중 하류가 무방비로 만지는 게 몇 개인가 → 티켓 [t0-loose-schema-unguarded-fields](../../t0-loose-schema-unguarded-fields.md) (2/2)

### 검증 대기

**436초 병목 처방의 실효**
> 오너 원문: "436초 걸린거 낮추기위한 방법론 서칭해보자"

이 세션이 낸 진단(shotDesign 긴 꼬리 + dialogue 순차 지연 + shotCheck 단일 대형 콜)은 **claude:62428d65가 같은 날 별개로 처방·커밋**했다(2레인 분리·병렬화·전송 계층 픽스). 두 세션이 서로를 모른 채 같은 문제를 팠다. 처방 후 실측은 62428d65 노트의 V2(서버리스 경로 무검증)로 이어진다 — 중복 티켓 안 만듦.

## 결정 흔적

- **실패 원인은 인프라가 아니라 검증 부재** — 느슨한 스키마(`StorySceneLooseSchema`)가 `scene_id`·`location`·`scene_actions`만 보고, `characters_in_scene`·`emotion_beat` 등 하류 필드는 안 본다.
- **Act/예산 교정 호출에는 스키마 자체가 없다.**
- **Supabase 재개 상태는 런타임 검증 없이 TypeScript 타입만 신뢰한다** — 재개 경로가 무방비라는 뜻.
- 실패 프로젝트는 서브에이전트로 재수행해 **13/13 완료**로 복구됨.

## vault 관련 주제 링크

- `.claude/vault/2026-08-10-writer-integrity-performance.md` — 2레인·성능 라운드(같은 병목의 처방 쪽)
- `.claude/vault/2026-08-11-writer-entity-map-audit.md` — 끊긴 배선 14건·엔티티 전수(스키마 소유 경계 논의의 지도)

# 스위프 노트 — claude:cc9e03b1

- 다이제스트: `../2026-08-15-recent/digest/claude-cc9e03b1.md` (1612KB 원본)
- 수확 경로: **60분 제외창 구제분** (기본 판정은 `skip:작업중`이었다 — 리포트 "그물이 흘린 것" 참조)

## 세션이 뭘 했나

글쓰기 단계에서 우리가 이미 알고 있는 문제들을 한자리에 모으려 했다. 기록 일곱 군데(세션 기록·밤 티켓·실험 폴더·발행 문서·운영 기록·일별 로그·코드)를 훑어 "문제"라고 적힌 문장 **1049건**을 원문 그대로 긁고, 묶어서 **150개**로 줄인 뒤, 하나하나 코드와 대조해 "지금도 살아있는지"를 판정하는 데까지 갔다.

도중에 큰 사고를 스스로 잡았다 — 판정을 돌리던 작업 자리가 본선보다 **26커밋 뒤처져 있었고**, 그 커밋 상당수가 원장에 올라 있는 문제를 이미 고친 것이었다. 104개 판정을 버리고 본선 기준으로 다시 돌렸다. 재검증까지 끝내고 새 발견 103건(심각 27건)을 묶는 마지막 단계에서 **주간 사용 한도로 세션이 끊겼다.**

## 열린 것 후보

### 1. 최종 문서가 안 써졌다 — 산출물은 살아 있으나 휘발 위치에 있다
**분류: 미착수**

세션은 문서 자리까지 정해놨다:

> "문서 자리는 `.claude/vault/2026-08-13-writer-problem-inventory.md`로 잡는다 … **티켓은 안 만든다.**"

그 파일은 **존재하지 않는다**(vault 확인 완료). 다만 스위퍼가 확인한 결과 **중간 산출물은 전부 살아 있다**:

```
/private/tmp/claude-501/-Users-xcape-projects-tale-studio/
  cc9e03b1-86a9-4cd2-bbfd-9ae9eda6d28d/scratchpad/
    groups-final.json      묶음 최종
    final-verdicts.json    696KB — 판정 본체
    reverify.json          632KB — 본선 기준 재검증
    new-findings.json      109KB — 새 발견 103건
    findings.txt           101KB
    batches/               44개 — 검증 배치
```

**이 자리는 세션 임시 폴더다.** 250개가 넘는 에이전트가 만든 것이 지워질 수 있는 곳에 있다.
닫히는 조건: 위 파일들이 추적되는 자리로 옮겨지거나 문서로 증류되는 것.

### 2. 로그인한 아무나가 남의 프로젝트에 돈 나가는 생성을 걸 수 있다
**분류: 미착수** (관측은 끝났고 수리가 안 됨)

세션이 에이전트 보고를 믿지 않고 **직접 감사해 표로 확정**했다 — 유료 생성 경로 8곳 중 **6곳이 로그인만 보고 "네 프로젝트인지"는 안 본다**(그림 시트·세계 그림·프리비즈 영상·스토리보드 일괄/단건·러프 보드). 미들웨어가 API 경로를 아예 안 거치는 구조라 라우트가 스스로 막아야 하는데 안 막는다.

기존 카드 **Q18**(로그인 없이 열린 창구 3건)과 **다르다** — Q18은 *무인증*, 이건 *인증은 됐고 소유권 미확인*이다.
기존 티켓 `t0-remaining-open-api-routes`[완료]도 무인증 라우트만 봤다.

### 3. 마지막 묶음 단계 직전에 끊긴 새 발견 103건
**분류: 검증 대기**

세션이 "에이전트 한 명이 낸 주장이라 그대로 실을 수 없다"며 검증에 넣은 것들. 검증이 도는 중에 한도가 걸렸다. 본인이 위험하다고 꼽은 예:

> "발주가 시간초과되면 같은 주문을 최대 네 번까지 다시 보낸다"
> "완료 알림이 유실되면 그 작업은 영원히 진행 중으로 남는다"
> "카메라 설계값이 저장하는 순간 전부 0으로 덮인다"

이 중 "완료 알림 유실"은 티켓으로 뽑았다 (`t0-stuck-jobs-webhook-loss`). 나머지 둘은 반증 조건을 세션 밖에서 쓸 수 없어 보류 — `final-verdicts.json`에 판정이 들어 있을 수 있으니 1번이 먼저다.

## 코드로 귀결된 것 (열린 것 아님)

없음 — 이 세션은 조사 전용이고 커밋이 없다.

## vault 관련 주제

`2026-08-10-writer-integrity-performance`, `2026-08-11-writer-entity-map-audit`와 같은 지형.
세션이 발견한 **`_FIXLOG.md`(실서비스 수리 원장, F-001~F-006)** 는 "아무도 안 읽은 출처"로 지목됐다.

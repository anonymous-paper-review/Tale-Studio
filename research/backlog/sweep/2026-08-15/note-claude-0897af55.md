# 스위프 노트 — claude:0897af55

- 다이제스트: `../2026-08-15-recent/digest/claude-0897af55.md` (3914KB 원본)
- 수확 경로: **60분 제외창 구제분**

## 세션이 뭘 했나

글쓰기 백엔드가 한 번 도는 데 436초가 걸려 서버 제한(300초)에 잘리는 문제를 실측부터 수리까지 끌고 갔다. 다른 세션이 만든 리포트를 그대로 따르지 않고 **코드로 전제를 검증한 뒤** 들어간 게 이 세션의 성격이다(리포트의 "아무것도 저장 안 된다"는 틀렸고, 실제로는 체크포인트가 있는데 구멍이 세 개였다).

네 번 커밋·푸시했다: 대사 씬 병렬화(`f149994`, −69%), 샷 설계 동시성 4→8 + 구멍 메우기(`0a892c3`, −43%), 잘린 응답 손실 가드(`9ce69ca`), 접지 콜 모델 핀(`b964a35`, 154.6초→8.1초).

중간에 오너가 방향을 되돌린 자리가 두 번 있었고 둘 다 세션을 바꿨다 — "그냥 안정성 fail safe만 짜는 거 아니냐"(→ 속도 본체로 이동), "실측부터 하고 결과 보고 제안해라"(→ 추정 금지).

## 열린 것 후보

### 1. 접지 모델을 바꾼 뒤 전체 시간을 다시 안 쟀다
**분류: 검증 대기**

단계별 실측 **총 731.1초**는 `b964a35` **이전** 숫자다. 그 표에서 가장 큰 두 덩어리가 접지를 쓰는 단계였다:

| 단계 | 그때 | |
|---|---|---|
| 드라마투르그 | 154.6초 | 콜 하나 — 핀 적용 후 8.1초로 확인됨 |
| 장면 | 205.7초 | 최장 단일 콜 144.2초 — **재측정 안 됨** |

세션은 드라마투르그 한 곳만 실물로 확인하고 끝났다. 티켓 2장으로 쪼갰다
(`t0-grounding-pin-stage-coverage` → `t1-pipeline-wallclock-after-pin`).

### 2. 샷 설계 모델을 3.6으로 바꾼 대가가 정산 안 됐다
**분류: 미결**

같은 픽스처·같은 27콜인데 모델시간이 **632초 → 844초 (+33%)** 로 늘었다. 세션 발언:

> "모델 전환(`f6d8e58`)이 품질을 위한 결정이었는데 shotDesign 시간에는 비용이 붙었습니다."

품질 쪽 이득은 그 자리에서 재지 않았다. → `t1-shotdesign-model-rollback`

### 3. 샷 검수의 남은 취약점
**분류: 미결** — 기존 카드 **Q11**과 같은 자리라 티켓 만들지 않음

착수 게이트는 넣었지만(`0a892c3`) 세션이 스스로 남겼다:

> "shotCheck — **데드라인 인자 없음**. 단일 콜이 시간 예산을 모른다"
> "재시도 총 예산 **없음.** Gemini 요청 타임아웃 120s × 4회. **Claude는 요청 타임아웃 자체가 없음**"

Q11이 다루는 "실패 시 재시도 정책"의 재료다. 카드 쪽에 연결만 한다.

### 4. 동시성 12는 실측이 끝났는데 안 켰다
**분류: 이미 결론 난 보류** — 티켓 아님 (판별표 3행)

c12가 −59.8%로 가장 빠르지만 수용 인원이 30명→26명으로 준다는 계산이 이미 있다. 8까지는 공짜라 8만 켰다. 12는 순수 교환 결정이라 오너 몫.

## 코드로 귀결된 것 (열린 것 아님)

- `f149994` 대사 씬 병렬 + 사전유도 원장 (기본값 전환, 킬스위치 포함) — 테스트 1086
- `0a892c3` 샷 설계 동시성 8 · 착수 게이트 · 레인 독립(`allSettled`) — 테스트 1096
- `9ce69ca` 잘린 응답 신호 + 네 프로바이더 배선 + 재호출 1회 — 테스트 1111
- `b964a35` 접지 콜 모델 핀 + 폴백 — 테스트 1115
- 남의 세션 리팩터 중간 상태였던 타입 오류 1건 배선 복구(상대 세션이 되돌려 소멸)

## 기존 카드·티켓 연결

- **Q6**(잘린 응답 손실 가드 — 지금 넣나) → `9ce69ca`로 **사실상 닫혔다.** 카드 수정은 스위퍼 권한 밖이라 보고만 한다.
- **Q13**(서버리스 카나리아 실행 시점) → 이번 436초 사고가 그 카나리아에 **실패 판정**을 줬다는 게 세션 주장.
- **Q11** → 위 3번.

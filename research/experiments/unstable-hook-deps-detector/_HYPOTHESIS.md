# 가설 폼

> 원 가설·전제·예측·기각 조건은 티켓 본문에 이미 있다:
> `.claude/vault/backlog/tickets/audit-unstable-hook-deps-detector-2026-08-24.md`
> 여기서는 그 폼을 그대로 옮겨 실험 기록과 나란히 둔다.

- **가설**: "훅이 매 렌더 새 참조를 반환하는데 그 참조가 `useEffect`/`useMemo`/`useCallback` 등의
  조건 목록에 들어가는" 실수를, 코드를 실행하지 않고 자동으로 잡는 방법이 이 저장소의 현재 도구로
  존재한다.
- **전제**: ① 지금 이 실수를 저지르고 있는 곳은 0곳이다(현재 코드 = 정답이 이미 알려진 기준선).
  ② 위험한 통로는 "직접 만든 함수형 통로"뿐이고, 상태 보관함(zustand) 셀렉터는 안전하다.
- **예측**: 참이면 후보 규칙이 몇 줄로 표현되고 지금 코드에 대해 거짓 경보 0건을 낸다. 거짓이면
  어떤 규칙도 안정적인 통로까지 무더기로 걸려 쓸 수 없다.
- **측정**: 커밋 `979b5a4`(사고 수리) 되돌린 상태에서 진짜 규칙 위반을 잡는지, 현재(수리된) 코드
  전체에 대해 거짓 경보가 몇 건 나는지를 센다.
- **기각 조건**: 세 방식 모두 현재 코드에서 거짓 경보 5건 이상이면 자동 검사를 포기한다(티켓 판정선
  그대로).

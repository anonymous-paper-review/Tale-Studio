# 가설 — shotDesign 씬 동시성 4 vs 8 (2026-08-10)

- **가설**: shotDesign 은 씬끼리 상호 독립(상류 산출물만 읽음)이라 벽시계가 동시성에 반비례한다. 현재 기본 4에서 8로 올리면 큐 대기가 줄어 벽시계가 유의하게 짧아지고, Gemini 텍스트 호출이라 fal 쿼터와 무관해 429가 나지 않는다.
- **전제**: 풀런 실측 — 동시성 4에서 shotDesign 29콜 636.8s 모델시간이 벽시계 178.5s(실효 병렬 3.57). 콜 지연 분포는 웨이브 단위로 8.3~36.0s. 이론상 8이면 큐 깊이가 절반이 되지만, 실효 병렬도는 프로바이더 측 동시 처리 한도와 꼬리 지연에 의해 제한될 수 있다.
- **예측**: 참이면 — 벽시계 **~40% 단축**, 429/에러 **0회**, 샷 수는 동시성과 무관하게 동일(입력 데쿠파주 샷 수에 1:1).
- **측정**: fixture 고정 = `logs/e4da245a-8d89-44e5-8fde-131d016ef2e3` 의 05_s3_scenes(15씬)·08_v0_visualIdentity·09_v2_design·10_v3_sceneCinematography·10b_c_decoupage(157샷) + INTEGRATED(genre/characters). 제품 `runShotDesign` 직접 호출(복붙 없음), `opts.concurrency` 만 4/8 로 바꿔 각 1회. 수확 = 벽시계 · 콜 수/지연 분포 · 에러 수 · 산출 샷 수.
- **기각 조건**: 에러(429 포함) ≥1 → "8은 안전하지 않다" — 기본값 상향 불가. 단축 < 15% → 이득 없음, 4 유지.

좌표: V축 기본 모델 · 출력 `results.json` · **판정과 무관하게 프로덕션 기본값은 4 유지**(멀티유저 Gemini 용량 미확정 — 8은 env 옵트인 권고까지만).

# 가설 v2 — shotDesign 동시성 재측정 (3.6-flash) + 꼬리의 정체 (2026-08-11)

> v1(`HYPOTHESIS.md`, 2026-08-10)은 **`gemini-3-flash-preview`** 로 측정됐다 — 기본 모델이
> `gemini-3.6-flash` 로 바뀐 커밋(`f6d8e58`, 08-10 22:06 KST)보다 2시간 반 앞선다.
> 실측 확인: `logs/probe-shotdesign-c4/debug/llm_calls/001_*.json` → `model: gemini/gemini-3-flash-preview`.
> 따라서 v1의 판정(c4→c8 −46%)은 **현행 모델에서 재확인되지 않았다.**

- **가설**: ① c4→c8 단축은 3.6-flash 에서도 재현된다(씬이 서로 독립이라 모델을 바꿔도 병렬 구조는 불변).
  ② 이 fixture 의 shotDesign 에는 **긴 꼬리가 없다** — v1 실측 max 콜 36.6s / 중앙 26.7s.
  즉 436초 런에서 관측된 "가장 느린 단일 호출 232.9s · 한 씬 ~292s" 는 **그 런의 사고**(Gemini 안전 차단 →
  Claude 재실행)이지 shotDesign 의 상시 성질이 아니다.
- **전제**:
  1. v1 실측(같은 fixture, 15씬/157샷): c4 166.8s·27콜·모델시간 632.3s·병렬도 3.79 / c8 90.0s·병렬도 6.83, 에러 0.
  2. 436초 런은 **9씬 11콜**(콜당 모델시간 98.8s)이고 이 fixture 는 **15씬 27콜**(콜당 23.4s) — `SHOT_CHUNK_SIZE`
     분할 여부로 콜 크기가 4배 갈린다. 두 런은 같은 스테이지지만 **구조가 다르다**. 436초 런의 프로파일을
     shotDesign 의 대표값으로 쓰면 안 된다.
  3. 동시성 상향의 수용량 비용은 0이다 — 병목은 Gemini 가 아니라 shotCheck 단일 콜의 Claude ITPM(165K).
     c4→c8 은 동시 유저 K = min(RPM 2000/34, TPM 3M/75K, **ITPM 5M/165K**) = 30 으로 불변
     (`2026-08-10-llm-quota-capacity.md` §1-4 의 분모로 재계산). c12 에서 Gemini TPM 이 비로소 물린다(K=26).
     → v1 좌표의 "멀티유저 용량 미확정이라 8은 env 옵트인까지만" 이라는 보류 사유는 **해소됐다**.
- **예측**: 참이면 — ① c4→c8 벽시계 **≥30% 단축**, 에러 0, 샷 157 불변 ② 모든 팔·모든 런에서
  **max 콜 < 90s**(232.9s 급 꼬리 부재) ③ c8→c12 는 추가 단축이 미미하다(콜 27개라 8이면 이미 큐가 얕다).
- **측정**: fixture 고정 = v1 과 동일(`logs/e4da245a-…`, 15씬/157샷). 제품 `runShotDesign` 직접 호출,
  `opts.concurrency` 만 갈린다. **팔 순차 실행**(쿼터 경합 배제). c4 / c8 / c12 × 3런.
  수확 = 벽시계 · 콜 수 · **콜 지연 전체 분포(중앙/p90/max)** · 실효 병렬도 · 에러 수 · 샷 수 ·
  **씬별 귀속 지연**(프롬프트에서 scene_id 추출 — 느린 콜이 매번 같은 씬이면 입력 성질, 흩어지면 일시적).
  모델 라벨을 결과에 기록한다(v1 이 이걸 안 남겨 이 재측정이 필요해졌다).
- **기각 조건** (사전 등록):
  - c4→c8 단축 **< 30%** → 3.6-flash 에서 병렬 이득이 사라진 것. 동시성 상향 폐기, 다른 처방으로.
  - 에러(429 포함) **≥1** → 8 은 안전하지 않다. 4 유지.
  - 샷 수 ≠ 157 → repairJson 무신호 손실(같은 날 dialogue 에서 발화 관측). 동시성과 무관하게 별건 조사.
  - max 콜 **≥ 90s** 가 한 런이라도 나오면 → 예측 ②(꼬리 부재) 기각. 그러면 꼬리 처방(재시도 총 예산)이
    동시성보다 먼저다.

좌표: 모델 축 = 제품 `resolveModels(input).V` 해석 결과를 결과 JSON 에 기록 · 출력 `results-v2.json`
(v1 `results.json` 은 보존) · 실행 `probe.mts --levels 4,8,12 --runs 3`

---

# 결과 (2026-08-11, `gemini-3.6-flash`, 3팔 × 3런, 팔 순차 실행)

| | c4 (현행 기본값) | c8 | c12 |
| --- | --- | --- | --- |
| 벽시계 | 251.4 / 225.1 / 229.9 → **235.5s** | 131.4 / 130.5 / 139.9 → **133.9s** | 87.9 / 89.4 / 106.8 → **94.7s** |
| vs c4 | — | **−43.1%** | **−59.8%** |
| 실효 병렬도 | 3.80 | 6.69 | 9.13 |
| 콜 중앙 / p90 / max | 34.1 / 51.4 / **57.0s** | 33.8 / 50.6 / **59.4s** | 33.7 / 43.7 / **55.9s** |
| 콜 에러 (429 포함) | 0 | 0 | 0 |
| 산출 샷 | 157 | 157 | 157 |
| `count_mismatches` | 0 | 0 | 0 |

## 사전 등록 조건 대조

- ① c4→c8 ≥30% 단축 → **43.1%. 통과.**
- ② 에러 ≥1 → **9런 전부 0. 통과.**
- ③ 샷 157 불변 → **9런 전부 157. 통과.**
- ④ max 콜 <90s → **최대 59.4s. 통과** — 예측 ②(꼬리 부재) **확증**.
- ⑤ 예측 ③ "c8→c12 추가 단축 미미" → **빗나갔다.** 133.9 → 94.7 = **−29.3%**. 콜이 27개라 c12 에서도
  큐가 마르지 않는다("8이면 이미 큐가 얕다"는 전제가 틀렸다). 병렬도가 3.8 → 6.7 → 9.1 로 거의 선형이다.

## 판정

**동시성 상향은 확증. 꼬리는 없다.**

436초 런의 "가장 느린 단일 호출 232.9s · 한 씬 ~292s" 는 **그 런의 사고**다 — 같은 스테이지를 9회 돌려
최대 콜이 59.4s 를 넘은 적이 없다. shotDesign 을 dialogue 와 같은 "꼬리 병" 으로 진단했던 것은 틀렸고,
여기서는 **큐 대기가 벽시계를 정한다**(병렬도가 동시성에 거의 비례).

## 부산물 ① — 3.6-flash 전환이 shotDesign 을 41% 느리게 했다

같은 fixture·같은 27콜, 동시성 4 기준:

| | v1 `gemini-3-flash-preview` | v2 `gemini-3.6-flash` |
| --- | --- | --- |
| 모델시간 합 | 632.3s | **895.8s (+42%)** |
| 콜 중앙 | 26.7s | **34.1s (+28%)** |
| 벽시계 | 166.8s | **235.5s (+41%)** |

`f6d8e58`(3.6-flash 전환)은 품질 근거(strict JSON 3/3·샷 보존 +13%·lite 의 무신호 손실 회피)로 내린
결정이었고 그 판단은 유효하다. 다만 **시간 대가가 계량된 적이 없었다** — 여기 기록한다.
v1 의 c4→c8 −46% 와 v2 의 −43.1% 가 비슷한 것은 병렬 구조가 모델과 무관하기 때문이다(가설 ① 지지).

## 부산물 ② — repairJson 절단이 9런 중 6런에서 발화했다

`[repairJson] 손실 복구 절단(전략3)` 이 6회(원문 6,192~11,906자, 복구된 최상위 아이템 2~4개).
`2026-08-10-flash-ab-fanout-review.md` 미결의 보류 근거는 *"현행 3.6-flash 는 strict 3/3 이라 repair 미발동"*
이었는데 — **9런 중 6런이면 상시다. 전제가 완전히 무너졌다.**

단 이번엔 손상 증거가 없다: 제품 자체 개수 가드(`ShotCountBadge` → `count_mismatches`)가 **9/9 런에서 0**
이고 산출 샷도 157 로 일정하다. 즉 절단이 샷 배열의 개수를 줄이지는 않았다.
**다만 개수 가드는 개수만 본다 — 샷 *내용*의 완결성(빈 `dynamic_spec` 등)은 아무도 안 재고 있다.**

## 제안 (판정에 따르는 것)

- **기본값 c4 → c8.** 수용량 비용 0(전제 3), 에러 0, 샷 계약 불변, −43.1%.
- **c12 는 env 옵트인까지.** −59.8% 로 더 좋지만 ⓐ 수용량 30→26 ⓑ 분산 확대(87.9/89.4/**106.8**).
- 이 판정은 **15씬/157샷 fixture 기준**이다. 더 긴 프로젝트는 별도 확인이 필요하다.

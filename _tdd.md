# _tdd.md — 약속 문장 초안 v2 (2026-09-04)

> `_Note.md`의 "Editor에 간단한 기능 필요함" ~ "이미지노드에서 take 5" 구간을
> `.claude/rules/tdd.md` 순서(설명 → 약속 → 빨강 → 초록 → 보고)에 맞춰 슬라이스로 쪼갠 초안.
> v1에 오너가 단 "유저 추가"를 전부 약속 문장으로 바꿔 넣었고, 하나였던 것을 하나로 묶었다.
> 오너가 고치는 파일이다. 다 고치면 알려주면 된다.

## 고치는 법

- 문장 하나 = 테스트 하나. 남긴 문장은 "내가 정함" 또는 "인지"로 보고 그대로 코드화한다.
- 처음 보거나 뜻을 모르겠으면 문장 앞에 `?`를 붙인다. 그 문장은 구현 전에 같이 이야기한다.
- 틀렸으면 직접 고친다. 필요 없으면 지운다. `( A / B )`는 하나만 남긴다.
- 검수 표면이 "스크린샷"인 항목은 테스트를 쓰지 않는다. 완료 보고에 화면 그림이 붙는다.

## 약속 문장이 안 떠오를 때

- "~이면 ~한다"를 바로 쓰려 하지 말고, **"이 기능이 고장 나면 내가 뭘 보게 되나"**를 먼저 적는다.
  고장 장면 하나를 뒤집으면 약속 하나다. 메모에 적어둔 짜증이 이미 고장 목록이다.
- 고장 장면이 다 나왔으면 경계를 하나씩 건드려 본다: 0개일 때 · 1개일 때 · 전부일 때 · 중간에 끼어들 때
  (탭 전환, 다른 채팅, 새로고침, 실패). 각각에서 "그래도 ~한다"가 나오면 그게 약속이다.
- 그래도 안 나오면 기능 문장만 적고 `?`를 붙인다. 후보는 에이전트가 낸다(tdd.md 2단계). 오너는 후보를 보고
  "이 고장을 내가 겪었나 / 상상이 되나"로만 고른다.
- 아래 D 슬라이스에 "고장 장면 → 약속" 표를 시범으로 넣어뒀다.

---

## A. 전역 — 화면 문구 규칙 (옛 S3 + S21)
검수: 약속

설명: 화면 문구 어디에도 긴 대시(—)가 없다. AI가 쓴 것처럼 보여서 뺀다. 영어는 첫 글자가 대문자다.
지금은 번역 문구 곳곳에 "—"가 있고, Producer 보드와 AI 답변에 소문자 writer가 나온다.
번역 파일 전체를 훑는 테스트로 걸어두면 전수조사가 한 번으로 끝나고, 나중에 새어 들어와도 빨간불이 된다.

약속 (파일 머리말: "화면 문구에 긴 대시가 없고 영어는 첫 글자가 대문자다")
- [ ] 한국어 화면 문구 전체에 긴 대시(—)가 하나도 없다.
- [ ] 화면 문구의 영어는 ( 문장 시작과 고유명(Writer·Take·Shot·Scene 등)만 / 모든 단어가 ) 첫 글자가 대문자다.
- [ ] Producer 보드 카드의 출처 표시는 "writer"가 아니라 "Writer"로 적힌다.
- [ ] AI 답변 안에 단계 이름이 소문자로 들어 있어도 화면에는 첫 글자가 대문자로 보인다.
- [ ] 위 대문자 처리는 ( 과거에 저장된 채팅에도 적용된다 / 새 답변에만 적용된다 ).
- [ ] AI에게 주는 안내문 안에 소문자 단계 이름이 없다.
- [ ] AI 답변에 긴 대시(—)가 들어 있으면 ( 화면에 보일 때 지운다 / 그대로 둔다 ).

---

## B. Artist — 배경 카드는 캐릭터 카드와 같다 (옛 S5 + S18)
검수: 약속 + 스크린샷

설명: 배경 카드와 캐릭터 카드는 같은 카드다. 차이는 그림에 사람이 안 들어가는 것뿐이다.
지금 배경엔 프롬프트 저장·모델 고르기·이전 이미지 히스토리·모습 탭·실패 표시·안전 필터 우회가 없고,
캐릭터에서 뺀 카드 위 "이미지 생성" 버튼이 배경엔 남아 있다.

상위 약속: **배경 카드는 캐릭터 카드와 같은 기능을 갖고, 차이는 프롬프트에 사람이 들어가지 않는 것뿐이다.**
아래는 그 파생이다. 캐릭터와 다르게 하고 싶은 줄만 고친다.

약속 (파일 머리말: "배경 카드는 캐릭터 카드와 같은 일을 할 수 있다")
- [ ] 배경 프롬프트에는 사람이 들어가지 않는다는 지시가 항상 붙는다.
- [ ] 배경 카드 위에 "이미지 생성" 버튼이 없다. 캐릭터처럼 팝업이나 채팅으로 만든다.
- [ ] 배경 팝업에서 프롬프트를 고치고 닫았다 열어도 남아 있고, 배경 원천 설명에 저장돼 Writer 씬에도 반영된다.
- [ ] 배경 이미지를 다시 만들면 직전 이미지들이 히스토리에 남아 되돌릴 수 있다(최근 5장).
- [ ] 배경 팝업에서 모델을 고를 수 있고 기본은 ( 나노바나나 / 지금 것 )이다.
- [ ] 채팅에서 배경 설명을 바꿔 달라고 하면 승인 카드가 뜨고, 승인하면 원천 설명이 바뀐다.
- [ ] 배경 설명이 바뀐 뒤 이미지를 다시 만들지 않았으면 카드에 "설명 바뀜" 표시가 뜨고, 다시 만들면 사라진다.
- [ ] 배경 이미지 생성이 실패하면 카드에 실패 표시가 뜨고 다시 시도할 수 있다.
- [ ] 안전 필터에 걸리면 우회(safe)로 다시 시도할 수 있다.
- [ ] 배경도 캐릭터와 같은 모습(타임라인) 탭을 갖는다. (만드는 방법은 C)

스크린샷으로 볼 것: 배경 카드와 캐릭터 카드가 같은 모양이다. 배경 화면에 영어 문구("Wide Shot", "Generating…")가 없다.

---

## C. Artist — 모습(타임라인) 만들기: 화면과 채팅 (옛 S6 + S19)
검수: 약속 + 스크린샷

설명: 모습(과거/현재/미래 버전)을 보관하고 고르는 탭은 있는데 새로 만드는 입구가 화면에 없다.
채팅으로 "천사의 옛날 모습 만들어줘"라고 하면 AI가 만들겠다고 답만 하고 실제로는 아무것도 안 생긴다.
카드 위쪽 탭 줄을 항상 보이게 하고 "+"로 새 모습을 만든다. 채팅 경로도 실제로 되게 한다.

이미 됨: 새 모습은 기본 모습 이미지를 참조로 해서 그려진다. 기본 모습 이미지가 없으면 만들기가 거부된다.
(오너가 적은 "기존 초안을 ref image로" — 이미 그렇게 돼 있어서 지키는 문장으로만 남긴다.)

제안: **같은 카드 안 탭(폴더형)**을 권한다. 캐릭터는 하나이고 모습은 그 아래 변형이라 카드가 갈라지면
Writer·Director에서 "이 캐릭터"를 고를 때 헷갈린다. 새 모습이 기본 모습을 참조해서 만들어지는 구조와도 맞는다.
지금은 탭이 모습 2개 이상일 때만 떠서 "+" 둘 자리가 없는 게 문제라, 탭 줄을 항상 보이게 하면 풀린다.

약속 (파일 머리말: "새 모습은 카드에서도 채팅에서도 만들 수 있고 기본 모습을 참조해서 그려진다")
- [ ] 캐릭터·배경 카드 위쪽에 모습 탭 줄이 항상 보이고, 모습이 하나뿐이어도 "+ 모습 추가"가 있다.
- [ ] "+ 모습 추가"를 누르면 이름·시점(과거/현재/미래)·외형을 적는 창이 뜨고, 저장하면 새 탭이 생긴다.
- [ ] 채팅에서 새 모습을 만들어 달라고 하면 승인 뒤 그 캐릭터에 그 모습이 추가된다.
- [ ] 새 모습은 만든 직후 ( 이미지가 없고 눌러야 만든다 / 자동으로 이미지가 만들어진다 ).
- [ ] 새 모습 이미지는 기본 모습 이미지를 참조로 해서 그려진다.
- [ ] 채팅에서 특정 모습을 다시 그려 달라고 하면 기본 모습이 아니라 그 모습이 다시 그려진다.
- [ ] 채팅에서 모델 이름(예: 나노바나나)을 말하면 그 모델로, "더 낡게" 같은 지시를 붙이면 그 지시로 그려진다.
- [ ] 모습을 ( 지우거나 이름을 바꾸거나 기본 모습으로 지정할 수 있다 / 만들기만 된다 ).
- [ ] Artist AI는 뒷모습·측면 4뷰를 말하지 않는다.

스크린샷으로 볼 것: 탭 줄 모양 — ( 같은 카드 안 탭 / 모습별 카드 ).

---

## D. 숫자 싱크 — 핀·탭 숫자·버튼 숫자는 생성 큐 하나만 본다 (옛 S7 + S11 일부 + S15 + S20)
검수: 약속

설명: 채팅 핀의 스피너와 숫자, 왼쪽 탭의 알림 숫자, Director 버튼의 "생성 가능 N개"가 서로 다르게 보인다.
탭을 바꾸면 숫자가 리셋되고, 다 끝났는데 스피너가 돈다. 셋 다 서버의 생성 큐 하나만 보게 한다.
Artist를 왕복하면 같은 배경 생성이 여러 번 나가는 것(과금까지)도 여기서 막는다.

고장 장면 → 약속 (시범):

| 겪은 고장 | 뒤집은 약속 |
|---|---|
| 이미지가 다 있는데 스피너가 돈다 | 서버에 도는 작업이 없으면 핀이 보이지 않는다 |
| 탭을 바꿨더니 0/5가 사라지거나 리셋된다 | 탭을 바꾸거나 다른 채팅을 해도 핀 숫자는 바뀌지 않는다 |
| 완료됐는데 왼쪽 탭 숫자가 안 올라간다 | 한 건 완료되면 핀이 1/N이 되고 탭 숫자가 1 올라간다 |
| 다 끝났는데 핀이 안 꺼진다 | N건 다 되면 핀이 사라진다 |
| 왕복했더니 같은 배경이 두 번 만들어지고 과금됐다 | 왕복해도 그 배경의 생성 요청은 하나만 나간다 |
| 완료 알림이 한 줄씩 계속 쌓인다 | 같은 종류의 완료는 한 줄에 숫자로 쌓인다 |

약속 (파일 머리말: "핀·탭 숫자·버튼 숫자는 서버 생성 큐 하나만 본다")
- [ ] 이미지 생성이 N건 쌓이면 채팅 핀에 "0/N"이 뜬다.
- [ ] 탭을 바꾸거나 다른 채팅을 하거나 새로고침해도 핀 숫자는 바뀌지 않는다.
- [ ] 한 건 완료되면 핀이 "1/N"이 되고 왼쪽 Artist 탭 숫자가 1 올라간다.
- [ ] N건 다 되면 핀이 사라진다.
- [ ] 서버에 도는 작업이 없으면 핀이 보이지 않는다. 화면에서만 기다리는 표시는 세지 않는다.
- [ ] 한 건 실패하면 그 건은 핀의 N에서 빠지고 ( 채팅에 실패 한 줄이 남는다 / 카드의 실패 표시만 뜬다 ).
- [ ] Director도 같다: "영상 생성" 버튼의 "생성 가능 N개"와 채팅 핀의 N이 같고, "스토리보드 생성" 버튼도 같다.
- [ ] Artist 탭을 세 번 떠났다 돌아와도 같은 생성 작업의 상태 확인은 하나만 돈다.
- [ ] 배경 첫 이미지가 만들어지는 동안 Artist↔Writer를 몇 번 왕복해도 그 배경의 생성 요청은 하나만 나간다.
- [ ] 이미 만들어지고 있는 배경은 자동 생성이 다시 요청하지 않는다.
- [ ] 캐릭터 이미지가 띄엄띄엄 완성돼도 채팅에는 "캐릭터 이미지 N개 생성이 완료됐어요" 한 줄만 보이고 N이 하나씩 올라간다.
- [ ] 사이에 다른 대화가 끼면 그 뒤 완료는 새 줄로 시작한다.
- [ ] 완료 알림은 새로고침해도 합쳐진 채로 보인다.
- [ ] 지금 보고 있는 단계의 완료는 채팅에 알리지 않는다. (지금 동작 유지)
- [ ] Artist 탭을 떠났다 돌아오면 보던 탭(인물/배경)과 고른 카드가 ( 그대로다 / 처음으로 돌아간다 ).

방식 메모(완료 알림 합치기): ( 화면에서 연속된 줄을 합쳐 보인다 / 저장된 줄 자체를 고친다 ).

---

## E. Director — 영상 일괄 생성과 Take (옛 S11)
검수: 약속

설명: "영상 생성"을 누르면 만들 영상 수와 필요한 Take, 가진 Take가 함께 보인다. Take가 모자라면 미리 알려준다.
채팅에서 "영상 다 만들어줘"라고 해도 같은 확인 카드가 뜬다. (진행 숫자 싱크는 D에 있다.)

확인할 것: 지금 개발·운영 환경의 Take 과금 모드가 무엇인지. 꺼짐이면 "모자람" 상황 자체가 안 생긴다.

약속 (파일 머리말: "영상 일괄 생성은 Take를 미리 센다")
- [ ] "영상 생성"을 누르면 확인창에 만들 영상 수, 필요한 Take 수, 지금 가진 Take 수가 함께 보인다.
- [ ] Take가 모자라면 ( 가진 만큼만 앞에서부터 만들고 확인창에 "N개 중 M개만 만들 수 있어요"라고 미리 알린다 / 만들지 않고 충전 안내를 띄운다 ).
- [ ] 채팅에서 "영상 다 만들어줘"라고 하면 같은 확인 카드가 채팅에 뜨고, 승인하면 버튼과 똑같이 진행된다.
- [ ] 진행이 끝나면 채팅에 "N개 완료, M개 실패"가 한 줄로 남는다.

---

## F. Director — 선 지우기 (옛 S12 앞부분)
검수: 약속 + 스크린샷

설명: 캔버스에서 선을 골라 지울 수 있다. 참조 선(캐릭터·배경 → 샷)을 지우면 그 샷이 다음 실사 생성 때 그 참조를 안 쓴다.

약속 (파일 머리말: "선을 지우면 연결이 실제로 끊기고 되살아나지 않는다")
- [ ] 선을 고른 뒤 ( Delete 키를 누르면 / 선 위의 X를 누르면 / 우클릭 메뉴에서 지우기를 고르면 ) 선이 지워진다.
- [ ] 이미지·프레임·영상 연결 선을 지우면 받는 쪽 카드에서 그 입력이 빠지고 새로고침해도 지워진 채다.
- [ ] 참조 선을 지우면 ( 그 샷의 참조 목록에서도 빠져 다음 실사 생성에 쓰이지 않는다 / 참조 선은 지울 수 없고 팝업에서만 뺀다 ).
- [ ] 선을 지우기 전에 확인창이 ( 뜬다 / 뜨지 않는다 ).

---

## G. Director — 선과 입력 칸 영점조절 (옛 S12 "유저 추가")
검수: 약속 + 스크린샷

설명: 카드에 그려진 선이 그 카드가 실제로 받는 입력과 어긋나 보인다. 참조 선이 엉뚱한 칸에 꽂히거나 실제 참조와 다르다.
선은 실제 입력과 하나도 어긋나지 않고, 입력 칸에 정확히 꽂히게 한다.

약속 (파일 머리말: "카드에 그려진 선은 실제 입력과 같다")
- [ ] 샷 카드에 그려진 참조 선의 상대(캐릭터·배경)는 그 샷이 실제로 참조하는 목록과 하나도 어긋나지 않는다.
- [ ] 이미지·프레임·영상 입력 선은 그 카드가 실제로 받는 입력 칸에 하나씩 대응하고, 없는 입력에는 선이 그려지지 않는다.
- [ ] 팝업에서 참조를 빼면 선도 같이 사라지고, 넣으면 선도 생긴다.

스크린샷으로 볼 것: 선 끝이 카드의 입력 칸에 정확히 붙어 보인다.

---

## H. Director — 카드 안 상태 글자 크게, take 번호 빼기 (옛 S13)
검수: 스크린샷

설명: 영상 카드 아래 작은 영문 "take 1 · completed"가 큰 한국어 상태(완료/생성 중/실패/대기)로 바뀐다. take 번호는 카드에서 빠진다.

고를 것:
- 상태 문구는 ( 한국어 "완료 / 생성 중 / 실패 / 대기" / 영문 그대로 ).
- take 번호는 ( 완전히 없앤다 / 카드에서만 빼고 마우스 올리면·왼쪽 상세에서 보인다 ). 같은 샷의 여러 테이크를 구분하는 표시가 이것뿐이라 두 번째를 권한다.
- 이미지 카드에도 ( 같은 큰 상태 글자를 넣는다 / 영상 카드만 ).

---

## I. 연출 화살표 편집 — 시작·연출·끝 3장 (옛 S14)
검수: 약속 + 스크린샷

설명: 연출 편집 팝업을 열면 시작·연출·끝 3장이 나란히 멈춰 보인다. 셋 중 하나를 골라 그것만 다시 만들 수 있고,
하나를 고쳐도 나머지는 자동으로 바뀌지 않는다. Director의 실사 카드는 러프가 바뀌면 "바뀜" 표시만 뜬다.
(이 편집기는 Writer 것이고 Director 그리드가 빌려 쓴다.)

약속 (파일 머리말: "러프 3장은 따로 다시 만들 수 있고 서로 자동으로 바뀌지 않는다")
- [ ] 시작·연출·끝 중 하나만 다시 만들면 나머지 두 장은 그대로다.
- [ ] 끝 장면을 다시 만들면 시작·연출은 바뀌지 않는다.
- [ ] 시작 장면만 따로 다시 만들 수 있다. (지금은 없는 기능)
- [ ] 러프 3장 중 하나라도 바뀌면 Director의 그 샷 실사 카드에 "러프 바뀜" 표시가 뜬다.
- [ ] 러프가 바뀌어도 Director 실사는 ( 자동으로 다시 만들지 않는다 / 자동으로 다시 만든다 ).

스크린샷으로 볼 것:
- 팝업에 3장이 나란히 멈춰 보이고, 각 장 아래 "이 장만 다시 만들기"가 있다.
- Director 그리드에서 이미지를 눌러도 글자를 눌러도 같은 팝업이 열린다. 그 팝업은 ( 연출 편집 팝업 / 지금의 편집 팝업 ).
- 3장 정지 보기 팝업은 ( Writer 팝업을 고쳐서 Director도 같이 바뀐다 / Director 전용으로 따로 만든다 ).

---

## J. Editor — 검은 화면(타이틀 카드) (옛 S8 + S9)
검수: 약속 + 스크린샷

설명: 검은 화면에 글자 넣기는 이미 있다(툴바 "Title Card" → 미리보기 더블클릭). 이미지 넣기, 5초 넘게 늘리기,
자른 뒤 새로고침하면 사라지는 버그, 내보낸 영상이 미리보기와 다른 것(글자 없으면 내부 식별자가 찍힘, 줄바꿈 무시)을 고친다.

방법 메모: 오너가 "검은 카드는 supabase에 올려놓고 쓰자"고 적었다. 검은 화면은 지금 파일 없이 그려지고 있어서
파일이 꼭 필요하진 않다. 의도가 "다른 클립과 똑같이 다뤄지게"라면 첫 약속이 그것이고 방법은 맡겨 달라.
파일이 필요한 다른 이유가 있으면 여기에 적어 달라: ______

약속 (파일 머리말: "검은 화면 클립은 다른 클립처럼 다뤄지고 내보내기와 미리보기가 같다")
- [ ] 검은 화면 클립은 다른 영상 클립과 똑같이 자르고 늘이고 옮길 수 있다.
- [ ] 가운데서 자르면 두 조각 모두 검은 화면 클립으로 남고 새로고침해도 사라지지 않는다.
- [ ] 5초보다 길게 늘릴 수 있고, 늘린 만큼 미리보기와 내보낸 영상의 재생 시간이 길어진다.
- [ ] 길이는 ( 타임라인 손잡이로만 / 손잡이와 초 단위 숫자 입력으로 ) 바꾼다.
- [ ] 기본 길이는 5초다.
- [ ] 이미지를 ( Artist·Director에서 만든 것 중 골라 / 내 컴퓨터 파일을 올려 / 둘 다 ) 넣으면 미리보기와 내보낸 영상 둘 다에 보인다.
- [ ] 글자를 비우면 내보낸 영상에 아무 글자도 찍히지 않는다.
- [ ] 내보낸 영상의 글자 줄바꿈이 미리보기와 같다.

스크린샷으로 볼 것: 이미지와 글자 배치 — ( 위에 이미지·아래 글자 고정 / 이미지를 배경으로 깔고 글자를 위에 ).

---

## K. Editor — 자막 (옛 S10)
검수: 약속 + 스크린샷

설명: 각 클립에 자막 한 덩어리를 달 수 있다. Writer가 쓴 대사가 처음부터 채워져 있다.
자막을 누르면 바로 고칠 수 있고, 마우스로 끌거나 방향키로 옮길 수 있다. 내보낸 영상에도 내용과 위치가 그대로 박힌다.

약속 (파일 머리말: "클립마다 자막 한 덩어리를 쓰고 옮길 수 있고 내보낸 영상에도 박힌다")
- [ ] 클립마다 자막 한 덩어리를 쓸 수 있다.
- [ ] 자막은 그 클립이 재생되는 동안만 보인다.
- [ ] Writer가 쓴 대사가 있는 클립은 그 대사가 자막 초기값으로 들어 있다.
- [ ] 미리보기에서 자막을 누르면 바로 고칠 수 있다.
- [ ] 자막을 마우스로 끌거나 방향키로 옮길 수 있고, 옮긴 위치가 저장된다.
- [ ] 자막 내용과 위치가 내보낸 영상에 그대로 반영된다.
- [ ] 자막을 지우면 아무것도 보이지 않는다.
- [ ] 자막은 새로고침해도 남는다.
- [ ] 처음 위치는 아래 가운데다.

스크린샷으로 볼 것: 글자 모양(흰 글자에 검은 테두리).

---

## L. Producer — 배경 카드 목적이 비었는데 채팅이 모르는 문제 (옛 S16)
검수: 약속

설명: 배경 카드 하나의 목적이 비어 빨간 느낌표가 떠도, 다른 배경이 완성돼 있으면 채팅은 "남은 필수 항목 없음"으로 알고
아무 말을 안 한다. 고치면 채팅과 왼쪽 제작 여정이 "어느 배경의 어느 칸"인지 이름으로 짚어준다.
Writer로 넘어가는 건 지금처럼 완성 배경 하나면 된다.

약속 (파일 머리말: "배경 카드에 빈 칸이 있으면 채팅과 여정이 그 카드를 짚는다")
- [ ] 배경이 둘이고 하나는 완성·하나는 목적이 비어 있으면, 넘김은 통과하되 안내 목록에 "○○의 목적이 비어 있어요"가 실린다.
- [ ] 완성된 배경이 하나 있으면 다른 배경의 빈 칸 때문에 Writer 넘김을 막지 않는다.
- [ ] 배경이 하나뿐이고 목적이 비어 있으면 넘김이 막히고, 안내에 카드 이름과 빈 칸 이름이 적힌다.
- [ ] 안내 목록의 빈 칸은 채팅 요청에 그대로 실려 간다.
- [ ] 빈 칸이 채워지면 그 안내는 사라진다.

두 번째 문장이 곧 결정이다. 빈 칸 있는 배경이 하나라도 있으면 넘김을 막고 싶으면 그 문장을
"빈 칸이 있는 배경이 하나라도 있으면 Writer 넘김을 막는다"로 고친다.

---

## M. Producer — 완드(마법봉)에 @ 붙이기 (옛 S17)
검수: 약속

설명: 인물·배경 카드의 완드를 누르면 "이 카드 채워줘"가 바로 보내지는데 어느 카드인지 이름이 안 붙어서
이름 없는 카드는 AI가 못 찾는다. 고치면 "@카드이름"이 앞에 붙는다.

약속 (파일 머리말: "완드 요청에는 어느 카드인지 이름이 붙는다")
- [ ] 인물·배경 카드의 완드를 누르면 ( "@카드이름"으로 시작하는 요청이 바로 보내진다 / 보내지 않고 입력창에 "@카드이름 "만 들어가고 커서가 그 뒤에 놓인다 ).
- [ ] 이름이 아직 없는 카드의 완드를 눌러도 AI가 어느 카드인지 알고 그 카드를 채운다.

별도 결정: Artist·Director·Editor 카드에도 완드를 새로 만들지. 지금은 Producer에만 있다.

---

## N. Producer — 자잘한 것 (옛 S4 + S22)
검수: 스크린샷

- 배경 카드 빨간 느낌표에 마우스를 올리면 1초 기다리지 않고 바로 "무엇이 비었는지"가 뜬다.
- 헤더 오른쪽 "Producer와 스토리 만들기" 버튼을 없앤다. 채팅에 직접 말하는 것과 같은 경로라 기능 손실 없음.
  Writer 엔진 선택기는 admin 전용 그대로 둔다. (오너 확정)

---

## O. Artist·Director — 자잘한 것 (옛 S1 + S2)
검수: 스크린샷

- Director 이미지 카드 아래 "Take 5개" 배지를 없앤다. 영상 카드 위쪽의 Take 표시는 그대로 남는다.
- Artist 캐릭터 팝업의 "후보 히스토리" 제목을 "히스토리"로 바꾼다.




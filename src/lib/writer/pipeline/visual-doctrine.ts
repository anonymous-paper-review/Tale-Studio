// 시각적 서술·커버리지 원칙 — 프롬프트 공용 조각 (#coverage-first 2026-09-02 오너 확정).
//
// 배경(실측 a497ac05 겨울_2, Shot2→3): 비트는 5/5 커버됐는데 영상이 이어지지 않았다.
//   빠진 건 비트가 아니라 **비트 사이의 연결 조직**이었다 — ① 원문 "눈을 뜨며 주위를 살핀다"에
//   '몸을 일으킴'이 없고(시각 서술 부족) ② 샷은 동사 하나(opens eyes)·정지 카메라 ③ 살피는
//   대상(대치 중인 두 수장)으로 넘어가는 리빌(시선 따라 팬/줌아웃·POV·반응)이 어느 단계에도 없었다.
//   원인은 파이프라인 전체가 런타임·인지 부하를 위해 **압축 깔때기**로 조율돼 있던 것:
//   예산(씬당 액션 N개, 모자라면 줄여라) → 씬 서술(한 액션=한 샷) → v3 타깃(=비트 수) →
//   데쿠파주(타깃 ±1 상한, 초과면 병합) → v4(동사 1~2·순차 금지·압축) → 카메라(시선=static).
//   그 비용이 "생략·단절"이다. 여기서 원칙을 **커버리지 우선**으로 뒤집는다: 비트는 지우지 않고,
//   커버리지 샷은 예산 밖이며, 시간은 하류(루브릭·재배분)가 비트당 초를 줄여 맞춘다.
//
// 두 조각은 각각 다른 스테이지에 주입된다:
//   VISUAL_BEAT_DOCTRINE  — 씬 서술(s1s3_merged/s3_scenes) + 데쿠파주 beat_summary
//   COVERAGE_GRAMMAR      — 데쿠파주(샷 분해)

export const VISUAL_BEAT_DOCTRINE = `[시각적 서술 원칙 — 카메라가 찍을 수 있는 문장만]
- 내면 상태가 아니라 그 상태의 **가시적 징후**를 써라: "슬펐다" ❌ → "눈물을 흘리며 고개를 떨군다" ⭕,
  "긴장했다" ❌ → "칼자루를 쥔 손에 힘이 들어간다" ⭕, "불안하다" ❌ → "시선이 출구를 향해 흔들린다" ⭕.
- **몸의 전이를 생략하지 마라**: 깨어남은 "눈을 뜬다 → 몸을 일으킨다 → 주위를 둘러본다"다.
  앉음·일어섬·입장·퇴장·돌아섬·다가감은 각각 보이는 동작으로 적는다. 결과 상태만 적지 마라.
- **시선에는 대상이 있다**: "주위를 살핀다" ❌ → "고개를 돌려 대치 중인 두 수장을 본다" ⭕
  — 무엇을 보는지 적어야 카메라가 그 시선을 따라갈 수 있다.
- 한 비트 = 한 사람의 한 행동 단위(주어·동사·대상·방향). 두 사람이 서로에게 반응하면 두 비트로
  나눈다(행동 → 반응). 세 사람이 동시에 움직이면 누가 먼저·어떻게인지 적는다.
- 소리·시간감("소음이 사라진다", "시간이 멈춘 듯")은 비트로 쓰지 않는다 — 그 순간 인물이
  **보이게 하는 행동**을 대신 쓴다.`; // i18n-ok: LLM 프롬프트 조각

export const COVERAGE_GRAMMAR = `== 커버리지 문법 (필수 — 비트 사이의 연결 조직) ==
비트를 하나씩 정지 샷으로 찍어 나열하면 관객은 사건의 연결을 잃는다(실측: 수장이 눈을 뜨는
WS 정지 샷 → 두 수장이 대치하는 MFS 정지 샷으로 점프 — 수장이 무엇을 봤고 상황이 어떻게
이어지는지 사라졌다). 아래 네 문법은 예산 밖의 **의무**다:
1. **다인 비트 = 행동 샷 + 반응 샷.** 두 명 이상이 관여하는 비트는 행동하는 이의 샷 뒤에
   받는 이의 반응(reaction, CU/MCU, 2~3s)을 붙인다. 반응 없는 다인 비트는 금지.
2. **시선·발견 비트 = 보는 이 → 보이는 것.** "본다·살핀다·둘러본다·발견한다·알아챈다" 비트는
   보는 이의 샷에서 그가 보는 대상으로 반드시 이어라. **기본은 인샷 카메라 무브**: 대상이 같은
   공간에 있으면 camera_intent=motivated_move 로 시선을 따라 팬·틸트·줌아웃해 대상을 같은 샷
   안에서 드러낸다(camera_move_motivation="X의 시선을 따라 Y를 드러낸다"). 컷(다음 샷을
   reveal/pov 로 added)은 대상이 다른 공간이거나 놀람을 위해 시선 컷이 필요할 때만. 정지
   카메라 + 마스터 샷으로 점프하는 것은 리빌이 아니다. 발견 대상이 프레임 밖 인물이면 예외 없이.
3. **상태 전이 비트 = 전이 동작이 화면에 있는 샷.** 깨어남·일어섬·입장·퇴장·돌아섬은 그
   동작 자체를 담는다 — 전이 전 정지 프레임이나 전이 후 결과 프레임만 보여주는 건 생략이다.
4. **주체가 바뀌는 인접 샷 사이에는 다리를 놓아라.** 앞 샷의 마지막 시선·움직임이 다음 샷의
   대상을 가리키게(카메라 회전·줌아웃·시선 컷·POV). 다리 없는 주체 점프는 금지.
커버리지 샷은 짧다(reaction·insert·pov 2~3s) — 예산이 빠듯하면 비트 샷을 줄이지 말고
커버리지 샷을 짧게 하라. 비트를 삭제·병합해 예산을 맞추는 것은 연출이 아니라 생략이다.
증폭 가드: 커버리지 샷(added)은 씬당 **비트 샷 수의 절반(올림)** 을 넘지 않는다 — 문법이 요구하는
곳에만 넣고, 반응 하나가 여러 비트를 받을 수 있으면 하나로 묶는다.`; // i18n-ok: LLM 프롬프트 조각
